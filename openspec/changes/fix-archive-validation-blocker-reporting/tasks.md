## Implementation Tasks

- [ ] Add a canonical validation-mode delta in `openspec/specs/cflx-proposal-validation/spec.md` and, if needed, `openspec/specs/agent-prompts/spec.md` so archive-side validation is explicitly limited to the native `cflx openspec validate` evidence enum `off|warn|error` without reopening the archived `align-archive-readiness-failure-reporting` failure-reporting scope. (verification: integration - `cflx openspec validate fix-archive-validation-blocker-reporting --strict --evidence warn` succeeds, and `src/main.rs` plus `src/cli.rs` remain aligned with `openspec/changes/fix-archive-validation-blocker-reporting/specs/cflx-proposal-validation/spec.md`)
- [ ] Update archive validation command generation in `src/orchestration/archive.rs`, `src/parallel/executor.rs`, and any relevant CLI plumbing so archive paths never emit `--evidence strict` and instead use `warn` or `error`. (verification: integration - `cargo test orchestration::archive::tests` and `cargo test parallel::tests::executor` exercise `src/orchestration/archive.rs` / `src/parallel/executor.rs`, and the resulting command/log output shows only `cflx openspec validate <id> --strict --evidence warn` or `--evidence error`)
- [ ] Update `skills/cflx-archive/SKILL.md` so archive guidance uses the same native validator examples and never documents an unsupported evidence mode. (verification: integration - `skills/cflx-archive/SKILL.md` stays aligned with `src/main.rs` and `src/cli.rs`, contains `--evidence warn` or `--evidence error`, and does not contain `--evidence strict`)
- [ ] Add non-regression tests for archive validation mode selection so CLI-facing and archive-path builders reject `strict` as an evidence mode name and preserve runnable `cflx openspec validate` coverage. (verification: integration - `cargo test orchestration::archive::tests` and CLI-facing tests around `src/cli.rs` cover the executable archive validation command path and confirm the generated command remains runnable with supported evidence values)
- [ ] Add a focused regression in `src/execution/archive.rs` or `src/parallel/tests/executor.rs` to confirm the archived `align-archive-readiness-failure-reporting` root-cause-preserving archive failure message remains intact while the unsupported evidence mode is removed. (verification: integration - `cargo test parallel::tests::executor` or equivalent `src/execution/archive.rs` / `src/parallel/tests/executor.rs` coverage keeps the preserved root-cause message and shows no `--evidence strict` in the exercised command path)
- [ ] Run proposal and repository verification for the touched archive validation paths. (verification: integration - `cflx openspec validate fix-archive-validation-blocker-reporting --strict --evidence warn`, `cargo test orchestration::archive::tests`, `cargo test parallel::tests::executor`, and `cargo clippy --all-targets --all-features -- -D warnings` all succeed)

## Future Work

- archive failure reason を dashboard/TUI で structured badge 表示する改善
- hook failure と spec blocker を別 kind で履歴集計できる telemetry 追加
