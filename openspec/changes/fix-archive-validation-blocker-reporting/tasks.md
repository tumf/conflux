## Implementation Tasks

- [ ] 1. `openspec/specs/cflx-proposal-validation/spec.md` と必要なら `openspec/specs/agent-prompts/spec.md` に、archive-side validation が native `cflx openspec validate` evidence enum (`off|warn|error`) に従う canonical rule を追加し、archived `align-archive-readiness-failure-reporting` の failure-reporting contractを再オープンしない境界を明記する (implementation: `openspec/specs/cflx-proposal-validation/spec.md`, `openspec/specs/agent-prompts/spec.md`; verification: integration - `cflx openspec validate fix-archive-validation-blocker-reporting --strict --evidence warn` が成功し、delta が evidence mode 契約を明示する)
- [ ] 2. archive 実行経路 (`src/orchestration/archive.rs`, `src/parallel/executor.rs`, 必要なら `src/main.rs` / `src/cli.rs`) を確認・更新し、`--evidence strict` を生成せず `warn` または `error` のみを使うよう統一する (implementation: `src/orchestration/archive.rs`, `src/parallel/executor.rs`, `src/main.rs`, `src/cli.rs`; verification: unit - archive validation command builder tests が `--evidence strict` を含まず、許容 enum のみを出力することを確認し、manual - representative archive path のログ/command text で `cflx openspec validate <id> --strict --evidence warn|error` のみが出ることを確認する)
- [ ] 3. `skills/cflx-archive/SKILL.md` と関連 archive guidance を更新し、native validator contract に一致する validation examples に揃える (implementation: `skills/cflx-archive/SKILL.md`; verification: integration - skill text が `cflx openspec validate <id> --strict --evidence warn|error` を参照し、存在しない mode を案内しないことを確認する)
- [ ] 4. invalid evidence mode regression を追加し、archive validation command が native CLI の enum 契約から逸脱しないことを確認する (implementation: `src/orchestration/archive.rs`, `src/parallel/executor.rs`, `src/cli.rs`; verification: unit - validator command generation tests または CLI-facing tests が `strict` mode 名を拒否し、archive path ではその mode が生成されないことを確認する)
- [ ] 5. archived `align-archive-readiness-failure-reporting` が導入した root-cause preserving archive failure message に非退行であることを確認する focused regression を追加する (implementation: `src/execution/archive.rs`, `src/parallel/tests/executor.rs` または同等 test; verification: unit/integration - archive failure tests が root-cause preserving message を維持しつつ、今回の変更で `--evidence strict` だけを除去したことを確認する)
- [ ] 6. proposal delta と関連実装変更をまとめて検証する (implementation: `src/orchestration/archive.rs`, `src/parallel/executor.rs`, `skills/cflx-archive/SKILL.md`; verification: integration - `cflx openspec validate fix-archive-validation-blocker-reporting --strict --evidence warn`, `cargo test orchestration::archive::tests`, `cargo test parallel::tests::executor`, `cargo clippy --all-targets --all-features -- -D warnings`)

## Future Work

- archive failure reason を dashboard/TUI で structured badge 表示する改善
- hook failure と spec blocker を別 kind で履歴集計できる telemetry 追加
