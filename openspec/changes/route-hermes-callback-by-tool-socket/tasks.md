## Implementation Tasks

- [x] Add one shared `RouteSelector`/connection resolver used by client CLI and MCP: absolute `project_dir`, explicit `unix_socket`, or default; reject two call-scoped selectors before contact, let one call-scoped selector override namespace defaults, and derive both canonical repository root and Git-common-dir socket from the selected project. Add `cflx client --project-dir <ABSOLUTE_PATH>` with clap-level conflict against `--unix-socket`. (verification-id: project-scoped-client-routing) (verification: unit - `cargo test --lib client`)
- [x] Expose `project_dir` on all six MCP client tools and route every operation through the shared selector without adding a stable-envelope outcome, project registry, owner startup, or change-ID inference. (verification-id: project-scoped-client-routing) (verification: integration - `cargo test --features heavy-tests --test client_mcp_integration`)
- [x] Make truthful `cflx_wait` evidence certification use the repository root derived from the selected project. Add a regression where MCP server CWD is project A, project B is selected, both contain the same change ID with different evidence, and only B may certify completion. (verification-id: project-scoped-client-routing) (verification: integration - `cargo test --features heavy-tests --test client_mcp_integration`)
- [x] Update the Hermes post-tool hook to preserve the admitted enqueue call's `project_dir` or low-level `unix_socket`, invoke `cflx client --project-dir ... notify set` or its socket equivalent with complete instance/execution/change binding, use `CFLX_UNIX_SOCKET` only when the host exposes no post-tool arguments object, and keep malformed/conflicting/unresolved routes observational. (verification-id: project-scoped-client-routing) (verification: integration - `cargo test --features heavy-tests --test hermes_auto_resume_example plugin_routes_registration_by_call_scoped_project`)
- [x] Add integration coverage using two independent repositories plus a linked worktree to prove per-call owner affinity, canonicalized directory handling, relative/bare/non-repository refusal, call-versus-namespace precedence, mutual-exclusion refusal with no contact, low-level socket compatibility, default-CWD behavior, wait evidence isolation, and callback registration against the enqueue-selected owner. The unreachable-after-MCP-conflict hook defense may use a synthetic admitted-result fixture. (verification-id: project-scoped-client-routing) (verification: integration - `cargo test --features heavy-tests --test hermes_auto_resume_example && cargo test --features heavy-tests --test client_mcp_integration`)
- [x] Update the Hermes auto-resume README, MCP tool descriptions, embedded Conflux operation skill, and `AGENTS.md` client section to present absolute `project_dir` as the normal public selector, `unix_socket` as a low-level override, truthful wait evidence selection, and one global MCP registration with no fixed project socket. (verification-id: project-scoped-client-routing) (verification: integration - `cargo test --features heavy-tests --test hermes_auto_resume_example documentation_describes_project_scoped_routing`)

## Notes

- Base-branch reconciliation: `main` carried the reviewed supersession of this
  change (project-directory routing) in commits 37cf0d41, 528dfb65, 5cc2a7c3.
  `main` was merged into this branch so the workspace change artifacts are the
  reviewed revision: `proposal.md` now declares the `project-scoped-client-routing`
  verification, `specs/cli/spec.md` is present, and
  `specs/external-lifecycle-integrations/spec.md` states project directory as the
  normal public selector. The task list below is `main`'s reviewed list; the three
  pre-review socket-only tasks it replaced are subsumed by tasks 4-6.
- Verification ownership: the reviewed list marks tasks 4 and 6 `unit`, but their
  named evidence drives `python3` subprocesses, temporary-directory fixtures, and
  fake `cflx`/`hermes` executables. That is integration evidence under this
  project's unit-test boundary policy, so the markers are recorded as `integration`
  and the genuinely unit-scoped route-selector logic is covered separately by
  `cargo test --lib client` in task 1. No planned coverage was dropped.

## Final Validation

Archive validation is the authoritative final OpenSpec gate.
Expected archive gate: `cflx openspec validate route-hermes-callback-by-tool-socket --archive-gate`

Run against the artifacts synced from `main` (merge commit `acc52d53`), with the
worktree-built binary rather than the installed release:

- `cflx openspec validate route-hermes-callback-by-tool-socket --strict`: passed.
- `cflx openspec validate route-hermes-callback-by-tool-socket --archive-gate`: passed.
- `cargo fmt --check`: clean.
- `cargo clippy --all-targets --features heavy-tests -- -D warnings`: clean.
- Reviewed rerun suite for `project-scoped-client-routing`:
  - `cargo test --features heavy-tests --test hermes_auto_resume_example`: 20 passed, 0 failed.
  - `cargo test --features heavy-tests --test client_mcp_integration`: 15 passed, 0 failed.
  - `cargo test --lib client`: 86 passed, 0 failed.
- `cargo test` (default suite): 0 failed across every target.
- `git merge-tree --write-tree main HEAD` reports no conflict, and the change
  directory now differs from `main` only in this `tasks.md`.

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-base-branch-proposal-supersession] (major) Workspace change artifacts are the pre-review revision; base branch main carries the reviewed supersession of this same change (project-directory routing) and the branch cannot integrate or archive truthfully until it absorbs it | evidence: main commits 37cf0d41, 528dfb65, 5cc2a7c3 revise openspec/changes/route-hermes-callback-by-tool-socket/ after this branch's merge-base d6ebb1a9; the branch holds only the pre-review revision; main:openspec/changes/route-hermes-callback-by-tool-socket/specs/external-lifecycle-integrations/spec.md says 'The normal public route selector MUST be a project directory' and 'The two selectors MUST NOT be accepted together', superseding workspace specs/external-lifecycle-integrations/spec.md:7 'MUST treat a non-empty string unix_socket ... as authoritative'; main adds openspec/changes/route-hermes-callback-by-tool-socket/specs/cli/spec.md (project_dir selector on all six MCP tools); the file is absent from the workspace change directory; git merge-tree --write-tree main HEAD reports CONFLICT (content) in openspec/changes/route-hermes-callback-by-tool-socket/tasks.md, so the post-archive resolve/merge path cannot integrate cleanly; archiving first would escalate to delete/modify conflicts and orphan main's specs/cli/spec.md; workspace proposal.md frontmatter declares only verification hermes-project-socket-routing; main's proposal declares project-scoped-client-routing whose rerun additionally requires cargo test --features heavy-tests --test client_mcp_integration && cargo test --lib client | required_changes: openspec/changes/route-hermes-callback-by-tool-socket/proposal.md — Merge main into the branch and adopt the reviewed proposal revision (project-directory routing, project-scoped-client-routing verification declaration, follow-up references); openspec/changes/route-hermes-callback-by-tool-socket/specs/cli/spec.md — Bring main's reviewed cli spec delta into the workspace change directory; openspec/changes/route-hermes-callback-by-tool-socket/specs/external-lifecycle-integrations/spec.md — Adopt main's reviewed external-lifecycle-integrations delta (project_dir as the normal selector, unix_socket as low-level override, selectors mutually exclusive, fail-closed rules); openspec/changes/route-hermes-callback-by-tool-socket/tasks.md — Resolve the merge conflict with main and reconcile task claims and Final Validation with the reviewed scope and verification ids | verification: openspec/changes/route-hermes-callback-by-tool-socket/tasks.md — Final Validation records archive-gate validation and the reviewed rerun suite passing against the synced artifacts, and the workspace change directory no longer differs from main's revision except for legitimate branch-side completions
  finding: {"evidence":["main commits 37cf0d41, 528dfb65, 5cc2a7c3 revise openspec/changes/route-hermes-callback-by-tool-socket/ after this branch's merge-base d6ebb1a9; the branch holds only the pre-review revision","main:openspec/changes/route-hermes-callback-by-tool-socket/specs/external-lifecycle-integrations/spec.md says 'The normal public route selector MUST be a project directory' and 'The two selectors MUST NOT be accepted together', superseding workspace specs/external-lifecycle-integrations/spec.md:7 'MUST treat a non-empty string unix_socket ... as authoritative'","main adds openspec/changes/route-hermes-callback-by-tool-socket/specs/cli/spec.md (project_dir selector on all six MCP tools); the file is absent from the workspace change directory","git merge-tree --write-tree main HEAD reports CONFLICT (content) in openspec/changes/route-hermes-callback-by-tool-socket/tasks.md, so the post-archive resolve/merge path cannot integrate cleanly; archiving first would escalate to delete/modify conflicts and orphan main's specs/cli/spec.md","workspace proposal.md frontmatter declares only verification hermes-project-socket-routing; main's proposal declares project-scoped-client-routing whose rerun additionally requires cargo test --features heavy-tests --test client_mcp_integration && cargo test --lib client"],"id":"acceptance-base-branch-proposal-supersession","required_changes":[{"description":"Merge main into the branch and adopt the reviewed proposal revision (project-directory routing, project-scoped-client-routing verification declaration, follow-up references)","file":"openspec/changes/route-hermes-callback-by-tool-socket/proposal.md"},{"description":"Bring main's reviewed cli spec delta into the workspace change directory","file":"openspec/changes/route-hermes-callback-by-tool-socket/specs/cli/spec.md"},{"description":"Adopt main's reviewed external-lifecycle-integrations delta (project_dir as the normal selector, unix_socket as low-level override, selectors mutually exclusive, fail-closed rules)","file":"openspec/changes/route-hermes-callback-by-tool-socket/specs/external-lifecycle-integrations/spec.md"},{"description":"Resolve the merge conflict with main and reconcile task claims and Final Validation with the reviewed scope and verification ids","file":"openspec/changes/route-hermes-callback-by-tool-socket/tasks.md"}],"severity":"major","summary":"Workspace change artifacts are the pre-review revision; base branch main carries the reviewed supersession of this same change (project-directory routing) and the branch cannot integrate or archive truthfully until it absorbs it","verification":[{"description":"Final Validation records archive-gate validation and the reviewed rerun suite passing against the synced artifacts, and the workspace change directory no longer differs from main's revision except for legitimate branch-side completions","file":"openspec/changes/route-hermes-callback-by-tool-socket/tasks.md"}]}
  evidence: proposal.md — merge commit acc52d53 merged main into this branch, so the workspace proposal is main's reviewed project-directory revision declaring verification project-scoped-client-routing
  evidence: specs/cli/spec.md — main's reviewed cli delta is now present in the workspace change directory; git diff main -- that path is empty
  evidence: specs/external-lifecycle-integrations/spec.md — adopted main's delta verbatim: project_dir as the normal public selector, unix_socket as the low-level override, mutually exclusive, fail-closed
  evidence: tasks.md — the merge conflict was resolved onto main's reviewed six-task list with its project-scoped-client-routing ids; git merge-tree --write-tree main HEAD now reports no conflict
  evidence: verification — Final Validation records the archive gate passing plus the reviewed rerun suite, and the change directory differs from main only in this tasks.md
- [x] [acceptance-project-dir-routing-unimplemented] (major) The reviewed proposal's project-directory routing scope is unimplemented: no project_dir selector exists in the client CLI, MCP tools, or Hermes hook, and no covering tests exist | evidence: grep -rn 'project_dir|project-dir' src/client/ examples/integrations/hermes-auto-resume/ returns no matches; examples/integrations/hermes-auto-resume/__init__.py:193 build_registration_argv always emits --unix-socket, and resolve_owner_socket in examples/integrations/hermes-auto-resume/cflx_hermes_resume.py:395 resolves only CALL_SOCKET_ARGUMENT='unix_socket'; src/client/mcp.rs tool schemas expose only unix_socket via with_connection/tool_descriptors; there is no project_dir selector, no same-call conflict validation, and no selected-project evidence root for cflx_wait; main's reviewed acceptance criteria require two-project routing via per-call project_dir, linked-worktree Git common-dir resolution, selector-conflict refusal before owner contact, and cflx_wait evidence isolation; tests/client_mcp_integration.rs and tests/hermes_auto_resume_example.rs contain no such coverage | required_changes: src/client/mod.rs — Add the shared call-scoped route resolution (absolute project_dir or unix_socket, mutually exclusive per call, overriding namespace default then CWD discovery) and the cflx client --project-dir namespace/CLI wiring, deriving the owner socket and repository-evidence root from the selected project's Git common directory; src/client/mcp.rs — Accept optional project_dir on all six MCP tools, reject two selectors in one call via the normal MCP validation error before owner contact, and make cflx_wait certify evidence only from the selected project's repository root; examples/integrations/hermes-auto-resume/cflx_hermes_resume.py — Resolve the qualifying call's connection selector with project_dir as the normal route and unix_socket as the low-level override, refusing conflicting or malformed selectors and failing closed without an authoritative route; examples/integrations/hermes-auto-resume/__init__.py — Register the completion sink via cflx client --project-dir for a project route, use --unix-socket only when that was the call's selector, and keep CFLX_UNIX_SOCKET only as the no-arguments compatibility fallback; examples/integrations/hermes-auto-resume/README.md — Document one socket-unbound global Hermes MCP registration with per-call project_dir routing and the environment-fallback migration; AGENTS.md — Update cflx client and MCP examples to present project_dir as the normal per-call selector and unix_socket as the low-level override | verification: tests/client_mcp_integration.rs — Add scenarios proving two independent projects route via per-call project_dir, a linked worktree resolves the common owner socket, project_dir plus unix_socket in one call is rejected before owner contact, a call selector overrides the namespace default, and wait certifies evidence only from the selected project; tests/hermes_auto_resume_example.rs — Prove the hook registers via cflx client --project-dir for a project-routed enqueue, uses --unix-socket only for the low-level override, and fails closed when the arguments object exposes no selector
  finding: {"evidence":["grep -rn 'project_dir|project-dir' src/client/ examples/integrations/hermes-auto-resume/ returns no matches","examples/integrations/hermes-auto-resume/__init__.py:193 build_registration_argv always emits --unix-socket, and resolve_owner_socket in examples/integrations/hermes-auto-resume/cflx_hermes_resume.py:395 resolves only CALL_SOCKET_ARGUMENT='unix_socket'","src/client/mcp.rs tool schemas expose only unix_socket via with_connection/tool_descriptors; there is no project_dir selector, no same-call conflict validation, and no selected-project evidence root for cflx_wait","main's reviewed acceptance criteria require two-project routing via per-call project_dir, linked-worktree Git common-dir resolution, selector-conflict refusal before owner contact, and cflx_wait evidence isolation; tests/client_mcp_integration.rs and tests/hermes_auto_resume_example.rs contain no such coverage"],"id":"acceptance-project-dir-routing-unimplemented","required_changes":[{"description":"Add the shared call-scoped route resolution (absolute project_dir or unix_socket, mutually exclusive per call, overriding namespace default then CWD discovery) and the cflx client --project-dir namespace/CLI wiring, deriving the owner socket and repository-evidence root from the selected project's Git common directory","file":"src/client/mod.rs"},{"description":"Accept optional project_dir on all six MCP tools, reject two selectors in one call via the normal MCP validation error before owner contact, and make cflx_wait certify evidence only from the selected project's repository root","file":"src/client/mcp.rs"},{"description":"Resolve the qualifying call's connection selector with project_dir as the normal route and unix_socket as the low-level override, refusing conflicting or malformed selectors and failing closed without an authoritative route","file":"examples/integrations/hermes-auto-resume/cflx_hermes_resume.py"},{"description":"Register the completion sink via cflx client --project-dir for a project route, use --unix-socket only when that was the call's selector, and keep CFLX_UNIX_SOCKET only as the no-arguments compatibility fallback","file":"examples/integrations/hermes-auto-resume/__init__.py"},{"description":"Document one socket-unbound global Hermes MCP registration with per-call project_dir routing and the environment-fallback migration","file":"examples/integrations/hermes-auto-resume/README.md"},{"description":"Update cflx client and MCP examples to present project_dir as the normal per-call selector and unix_socket as the low-level override","file":"AGENTS.md"}],"severity":"major","summary":"The reviewed proposal's project-directory routing scope is unimplemented: no project_dir selector exists in the client CLI, MCP tools, or Hermes hook, and no covering tests exist","verification":[{"description":"Add scenarios proving two independent projects route via per-call project_dir, a linked worktree resolves the common owner socket, project_dir plus unix_socket in one call is rejected before owner contact, a call selector overrides the namespace default, and wait certifies evidence only from the selected project","file":"tests/client_mcp_integration.rs"},{"description":"Prove the hook registers via cflx client --project-dir for a project-routed enqueue, uses --unix-socket only for the low-level override, and fails closed when the arguments object exposes no selector","file":"tests/hermes_auto_resume_example.rs"}]}
  evidence: src/client/mod.rs — RouteSelector::from_inputs/or_default takes an absolute call-scoped project_dir or unix_socket, refuses both together, and resolve_project derives the repo root plus <git-common-dir>/cflx-api.sock
  evidence: src/cli.rs — the client namespace gains --project-dir with clap conflicts_with unix_socket, and src/client/session.rs Connection::resolve_route takes both socket and evidence root from the selected project
  evidence: src/client/mcp.rs — ConnectionArgs gains project_dir, ClientToolHost::route resolves the selector for all six tools and returns a ToolError (isError, no envelope) for two selectors or an unusable project before connecting, and cflx_wait certifies through the selected project's repo_root
  evidence: examples/integrations/hermes-auto-resume/cflx_hermes_resume.py — resolve_owner_route returns (PROJECT_ROUTE|SOCKET_ROUTE, path) and refuses both selectors, a malformed selector, and arguments naming no route at all
  evidence: examples/integrations/hermes-auto-resume/__init__.py — build_registration_argv emits ROUTE_OPTIONS[route_kind], so a project call registers via cflx client --project-dir and a socket call via --unix-socket; CFLX_UNIX_SOCKET is reached only when the host exposes no arguments object
  evidence: examples/integrations/hermes-auto-resume/README.md — the routing section now documents one socket-unbound global MCP registration with per-call project_dir, a low-level unix_socket subsection, and the CFLX_UNIX_SOCKET migration plus fail-closed rules
  evidence: AGENTS.md and skills/cflx-run/SKILL.md — both present --project-dir/project_dir as the normal per-call selector and --unix-socket/unix_socket as the low-level override
  evidence: verification tests/client_mcp_integration.rs — two-project routing, linked-worktree/nested/symlink common-dir resolution, selector-conflict and unusable-project refusal with no admission, call-over-namespace-default precedence, wait evidence isolation for a colliding change id, and default-CWD compatibility all pass
  evidence: verification tests/hermes_auto_resume_example.rs — plugin_routes_registration_by_call_scoped_project, plugin_preserves_the_low_level_socket_route, plugin_prefers_the_call_scoped_project_over_the_environment, and plugin_registers_nothing_without_a_usable_owner_route all pass
