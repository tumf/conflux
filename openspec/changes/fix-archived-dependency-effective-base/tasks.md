## Implementation Tasks

- [x] Define effective dependency base selection for archived dependency dispatch checks in the scheduler path. (verification: unit - focused `src/parallel/tests/executor.rs` test fails if `is_dependency_resolved()` always uses the original startup branch when an integration branch has advanced)
- [x] Preserve the archive-only safety guard while checking merge evidence against the selected effective dependency base. (verification: unit - existing or new `src/parallel/tests/executor.rs` regression keeps archived-but-not-merged dependencies blocked and fails if archive evidence alone unblocks dispatch)
- [x] Update dependency resolution diagnostics/status wording so archived-but-not-merged blockers identify the checked effective base and do not conflict with analysis/status `done` wording. (verification: unit - `src/parallel/tests/executor.rs` or related diagnostics assertions fail if an archived dependency is reported as dispatch-satisfied while still blocked without merge evidence)
- [x] Add regression coverage for stacked orchestration dependency unblocking after merge into the effective integration base. (verification: unit - `src/parallel/tests/executor.rs` constructs a dependency merged into the effective integration branch while the original branch lacks it, and asserts the dependent becomes dispatchable)
- [x] Run focused verification for dependency dispatch behavior. (verification: integration - `cargo test archived_dependency` passed; agent-exec job `a2c39fd2948af459a51a3b28cb0a5a2b`)
- [x] Run broader repository checks appropriate for changed Rust code. (verification: integration - `cargo test` passed; agent-exec job `0f9a984bccf61430116f07fcff914c36`)

## Future Work

- Consider a separate UX proposal if TUI/OpenSpec list output should expose `archived, waiting for merge into <base>` as a first-class dependency status.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-archived-dependency-effective-base --archive-gate`

## Acceptance #1 Failure Follow-up
- [x] Strengthen the test so it FAILS when effective_dependency_base() always uses the original startup branch: capture original_branch as `main` before `git checkout -b integration` (e.g. first-phase dependency archived-but-not-merged on `main` so effective_dependency_base() runs and the dependent stays blocked on missing base-merge evidence), then advance the integration branch and assert the dependent unblocks; assert the captured original branch (main) differs from the effective base (integration). (verification: unit - `cargo test test_archived_dependency_uses_effective_integration_base_after_startup` passed; agent-exec job `caf0844bfc98baa1873aa5fe25edf395`)
- [x] src/parallel/tests/executor.rs:3057 test_archived_dependency_uses_effective_integration_base_after_startup does not guard the fix: it still passes when effective_dependency_base() (src/parallel/queue_state.rs:426-444) is reverted to `Ok(original_branch)` (verified during acceptance). Root cause: GitWorkspaceManager.original_branch is lazily initialized (src/vcs/git/mod.rs:74,124-138); the test's first select_changes_for_dispatch classifies `policy` as Missing (archive committed only afterward), hitting the Missing short-circuit at src/parallel/queue_state.rs:589-593 before effective_dependency_base() is called, so original_branch is first captured as `integration` on the second dispatch (after `git checkout -b integration`), making current==original==integration and never exercising the current_branch != original_branch path at src/parallel/queue_state.rs:427. (verification: unit - fixture now creates archive evidence before first dispatch without committing it to `main`, forcing the archived merge check to capture `main` before switching to `integration`)
- [x] tasks.md Task 1 (line 3) and Task 4 (line 6) verification claims are untruthful: the captured original startup branch in the test is `integration` (which contains the merge), so 'test fails if is_dependency_resolved() always uses the original startup branch' and 'constructs a dependency merged into the effective integration branch while the original branch lacks it' do not hold; the proposal's Explicit Completion Condition for effective-base unblocking is not genuinely covered. (verification: unit - the strengthened test asserts `main` lacks the archive both before and after the integration commit, while `integration` contains it and dispatches the dependent)
