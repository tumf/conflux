## Implementation Tasks

- [x] Reproduce the owner-before-proposal race in a repository-backed scheduler test: start with an absent active change, admit queue intent, add the proposal to the base repository, and prove the same owner can discover it without restart. (verification: integration - `cargo test parallel::tests::manual_resolve --lib`; verification-id: candidate-refresh-tests)
- [x] Change dynamic queue and reducer reconciliation so an initial catalog miss cannot consume the only scheduler wake while retaining permanent queued intent; use a fresh repository-visible lookup and preserve or explicitly resolve the intent. (verification: integration - `cargo test parallel::tests::manual_resolve --lib`; verification-id: candidate-refresh-tests)
- [x] Cover a genuinely absent candidate and assert that status-facing reducer state does not remain a ghost queued row, while diagnostics are bounded and execution marks retain their independent contract. (verification: integration - `cargo test parallel::tests::manual_resolve --lib`; verification-id: candidate-refresh-tests)
- [x] Keep API and TUI control routes behaviorally aligned through the shared reducer/scheduler path, with regression assertions that no route needs owner restart after a base catalog update. (verification: integration - `cargo test parallel::tests::manual_resolve --lib`; verification-id: candidate-refresh-tests)

## Notes

- Implementation: `src/parallel/queue_state.rs` — a candidate miss is now re-checked
  against a fresh repository-visible active-change view before any verdict.
  `FreshCandidateLookup` keeps "the catalog says no" and "the catalog could not be
  read" apart; only the first may justify `RemoveFromQueue`, which is submitted
  under the reducer write guard after re-reading the revision it mutates and is
  refused for any typed wait, block, active, or terminal row. Dynamic-queue
  ingestion retains its hint on an unreadable catalog instead of spending it, and
  both missing-candidate diagnostics go through the existing dedupe store.
- Evidence type: integration. The tests drive the real reducer, the real
  `DynamicQueue`, the real shared `OperatorCommandService` boundary, and a real
  `openspec/changes` tree on disk, which matches the planned `integration`
  verification for `candidate-refresh-tests`.
- evidence: `cargo test parallel::tests::manual_resolve --lib` — 24 passed, 0 failed
- evidence: `cargo test` — 4220 lib tests and every integration target passed, 0 failed (acceptance repair attempt 1)
- evidence: `cargo clippy --all-targets --all-features` clean; `cargo fmt --check` clean
- Acceptance repair (attempt 1), extra files and their relationship to the open
  findings: `src/parallel/tests/executor.rs` is changed only to serve the
  verification declared by `acceptance-inconclusive-repair-evidence-settles-queue-intent`.
  It gains `TestWorkspaceManager::with_failing_existing_workspace_lookup()` so
  the workspace-lookup-error half of "repair evidence could not be gathered" is
  reachable from `manual_resolve.rs`, and
  `test_scheduler_reconciliation_missing_candidate_warn_is_observable_but_bounded`
  now installs the default `TestWorkspaceManager` because its bare temp
  directory is not a Git repository: after the fix that fixture yields
  undetermined repair evidence and correctly defers, so the double supplies the
  conclusive negative the bounded-warning assertion was always about.
- Pre-existing unrelated failures: the nine `--ignored` tests (six env-mutating
  `config::tests`, `execution::apply::tests::apply_process_group_barrier_*`,
  `parallel::tests::conflict::sequential_resolve_*`, and
  `tui::…::per_change_upstream_explicit_tui_retry_resumes_publication`) fail
  identically on the parent commit, and the last one passes in isolation on this
  branch.

## Final Validation

Archive validation is the authoritative OpenSpec gate. Expected commands:

`cflx openspec validate fix-ghost-queue-candidate-refresh --strict --evidence warn`

`cflx openspec validate fix-ghost-queue-candidate-refresh --archive-gate`

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-inconclusive-repair-evidence-settles-queue-intent] (major) Unresolvable archived-dirty repair evidence is treated as proven absence, so the new reconciler silently clears operator queue intent | evidence: src/parallel/queue_state.rs:2924-2958 maps every archived-dirty repair evidence failure to `None`: resolve_archived_dirty_repair_base_branch() returns None on any git failure (src/parallel/queue_state.rs:2606-2628), and a find_existing_workspace error is warned and mapped to None (src/parallel/queue_state.rs:2947-2954).; src/parallel/queue_state.rs:2973-2981 routes that `None` into resolve_missing_queued_candidate, whose FreshCandidateLookup::Absent arm submits ReducerCommand::RemoveFromQueue (src/parallel/queue_state.rs:3112-3127 and 3200-3205) and logs candidate_unavailable, even though repair availability was never determined.; The base-branch result is memoized per pass (src/parallel/queue_state.rs:2877 and 2924-2927), so a single transient git failure settles every missing queued candidate in that pass, and because mark settlement is edge-triggered the intent is not restored without an explicit operator Start.; This contradicts the change's own invariant that incomplete evidence must retain a wake edge (openspec/changes/fix-ghost-queue-candidate-refresh/design.md:5-11 and :29) and the delta precondition 'no archived-dirty repair candidate or typed lane wait applies' (openspec/changes/fix-ghost-queue-candidate-refresh/specs/parallel-execution/spec.md:150).; The new negative test at src/parallel/tests/manual_resolve.rs:852-923 runs against a bare TempDir that is not a Git repository, so base-branch resolution cannot succeed there, yet it asserts unavailable_reconciled == 1, locking the inconclusive-evidence settle in as expected behavior. | required_changes: src/parallel/queue_state.rs — Distinguish 'no archived-dirty repair candidate applies' from 'repair evidence could not be gathered'. When base-branch resolution or find_existing_workspace fails, defer with queue intent and the wake edge intact instead of taking the Absent settle path, mirroring how FreshCandidateLookup::Unreadable already defers. | verification: src/parallel/tests/manual_resolve.rs — Add a reconciliation test where the missing queued candidate's archived-dirty repair evidence is unavailable (base branch unresolvable or workspace lookup error) asserting queue intent is preserved and unavailable_reconciled == 0, and give the existing genuinely-absent test a real Git repository fixture so it still proves settlement on conclusive evidence.
  finding: {"evidence":["src/parallel/queue_state.rs:2924-2958 maps every archived-dirty repair evidence failure to `None`: resolve_archived_dirty_repair_base_branch() returns None on any git failure (src/parallel/queue_state.rs:2606-2628), and a find_existing_workspace error is warned and mapped to None (src/parallel/queue_state.rs:2947-2954).","src/parallel/queue_state.rs:2973-2981 routes that `None` into resolve_missing_queued_candidate, whose FreshCandidateLookup::Absent arm submits ReducerCommand::RemoveFromQueue (src/parallel/queue_state.rs:3112-3127 and 3200-3205) and logs candidate_unavailable, even though repair availability was never determined.","The base-branch result is memoized per pass (src/parallel/queue_state.rs:2877 and 2924-2927), so a single transient git failure settles every missing queued candidate in that pass, and because mark settlement is edge-triggered the intent is not restored without an explicit operator Start.","This contradicts the change's own invariant that incomplete evidence must retain a wake edge (openspec/changes/fix-ghost-queue-candidate-refresh/design.md:5-11 and :29) and the delta precondition 'no archived-dirty repair candidate or typed lane wait applies' (openspec/changes/fix-ghost-queue-candidate-refresh/specs/parallel-execution/spec.md:150).","The new negative test at src/parallel/tests/manual_resolve.rs:852-923 runs against a bare TempDir that is not a Git repository, so base-branch resolution cannot succeed there, yet it asserts unavailable_reconciled == 1, locking the inconclusive-evidence settle in as expected behavior."],"id":"acceptance-inconclusive-repair-evidence-settles-queue-intent","required_changes":[{"description":"Distinguish 'no archived-dirty repair candidate applies' from 'repair evidence could not be gathered'. When base-branch resolution or find_existing_workspace fails, defer with queue intent and the wake edge intact instead of taking the Absent settle path, mirroring how FreshCandidateLookup::Unreadable already defers.","file":"src/parallel/queue_state.rs"}],"severity":"major","summary":"Unresolvable archived-dirty repair evidence is treated as proven absence, so the new reconciler silently clears operator queue intent","verification":[{"description":"Add a reconciliation test where the missing queued candidate's archived-dirty repair evidence is unavailable (base branch unresolvable or workspace lookup error) asserting queue intent is preserved and unavailable_reconciled == 0, and give the existing genuinely-absent test a real Git repository fixture so it still proves settlement on conclusive evidence.","file":"src/parallel/tests/manual_resolve.rs"}]}
  evidence: required_change src/parallel/queue_state.rs — new `ArchivedDirtyRepairEvidence` enum (Candidate/NotApplicable/Undetermined) replaces the `Option<Change>` probe result, so an unresolved base branch maps to `Undetermined("base_branch_unresolved")` and a `find_existing_workspace` error to `Undetermined("workspace_lookup_failed")` instead of collapsing to `None`.
  evidence: required_change src/parallel/queue_state.rs — the reconciliation arm now matches that enum: only `NotApplicable` reaches `resolve_missing_queued_candidate`, while `Undetermined` defers with queue intent and the wake edge untouched (mirroring `FreshCandidateLookup::Unreadable`), emits the deduped `repair_evidence_unavailable` diagnostic, and increments the new `QueueReconciliationOutcome::repair_evidence_deferred`.
  evidence: verification src/parallel/tests/manual_resolve.rs — added `undetermined_repair_evidence_defers_instead_of_settling_queue_intent` (base branch unresolvable) and `a_failed_workspace_lookup_defers_instead_of_settling_queue_intent`, both asserting `unavailable_reconciled == 0`, `repair_evidence_deferred == 1`, preserved reducer queue intent, no `candidate_unavailable` log, and bounded repeat diagnostics.
  evidence: verification src/parallel/tests/manual_resolve.rs — `a_genuinely_absent_candidate_does_not_remain_a_ghost_queued_row` and `the_shared_queue_command_boundary_needs_no_owner_restart_after_a_catalog_update` now run on a real Git repository via `init_minimal_git_repo`, still asserting `unavailable_reconciled == 1` plus the new `repair_evidence_deferred == 0`, so settlement is proven only on conclusive evidence.
  evidence: `cargo test parallel::tests::manual_resolve --lib` — 28 passed, 0 failed.
- [x] [acceptance-unreadable-catalog-edge-retention-untested] (minor) The wake-edge retention path for an unreadable active-change catalog has no regression coverage | evidence: src/parallel/queue_state.rs:2489-2515 adds queue.requeue_front(dynamic_id) + break so a catalog read failure does not spend the hint, and src/parallel/queue_state.rs:3065-3089 adds the FreshCandidateLookup::Unreadable arm that defers instead of settling.; The change adds the normative scenario 'transient catalog miss retains an execution edge' at openspec/changes/fix-ghost-queue-candidate-refresh/specs/parallel-execution/spec.md:138-144 requiring exactly this behavior.; No test exercises either path: `grep -rn candidate_load_failed src/parallel/tests/` returns nothing, and the four tests added at src/parallel/tests/manual_resolve.rs:728, :798, :852 and :925 cover only the loadable and genuinely-absent arms. | required_changes: src/parallel/tests/manual_resolve.rs — Add a regression test that makes crate::openspec::list_changes_native_from fail for the executor's repo root (for example by creating openspec/changes as a regular file) and asserts the dynamic queue hint is retained via requeue_front and reducer queue intent survives with unavailable_reconciled == 0. | verification: src/parallel/tests/manual_resolve.rs — Prove via `cargo test parallel::tests::manual_resolve --lib` that an unreadable catalog leaves both the wake edge and the queued intent intact, matching the 'transient catalog miss retains an execution edge' scenario.
  finding: {"evidence":["src/parallel/queue_state.rs:2489-2515 adds queue.requeue_front(dynamic_id) + break so a catalog read failure does not spend the hint, and src/parallel/queue_state.rs:3065-3089 adds the FreshCandidateLookup::Unreadable arm that defers instead of settling.","The change adds the normative scenario 'transient catalog miss retains an execution edge' at openspec/changes/fix-ghost-queue-candidate-refresh/specs/parallel-execution/spec.md:138-144 requiring exactly this behavior.","No test exercises either path: `grep -rn candidate_load_failed src/parallel/tests/` returns nothing, and the four tests added at src/parallel/tests/manual_resolve.rs:728, :798, :852 and :925 cover only the loadable and genuinely-absent arms."],"id":"acceptance-unreadable-catalog-edge-retention-untested","required_changes":[{"description":"Add a regression test that makes crate::openspec::list_changes_native_from fail for the executor's repo root (for example by creating openspec/changes as a regular file) and asserts the dynamic queue hint is retained via requeue_front and reducer queue intent survives with unavailable_reconciled == 0.","file":"src/parallel/tests/manual_resolve.rs"}],"severity":"minor","summary":"The wake-edge retention path for an unreadable active-change catalog has no regression coverage","verification":[{"description":"Prove via `cargo test parallel::tests::manual_resolve --lib` that an unreadable catalog leaves both the wake edge and the queued intent intact, matching the 'transient catalog miss retains an execution edge' scenario.","file":"src/parallel/tests/manual_resolve.rs"}]}
  evidence: required_change src/parallel/tests/manual_resolve.rs — added `an_unreadable_catalog_retains_the_dynamic_queue_hint_and_queue_intent`, which writes `openspec/changes` as a regular file so `list_changes_native_from` errors, then asserts ingestion returns false, `dynamic_queue.len() == 1` with the same ID popped from the front (the `requeue_front` retention), surviving reducer queue intent, a `candidate_load_failed` log, and no `candidate_not_found`.
  evidence: required_change src/parallel/tests/manual_resolve.rs — added `an_unreadable_refresh_leaves_queue_intent_exactly_as_it_was`, driving `resolve_missing_queued_candidate` against the same unreadable catalog to cover the `FreshCandidateLookup::Unreadable` arm: `unavailable_reconciled == 0`, `queued_added == 0`, queue intent preserved, and the second call emits no repeated `candidate_load_failed`.
  evidence: verification `cargo test parallel::tests::manual_resolve --lib` — 28 passed, 0 failed; both new tests pass, proving the wake edge and the queued intent both survive an unreadable catalog.
