## Implementation Tasks

- [ ] 1. Add operation-specific skill directories `skills/cflx-analyze/`, `skills/cflx-apply/`, `skills/cflx-rejecting/`, `skills/cflx-cleanup-review/`, `skills/cflx-accept/`, `skills/cflx-archive/`, and `skills/cflx-resolve/`, each with scoped `SKILL.md` and only the auxiliary references needed for that operation, while keeping `scripts/cflx.py` absent from every dedicated skill (verification: repository contains the new skill directories and `cargo test embedded_skills::tests::test_embedded_skills_names -- --nocapture` observes all expected skill names).
- [ ] 2. Shrink `skills/cflx-workflow/SKILL.md` into a self-contained compatibility router that preserves legacy-equivalent operation execution for `load skills: cflx-workflow` prompts without requiring additional skill loads or cross-skill auxiliary file access (verification: `skills/cflx-workflow/SKILL.md` documents self-contained router behavior and backward-compatibility for apply / rejecting / cleanup-review / accept / archive, and no bundled skill directory contains `scripts/cflx.py`).
- [ ] 3. Update prompt construction so each orchestrator operation loads its dedicated skill directly in `src/agent/prompt.rs`, `src/orchestration/rejection.rs`, `src/orchestration/selection.rs`, and `src/parallel/conflict.rs` (verification: unit tests assert `cflx-analyze`, `cflx-apply`, `cflx-accept`, `cflx-archive`, `cflx-cleanup-review`, `cflx-rejecting`, and `cflx-resolve` are emitted on the corresponding paths, including analyze / archive / cleanup-review / resolve builders).
- [ ] 4. Move fixed analyze / resolve guidance out of inline Rust prompt bodies and into dedicated skill source, leaving Rust code responsible for operation-specific variable context injection such as change lists, conflict files, VCS state, and retry history (verification: `src/orchestration/selection.rs` and `src/parallel/conflict.rs` no longer contain the full fixed guidance as the primary prompt source).
- [ ] 5. Update embedded skill registration and install-skills packaging in `src/embedded_skills.rs` so bundled skill installation includes the new operation-specific skills and each skill's own auxiliary references while keeping `cflx-workflow` installable as a self-contained compatibility router and keeping bundled distributions free of `scripts/cflx.py` (verification: `cargo test embedded_skills -- --nocapture` and `cargo test --test install_skills_test -- --nocapture`).
- [ ] 6. Refresh skill-facing docs and distribution metadata such as `skills/README.md` to describe the router + per-operation skill architecture, the added analyze / resolve skills, and the native-CLI-only helper model accurately (verification: docs mention `cflx-workflow` as compatibility router, list all operation-specific skills, and do not describe any bundled `cflx.py` exception).
- [ ] 7. Update OpenSpec coverage for agent prompt / install-skill behavior so canonical specs describe direct per-operation skill loading, retained `cflx-workflow` compatibility routing, and bundled distributions without `scripts/cflx.py` (verification: strict validation passes with spec deltas under `openspec/changes/split-workflow-skills-by-operation/specs/`).
- [ ] 8. Run focused validation for the new proposal and repository checks before handoff (verification: `cflx openspec validate split-workflow-skills-by-operation --strict`, plus the targeted Rust tests/commands used above, all pass).

## Future Work

- Add dedicated qualitative/eval coverage for the skill text itself if Conflux later starts benchmarking skill-trigger quality independently of prompt-builder tests
