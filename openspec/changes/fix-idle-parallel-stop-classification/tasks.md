## Implementation Tasks

- [x] Introduce one runtime stop snapshot that separately reports registered/reducer-visible agent execution and pending background merge/base-lane shutdown work, fails safe on unavailable execution evidence, and is consumed by both local key and command-dispatch stop paths without durable workflow state. (verification: unit - the shared classifier contains `idle_parallel_stop_*` matrix tests for active handles, reducer activity, idle waits, pending merge, and unavailable evidence, run by `cargo test --lib idle_parallel_stop`; verification-id: idle-parallel-stop-local)

- [x] Route second-Esc through `TuiCommand::ForceStop` or an equivalent single asynchronous command path so both entrypoints use one snapshot, one cancellation request, and truthful force-vs-ordinary reporting without duplicating stop side effects. (verification: integration - `src/tui/key_handlers.rs` and `src/tui/command_handlers.rs` tests assert command routing, one cancellation call, and active/idle emitted logs via `cargo test --lib idle_parallel_stop`; verification-id: idle-parallel-stop-local)

- [x] Rework the outer parallel cancellation boundary to keep polling the scheduler future after requesting cancellation, await inner abort/drain, execution-handle release, pending merge/base-lane result handling, and workspace-guard drop behind a bounded barrier, and preserve cancellation classification through any managed escalation. (verification: integration - paused-time/channel tests in `src/tui/orchestrator.rs` prove the service future is not dropped on cancel, active cleanup completes before terminal stop, pending merge is not mislabeled as force-stopped, and timeout escalation emits no execution failure via `cargo test --lib idle_parallel_stop`; verification-id: idle-parallel-stop-local)

- [x] Represent the completed operator stop as a stopped/cancelled outcome instead of `OrchestratorError::AgentCommand`, and suppress failure, error-completion, success-completion, and `AllCompleted` output while preserving genuine-error reporting. (verification: integration - `src/tui/orchestrator.rs` regression tests distinguish cancellation output from a genuine command failure via `cargo test --lib idle_parallel_stop`; verification-id: idle-parallel-stop-local)

- [x] Make stopped-state handling terminal-message idempotent: the first transition owns `Processing stopped`, while repeated or late `Stopped` delivery only reconciles state; preserve not-queued reset and execution marks. (verification: unit - `src/tui/state/event_handlers/processing.rs` tests cover repeated stop delivery, exactly one stop message, queue reset, and mark preservation via `cargo test --lib idle_parallel_stop`; verification-id: idle-parallel-stop-local)

- [x] Add the `idle_parallel_stop` repository-local regression suite covering active force stop, archived `MergeWait` with no process, deferred merge, pending background merge/base-lane shutdown, normal graceful stop, genuine execution failure, bounded cleanup escalation, and absence of duplicate or contradictory terminal messages. (verification: integration - cargo test --lib idle_parallel_stop -- --list | grep -q idle_parallel_stop && cargo test --lib idle_parallel_stop; verification-id: idle-parallel-stop-local)

## Future Work

- Runtime telemetry may later expose separate scheduler-active and child-process-active fields to remote clients; this proposal does not add an API contract.

## Notes

- evidence: `cargo test --lib idle_parallel_stop` passes 35/35 after acceptance repair (33 prior + 2 new scheduler cancellation-exit regressions).
- evidence: `cargo test --lib` passes 2868 tests, 0 failed (7 ignored heavy tests).
- evidence: `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings` exit 0.
- Acceptance-repair support files (each changed only to make the two open findings' declared behavior and proof true): `src/tui/queue.rs` adds `DynamicQueue::release_all_execution_handles`, the queue-side release primitive the `idle-stop-cancel-handle-release` cancellation exit calls; `src/parallel/mod.rs` and `src/parallel/builder.rs` add the `#[cfg(test)] merge_result_channel_override` seam (default `None`, no production behavior change) so the `idle-stop-cancel-pending-merge-drain` regression can leave a pending merge outstanding across cancellation with a channel double instead of a real detached merge task; `src/parallel/tests/executor.rs` only adds that field to five existing `ParallelExecutor` struct literals; `src/parallel/tests/idle_parallel_stop.rs` holds both new regressions registered by `src/parallel/tests/mod.rs`.
- Both new regressions were confirmed to fail with the two cancellation-exit calls removed (`registered_execution_count()` stayed 1; `Stopped` was emitted while the merge was pending) and to pass with them present, so they are genuine regressions rather than tautologies.
- The new parallel regressions run on real Tokio time rather than `start_paused`: the real scheduler loop shells out to git during `prepare_for_parallel`, and paused-time auto-advance fires the test timers instead of waiting for those child processes. Their waits are bounded and short (150 ms no-stop window, 500 ms event waits only reached on failure); both tests together finish in 0.50s, each well under the 1s default-suite limit.
- evidence: `cflx openspec validate fix-idle-parallel-stop-classification --strict` passes.
- Verification ownership: `idle-parallel-stop-local` (repository-local, change-blocking). Tasks 1 and 5 use unit-scoped evidence (pure classifier and reducer/`AppState` fixtures, no external boundaries); tasks 2, 3, 4, and 6 use in-process integration evidence over the TUI command/key channels and paused-time scheduler futures, matching their declared verification types.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-idle-parallel-stop-classification --archive-gate`

## Current Acceptance Follow-up
- attempt: 1
- [x] [idle-stop-cancel-handle-release] (major) Registered execution handles are not released on the cancellation abort path, leaving stale evidence that later misclassifies idle stops as force stops | evidence: src/parallel/orchestration.rs:326-335 drains aborted join results without calling handle_workspace_completion, and src/parallel/queue_state.rs:891-897 is the only unregister_kill_token site, so kill tokens registered at src/parallel/dispatch.rs:1524 survive a cancelled run and their done tokens never fire; DynamicQueue is created once per TUI session (src/tui/runner.rs:414) and reused across runs, so after force-stopping an active run the stale entries keep registered_execution_count()>0 (src/tui/queue.rs:204-207); collect_stop_activity_snapshot then reports ExecutionEvidence::Known{registered>0} and a later idle second Esc in the same session logs 'Force stopped' with no agent process, contradicting the parallel-execution delta clause 'registered execution-handle cleanup ... precede terminal stop', design.md 'released registered execution handles', and acceptance criteria 2 and 4 | required_changes: src/parallel/orchestration.rs — In the cancellation exit, release the registered execution handles for aborted in-flight changes (unregister their kill tokens and fire the done handshake via the dynamic queue) before the scheduler emits ParallelEvent::Stopped, so no stale positive execution evidence survives into later runs | verification: src/parallel/tests/mod.rs — Add an idle_parallel_stop-family regression asserting registered_execution_count() is 0 after the real scheduler loop exits through the cancellation branch with previously registered in-flight handles, before/at ParallelEvent::Stopped emission, under the 1s default-suite limit
  finding: {"evidence":["src/parallel/orchestration.rs:326-335 drains aborted join results without calling handle_workspace_completion, and src/parallel/queue_state.rs:891-897 is the only unregister_kill_token site, so kill tokens registered at src/parallel/dispatch.rs:1524 survive a cancelled run and their done tokens never fire","DynamicQueue is created once per TUI session (src/tui/runner.rs:414) and reused across runs, so after force-stopping an active run the stale entries keep registered_execution_count()>0 (src/tui/queue.rs:204-207); collect_stop_activity_snapshot then reports ExecutionEvidence::Known{registered>0} and a later idle second Esc in the same session logs 'Force stopped' with no agent process, contradicting the parallel-execution delta clause 'registered execution-handle cleanup ... precede terminal stop', design.md 'released registered execution handles', and acceptance criteria 2 and 4"],"id":"idle-stop-cancel-handle-release","required_changes":[{"description":"In the cancellation exit, release the registered execution handles for aborted in-flight changes (unregister their kill tokens and fire the done handshake via the dynamic queue) before the scheduler emits ParallelEvent::Stopped, so no stale positive execution evidence survives into later runs","file":"src/parallel/orchestration.rs"}],"severity":"major","summary":"Registered execution handles are not released on the cancellation abort path, leaving stale evidence that later misclassifies idle stops as force stops","verification":[{"description":"Add an idle_parallel_stop-family regression asserting registered_execution_count() is 0 after the real scheduler loop exits through the cancellation branch with previously registered in-flight handles, before/at ParallelEvent::Stopped emission, under the 1s default-suite limit","file":"src/parallel/tests/mod.rs"}]}
  evidence: src/parallel/orchestration.rs cancellation exit now calls release_execution_handles_after_cancellation() right after the aborted JoinSet is drained and before ParallelEvent::Stopped, releasing every registered per-change handle through the new DynamicQueue::release_all_execution_handles (src/tui/queue.rs), which fires each done handshake.
  evidence: src/parallel/tests/mod.rs registers `mod idle_parallel_stop`; its idle_parallel_stop_cancelled_scheduler_releases_registered_execution_handles drives the real scheduler loop, asserts registered_execution_count()==0 and done.is_cancelled() at the moment Stopped is received, passes in 0.5s with the other regression, and fails (left: 1, right: 0) when the release call is removed.
- [x] [idle-stop-cancel-pending-merge-drain] (major) Scheduler cancellation exit establishes terminal stop without receiving or safely bounding pending background merge/base-lane outcomes | evidence: src/parallel/orchestration.rs:310-336 cancellation branch aborts the workspace JoinSet, discards join results, clears in_flight, and breaks without draining merge_result_rx or waiting for pending_merge_count to reach 0; src/parallel/orchestration.rs:460-463 then sends ParallelEvent::Stopped and returns while post-archive merge tasks spawned by spawn_merge_task (src/parallel/queue_state.rs:966-1035, detached tokio::spawn) may still be mutating the base repository; their results are only consumed by the select arm at src/parallel/orchestration.rs:566-573, unreachable after the break; The frontend defers terminal stop for this case (ChangeArchived marks ActivityState::Resolving at src/orchestration/state.rs:1722-1725, so the snapshot reports shutdown work), but the scheduler's premature Stopped overrides the wait: 'Processing stopped' is displayed while the merge is in flight, violating the cli delta scenario '進行中background mergeを安全に停止する', the parallel-execution delta clause 'pending merge/base-lane result handling precede terminal stop', and design.md 'received or safely bounded pending merge/base-lane outcomes' | required_changes: src/parallel/orchestration.rs — In the cancellation exit, after aborting and draining workspace tasks, keep receiving merge_result_rx via handle_merge_result_with_tx until pending_merge_count reaches 0 (or a bounded deadline compatible with the outer 120s barrier escalates) before sending ParallelEvent::Stopped and returning, so terminal stop follows pending merge/base-lane result handling | verification: src/parallel/tests/mod.rs — Add (or route via a new module registered here) an idle_parallel_stop-family regression proving that cancelling the real scheduler loop with a pending background merge handles the merge result before ParallelEvent::Stopped is emitted, using paused-time/channel doubles and staying under the 1s default-suite limit
  finding: {"evidence":["src/parallel/orchestration.rs:310-336 cancellation branch aborts the workspace JoinSet, discards join results, clears in_flight, and breaks without draining merge_result_rx or waiting for pending_merge_count to reach 0","src/parallel/orchestration.rs:460-463 then sends ParallelEvent::Stopped and returns while post-archive merge tasks spawned by spawn_merge_task (src/parallel/queue_state.rs:966-1035, detached tokio::spawn) may still be mutating the base repository; their results are only consumed by the select arm at src/parallel/orchestration.rs:566-573, unreachable after the break","The frontend defers terminal stop for this case (ChangeArchived marks ActivityState::Resolving at src/orchestration/state.rs:1722-1725, so the snapshot reports shutdown work), but the scheduler's premature Stopped overrides the wait: 'Processing stopped' is displayed while the merge is in flight, violating the cli delta scenario '進行中background mergeを安全に停止する', the parallel-execution delta clause 'pending merge/base-lane result handling precede terminal stop', and design.md 'received or safely bounded pending merge/base-lane outcomes'"],"id":"idle-stop-cancel-pending-merge-drain","required_changes":[{"description":"In the cancellation exit, after aborting and draining workspace tasks, keep receiving merge_result_rx via handle_merge_result_with_tx until pending_merge_count reaches 0 (or a bounded deadline compatible with the outer 120s barrier escalates) before sending ParallelEvent::Stopped and returning, so terminal stop follows pending merge/base-lane result handling","file":"src/parallel/orchestration.rs"}],"severity":"major","summary":"Scheduler cancellation exit establishes terminal stop without receiving or safely bounding pending background merge/base-lane outcomes","verification":[{"description":"Add (or route via a new module registered here) an idle_parallel_stop-family regression proving that cancelling the real scheduler loop with a pending background merge handles the merge result before ParallelEvent::Stopped is emitted, using paused-time/channel doubles and staying under the 1s default-suite limit","file":"src/parallel/tests/mod.rs"}]}
  evidence: src/parallel/orchestration.rs cancellation exit now calls drain_pending_merge_results_after_cancellation(), which keeps receiving merge_result_rx via handle_merge_result_with_tx until pending_merge_count reaches 0 before ParallelEvent::Stopped, bounded by CANCELLATION_MERGE_DRAIN_DEADLINE (90s < the outer 120s barrier) with an operator-visible wait/escalation log.
  evidence: src/parallel/tests/mod.rs-registered idle_parallel_stop_cancelled_scheduler_handles_pending_merge_before_stopped uses a merge-channel double to hold one pending merge across cancellation, asserts no Stopped for 150ms, then that the injected result is handled (pending_merge_count==0) before Stopped; it fails without the drain because Stopped arrives immediately.
