## Implementation Tasks

- [ ] Task 1: Update inline verification parsing in `src/openspec_cmd.rs` so `(verification: ...)` is detected anywhere in a checkbox task line while preserving captured note text for repository-evidence and ownership checks. Completion condition: line-terminal notes, mid-line notes followed by completion-condition prose, and standalone indented `verification:` lines are all parsed through the same evidence validation path. (verification: unit - run `cargo test openspec_cmd --lib` with focused tests for `validate_tasks_content`)
- [ ] Task 2: Add a regression test matching the logged archive-gate failure shape: a behavior-bearing checkbox line containing `(verification: manual - ...repository path or command...)` followed by additional prose must pass `evidence=error` validation. Completion condition: the test fails on the current end-anchored regex and passes after the parser fix. (verification: unit - add a test in `src/openspec_cmd.rs` and run `cargo test test_validate_tasks_accepts_inline_verification_before_completion_prose --lib`)
- [ ] Task 3: Preserve existing negative coverage for missing, weak, and self-referential verification notes. Completion condition: tests continue to assert missing verification fails, weak `manual review` evidence warns/fails as configured, and same-change final validation checkbox still emits the targeted self-referential diagnostic. (verification: unit - run `cargo test test_validate_tasks_evidence_error test_validate_tasks_with_weak_verification test_rejects_self_referential_final_validation_checkbox --lib`)
- [ ] Task 4: Update canonical validator spec coverage for inline verification notes followed by completion-condition text. Completion condition: `openspec/specs/cflx-proposal-validation/spec.md` receives a scenario requiring this syntax to be accepted. (verification: manual - inspect `openspec/specs/cflx-proposal-validation/spec.md`, run `cflx openspec validate fix-inline-verification-note-parsing --strict --evidence warn`, and keep validator regression coverage in `cargo test openspec_cmd --lib`; manual is intentional spec-review coverage for canonical wording in `openspec/specs/`)
- [ ] Task 5: Run focused repository verification for the validator change. Completion condition: formatting and targeted validator tests pass, or any unrelated long-running/default-suite blocker is documented with exact command output. (verification: integration - run `cargo fmt --check`, `cargo test openspec_cmd --lib`, and `cflx openspec validate fix-inline-verification-note-parsing --archive-gate`)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-inline-verification-note-parsing --archive-gate`
