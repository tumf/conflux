## Implementation Tasks

- [x] Define a shared native task-section classifier in `src/openspec_cmd/validation.rs` for active tasks, narrative/non-task sections, and runtime-owned acceptance follow-up, and use it from both task counting and task validation; completion requires Final Validation, Implementation Blocker, Future Work, Out of Scope, Notes, and Acceptance Notes to share consistent behavior without weakening active bare-task detection (verification: unit - extend `src/openspec_cmd.rs` validator/count tests and run `cargo test openspec_cmd --lib` to cover active bare bullets, permitted narrative bullets, rejected narrative checkboxes, runtime follow-up, and section transitions).
- [x] Add a worktree-local task-format validation gate to the apply completion path before acceptance dispatch, returning invalid-task diagnostics to apply rather than consuming an acceptance attempt; completion requires an all-checked but malformed `tasks.md` to stay in apply and a valid file to preserve the existing acceptance handoff (verification: integration - extend `src/execution/apply.rs` tests and run `cargo test execution::apply --lib` with temporary workspaces and an acceptance invocation spy/counter).
- [x] Preserve pre-accept validation diagnostics as workspace-derived retry context for the next apply attempt without adding out-of-worktree workflow authority; completion requires the repair prompt to identify the failing `tasks.md` lines and a restart to derive the same next action from repository state (verification: integration - add restart and diagnostic handoff cases to the apply/agent prompt tests under `src/execution/apply.rs` and `src/agent/tests.rs`, then run `cargo test execution::apply agent::tests --lib`).
- [x] Align `src/agent/prompt.rs`, `skills/cflx-apply/SKILL.md`, its bundled reference, and proposal guidance with the canonical classifier, explicitly forbidding active-section top-level evidence bullets and preserving exact runtime-owned `  evidence:` syntax; completion requires no distributed guidance to describe Final Validation or Implementation Blocker formatting that native validation rejects (verification: unit - extend `src/agent/tests.rs` and `src/embedded_skills.rs` contract tests, then run `cargo test agent::tests embedded_skills --lib`).
- [x] Verify the complete default path and regression boundary; completion requires formatting, lint, validator/apply/prompt tests, and the default non-heavy suite to pass with no test exceeding the repository one-second policy unless marked heavy (verification: integration - run `cargo fmt --check`, `cargo clippy -- -D warnings`, and `cargo test`; retain command output as acceptance evidence).

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected command: `cflx openspec validate prevent-task-format-retry-loops --archive-gate`.
