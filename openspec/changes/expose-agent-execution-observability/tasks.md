## Implementation Tasks

- [ ] Add closed execution-observability DTOs and authenticated `GET /api/v2/execution-status` routing in `src/web/remote_control_api/dto.rs`, `reads.rs`, `mod.rs`, and OpenAPI declarations. Completion requires process and change execution state, current and completed phase, iteration, absolute UTC RFC 3339 boundaries, `observed_at`, latest activity, and latest structured log fields with nullable unknown evidence; no elapsed/age fields or relative-time text may exist. (verification: integration - add focused route/schema tests named `agent_execution_observability_status_*` and run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Implement one projection-owner read that coherently joins snapshot and retained structured logs without changing revision semantics. Completion requires exact-`change_id` latest-log selection, a process latest log, fixed-time test injection, `state_revision` stability across log-only updates, `event_sequence` advancement where applicable, and no fallback to persistent files, display strings, or substring matching. (verification: unit - extend `src/web/remote_control_api/tests/projection_tests.rs` and read tests with `agent_execution_observability_projection_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Add shared process-local lifecycle phase facts fed by typed execution events and consumed by execution status plus stop settlement. Completion requires closed current/last-completed phase transitions, scheduler liveness distinct from `has_active_work`, restart-empty behavior, and no use of logs, API records, or out-of-worktree state for workflow routing. (verification: integration - add event/projection tests covering apply, acceptance, archive, resolve, merge, push, stopping, completion, error, and unknown cases under `agent_execution_observability_phase_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Extend `OperatorOutcome::Dequeued`, application settlement, `ExecutionSummary`, `CommandOutcome`, and `CommandRecord` with a closed typed stop result captured after confirmed termination and final revalidation. Completion requires `cancelled_phase`, `last_completed_phase`, nullable Apply commit evidence, `effects_rolled_back=false`, presentation detail that explicitly denies rollback, and unchanged failure/no-dequeue semantics. (verification: integration - extend shared coordinator, executor, registry, and command tests with `agent_execution_observability_stop_result_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Add a managed-worktree Git evidence port for final Apply commit presence and OID at stop settlement. Completion requires repository identity and lifecycle relation to be verified rather than trusting task counts, display status, logs, or commit subject alone; unreadable or ambiguous evidence must produce null evidence without exposing branch or path fields or changing the dequeue decision. (verification: unit - add fake and temporary-repository cases under `agent_execution_observability_apply_commit_*` for present, absent, unrelated same-subject, ambiguous, and command-failure evidence; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Add deterministic stop race coverage using barriers or channels. Completion requires a test where the final Apply commit and Apply-completed transition occur before acceptance cancellation settles, proving the stored result identifies acceptance as cancelled, Apply as completed, the exact retained commit OID, and no rollback; companion cases must cover pre-commit Apply cancellation, archive/resolve cancellation, and unknown phase without short wall-clock correctness thresholds. (verification: integration - add `agent_execution_observability_apply_commit_stop_race` and table cases in coordinator/API tests; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Enforce the transport-independent privacy boundary and API log access contract. Completion requires UDS and TCP responses, capabilities, OpenAPI, execution status, command results, logs, and events to contain no persistent log path, workspace/repository path used as a log locator, or file URL; `/api/v2/logs` and live transports remain the only complete retained-log interfaces and the new route follows existing authentication/origin rules. (verification: integration - add authenticated/unauthenticated router cases and serialized-response privacy assertions under `agent_execution_observability_privacy_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [ ] Update canonical OpenAPI contract assertions and operator documentation for the new route, absolute-time contract, log access boundary, typed phase values, and typed command result. Completion requires `cflx openapi` and the live OpenAPI endpoint to expose matching schemas, generated contract tests to fail if any route/result/privacy field is omitted, and docs to instruct agents not to infer Apply commit absence from `display_status=applying` or generic command success. (verification: integration - extend `tests/openapi_contract_tests.rs`, `tests/openapi_cli_tests.rs`, and relevant tracked API docs with `agent_execution_observability` assertions; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate expose-agent-execution-observability --archive-gate`.

## Future Work

- Increasing retained API log history or adding structured pagination may be proposed separately if the existing 1000-entry ring is insufficient.
- A browser-specific relative-time presentation may format absolute API timestamps independently; it must not change the wire contract.
