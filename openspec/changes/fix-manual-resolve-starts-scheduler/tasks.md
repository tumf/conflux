## Implementation Tasks

- [ ] Expose scheduler liveness to `ResolveMerge` command handling. Completion condition: `src/tui/runner.rs` and `src/tui/command_handlers.rs` let `TuiCommand::ResolveMerge` distinguish a live orchestrator task from `None`/finished without relying on display state alone. (verification: unit - add or update `src/tui/command_handlers.rs` tests and run `cargo test tui::command_handlers::test_resolve_merge_scheduler_liveness_none_finished_live` or equivalent covering `orchestrator_handle: None`, finished handle, and live handle cases)

- [ ] Start a scheduler-owned run when manual resolve has no live scheduler. Completion condition: handling `TuiCommand::ResolveMerge` with no live scheduler records `ReducerCommand::ResolveMerge`, starts `run_orchestrator_parallel` or an equivalent scheduler-owned path, and returns a `JoinHandle` so `runner` tracks it. (verification: unit/integration - `cargo test tui::command_handlers::test_resolve_merge_starts_parallel_scheduler_when_idle` or equivalent focused test)

- [ ] Ensure manual resolve scheduler startup can consume `ResolveWait` without new apply/archive queue work. Completion condition: the scheduler started for manual resolve keeps reducer-owned `ResolveWait` intent intact and reaches `maybe_dispatch_resolve_wait_retry()` / `retry_deferred_merges()` even when no `not queued` changes are selected. (verification: integration - `cargo test parallel::tests::executor::test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work` plus a new TUI/orchestrator test for `ResolveWait` startup)

- [ ] Prevent duplicate scheduler spawns when one is already alive. Completion condition: handling `TuiCommand::ResolveMerge` with a live orchestrator task only records reducer intent and calls `DynamicQueue::notify_scheduler()`, with no second `run_orchestrator_parallel` task started. (verification: unit - add or update `src/tui/command_handlers.rs` test `cargo test tui::command_handlers::test_resolve_merge_notifies_live_scheduler_without_duplicate_spawn` or equivalent proving notification-only behavior)

- [ ] Make manual resolve logs truthful. Completion condition: `src/tui/command_handlers.rs` logs separate messages for `notified existing scheduler`, `started scheduler for manual resolve`, and failure/no scheduler cases, so logs do not claim execution will start when no scheduler exists. (verification: unit - assert emitted `LogEntry` messages in `src/tui/command_handlers.rs` tests, e.g. `cargo test tui::command_handlers::test_resolve_merge_logs_scheduler_start_or_notify_truthfully`)

- [ ] Verify real avacus pending scenario. Completion condition: running `cflx` in `/Users/tumf/wakumo/avacus/avacuscc-dbot`, pressing `M` on `refactor-agent-tools-error-handling`, and checking `/Users/tumf/.local/state/cflx/logs/avacuscc-dbot-f6307a82/2026-05-02.log` or current-date equivalent shows one of `Queue notification received`, `Retrying deferred merge`, `MergeStarted`, `ResolveStarted`, `MergeDeferred`, `ResolveFailed`, or `MergeCompleted` after the manual resolve command log. (verification: e2e - add/run `cargo test tui::command_handlers::test_resolve_merge_starts_parallel_scheduler_when_idle` and manually run `cflx` from `/Users/tumf/wakumo/avacus/avacuscc-dbot`, press `M`, then inspect `/Users/tumf/.local/state/cflx/logs/avacuscc-dbot-f6307a82/*.log` for the listed scheduler/retry lines)

- [ ] Run required Rust checks. Completion condition: formatting and targeted tests pass, and any test taking over 1 second is optimized or marked heavy per repository policy. (verification: integration - `cargo fmt --check`; targeted `cargo test` for affected TUI and parallel modules)

## Future Work

- If manual resolve startup requires a larger scheduler API split, create a separate refactor proposal for explicit scheduler command types instead of overloading `StartProcessing` and `ResolveMerge` command handling.
