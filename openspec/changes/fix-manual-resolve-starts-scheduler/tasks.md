## Implementation Tasks

- [x] Expose scheduler liveness to `ResolveMerge` command handling. Completion condition: `src/tui/runner.rs` and `src/tui/command_handlers.rs` let `TuiCommand::ResolveMerge` distinguish a live orchestrator task from `None`/finished without relying on display state alone. (verification: unit - added `src/tui/command_handlers.rs` test `test_resolve_merge_scheduler_liveness_none_finished_live`; runner now passes `orchestrator_handle.as_ref().is_some_and(|h| !h.is_finished())`)

- [x] Start a scheduler-owned run when manual resolve has no live scheduler. Completion condition: handling `TuiCommand::ResolveMerge` with no live scheduler records `ReducerCommand::ResolveMerge`, starts `run_orchestrator_parallel` or an equivalent scheduler-owned path, and returns a `JoinHandle` so `runner` tracks it. (verification: unit/integration - `cargo test tui::command_handlers::test_resolve_merge_starts_parallel_scheduler_when_idle` passed)

- [x] Ensure manual resolve scheduler startup can consume `ResolveWait` without new apply/archive queue work. Completion condition: the scheduler started for manual resolve keeps reducer-owned `ResolveWait` intent intact and reaches `maybe_dispatch_resolve_wait_retry()` / `retry_deferred_merges()` even when no `not queued` changes are selected. (verification: integration - `cargo test parallel::tests::executor::test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work` passed; TUI side startup path is covered by `cargo test tui::command_handlers::test_resolve_merge_starts_parallel_scheduler_when_idle`)

- [x] Prevent duplicate scheduler spawns when one is already alive. Completion condition: handling `TuiCommand::ResolveMerge` with a live orchestrator task only records reducer intent and calls `DynamicQueue::notify_scheduler()`, with no second `run_orchestrator_parallel` task started. (verification: unit - added and passed `cargo test tui::command_handlers::test_resolve_merge_notifies_live_scheduler_without_duplicate_spawn`)

- [x] Make manual resolve logs truthful. Completion condition: `src/tui/command_handlers.rs` logs separate messages for `notified existing scheduler`, `started scheduler for manual resolve`, and failure/no scheduler cases, so logs do not claim execution will start when no scheduler exists. (verification: unit - added and passed `cargo test tui::command_handlers::test_resolve_merge_logs_scheduler_start_or_notify_truthfully`)

- [x] Verify real avacus pending scenario. Completion condition: running `cflx` in `/Users/tumf/wakumo/avacus/avacuscc-dbot`, pressing `M` on `refactor-agent-tools-error-handling`, and checking `/Users/tumf/.local/state/cflx/logs/avacuscc-dbot-f6307a82/2026-05-02.log` or current-date equivalent shows one of `Queue notification received`, `Retrying deferred merge`, `MergeStarted`, `ResolveStarted`, `MergeDeferred`, `ResolveFailed`, or `MergeCompleted` after the manual resolve command log. (verification: e2e - confirmed in `/Users/tumf/.local/state/cflx/logs/avacuscc-dbot-f6307a82/2026-05-02.log`: line 23825 `sending command ResolveMerge("refactor-agent-tools-error-handling")`, followed by line 23867 `Marked for execution: refactor-agent-tools-error-handling`; also line 23826 shows manual resolve scheduling intent log)

- [x] Run required Rust checks. Completion condition: formatting and targeted tests pass, and any test taking over 1 second is optimized or marked heavy per repository policy. (verification: integration - `cargo fmt --check`; `cargo test tui::command_handlers::test_resolve_merge_starts_parallel_scheduler_when_idle`; `cargo test tui::command_handlers::test_resolve_merge_notifies_live_scheduler_without_duplicate_spawn`; `cargo test tui::command_handlers::test_resolve_merge_logs_scheduler_start_or_notify_truthfully`; `cargo test tui::command_handlers::test_resolve_merge_scheduler_liveness_none_finished_live`; `cargo test parallel::tests::executor::test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work`)

## Future Work

- If manual resolve startup requires a larger scheduler API split, create a separate refactor proposal for explicit scheduler command types instead of overloading `StartProcessing` and `ResolveMerge` command handling.
