## Implementation Tasks

- [ ] Add Unix-only `--web-unix-socket PATH` and `--no-web-unix-socket` to default TUI, `tui`, and `run` argument paths with mutual exclusion and resolution rules for explicit path, opt-out, or `${GIT_COMMON_DIR}/cflx-api.sock`; keep non-Unix builds TCP-only without advertising these flags. Completion requires parser/help/path tests for every invocation shape, linked and distinct repositories, non-Git explicit/opt-out/default cases, and non-Unix cfg compilation. (verification: unit - `cargo test --features web-monitoring --lib` and `cargo test --no-default-features` cover parsing/resolution and feature-disabled compilation; verification-id: unix-api-socket-tests)

- [ ] Implement Unix path validation for absolute valid-UTF-8 paths, platform `sun_path` limits, existing canonical parent, final-component symlink rejection, and parent-chain ownership/mode/sticky semantics that prevent replacement by another effective UID. Completion requires tests for trusted private paths, shared/group-writable Git directories, sticky directories, symlink components, relative/non-UTF-8/overlong paths, missing parent, and actionable explicit-path/opt-out guidance. (verification: unit - `cargo test --features web-monitoring --lib` exercises repository-local path fixtures; verification-id: unix-api-socket-tests)

- [ ] Add a fail-closed target-entry classifier in `src/web/` that refuses every pre-existing file, directory, symlink, reachable socket, and unreachable socket without removal. Completion requires filesystem tests proving all entries survive unchanged and errors provide manual verified-stale cleanup guidance; no connect failure may authorize unlink. (verification: unit/integration - `cargo test --features web-monitoring --lib` verifies preservation and concurrent-start behavior; verification-id: unix-api-socket-tests)

- [ ] Introduce the Unix listener lifecycle with a panic-safe restrictive umask guard during synchronous pre-task bind, verified mode `0600` before publication, retained device/inode identity, real HTTP service over the shared app, and identity-checked cleanup under the documented trusted-same-UID boundary. Completion requires umask-000 subprocess coverage, mode assertions, `/api/v2/health` over `UnixStream`, replacement-preserving cleanup, and restoration of the prior umask. (verification: integration - `cargo test --features web-monitoring --lib` exercises real UDS requests and permission fixtures; verification-id: unix-api-socket-tests)

- [ ] Refactor orchestration startup into read-only validation, fatal repository-lock acquisition, all-listener bind/permission verification, atomic endpoint publication, effectful upstream preparation, lifecycle adapter, then orchestration. Eliminate post-listener `process::exit` paths and route all errors through one server owner. Completion requires process tests proving bind/lock failure starts no upstream fixture, lifecycle adapter, AI subprocess, or orchestration and dual-bind rollback leaves no published endpoint or owned socket. (verification: e2e - `cargo test --features web-monitoring,heavy-tests --test run_exit_tests unix_socket_`; verification-id: unix-api-socket-tests)

- [ ] Retain `--web` and `web.enabled = true` as additional TCP/Web UI activation sharing the same router/state with UDS, while preserving non-loopback auth, auto-port discovery, allowed origins, accessible URL logging, and TUI QR state for TCP only. Completion requires dual-listener tests proving one instance/state over both transports and retained TCP port-in-use, auto-port, URL, config, and QR regressions. (verification: integration - `cargo test --features web-monitoring --lib`; verification-id: unix-api-socket-tests)

- [ ] Resolve one authentication configuration for every listener: keep health public; retain browser SSE `fetch()` and WebSocket header-only rules; enforce configured bearer auth over UDS/TCP; and fail closed when a selected token environment is missing, non-Unicode, or empty. Completion requires authenticated/unauthenticated UnixStream requests and retained HTTP/SSE/WebSocket/CORS tests. (verification: integration - `cargo test --features web-monitoring --lib`; verification-id: unix-api-socket-tests)

- [ ] Evolve repository-lock metadata to the exact ordered `api_endpoints[{transport,address}]` schema, dual-write `api_url` only for TCP, and implement legacy/new/mixed/malformed parsing with validation and deduplication. Keep endpoint data observational. Completion requires fixed JSON fixtures for old writer/new reader, new writer/old-reader degradation, UDS-only, dual transport, mixed duplicates, partial publication rollback, and conflict rendering. (verification: unit/integration - `cargo test --features web-monitoring --lib`; verification-id: unix-api-socket-tests)

- [ ] Implement coordinated bounded shutdown for success, run error, SIGINT, SIGTERM, TUI exit, startup rollback, and unexpected listener failure: cancel refresh/SSE/WebSocket producers, allow a fixed HTTP grace period, abort/await remaining tasks, clear metadata best-effort, and clean the identity-matching socket. Completion requires fast state-machine tests plus heavy process tests with active SSE/WS clients proving finite exit and replacement preservation. (verification: integration/e2e - `cargo test --features web-monitoring --lib` and `cargo test --features web-monitoring,heavy-tests --test run_exit_tests unix_socket_`; verification-id: unix-api-socket-tests)

- [ ] Update `README.md`, `AGENTS.md`, `docs/guides/WEBUI.md`, `docs/guides/WEBUI.ja.md`, and `docs/guides/USAGE.md` with default UDS, trusted-path refusal, manual stale cleanup, override, opt-out, `curl --unix-socket`, dual listeners, authentication, proxy usage, non-Unix behavior, and no direct browser UDS claim. Update `docs/test-coverage-mapping.md` for every added/modified scenario. Completion requires CLI help assertions and repository review against implemented flags and canonical specs. (verification: unit/manual - `cargo test --features web-monitoring --lib` verifies help; tracked docs and coverage mapping provide repository-verifiable operator/spec evidence; verification-id: unix-api-socket-tests)

- [ ] Run the complete repository-local gate and fix failures without adding automatic stale deletion, temporary fallback, daemon behavior, or durable workflow-control state. Completion requires `cargo fmt --check`, `cargo test`, `cargo test --features web-monitoring --lib`, `cargo test --features web-monitoring,heavy-tests --test run_exit_tests unix_socket_`, `cargo test --no-default-features`, `cargo clippy --all-targets --features web-monitoring -- -D warnings`, and `cargo clippy --all-targets --no-default-features -- -D warnings` to exit successfully; long process tests remain heavy while fast default coverage is retained. (verification: integration - execute `cargo fmt --check && cargo test && cargo test --features web-monitoring --lib && cargo test --features web-monitoring,heavy-tests --test run_exit_tests unix_socket_ && cargo test --no-default-features && cargo clippy --all-targets --features web-monitoring -- -D warnings && cargo clippy --all-targets --no-default-features -- -D warnings`; verification-id: unix-api-socket-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate add-default-unix-api-socket --archive-gate`.

## Future Work

- Reconsider automatic stale cleanup only if a platform-specific atomic endpoint lease/removal primitive can prove ownership without connect-probe inference.
- Add reverse-proxy configuration helpers only if repeated operator setup demonstrates a stable cross-proxy contract.
