## Implementation Tasks

- [ ] Replace the fixed 200-character `tool_result` textification cutoff in `src/stream_json_textifier.rs` with the existing shared operator-facing safety bound while retaining one semantic summary record, raw-JSON suppression, explicit truncation signaling, and existing tool-use body redaction. Completion requires a result above 200 characters to remain intact and an over-bound result to stay bounded and marked. (verification: unit - extend `src/stream_json_textifier.rs` tests and run `cargo test --lib stream_json_textifier`; verification-id: tui-log-width-regressions)
- [ ] Keep `src/tui/render.rs` Logs-panel rendering width-owned and multi-line: first-line capacity follows timestamp/header plus the current inner width, continuation lines consume the full inner width without indentation, and the retained first 200 characters are represented across wrapped/scrollable display lines on narrow terminals. Completion requires tests comparing narrow and wide buffers and reconstructing Unicode-safe wrapped content without a fixed 200-character ellipsis. (verification: unit - extend `src/tui/render.rs` wrap and TestBackend rendering tests and run `cargo test --lib tui::render::tests`; verification-id: tui-log-width-regressions)
- [ ] Preserve Changes-row previews as a separate strictly single-line policy that consumes the current remaining row width and truncates only there. Completion requires narrow/wide TestBackend cases proving wider rows reveal more retained content, narrow rows use an ellipsis, and no preview creates a continuation row. (verification: unit - extend both Select and Running list rendering cases in `src/tui/render.rs` and run `cargo test --lib tui::render::tests`; verification-id: tui-log-width-regressions)
- [ ] Add regression coverage for CJK and emoji display width, contextual timestamp/operation headers, wrapped display-line range calculation, newest-log visibility, and manual/automatic log scrolling so richer retained messages do not regress existing Logs-panel navigation. (verification: integration - focused state plus render tests under `cargo test --lib tui::render::tests` exercise full `LogEntry` to TestBackend output; verification-id: tui-log-width-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate fix-width-aware-tui-log-display --archive-gate`.
