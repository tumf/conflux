## Implementation Tasks

- [x] Update `skills/cflx-archive/SKILL.md` to state that archive mutation MUST be performed only by `cflx openspec archive <change_id> --yes` or, for tooling-only changes, `cflx openspec archive <change_id> --yes --skip-specs`; direct archive directory creation or movement with `mkdir`, `mv`, `git mv`, scripts, or equivalent filesystem writes is forbidden. (verification: manual - inspect the skill text and run a text scan confirming the prohibition and terminal-failure rule are present)
- [x] Update `skills/cflx-archive/references/cflx-archive.md` with the same supported-command-only contract and state that a failed CLI archive command is terminal for the archive agent, not a prompt to repair layout manually. (verification: manual - inspect the reference guidance and confirm it does not describe a manual fallback path)
- [x] Add archive layout validation for matching archive entries so valid layouts are `openspec/changes/archive/<change_id>/` and `openspec/changes/archive/YYYY-MM-DD-<change_id>/`, while nested entries such as `openspec/changes/archive/YYYY-MM-DD/<change_id>/` are invalid. (verification: unit - add or update tests near `src/execution/archive.rs` or a shared helper covering valid direct, valid dated, and invalid nested layouts)
- [x] Strengthen archive completion checks so completion requires the active change directory to be absent, a valid archive entry to exist, and no invalid nested archive entry to exist. (verification: unit - `cargo test execution::archive --lib` includes a regression where only `openspec/changes/archive/2026-07-09/<change_id>/tasks.md` exists and completion is not successful)
- [x] Make task progress/archive lookup report invalid archive layout explicitly when nested archive evidence exists for the requested change. (verification: unit - `cargo test task_parser --lib` includes a regression where lookup for a nested archived change returns an error mentioning invalid archive layout and the offending path)
- [x] Align native OpenSpec archived change resolution with the same layout rules so `cflx openspec show <change_id>` does not silently accept nested archive layout as a valid archived change. (verification: unit - tests for `src/openspec_cmd.rs` or related modules cover valid dated/direct archive resolution and invalid nested archive rejection)
- [x] Ensure archive finalization and resume paths do not create or accept an archive-success commit when archive layout validation fails. (verification: integration - extend existing parallel/archive tests to assert invalid nested layout produces terminal archive failure with explicit layout diagnostic rather than successful archive completion)
- [x] Run focused and final verification commands after implementation. (verification: integration - `cargo test execution::archive --lib`, `cargo test task_parser --lib`, relevant `openspec_cmd` tests, relevant parallel archive regression test, and `cflx openspec validate forbid-manual-archive-fallback --strict`)

## Final Validation

Archive validation itself remains the authoritative final OpenSpec gate.
Expected archive gate: `cflx openspec validate forbid-manual-archive-fallback --archive-gate`
