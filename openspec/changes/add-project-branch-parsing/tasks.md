## 1. 入力仕様とパース

- [ ] 1.1 `cflx project add` の引数をブランチ省略可能にする（確認: `cflx project add <url>` がパースできる）
- [ ] 1.2 URL から `/tree/<branch>` と `#<branch>` を抽出して正規化するヘルパーを追加する（確認: 正規化後の `remote_url` と `branch` がテストで一致）
- [ ] 1.3 引数ブランチがある場合は URL 内のブランチを無視する（確認: 両方指定時の優先順がテストで検証される）

## 2. デフォルトブランチ解決

- [ ] 2.1 ブランチ未指定時にデフォルトブランチを解決する処理を追加する（確認: 解決結果が `branch` として使用される）
- [ ] 2.2 デフォルトブランチ解決が失敗した場合は明示的なエラーを返す（確認: エラーが表示され、非 0 で終了する）
- [ ] 2.3 外部依存を避けるため、デフォルトブランチ解決をモック可能にする（確認: テストで固定値を返せる）

## 3. テスト

- [ ] 3.1 `/tree/<branch>` と `#<branch>` のパーステストを追加する（確認: 正規化結果が一致する）
- [ ] 3.2 ブランチ省略時にデフォルトブランチが使用されるテストを追加する（確認: モック解決値が使われる）
- [ ] 3.3 引数ブランチが URL ブランチを上書きするテストを追加する（確認: 引数が優先される）
