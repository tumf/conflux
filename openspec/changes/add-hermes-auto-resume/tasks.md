## Implementation Tasks

- [ ] Implement the Hermes `post_tool_call` plugin with exact enqueue-tool matching, strict admitted-envelope validation, request-scoped messaging target capture, and one execution-scoped `notify set` registration using a bounded direct argv. Completion: invalid envelopes or missing messaging context cause no subprocess/registration, while a valid admitted fixture produces the exact binding and target callback argv. (verification-id: hermes-auto-resume-example) (verification: unit - `cargo test --test hermes_auto_resume_example plugin_`)
- [ ] Implement the bounded Hermes callback and message composition with explicit `HOME`, `PATH`, and `HERMES_HOME`, fixed-argv `hermes send --quiet --to` delivery, mandatory automation marker, bounded event parsing, and no secret values in argv/output. Completion: a fake Hermes executable observes the expected environment, target, and body under a scrubbed environment. (verification-id: hermes-auto-resume-example) (verification: integration - `cargo test --test hermes_auto_resume_example callback_`)
- [ ] Add the optional example manifest and concise setup/security documentation covering plugin enablement, messaging-route capture, callback env scrubbing, pre-registration delivery testing, sink readback, repository verification after notification, and explicit non-goals. Completion: a deterministic repository test asserts the documented commands, variables, marker, and opt-in packaging boundary. (verification-id: hermes-auto-resume-example) (verification: unit - `cargo test --test hermes_auto_resume_example documentation_`)
- [ ] Add repository-local regression tests that run the Python reference code without external credentials or live services and keep the default targeted test under one second or mark only unavoidable slow cases heavy. Completion: all named success and fail-closed cases execute via the test binary. (verification-id: hermes-auto-resume-example) (verification: integration - `cargo test --test hermes_auto_resume_example`)

## Future Work

Consider a reusable Hermes ingress abstraction only if a supported non-messaging surface needs the same execution callback semantics.
