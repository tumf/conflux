## Implementation Tasks

- [x] Extend the existing `VerificationDeclaration` metadata with fixed execution-class and completion-role fields instead of adding a parallel completion-gate block. Preserve legacy declarations with migration warnings, reject unknown values, and reject inconsistent combinations such as post-integration or non-local verification marked change-blocking. (verification: unit - extend `src/openspec.rs` parser tests and run `cargo test openspec --lib`; verification-id: release-gate-dependency-validation)
- [x] Require each active implementation checkbox to reference a declared change-blocking verification ID, reject missing/unknown references and operational-observation references, and keep Future Work and narrative sections outside this requirement. Completion requires non-local acceptance written only as task prose to fail before apply. (verification: unit - extend task validation tests in `src/openspec_cmd.rs` and run `cargo test openspec_cmd --lib`; verification-id: release-gate-dependency-validation)
- [x] Extend native strict/archive validation to classify hazardous active dependency edges from valid structured verification declarations. Repository-local implementation dependents must fail on non-local change blockers with diagnostics naming dependent, target, verification ID, and split/remove remedy; observational release-to-release chains and ordinary local dependencies must remain valid. (verification: unit - extend `src/openspec_cmd.rs` and `src/openspec_cmd/validation.rs` tests and run `cargo test openspec_cmd --lib`; verification-id: release-gate-dependency-validation)
- [x] Define migration and error ownership behavior: malformed metadata is reported on the target proposal, dependent diagnostics reference the target error, legacy missing fields warn, and reverse-impact diagnostics list affected queued dependents when an active target gains a blocker. Enforcement must not abort already in-flight work and starts at the next dispatch eligibility decision. (verification: integration - add repository fixtures for target edits, queued/in-flight dependents, and diagnostic attribution under `src/openspec_cmd.rs` or `tests/`, then run `cargo test openspec_cmd --lib`; verification-id: release-gate-dependency-validation)
- [x] Add adversarial regression coverage reproducing the original 15-dependent fan-out plus valid local dependencies, invalid deployed-service and physical-device change blockers, observational release chains, legacy declarations, malformed metadata ownership, reverse-impact diagnostics, credentialed repository-automation boundary cases, and in-flight non-interruption. Completion requires hazardous declarations and queued graphs to fail before dispatch without changing scheduler dependency resolution. (verification: integration - native validator and scheduler-boundary fixtures run by `cargo test openspec_cmd --lib` and targeted parallel dependency tests; verification-id: release-gate-dependency-validation)
- [x] Update `skills/cflx-proposal/SKILL.md` to define dependency eligibility, forbid release sequencing through hard implementation dependencies, require reverse-impact review, reuse structured verification fields, and prescribe implementation/release-observation splitting. Completion requires a repository test to fail if bundled guidance omits these rules. (verification: unit - extend skill-content assertions and run `cargo test --lib`; verification-id: release-gate-dependency-validation)

## Future Work

- Existing downstream repositories may migrate structured verification fields incrementally; diagnostics provide the manual split path for already stuck graphs.
- Scheduler presentation may later surface a non-authoritative release-gate bottleneck hint without changing dispatch semantics.
- Automated proposal rewriting remains a separate migration tool if operational demand appears.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate. Expected archive gate: `cflx openspec validate prevent-release-gate-dependency-blocking --archive-gate`.

## Current Acceptance Follow-up
- attempt: 1
- [x] openspec/changes/prevent-release-gate-dependency-blocking/proposal.md frontmatter verifications[0] (execution_class/completion_role lines): installed cflx v0.6.194 that executes the archive gate rejects these keys with 'Unknown verification declaration key at index 0: execution_class / completion_role' (cflx openspec validate prevent-release-gate-dependency-blocking --archive-gate exits 1), so the archive step cannot succeed. Remove the execution_class: repository-local and completion_role: change-blocking lines from this proposal's own verifications entry to use the legacy declaration shape (verified: installed binary then passes with exit 0; repo-built validator passes with only the expected migration warning). Do not modify the new parser/validator code.
  evidence: Removed execution_class/completion_role from proposal.md verifications[0]; installed cflx v0.6.194 `--archive-gate` now exits 0, repo-built `--archive-gate` and `--strict` exit 0 with only the legacy migration warning, and `cargo test openspec_cmd --lib` (116 passed) plus `cargo test openspec --lib` (154 passed) pass with no parser/validator code changes.
