## Implementation Tasks

- [ ] In `src/client/mcp.rs`, replace `read_line` with a byte-bounded reader that terminates on oversized or invalid-UTF-8 frames before dispatch; add newline-free overflow tests in `tests/client_mcp_integration.rs`. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_mcp_integration --features heavy-tests`)
- [ ] In `src/web/completion_sink.rs`, replace `wait_with_output` with concurrent fixed-retention drains that continue discarding after overflow; timeout/cancellation must call kill and wait, while successful high-volume output exits normally with truncation diagnostics. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [ ] Require exact instance/execution/change binding for sink GET and return registered argv only over UDS; TCP GET exposes presence/history but not argv. Update OpenAPI and add omission/mismatch/TCP tests. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [ ] Create event files as `0400` in `0700`, remove the false immutability comment, never re-read payloads, and test default write refusal only under unprivileged EUID plus cleanup after reap. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [ ] Enforce JSON-RPC 2.0 and initialization-at-successful-`initialize` response, allow pre-init `ping`, idempotently accept `notifications/initialized`, reject batches, and update existing unit/integration tests to handshake. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_mcp_integration --features heavy-tests`)
- [ ] Keep delivery serialized; add test-injectable callback/shutdown deadlines and cancellation wiring that forbids new artifacts/deliveries after shutdown, kills/waits active children, and cleans only after reap. Verify ordering deterministically without performance thresholds. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [ ] Update capability/docs/comments in `src/web/remote_control_api/mod.rs`, `src/web/completion_sink.rs`, and client documentation for truncation, UDS-only argv disclosure, default-read-only event artifacts, and serialized shutdown; run format and clippy. (verification-id: core-mcp-sink-hardening) (verification: unit - `cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && python3 -c "from pathlib import Path; p=Path('src/web/completion_sink.rs').read_text(); assert 'immutable for the callback' not in p"`)
- [ ] Run focused and default regression suites and preserve existing execution identity, late registration, terminal classification, and callback failure behavior. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test`)

## Final Validation

Expected archive gate: `cflx openspec validate harden-client-mcp-completion-sinks --archive-gate`
