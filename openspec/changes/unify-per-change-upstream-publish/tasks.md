## Implementation Tasks

- [x] **Task 1: Normalize the upstream CLI contract across run and local TUI** in `src/cli.rs` and `src/main.rs`: expose `-u`, value-less `--integrate-upstream`, `--integrate-upstream=<remote>`, and `--upstream-verify-command` on bare local TUI and explicit `tui`; reuse the run option parser/configuration; reject `--push`, serial effective mode, remote-client `tui --server`, server orchestration, detached HEAD, non-Git, invalid remote/base, and empty verification command before mutation. (verification: unit - parser/startup cases named `per_change_upstream_*` prove run/bare-TUI/explicit-TUI equivalence, `origin` defaulting, explicit remote parsing, help exposure, default-off behavior, and every rejected combination; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 2: Wire optional upstream configuration through the local TUI parallel runtime** in `src/tui/runner.rs`, `src/tui/orchestrator.rs`, and the shared `ParallelRunService`/`ParallelExecutor` builder path so `run` and TUI construct one publication service; do not add TUI-specific Git operations or install upstream state for remote-client TUI. (verification: unit - constructor/runtime tests named `per_change_upstream_*` prove absent configuration installs nothing, local TUI and run install equivalent coordinators, persistent TUI reconstruction retains configuration, and remote-client TUI installs none; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 3: Refactor run-final upstream push into an idempotent change-scoped publication operation** in `src/upstream/coordinator.rs` and `src/parallel/upstream_lane.rs`: accept a completed change ID, reconcile remote, perform complete verification, native non-force porcelain push, confirm with `ls-remote`, and allow successive confirmed cumulative HEADs in one process without repeating an already confirmed HEAD; use the in-process confirmed-HEAD record only as an optimization after remote observation, never as restart or ambiguous-outcome routing authority. (verification: unit - scripted coordinator cases named `per_change_upstream_*` prove one publication per new cumulative HEAD, no duplicate confirmed push, multiple successive publications, no-op recovery only after remote-observed confirmation, restart/ambiguous-outcome re-observation, and retained existing race/repair bounds; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 4: Attach publication atomically to post-archive cumulative-base integration** in `src/parallel/merge.rs`: while the base lane remains owned, reconcile upstream before result integration, merge the archived change with recognizable publication-required trailers binding change ID/remote/branch, run `on_merged`, run full verification, publish and remotely confirm cumulative HEAD, then release the lane; keep independent worktree apply/acceptance concurrent while later base results wait. (verification: integration - executor tests named `per_change_upstream_*` prove durable opted-in trailer identity even without an upstream merge, exact merge/hook/verify/push/confirm ordering, lane serialization, later-result waiting, independent worktree progress, and absence of scheduler-drain dependency; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 5: Make `pushed` the opted-in change terminal state** through `src/events.rs`, `src/orchestration/state.rs`, TUI state/rendering, and web projections: do not finalize opted-in local integration as `merged`; emit change-scoped `PushStarted`/`PushCompleted` only for cumulative-base publication; transition to `TerminalState::Pushed` after remote confirmation; preserve `merged` for disabled cumulative merge and preserve existing `PushToRemote` semantics. (verification: unit - reducer and frontend projection cases named `per_change_upstream_*` prove enabled `merged` is non-terminal/internal, confirmed publication displays `pushed`, late success supersedes recoverable publish failure, final `pushed` cannot requeue, and disabled mode remains `merged`; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 6: Add resumable publication failure and explicit TUI retry routing** so fetch, merge, verification, push, and confirmation failures do not emit `PushCompleted`, do not redispatch apply/acceptance, preserve repository-derived unpublished evidence, block later base integration, and project exhausted persistent-TUI publication failure into existing Error-mode F5/local-web retry; successful retry resumes publication from current Git/archive/remote evidence and releases the lane only after confirmation, while operator stop ends orchestration without success. (verification: integration - executor/coordinator/TUI cases named `per_change_upstream_*` cover verification failure, non-repairable push failure, repairable repository mutation, remote race, Error-mode projection, F5 and local-web retry, waiting-result lane blocking and release, operator stop, interruption after local merge, interruption after push before confirmation, and successful retry without duplicate merge or confirmed success; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 7: Make startup and zero-change recovery honor durable publication identity** in `src/upstream/startup.rs`, coordinator recovery scanning, and reducer/frontend reconstruction: refuse option-less startup when a publication-required integration is not proven reachable from its bound remote branch, require matching `-u` plus a fresh verification command to resume it, recognize only explicit opted-in integration/upstream trailers for zero-change recovery, and never retroactively promote disabled-mode terminal `merged` changes. (verification: integration - startup/recovery cases named `per_change_upstream_*` prove option-less refusal before mutation, no recovered final `merged`, enabled restart publication-only dispatch, no-op for arbitrary first-parent history, zero-change recovery from explicit markers, and no retroactive Merged-to-Pushed transition; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 8: Align frontend completion semantics without duplicating orchestration logic**: finite `run` emits `AllCompleted` and exits only after all targeted successful changes are `pushed`; persistent local TUI publishes on each change completion, remains active, and can publish a later queue addition through a new cycle; cancelled or stalled publication reports no successful completion. (verification: integration - run service and TUI scheduler cases named `per_change_upstream_*` prove finite completion ordering, persistent multi-cycle publication, queue reuse, cancellation suppression, and shared service calls rather than frontend-specific Git operations; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] **Task 9: Add real-Git heavy E2E coverage** in `tests/e2e_git_worktree_tests.rs` using local bare remotes for run and local TUI parity, durable publication-required integration trailers, one and multiple changes, fresh remote advances, non-fast-forward races, failed verification, TUI explicit retry and lane release, push rejection, remote confirmation, option-less restart refusal, enabled restart recovery, zero-change recovery boundaries, and default-off `merged` behavior; keep all tests behind `heavy-tests`. (verification: e2e - `cargo test --features heavy-tests --test e2e_git_worktree_tests per_change_upstream` executes real Git/process boundaries and fails for stubbed publication, run-only wiring, missing crash evidence, premature `merged`, stuck TUI retry, or unconfirmed push success; verification-id: per-change-upstream-e2e)

## Verification Coverage Follow-up

- [x] Add an executor-level test that drives a full post-archive merge with upstream enabled and asserts the exact merge → `on_merged` → verification → push → confirmation call ordering, rather than relying on the straight-line ordering in `publish_base_integration` (verification: integration - executor case named `per_change_upstream_*` observes the emitted event sequence for one opted-in completed result; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] Add a TUI-level test that drives F5 / local web-control retry through `handle_start_processing_command` into publication resumption, instead of covering the reducer transition and the resumption entry point separately (verification: integration - TUI command-handler case named `per_change_upstream_*` asserts explicit retry reaches publication and creates no apply dispatch; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] Add a finite-run test that asserts `AllCompleted` is withheld until every targeted change reaches `pushed`, instead of covering the `finalize_upstream` gate at the coordinator level only (verification: integration - run-service case named `per_change_upstream_*` observes completion event ordering for an opted-in finite run; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)

## Defects Found By The Verification Follow-up

- [x] Fix `run_parallel_order_based_with_executor` short-circuiting an opted-in run with an empty change set: an archived, integrated, publication-required change is no longer an active change, so the TUI/server run path returned before reaching the executor's upstream boundary and no explicit retry could ever resume publication (`src/parallel_run_service.rs`). (verification: integration - `per_change_upstream_explicit_tui_retry_resumes_publication` fails without the fix; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] Pass the TUI's own repository root into `run_orchestrator_parallel` instead of re-deriving `std::env::current_dir()`, so the local TUI retry path is drivable end-to-end (`src/tui/orchestrator.rs`, `src/tui/command_handlers.rs`). (verification: integration - the TUI command-handler case runs against a temporary repository; run `cargo test per_change_upstream`; verification-id: per-change-upstream-unit)
- [x] Serialize `per_change_upstream_retry_resumes_publication_without_apply_dispatch` on the base-lane test mutex; it took the global merge lock without the guard and failed when another base-lane test ran concurrently (`src/parallel/tests/per_change_upstream.rs`). (verification: integration - `cargo test per_change_upstream` passes as a suite; verification-id: per-change-upstream-unit)

## Future Work

- Persistent project configuration for upstream integration after invocation-scoped behavior is proven.
- Remote-client TUI or server-owned upstream publication after an authenticated remote control contract can carry verification and remote configuration safely.
- Tiered verification or publication batching based on measured cost; this change intentionally preserves complete verification per published change.

## Acceptance Repair Notes (attempt 1)

Two changed files are not named by any finding's `required_changes`; each exists only to carry a declared change:

- `src/upstream/options.rs` — `acceptance-cli-short-u-equals-remote` required splitting `-u` into its own value-less clap argument. `selected_upstream_remote(short, long)` is the shared collapse of the two spellings back into one remote, so `run`, bare TUI, and explicit `tui` keep resolving through one normalization path instead of three copies of the same `or(DEFAULT)`.
- `src/parallel/tests/conflict.rs` — `acceptance-fresh-publication-resolvecompleted-terminal-merged` required a new `publication_owns_completion` field on `ResolveMergesWithRetryArgs`. The two existing struct-construction tests set it to `false` so they keep compiling and keep asserting the disabled-mode default.

Regression proof for the `ResolveCompleted` suppression: with `publication_owns_completion` forced back to `false` in `src/parallel/merge.rs`, `per_change_upstream_base_lane_orders_merge_hook_verification_and_publication` fails at the projected `ResolveCompleted { change_id: "alpha" }` event with `left: "merged", right: "merged"`; with the fix it passes.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate unify-per-change-upstream-publish --archive-gate`

Repository quality gates expected before acceptance: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test per_change_upstream`, `cargo test upstream_integration`, and `cargo test --features heavy-tests --test e2e_git_worktree_tests per_change_upstream`. Tests exceeding one second remain behind `heavy-tests` and do not enter the default suite.

## Current Acceptance Follow-up
- attempt: 1
- [ ] [acceptance-cli-short-u-equals-remote] (minor) -u=<remote> is accepted although the spec allows a named remote only as --integrate-upstream=<remote> | evidence: clap require_equals on the shared arg makes -u=upstream parse successfully on run, bare TUI, and explicit tui (probed against the built binary; src/cli.rs:125-133, 455-463, 604-612); specs/upstream-integration delta: 'A named remote MUST be accepted only as --integrate-upstream=<remote> with = required'; proposal acceptance criterion 2: 'explicit remotes require --integrate-upstream=<remote>' | required_changes: src/cli.rs — Make short -u strictly value-less (reject -u=<remote>) on all three entrypoints while keeping --integrate-upstream[=<remote>] behavior unchanged | verification: src/cli.rs — Parser cases named per_change_upstream_* assert -u=<remote> is rejected while --integrate-upstream=<remote> still parses on run, bare TUI, and explicit tui
  finding: {"evidence":["clap require_equals on the shared arg makes -u=upstream parse successfully on run, bare TUI, and explicit tui (probed against the built binary; src/cli.rs:125-133, 455-463, 604-612)","specs/upstream-integration delta: 'A named remote MUST be accepted only as --integrate-upstream=<remote> with = required'; proposal acceptance criterion 2: 'explicit remotes require --integrate-upstream=<remote>'"],"id":"acceptance-cli-short-u-equals-remote","required_changes":[{"description":"Make short -u strictly value-less (reject -u=<remote>) on all three entrypoints while keeping --integrate-upstream[=<remote>] behavior unchanged","file":"src/cli.rs"}],"severity":"minor","summary":"-u=<remote> is accepted although the spec allows a named remote only as --integrate-upstream=<remote>","verification":[{"description":"Parser cases named per_change_upstream_* assert -u=<remote> is rejected while --integrate-upstream=<remote> still parses on run, bare TUI, and explicit tui","file":"src/cli.rs"}]}
  evidence: src/cli.rs: -u is now a value-less short-only ArgAction::SetTrue arg (conflicts_with integrate_upstream) on Cli/RunArgs/TuiArgs and --integrate-upstream keeps long-only require_equals; upstream/options.rs selected_upstream_remote collapses both spellings; per_change_upstream_short_flag_never_carries_a_remote proves -u=upstream is a parse error on run/bare/explicit tui while --integrate-upstream=upstream still resolves, and per_change_upstream_short_flag_still_selects_the_default_remote proves -u still selects origin (cargo test --lib cli::tests: 108 passed)
- [ ] [acceptance-clippy-cloned-ref-lint] (major) The archive-blocking clippy gate fails on a lint in the new publication test file | evidence: cargo clippy --locked --all-targets --all-features -- -D warnings fails: error: this call to `clone` can be replaced with `std::slice::from_ref` at src/parallel/tests/per_change_upstream.rs:466 (clippy::cloned-ref-to-slice-refs implied by -D warnings); The prek pre-commit hook (.pre-commit-config.yaml clippy hook) runs the same command on every commit in this repository, so the final archive commit fails on it; A supplementary run with -A clippy::cloned-ref-to-slice-refs is otherwise clean, so this is the only lint blocker | required_changes: src/parallel/tests/per_change_upstream.rs — Replace `&[workspace_path.clone()]` at line 466 with std::slice::from_ref(&workspace_path) (or equivalent) so clippy -D warnings passes | verification: src/parallel/tests/per_change_upstream.rs — cargo clippy --locked --all-targets --all-features -- -D warnings exits 0 (the same command the prek pre-commit hook runs on the archive commit path)
  finding: {"evidence":["cargo clippy --locked --all-targets --all-features -- -D warnings fails: error: this call to `clone` can be replaced with `std::slice::from_ref` at src/parallel/tests/per_change_upstream.rs:466 (clippy::cloned-ref-to-slice-refs implied by -D warnings)","The prek pre-commit hook (.pre-commit-config.yaml clippy hook) runs the same command on every commit in this repository, so the final archive commit fails on it","A supplementary run with -A clippy::cloned-ref-to-slice-refs is otherwise clean, so this is the only lint blocker"],"id":"acceptance-clippy-cloned-ref-lint","required_changes":[{"description":"Replace `&[workspace_path.clone()]` at line 466 with std::slice::from_ref(&workspace_path) (or equivalent) so clippy -D warnings passes","file":"src/parallel/tests/per_change_upstream.rs"}],"severity":"major","summary":"The archive-blocking clippy gate fails on a lint in the new publication test file","verification":[{"description":"cargo clippy --locked --all-targets --all-features -- -D warnings exits 0 (the same command the prek pre-commit hook runs on the archive commit path)","file":"src/parallel/tests/per_change_upstream.rs"}]}
  evidence: src/parallel/tests/per_change_upstream.rs:466 now passes std::slice::from_ref(&workspace_path); cargo clippy --locked --all-targets --all-features -- -D warnings finished with exit 0 (the prek pre-commit hook's command)
- [ ] [acceptance-completion-without-change-scoped-publication] (major) A finite opted-in run can emit AllCompleted while the targeted marked change never receives change-scoped PushCompleted, and TUI explicit retry can strand publication | evidence: src/parallel/upstream_lane.rs:229-247 resume_pending_publications abandons resumption when global_merge_lock().try_lock() fails or one attempt errors, and it runs only once per run (src/parallel/orchestration.rs:269-274) with no later retry within the run; src/upstream/coordinator.rs finalize() counts a publication-required marker as has_explicit_recovery_evidence and publishes it through run_publication_cycle(None) with no change attribution and no ParallelEvent::PushCompleted, returning Completed; src/parallel/orchestration.rs:476-490 then emits AllCompleted, so the run reports completion while the reducer never transitions the marked change to pushed (also reachable after a transient resume failure followed by successful finalize recovery); cargo test per_change_upstream fails on exactly this: src/parallel_run_service.rs:1930 'the targeted change must reach confirmed publication' and src/tui/command_handlers.rs:1586 'explicit retry must reach publication rather than hang: Elapsed(())' (51 passed, 2 failed; both pass with --test-threads=1); Violates proposal acceptance criteria 7-8 and the upstream-integration delta: a finite run MUST emit AllCompleted only after every targeted successful change is remotely confirmed as pushed | required_changes: src/parallel/upstream_lane.rs — Make resume_pending_publications robust: wait for or re-attempt base-lane acquisition instead of silently skipping, and attribute resumed marker publications to their recorded change IDs with change-scoped events; src/parallel/orchestration.rs — Do not report run completion while a publication-required marker for a targeted change lacks change-scoped confirmation; route finalize-recovered marked publications through change-scoped PushCompleted | verification: src/parallel_run_service.rs — per_change_upstream_finite_run_completes_only_after_confirmation passes in the default multi-threaded suite and observes change-scoped PushCompleted before completion; src/tui/command_handlers.rs — per_change_upstream_explicit_tui_retry_resumes_publication passes in the default multi-threaded suite without approaching its hang timeout
  finding: {"evidence":["src/parallel/upstream_lane.rs:229-247 resume_pending_publications abandons resumption when global_merge_lock().try_lock() fails or one attempt errors, and it runs only once per run (src/parallel/orchestration.rs:269-274) with no later retry within the run","src/upstream/coordinator.rs finalize() counts a publication-required marker as has_explicit_recovery_evidence and publishes it through run_publication_cycle(None) with no change attribution and no ParallelEvent::PushCompleted, returning Completed; src/parallel/orchestration.rs:476-490 then emits AllCompleted, so the run reports completion while the reducer never transitions the marked change to pushed (also reachable after a transient resume failure followed by successful finalize recovery)","cargo test per_change_upstream fails on exactly this: src/parallel_run_service.rs:1930 'the targeted change must reach confirmed publication' and src/tui/command_handlers.rs:1586 'explicit retry must reach publication rather than hang: Elapsed(())' (51 passed, 2 failed; both pass with --test-threads=1)","Violates proposal acceptance criteria 7-8 and the upstream-integration delta: a finite run MUST emit AllCompleted only after every targeted successful change is remotely confirmed as pushed"],"id":"acceptance-completion-without-change-scoped-publication","required_changes":[{"description":"Make resume_pending_publications robust: wait for or re-attempt base-lane acquisition instead of silently skipping, and attribute resumed marker publications to their recorded change IDs with change-scoped events","file":"src/parallel/upstream_lane.rs"},{"description":"Do not report run completion while a publication-required marker for a targeted change lacks change-scoped confirmation; route finalize-recovered marked publications through change-scoped PushCompleted","file":"src/parallel/orchestration.rs"}],"severity":"major","summary":"A finite opted-in run can emit AllCompleted while the targeted marked change never receives change-scoped PushCompleted, and TUI explicit retry can strand publication","verification":[{"description":"per_change_upstream_finite_run_completes_only_after_confirmation passes in the default multi-threaded suite and observes change-scoped PushCompleted before completion","file":"src/parallel_run_service.rs"},{"description":"per_change_upstream_explicit_tui_retry_resumes_publication passes in the default multi-threaded suite without approaching its hang timeout","file":"src/tui/command_handlers.rs"}]}
  evidence: src/parallel/upstream_lane.rs resume_pending_publications now awaits the base lane instead of try_lock, retries transient base dirtiness up to 30x100ms, re-checks repository evidence after each attempt, attributes every attempt to its marker's change ID via publish_base_integration (change-scoped PushStarted/PushCompleted), and returns the still-unpublished IDs; src/parallel/orchestration.rs discharges markers under their own change IDs immediately before finalize_upstream and re-scans repository evidence after it, emitting Error and withholding AllCompleted while any marker survives; per_change_upstream_finite_run_completes_only_after_confirmation and per_change_upstream_explicit_tui_retry_resumes_publication now pass in the default multi-threaded suite (cargo test --lib per_change_upstream: 60 passed, 0 failed, 26.48s)
- [ ] [acceptance-fresh-publication-resolvecompleted-terminal-merged] (major) A fresh opted-in cumulative integration reaches terminal displayed merged because the git merge path's unconditional ResolveCompleted finalizes the reducer before publication | evidence: src/parallel/merge.rs:771-792 (fresh non-resume path) emits ResolveStarted, then merge_and_resolve routes every git-backend cumulative merge through conflict::resolve_merges_with_retry, which emits per-change ResolveCompleted on success (src/parallel/conflict.rs:902-912) with no upstream gating; src/orchestration/state.rs:1787-1798 maps ResolveCompleted with activity=Resolving to transition_change_to_merged (terminal Merged); PushCompleted only supersedes None|Error (can_success_supersede_terminal, src/orchestration/state.rs:259-261) so the change never becomes Pushed, and PushFailed is swallowed by the !rt.is_terminal() guard (src/orchestration/state.rs:1183-1189), leaving no recoverable Error for F5 retry; Shipped tests dodge the sequence: the reducer case per_change_upstream_local_integration_is_not_terminal_merged (src/orchestration/state.rs:5052) feeds only PushStarted, and the executor ordering test asserts only that MergeCompleted is absent; violates the orchestration-state delta (opted-in local merge remains non-terminal; PushCompleted becomes Pushed) and proposal acceptance criterion 4 | required_changes: src/parallel/merge.rs — Thread publication-owned completion into the cumulative merge/resolve path so a fresh opted-in integration does not emit the merged-finalizing per-change ResolveCompleted (mirror the existing MergeCompleted suppression); src/parallel/conflict.rs — Accept the suppression (or emit a non-finalizing status event instead) for the per-change ResolveCompleted emission on successful resolve when publication owns completion | verification: src/orchestration/state.rs — Reducer case named per_change_upstream_* feeds the real fresh enabled event sequence (ResolveStarted, the post-fix resolve events, PushStarted, then PushCompleted and separately PushFailed) and asserts the change is never displayed merged, becomes pushed on PushCompleted, and surfaces a recoverable error on PushFailed; src/parallel/tests/per_change_upstream.rs — The executor-level fresh-merge case projects every emitted ParallelEvent into the reducer and asserts final display status pushed, never merged
  finding: {"evidence":["src/parallel/merge.rs:771-792 (fresh non-resume path) emits ResolveStarted, then merge_and_resolve routes every git-backend cumulative merge through conflict::resolve_merges_with_retry, which emits per-change ResolveCompleted on success (src/parallel/conflict.rs:902-912) with no upstream gating","src/orchestration/state.rs:1787-1798 maps ResolveCompleted with activity=Resolving to transition_change_to_merged (terminal Merged); PushCompleted only supersedes None|Error (can_success_supersede_terminal, src/orchestration/state.rs:259-261) so the change never becomes Pushed, and PushFailed is swallowed by the !rt.is_terminal() guard (src/orchestration/state.rs:1183-1189), leaving no recoverable Error for F5 retry","Shipped tests dodge the sequence: the reducer case per_change_upstream_local_integration_is_not_terminal_merged (src/orchestration/state.rs:5052) feeds only PushStarted, and the executor ordering test asserts only that MergeCompleted is absent; violates the orchestration-state delta (opted-in local merge remains non-terminal; PushCompleted becomes Pushed) and proposal acceptance criterion 4"],"id":"acceptance-fresh-publication-resolvecompleted-terminal-merged","required_changes":[{"description":"Thread publication-owned completion into the cumulative merge/resolve path so a fresh opted-in integration does not emit the merged-finalizing per-change ResolveCompleted (mirror the existing MergeCompleted suppression)","file":"src/parallel/merge.rs"},{"description":"Accept the suppression (or emit a non-finalizing status event instead) for the per-change ResolveCompleted emission on successful resolve when publication owns completion","file":"src/parallel/conflict.rs"}],"severity":"major","summary":"A fresh opted-in cumulative integration reaches terminal displayed merged because the git merge path's unconditional ResolveCompleted finalizes the reducer before publication","verification":[{"description":"Reducer case named per_change_upstream_* feeds the real fresh enabled event sequence (ResolveStarted, the post-fix resolve events, PushStarted, then PushCompleted and separately PushFailed) and asserts the change is never displayed merged, becomes pushed on PushCompleted, and surfaces a recoverable error on PushFailed","file":"src/orchestration/state.rs"},{"description":"The executor-level fresh-merge case projects every emitted ParallelEvent into the reducer and asserts final display status pushed, never merged","file":"src/parallel/tests/per_change_upstream.rs"}]}
  evidence: src/parallel/conflict.rs ResolveMergesWithRetryArgs gained publication_owns_completion and routes all three per-change ResolveCompleted emissions through send_resolve_completed, which suppresses the merged-finalizing event when publication owns completion; src/parallel/merge.rs threads self.upstream_enabled() into that field; reducer cases per_change_upstream_fresh_integration_never_displays_merged and per_change_upstream_fresh_integration_failure_stays_recoverable feed MergeStarted/ResolveStarted/ConflictResolutionStarted/ConflictResolutionCompleted then PushStarted plus PushCompleted or PushFailed and assert never-merged, pushed on confirmation, and a retryable Error on failure; the executor case per_change_upstream_base_lane_orders_merge_hook_verification_and_publication now projects every emitted ParallelEvent into the real reducer and asserts final display status pushed, never merged — reverting the merge.rs flag to false makes it fail on the ResolveCompleted event with left/right "merged"
- [ ] [acceptance-suite-global-lock-test-mutex] (major) Two new publication tests take the process-global merge lock without the mandated base-lane test mutex, so the declared cargo test per_change_upstream gate fails in default multi-threaded runs | evidence: src/parallel/mod.rs:111-118 mandates merge_lock_test_mutex for tests contending on the process-global merge lock; per_change_upstream_finite_run_completes_only_after_confirmation (src/parallel_run_service.rs) and per_change_upstream_explicit_tui_retry_resumes_publication (src/tui/command_handlers.rs) do not hold it, unlike the sibling fix already recorded in tasks.md; Observed: cargo test per_change_upstream 51 passed / 2 failed (62.34s including a 60s hang); full default suite 2870 passed / 2 failed with the same two tests; both pass with --test-threads=1; tasks.md claims 'cargo test per_change_upstream passes as a suite', which current repository evidence contradicts (Constitution law 3: truthful completion) | required_changes: src/parallel_run_service.rs — Serialize the finite-run publication tests on the base-lane test mutex per the src/parallel/mod.rs convention; src/tui/command_handlers.rs — Serialize the explicit-retry publication test on the base-lane test mutex per the same convention | verification: src/parallel_run_service.rs — cargo test per_change_upstream passes as a full default multi-threaded suite; src/tui/command_handlers.rs — The retry test completes well under its timeout in repeated default multi-threaded suite runs
  finding: {"evidence":["src/parallel/mod.rs:111-118 mandates merge_lock_test_mutex for tests contending on the process-global merge lock; per_change_upstream_finite_run_completes_only_after_confirmation (src/parallel_run_service.rs) and per_change_upstream_explicit_tui_retry_resumes_publication (src/tui/command_handlers.rs) do not hold it, unlike the sibling fix already recorded in tasks.md","Observed: cargo test per_change_upstream 51 passed / 2 failed (62.34s including a 60s hang); full default suite 2870 passed / 2 failed with the same two tests; both pass with --test-threads=1","tasks.md claims 'cargo test per_change_upstream passes as a suite', which current repository evidence contradicts (Constitution law 3: truthful completion)"],"id":"acceptance-suite-global-lock-test-mutex","required_changes":[{"description":"Serialize the finite-run publication tests on the base-lane test mutex per the src/parallel/mod.rs convention","file":"src/parallel_run_service.rs"},{"description":"Serialize the explicit-retry publication test on the base-lane test mutex per the same convention","file":"src/tui/command_handlers.rs"}],"severity":"major","summary":"Two new publication tests take the process-global merge lock without the mandated base-lane test mutex, so the declared cargo test per_change_upstream gate fails in default multi-threaded runs","verification":[{"description":"cargo test per_change_upstream passes as a full default multi-threaded suite","file":"src/parallel_run_service.rs"},{"description":"The retry test completes well under its timeout in repeated default multi-threaded suite runs","file":"src/tui/command_handlers.rs"}]}
  evidence: src/parallel_run_service.rs both per_change_upstream finite-run tests and src/tui/command_handlers.rs per_change_upstream_explicit_tui_retry_resumes_publication now take crate::parallel::merge_lock_test_mutex() first per the src/parallel/mod.rs convention; cargo test --lib per_change_upstream passes as a full default multi-threaded suite (60 passed, 0 failed) and the retry test finishes far under its 60s timeout
- [ ] [acceptance-top-level-upstream-flags-dropped-before-subcommand] (minor) Top-level upstream options before an explicit subcommand (cflx -u run ...) are silently ignored instead of rejected, so an explicit opt-in can run and succeed in merged mode | evidence: Top-level Cli accepts -u/--integrate-upstream/--upstream-verify-command for bare TUI (src/cli.rs:125-139), but the Commands::Run and Commands::Tui dispatch branches (src/main.rs:707-712) never read them; probe: cflx -u run --all --parallel parses and runs with upstream disabled; specs/upstream-integration delta line 9 requires rejecting unsupported or incomplete invocation combinations before workspace mutation; silently dropping the opt-in contradicts the opted-in success contract in which publication is part of completion | required_changes: src/cli.rs — Provide a validation seam detecting top-level upstream options combined with an explicit subcommand; src/main.rs — Reject that combination with a clear diagnostic before any orchestration starts (or forward the options with proven equivalence) | verification: src/cli.rs — Case named per_change_upstream_* asserts cflx -u <subcommand> and cflx --upstream-verify-command <cmd> <subcommand> are rejected or proven forwarded, not silently dropped
  finding: {"evidence":["Top-level Cli accepts -u/--integrate-upstream/--upstream-verify-command for bare TUI (src/cli.rs:125-139), but the Commands::Run and Commands::Tui dispatch branches (src/main.rs:707-712) never read them; probe: cflx -u run --all --parallel parses and runs with upstream disabled","specs/upstream-integration delta line 9 requires rejecting unsupported or incomplete invocation combinations before workspace mutation; silently dropping the opt-in contradicts the opted-in success contract in which publication is part of completion"],"id":"acceptance-top-level-upstream-flags-dropped-before-subcommand","required_changes":[{"description":"Provide a validation seam detecting top-level upstream options combined with an explicit subcommand","file":"src/cli.rs"},{"description":"Reject that combination with a clear diagnostic before any orchestration starts (or forward the options with proven equivalence)","file":"src/main.rs"}],"severity":"minor","summary":"Top-level upstream options before an explicit subcommand (cflx -u run ...) are silently ignored instead of rejected, so an explicit opt-in can run and succeed in merged mode","verification":[{"description":"Case named per_change_upstream_* asserts cflx -u <subcommand> and cflx --upstream-verify-command <cmd> <subcommand> are rejected or proven forwarded, not silently dropped","file":"src/cli.rs"}]}
  evidence: src/cli.rs adds Cli::validate_upstream_option_placement plus TopLevelUpstreamOptionError, which names the option and the subcommand that would ignore it; src/main.rs calls it immediately after Cli::parse and exits 2 before any logging, orchestration, or workspace mutation; per_change_upstream_top_level_options_are_rejected_before_a_subcommand covers cflx -u run, cflx --integrate-upstream=upstream run, cflx --upstream-verify-command <cmd> run, and cflx -u tui, and per_change_upstream_top_level_options_stay_valid_for_bare_invocation keeps bare cflx -u accepted
- [ ] [acceptance-tui-serial-toggle-bypasses-publication-contract] (minor) The TUI parallel/serial toggle can move an opted-in session onto the serial dispatch branch, which carries no upstream runtime, silently reverting completions to merged semantics | evidence: toggle_parallel_mode is allowed in Select/Stopped states (src/orchestration/state.rs:1059) and the serial dispatch branch in src/tui/command_handlers.rs:301-323 carries no upstream_runtime, so later completions use merged terminal semantics despite -u; Startup rejects -u with serial effective mode (NotParallelMode, src/upstream/options.rs; cli delta: upstream integration SHALL be rejected for serial effective execution before work starts), but the runtime toggle bypasses that validation | required_changes: src/tui/command_handlers.rs — When an upstream runtime is installed, refuse serial toggle/dispatch (or carry the publication contract into it) so an opted-in session cannot silently revert to merged semantics | verification: src/tui/command_handlers.rs — Case named per_change_upstream_* asserts an opted-in TUI cannot dispatch serial work that abandons the publication contract
  finding: {"evidence":["toggle_parallel_mode is allowed in Select/Stopped states (src/orchestration/state.rs:1059) and the serial dispatch branch in src/tui/command_handlers.rs:301-323 carries no upstream_runtime, so later completions use merged terminal semantics despite -u","Startup rejects -u with serial effective mode (NotParallelMode, src/upstream/options.rs; cli delta: upstream integration SHALL be rejected for serial effective execution before work starts), but the runtime toggle bypasses that validation"],"id":"acceptance-tui-serial-toggle-bypasses-publication-contract","required_changes":[{"description":"When an upstream runtime is installed, refuse serial toggle/dispatch (or carry the publication contract into it) so an opted-in session cannot silently revert to merged semantics","file":"src/tui/command_handlers.rs"}],"severity":"minor","summary":"The TUI parallel/serial toggle can move an opted-in session onto the serial dispatch branch, which carries no upstream runtime, silently reverting completions to merged semantics","verification":[{"description":"Case named per_change_upstream_* asserts an opted-in TUI cannot dispatch serial work that abandons the publication contract","file":"src/tui/command_handlers.rs"}]}
  evidence: src/tui/command_handlers.rs handle_start_processing_command refuses dispatch (returns None, starts no orchestrator, sets an operator-visible warning) when an upstream runtime is installed and app.parallel_mode is false, so the runtime = toggle cannot route an opted-in session onto the upstream-less serial branch; per_change_upstream_serial_dispatch_is_refused_while_publication_is_owed toggles to serial in Select mode, asserts no JoinHandle, no cancellation token, no apply command, and a diagnostic naming -u/--integrate-upstream
- [ ] [acceptance-upstream-verify-command-help-stale] (minor) Help text for --upstream-verify-command claims it is ignored without -u, but it is actually rejected | evidence: Doc comments at src/cli.rs:137-139, 468-470, 616-618 say 'Required when upstream integration is enabled; ignored otherwise.'; Actual behavior rejects an orphan --upstream-verify-command with VerifyCommandWithoutOption (src/upstream/options.rs:188-193; parser tests src/cli.rs:1163-1177, 1335-1339) | required_changes: src/cli.rs — Correct the three help strings to state the option requires -u/--integrate-upstream and is rejected otherwise | verification: src/cli.rs — Help exposure test asserts the corrected wording for all three entrypoints
  finding: {"evidence":["Doc comments at src/cli.rs:137-139, 468-470, 616-618 say 'Required when upstream integration is enabled; ignored otherwise.'","Actual behavior rejects an orphan --upstream-verify-command with VerifyCommandWithoutOption (src/upstream/options.rs:188-193; parser tests src/cli.rs:1163-1177, 1335-1339)"],"id":"acceptance-upstream-verify-command-help-stale","required_changes":[{"description":"Correct the three help strings to state the option requires -u/--integrate-upstream and is rejected otherwise","file":"src/cli.rs"}],"severity":"minor","summary":"Help text for --upstream-verify-command claims it is ignored without -u, but it is actually rejected","verification":[{"description":"Help exposure test asserts the corrected wording for all three entrypoints","file":"src/cli.rs"}]}
  evidence: src/cli.rs replaced the three 'Required when upstream integration is enabled; ignored otherwise.' doc comments (Cli, RunArgs, TuiArgs) with 'Requires `-u`/`--integrate-upstream`; rejected without it.'; per_change_upstream_verify_command_help_states_it_is_rejected_alone renders long help for run, tui, and the top-level command and asserts the new wording is present and 'ignored otherwise' is gone
