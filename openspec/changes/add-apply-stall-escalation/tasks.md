## Implementation Tasks

- [ ] Add configuration support for empty-WIP escalation and diagnosis. Completion condition: `src/config/types.rs`, `src/config/defaults.rs`, and config validation/parsing tests support optional top-level `apply_escalation_command` / `apply_stall_diagnose_command` plus configurable escalation trigger/count settings under `stall_detection`, and invalid combinations (for example escalation starting at or after the final threshold) fail config validation. (verification: unit - `src/config/mod.rs` tests cover defaults, valid custom values, and invalid boundary cases for the new config keys)
- [ ] Switch late empty-WIP apply retries to the escalation command. Completion condition: the canonical apply loop in `src/execution/apply.rs` tracks consecutive empty WIP commits per change/run, continues using `apply_command` before the configured trigger, and replaces subsequent eligible retries with `apply_escalation_command` for at most the configured number of uses before final stall handling. (verification: integration - targeted apply-loop tests under `src/execution/apply.rs` assert command selection transitions from base apply to escalation at the configured retry counts)
- [ ] Run stall diagnosis once before final empty-WIP stall classification. Completion condition: when the empty-WIP threshold is reached after exhaustion of allowed escalation retries, the runtime executes `apply_stall_diagnose_command` exactly once, captures its output as diagnostic evidence/logging, and then preserves the original empty-WIP stall as the primary failure outcome. (verification: integration - targeted apply-loop tests under `src/execution/apply.rs` assert single diagnose execution, preserved stall root cause text, and warning-only degradation when the diagnose command itself fails)
- [ ] Preserve existing no-config behavior and constitutional boundaries. Completion condition: when neither new command is configured, the apply loop behaves exactly as today; escalation bookkeeping remains runtime-ephemeral and is not used as durable resume-routing state. (verification: integration - targeted apply-loop tests under `src/execution/apply.rs` confirm existing threshold-based stall behavior is unchanged without new config; unit - `src/orchestration/state.rs` and related resume-routing tests continue to require only workspace/git/base-tree evidence)
- [ ] Document and verify observable operator behavior. Completion condition: logs/events clearly state when escalation starts, which retries use escalation, when diagnosis runs, and what stall outcome is finalized, without silently hiding the original apply context. (verification: integration - event/log assertions in targeted apply-loop tests under `src/execution/apply.rs` verify escalation-start, diagnose-start/completion, and final stall-report messages)
- [ ] Run proposal and repository verification. Completion condition: OpenSpec validation passes and targeted Rust tests for config parsing, apply escalation routing, and diagnose-before-stall behavior succeed. (verification: manual - run `cflx openspec validate add-apply-stall-escalation --strict --evidence warn`, `cargo fmt --all -- --check`, and targeted Rust test commands covering `src/config/mod.rs` and `src/execution/apply.rs`)

## Future Work

- Consider a follow-up change if the project later wants empty-WIP stall to surface as non-terminal `stalled` instead of the current final stall error semantics.
- Consider provider-specific template presets for “deep reasoning” escalation commands, but keep those presets outside core runtime policy.
