## Implementation Tasks

- [ ] Update dependency-blocked TUI log handling in `src/tui/state/event_handlers/refresh.rs` so repeated blocked→blocked events update display state without appending duplicate blocked logs. (verification: unit - add or update AppState/event-handler tests in `src/tui/state.rs` or `src/tui/state/event_handlers/*` that call `DependencyBlocked` twice for the same change and assert only one `Change '<id>' blocked by dependencies` log is appended; completion condition: test fails on current unconditional logging and passes after the handler change)
- [ ] Preserve fresh blocked logging after an intervening dependency resolution or unblocked state in `src/tui/state/event_handlers/refresh.rs`. (verification: unit - add a regression test in `src/tui/state.rs` or `src/tui/state/event_handlers/*` that applies `DependencyBlocked`, `DependencyResolved`, then `DependencyBlocked` again and asserts two blocked log entries plus final blocked display status; completion condition: test demonstrates dedupe does not permanently mute the change)
- [ ] Update analysis-started TUI log handling in `src/tui/state/event_handlers/processing.rs` so consecutive unchanged `remaining_changes` values do not append duplicate `Re-analyzing queued changes for dispatch` logs. (verification: unit - add or update AppState/event-handler tests in `src/tui/state.rs` or `src/tui/state/event_handlers/*` applying repeated `AnalysisStarted { remaining_changes: 1 }` and asserting one analysis log; completion condition: test fails on current unconditional logging and passes after dedupe)
- [ ] Reset analysis-started suppression on meaningful progress or changed analysis state in the relevant `src/tui/state/event_handlers/*.rs` handlers so new useful diagnostics remain visible. (verification: unit - add a test in `src/tui/state.rs` or `src/tui/state/event_handlers/*` proving either a different `remaining_changes` value or an intervening progress/reset event allows a fresh analysis log; completion condition: test proves suppression is bounded to unchanged consecutive no-progress state)
- [ ] Ensure log dedupe state remains TUI-local observability state and is not used by scheduler, reducer, dependency resolution, archive routing, or next-action decisions. (verification: integration - inspect `src/tui/state*`, `src/parallel/queue_state.rs`, and `src/orchestration/state.rs`, then run existing tests covering `DependencyBlocked`, `DependencyResolved`, and parallel queue analysis such as `cargo test dependency_blocked` or the nearest repository-supported filtered cargo test; completion condition: no production scheduling branch reads TUI log-dedupe state)
- [ ] Run repository verification for the touched Rust code and default gates. (verification: integration - run repository-configured commands from `Cargo.toml`, `Makefile`, or project docs, expected at minimum `cargo test` or a narrower affected equivalent plus any configured lint/typecheck command such as `cargo clippy`/`cargo fmt --check`; completion condition: commands pass or failures are documented with exact output and rationale)

## Future Work

- If Web UI logs show the same pattern independently, create a separate scoped proposal for Web UI log presentation dedupe.
- Optional summary/rate-limit UX, such as `suppressed N repeated dependency-blocked logs`, can be proposed separately after the minimal dedupe fix lands.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-tui-dependency-blocked-log-spam --archive-gate`.
