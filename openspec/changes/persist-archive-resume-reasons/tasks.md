## Implementation Tasks

- [x] 1. `src/history.rs`, `src/agent/history_ops.rs`, `src/orchestration/archive.rs`, `src/parallel/executor.rs` に archive attempt reason enum / structured field を実装し、primary archive failure reason を型で保持する (verification: unit - archive history formatting / attempt recording tests が verification failure, prerequisite blocker, command failure, stall を別 reason として保持することを確認する)
- [x] 2. `src/orchestration` / `src/parallel` 配下に worktree 外 durable archive resume state の保存先・読込 helper・保持項目（change_id, revision, attempt, status, primary_reason, summary, updated_at）を実装する (verification: unit - state roundtrip tests が保存/復元/削除と revision mismatch handling を確認する)
- [x] 3. `src/execution/state.rs` と `src/parallel/dispatch.rs` に `Archiving`/`Archived` 判定時の durable archive state 参照ロジックを実装し、reason-aware handoff / retry context を生成する（`Archived` terminal merge handoff は維持） (verification: integration - workspace resume tests が `Archiving` resume で previous archive reason を復元し、`Archived` workspace は引き続き merge handoff のみへ進むことを確認する)
- [x] 4. `src/parallel/executor.rs`, `src/orchestration/archive.rs`, `src/events.rs` に archive retry / resume / failure event の structured reason + summary 伝播を実装する (verification: integration - executor / event bridge tests が archive retry scheduled, archive resumed, archive failed の各経路で reason-aware log/event payload を確認する)
- [x] 5. `src/orchestration/archive.rs` と `src/agent/history_ops.rs` に restart 後の prior reason 復元（archive history 復元または durable state injection）を実装する (verification: unit/integration - simulated restart test が prior archive reason を失わず次回 archive prompt/context に含めることを確認する)
- [x] 6. `src/orchestration/archive.rs` / `src/events.rs` の archive reason taxonomy 定義と整合するように `openspec/changes/persist-archive-resume-reasons/design.md` と `openspec/specs/{parallel-execution,orchestration-events}/spec.md` を更新し、`align-archive-readiness-failure-reporting` と矛盾しない resume observability を明文化する (verification: unit/manual - taxonomy mapping test assertions と proposal/design/spec を照合し、root-cause surfacing と durable persistence の責務境界が重複せず補完関係になっていることをレビューする)
- [x] 7. `src/parallel/executor.rs`, `src/orchestration/archive.rs`, `src/events.rs`, `src/agent/history_ops.rs` の archive reason persistence 実装に対応するテスト・検証コードパスを更新し、verification（`cflx openspec validate persist-archive-resume-reasons --strict --evidence warn`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings`）で回帰がないことを確認する
- [x] 8. `src/parallel/dispatch.rs` と `src/execution/state.rs` の archive resume/runtime handoff 判定実装（`Archiving`/`Archived` 分岐と durable reason 読込）を更新し、再起動後の archive 継続時に実装コード経由で reason-aware context が再構築されることを integration test で確認する
