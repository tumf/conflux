## Implementation Tasks

- [x] Introduce one per-change active-run Apply-attempt counter shared by serial CLI, TUI, and parallel execution across every `execute_apply_loop` entry, including Acceptance FAIL-to-Apply re-entry, ordinary command recovery, task-format repair, escalation, and final-commit repair. Remove/delegate independent workflow-loop Apply ceilings and make parallel initial-iteration plumbing carry this owner or disappear. Completion requires cross-adapter tests proving one cumulative count, per-change isolation, restart reset, and command-queue attempts excluded. (verification: integration - `cargo test --lib orchestrator && cargo test --lib tui::orchestrator && cargo test --lib serial_run_service && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Make the shared owner enforce positive `max_iterations` before dispatch, emit the 80% warning once at the threshold, and return a typed `iteration_limit` outcome with exact cumulative attempts and latest actionable diagnosis. Wire CLI, TUI, and parallel run boundaries so existing `on_finish` receives `status = iteration_limit` and the exact count once. Completion requires exact-ceiling/no-extra-dispatch, warning cardinality, all-boundary propagation, and hook cardinality tests. (verification: integration - `cargo test --lib execution::apply && cargo test --lib orchestrator && cargo test --lib tui::orchestrator && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Update `src/execution/apply.rs` so an ordinary non-zero Apply result records existing bounded `ApplyHistory`, runs `on_error` once, then reaches fresh task/Git inspection, completion and blocked/rejecting routing, permission classification, progress/WIP/stall accounting, and existing post-hook eligibility before redispatch. Completion requires tests proving diagnostic injection, owned-outcome preservation, no success-style `post_apply` after ordinary failure, and no-progress failures reaching stalled termination with `max_iterations = 0`. (verification: integration - `cargo test --lib execution::apply`; verification-id: workflow-command-recovery-tests)

- [x] Add the shared Acceptance command-failure decision in `src/orchestration/acceptance.rs`, fixed at two retries after the initial failure and independent from missing-verdict/protocol, explicit-CONTINUE, and FAIL-to-Apply counters. Reset it before routing every completed non-command-failure result. Completion requires table-driven retry/exhaustion/reset tests including `CommandFailed → MissingVerdict → CommandFailed`, canonical outcomes, malformed protocol output, cancellation, bounded latest evidence, and no fourth attempt. (verification: unit - `cargo test --lib orchestration::acceptance`; verification-id: workflow-command-recovery-tests)

- [x] Extend `src/agent/runner.rs` and `src/agent/prompt.rs` with latest-only Acceptance command-recovery context separate from canonical/protocol history, and wire `src/parallel/executor.rs` plus serial execution to set and clear it at the shared reset boundary. Completion requires prompt tests proving bounded untrusted evidence, fresh-verdict instruction, failed verdict-like text cannot route or append findings, and all-attempt history is not replayed. (verification: unit/integration - `cargo test --lib agent::prompt && cargo test --lib parallel::executor::tests && cargo test --lib serial_run_service`; verification-id: workflow-command-recovery-tests)

- [x] Rewire `src/serial_run_service.rs` and `src/parallel/dispatch.rs` to consume the shared Acceptance policy and rerun only Acceptance against the same applied, clean workspace. Completion requires invocation-count tests proving no Apply/cleanup-review rerun, no FAIL task append, no unrelated counter consumption, no outer cycle increment, and unchanged routing after any completed non-command-failure result. (verification: integration - `cargo test --lib serial_run_service && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Refactor post-Apply cleanup-review in `src/parallel/executor.rs` into at most three operation attempts above command-queue transport retries, carrying only the latest failure kind, exit code, existing bounded stdout/stderr tails, marker count, and fresh bounded porcelain status. Completion requires command-failure, marker-missing/duplicate, dirty-remains, status-inspection, latest-only context, second-attempt recovery, and exact exhaustion tests. (verification: unit/integration - `cargo test --lib parallel::executor::tests && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Enforce cleanup-review's ordered command, exactly-one-marker, and fresh-clean-worktree gate; observe per-change cancellation during stream and status wait; and route classified cleanup permission denial immediately to the existing non-terminal permission hold with no corrective attempt or generic counter increment. Completion requires marker-only, clean-only, duplicate-marker, cancellation child termination, immediate permission hold, zero retry/count on permission, and preserved dirty workspace on exhaustion. (verification: integration - `cargo test --lib parallel::executor::tests && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Extend cleanup prompt construction and `skills/cflx-cleanup-review/SKILL.md` with trusted corrective action plus delimited untrusted latest diagnostics while retaining blind-staging prohibition and immutable success ownership. Completion requires prompt tests for existing bounded-tail helper use, failure/status fields, injection resistance, no session/report dependency, and no marker or clean-state relaxation. (verification: unit - `cargo test --lib agent::prompt`; verification-id: workflow-command-recovery-tests)

- [x] Preserve every existing managed-worktree cleanup handoff while adding recovery: dirty completion triggers cleanup, clean completion skips it, task-complete and blocked-handoff grace termination remain finite, and incomplete Apply never receives success-equivalent treatment. Add restart/artifact regressions proving all new counters are process-local and fresh routing uses only workspace/Git evidence. (verification: integration - `cargo test --lib execution::apply && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [x] Run the complete repository-local gate and resolve formatting, lint, compile, and targeted test failures without broadening Archive, command-queue, verdict, or frontend semantics. Completion requires every declared command to exit successfully; default tests remain under one second, while unavoidable process-boundary tests use `heavy-tests` and retain fast default state-machine coverage. (verification: integration - `cargo test --lib execution::apply && cargo test --lib orchestration::acceptance && cargo test --lib orchestrator && cargo test --lib tui::orchestrator && cargo test --lib serial_run_service && cargo test --lib parallel::executor::tests && cargo test --lib parallel::tests::executor && cargo test --lib agent::prompt && cargo fmt --check && cargo clippy -- -D warnings`; verification-id: workflow-command-recovery-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate recover-workflow-command-failures --archive-gate`.

## Notes

- evidence: the complete declared gate passed in one run — `execution::apply` 67 passed, `orchestration::acceptance` 55, `orchestrator` 56, `tui::orchestrator` 24, `serial_run_service` 55, `parallel::executor::tests` 19, `parallel::tests::executor` 127, `agent::prompt` 41, plus `cargo fmt --check` and `cargo clippy -- -D warnings` clean.
- evidence: the cleanup-review recovery tests are nested as `parallel::executor::tests::cleanup_review_recovery` so the declared `cargo test --lib parallel::executor::tests` command actually covers them.
- evidence: acceptance-repair round 1 re-ran the complete declared gate green — `execution::apply` 74 passed, `orchestration::acceptance` 57, `orchestrator` 58, `tui::orchestrator` 24, `serial_run_service` 60, `parallel::executor::tests` 24, `parallel::tests::executor` 129 (1 ignored), `agent::prompt` 42, plus `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` clean.
- evidence: no `heavy-tests` gating was needed — on an unloaded machine each new cleanup-review test runs in 0.40-0.85s and the whole `parallel::executor::tests` module in 2.58s. Multi-second readings observed earlier came from concurrent cargo builds in sibling worktrees contending for the shared target directory, not from the tests themselves.

## Future Work

- Evaluate Archive operation-level recovery separately if concrete logs show repository-local evidence can distinguish recoverable command crashes from unsafe partial archive state.
- Consider configurable operation retry bounds only if fixed safety bounds prove operationally insufficient and a stable cross-operation configuration contract is justified.

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-command-history-isolation] (major) Acceptance command failures pollute canonical Acceptance history | evidence: src/orchestration/acceptance.rs:1694-1715 records failed command output as AcceptanceAttempt; src/parallel/executor.rs:1981-1997 does the same in parallel despite dedicated latest-only recovery context | required_changes: src/orchestration/acceptance.rs — Keep serial command-failure diagnostics out of canonical and protocol Acceptance history; src/parallel/executor.rs — Keep parallel command-failure diagnostics out of canonical history and previous-findings context | verification: src/agent/prompt.rs — Verify verdict-like failed output appears exactly once only in the dedicated untrusted command-recovery block; src/serial_run_service.rs — Verify serial command failures do not affect canonical attempt history; src/parallel/tests/executor.rs — Verify parallel command failures do not affect canonical attempt history
  finding: {"evidence":["src/orchestration/acceptance.rs:1694-1715 records failed command output as AcceptanceAttempt","src/parallel/executor.rs:1981-1997 does the same in parallel despite dedicated latest-only recovery context"],"id":"acceptance-command-history-isolation","required_changes":[{"description":"Keep serial command-failure diagnostics out of canonical and protocol Acceptance history","file":"src/orchestration/acceptance.rs"},{"description":"Keep parallel command-failure diagnostics out of canonical history and previous-findings context","file":"src/parallel/executor.rs"}],"severity":"major","summary":"Acceptance command failures pollute canonical Acceptance history","verification":[{"description":"Verify verdict-like failed output appears exactly once only in the dedicated untrusted command-recovery block","file":"src/agent/prompt.rs"},{"description":"Verify serial command failures do not affect canonical attempt history","file":"src/serial_run_service.rs"},{"description":"Verify parallel command failures do not affect canonical attempt history","file":"src/parallel/tests/executor.rs"}]}
  evidence: src/orchestration/acceptance.rs and src/parallel/executor.rs no longer build an AcceptanceAttempt for a failed command (both return attempt number 0); agent::prompt failed_command_output_appears_once_and_only_inside_the_recovery_block, serial_run_service a_failed_acceptance_command_records_no_canonical_attempt, and parallel::tests::executor test_acceptance_command_failure_does_not_create_acceptance_report all pass
- [x] [acceptance-exhaustion-complete-diagnostics] (minor) Acceptance exhaustion drops stdout whenever stderr is present | evidence: src/orchestration/acceptance.rs:274-283 uses else-if between stderr and stdout; src/orchestration/acceptance.rs:373-383 builds the terminal error only from that incomplete summary | required_changes: src/orchestration/acceptance.rs — Include independently bounded stdout and stderr in terminal command-recovery diagnostics | verification: src/orchestration/acceptance.rs — Test exhaustion output includes exit code plus both distinct bounded stdout and stderr tails
  finding: {"evidence":["src/orchestration/acceptance.rs:274-283 uses else-if between stderr and stdout","src/orchestration/acceptance.rs:373-383 builds the terminal error only from that incomplete summary"],"id":"acceptance-exhaustion-complete-diagnostics","required_changes":[{"description":"Include independently bounded stdout and stderr in terminal command-recovery diagnostics","file":"src/orchestration/acceptance.rs"}],"severity":"minor","summary":"Acceptance exhaustion drops stdout whenever stderr is present","verification":[{"description":"Test exhaustion output includes exit code plus both distinct bounded stdout and stderr tails","file":"src/orchestration/acceptance.rs"}]}
  evidence: AcceptanceCommandDiagnostic::summary now pushes independently bounded stdout and stderr instead of else-if; new orchestration::acceptance tests the_terminal_error_reports_exit_code_and_both_distinct_bounded_tails and retry_progress_also_reports_both_streams pass
- [x] [apply-natural-blocked-handoff-order] (major) Natural APPLY_BLOCKED handoff can be superseded by stall routing | evidence: src/execution/apply.rs:1622-1631 checks only REJECTED.md after natural command exit; src/execution/apply.rs:1701-1839 can return a stall before APPLY_BLOCKED is checked at the next loop entry | required_changes: src/execution/apply.rs — Detect both APPLY_BLOCKED and REJECTED.md immediately after fresh post-command inspection and before hooks or stall accounting | verification: src/execution/apply.rs — Test non-zero natural exit creating APPLY_BLOCKED with stall threshold one routes to blocked handoff without another dispatch
  finding: {"evidence":["src/execution/apply.rs:1622-1631 checks only REJECTED.md after natural command exit","src/execution/apply.rs:1701-1839 can return a stall before APPLY_BLOCKED is checked at the next loop entry"],"id":"apply-natural-blocked-handoff-order","required_changes":[{"description":"Detect both APPLY_BLOCKED and REJECTED.md immediately after fresh post-command inspection and before hooks or stall accounting","file":"src/execution/apply.rs"}],"severity":"major","summary":"Natural APPLY_BLOCKED handoff can be superseded by stall routing","verification":[{"description":"Test non-zero natural exit creating APPLY_BLOCKED with stall threshold one routes to blocked handoff without another dispatch","file":"src/execution/apply.rs"}]}
  evidence: execute_apply_loop now checks detect_apply_blocked_handoff alongside REJECTED.md straight after the fresh post-command inspection, ahead of hooks/WIP/stall routing; execution::apply a_non_zero_natural_exit_that_wrote_apply_blocked_hands_off_without_another_dispatch (stall threshold 1) and its REJECTED.md sibling pass
- [x] [apply-pre-hook-before-dispatch] (major) Apply child starts before pre_apply succeeds | evidence: src/execution/apply.rs:1232-1324 reserves budget and launches the Apply child; src/execution/apply.rs:1332-1354 runs pre_apply afterward and returns without terminating the launched child on hook failure | required_changes: src/execution/apply.rs — Run pre_apply before launching the Apply child and reserve budget only immediately before an authorized dispatch | verification: src/execution/apply.rs — Test that a failing pre_apply starts no command, consumes no dispatch budget, and leaves no child running
  finding: {"evidence":["src/execution/apply.rs:1232-1324 reserves budget and launches the Apply child","src/execution/apply.rs:1332-1354 runs pre_apply afterward and returns without terminating the launched child on hook failure"],"id":"apply-pre-hook-before-dispatch","required_changes":[{"description":"Run pre_apply before launching the Apply child and reserve budget only immediately before an authorized dispatch","file":"src/execution/apply.rs"}],"severity":"major","summary":"Apply child starts before pre_apply succeeds","verification":[{"description":"Test that a failing pre_apply starts no command, consumes no dispatch budget, and leaves no child running","file":"src/execution/apply.rs"}]}
  evidence: pre_apply and the new ApplyBudget::exhaustion refusal both run before the child launch and before budget.reserve; execution::apply a_failing_pre_apply_starts_no_command_and_spends_no_budget, a_succeeding_pre_apply_still_authorizes_the_dispatch, and a_refused_dispatch_never_reaches_pre_apply pass
- [x] [apply-warning-threshold-rounding] (minor) The 80 percent warning threshold is rounded down incorrectly | evidence: src/execution/apply.rs:305-308 floors max_iterations times 0.8; For max_iterations three it warns at attempt two, below 80 percent, and for one it never warns | required_changes: src/execution/apply.rs — Use integer ceiling semantics for the first dispatch reaching at least 80 percent | verification: src/execution/apply.rs — Add table tests for limits one through five and 100 with exactly one warning at the correct attempt
  finding: {"evidence":["src/execution/apply.rs:305-308 floors max_iterations times 0.8","For max_iterations three it warns at attempt two, below 80 percent, and for one it never warns"],"id":"apply-warning-threshold-rounding","required_changes":[{"description":"Use integer ceiling semantics for the first dispatch reaching at least 80 percent","file":"src/execution/apply.rs"}],"severity":"minor","summary":"The 80 percent warning threshold is rounded down incorrectly","verification":[{"description":"Add table tests for limits one through five and 100 with exactly one warning at the correct attempt","file":"src/execution/apply.rs"}]}
  evidence: ApplyBudget::warning_threshold uses (max*4).div_ceil(5); execution::apply table tests the_warning_threshold_is_the_integer_ceiling_of_eighty_percent and every_positive_ceiling_warns_exactly_once_at_its_ceiling_threshold cover limits 1,2,3,4,5,100 with exactly one warning each
- [x] [cleanup-marker-stream-memory-bound] (minor) Cleanup marker validation accumulates unbounded stdout | evidence: src/parallel/executor.rs:362-365 creates an unbounded String; src/parallel/executor.rs:393-418 appends all stdout before counting markers | required_changes: src/parallel/executor.rs — Count standalone markers with bounded streaming state instead of retaining complete stdout | verification: src/parallel/executor.rs — Test large output and fenced markers while proving marker state remains bounded
  finding: {"evidence":["src/parallel/executor.rs:362-365 creates an unbounded String","src/parallel/executor.rs:393-418 appends all stdout before counting markers"],"id":"cleanup-marker-stream-memory-bound","required_changes":[{"description":"Count standalone markers with bounded streaming state instead of retaining complete stdout","file":"src/parallel/executor.rs"}],"severity":"minor","summary":"Cleanup marker validation accumulates unbounded stdout","verification":[{"description":"Test large output and fenced markers while proving marker state remains bounded","file":"src/parallel/executor.rs"}]}
  evidence: run_cleanup_review_attempt drops the retained stdout String for the fixed-size CleanupMarkerScanner; parallel::executor::tests the_marker_scanner_state_is_fixed_size_regardless_of_stream_length (400k lines, size_of asserted), the_marker_scanner_ignores_fenced_markers_across_chunk_boundaries, and a_large_stream_still_validates_the_marker_exactly_once pass
- [x] [cleanup-status-inspection-diagnostic] (minor) Cleanup command or marker failures discard simultaneous status-inspection errors | evidence: src/parallel/executor.rs:430-439 and 443-457 convert current_porcelain_status errors to None; The corrective prompt therefore cannot distinguish unavailable status evidence from an empty status | required_changes: src/parallel/executor.rs — Retain bounded status-inspection errors alongside the primary cleanup failure kind | verification: src/parallel/executor.rs — Test command-failure plus status-failure and marker-failure plus status-failure prompts and exhaustion diagnostics
  finding: {"evidence":["src/parallel/executor.rs:430-439 and 443-457 convert current_porcelain_status errors to None","The corrective prompt therefore cannot distinguish unavailable status evidence from an empty status"],"id":"cleanup-status-inspection-diagnostic","required_changes":[{"description":"Retain bounded status-inspection errors alongside the primary cleanup failure kind","file":"src/parallel/executor.rs"}],"severity":"minor","summary":"Cleanup command or marker failures discard simultaneous status-inspection errors","verification":[{"description":"Test command-failure plus status-failure and marker-failure plus status-failure prompts and exhaustion diagnostics","file":"src/parallel/executor.rs"}]}
  evidence: split_status_inspection keeps a failed porcelain query in the new CleanupReviewDiagnostic::status_error beside the primary kind, surfaced as status_error in the exhaustion diagnostic and status_inspection_error in the corrective prompt; parallel::executor::tests a_command_failure_that_also_breaks_status_reports_both and a_marker_failure_that_also_breaks_status_reports_both pass
- [x] [parallel-apply-hooks-disconnected] (major) Parallel Apply drops configured hooks | evidence: src/parallel/dispatch.rs:2002-2011 passes None for hooks and parallel context; src/parallel/executor.rs:643-655 forwards those values into the shared Apply loop | required_changes: src/parallel/dispatch.rs — Pass configured HookRunner and ParallelHookContext into parallel Apply execution | verification: src/parallel/tests/executor.rs — Verify parallel failure recovery invokes pre_apply and on_error correctly and does not invoke post_apply for failed attempts
  finding: {"evidence":["src/parallel/dispatch.rs:2002-2011 passes None for hooks and parallel context","src/parallel/executor.rs:643-655 forwards those values into the shared Apply loop"],"id":"parallel-apply-hooks-disconnected","required_changes":[{"description":"Pass configured HookRunner and ParallelHookContext into parallel Apply execution","file":"src/parallel/dispatch.rs"}],"severity":"major","summary":"Parallel Apply drops configured hooks","verification":[{"description":"Verify parallel failure recovery invokes pre_apply and on_error correctly and does not invoke post_apply for failed attempts","file":"src/parallel/tests/executor.rs"}]}
  evidence: dispatch.rs now forwards self.hooks and a per-worktree ParallelHookContext into execute_apply_in_workspace; parallel::tests::executor parallel_apply_runs_configured_hooks_across_command_failure_recovery asserts pre_apply x2, on_error x1, post_apply x1 over a failed-then-successful attempt
- [x] [parallel-iteration-limit-finish] (major) Parallel iteration_limit loses typed finish-hook routing | evidence: src/parallel/dispatch.rs:2080-2106 converts IterationLimit into WorkspaceResult.error; src/orchestrator.rs:987-991 returns directly from parallel execution before src/orchestrator.rs:1104-1112 invokes on_finish | required_changes: src/parallel/dispatch.rs — Preserve a typed iteration-limit result and exact attempt count across the parallel boundary; src/orchestrator.rs — Invoke on_finish exactly once with iteration_limit and the cumulative attempt count for parallel runs | verification: src/parallel/tests/executor.rs — Verify no extra dispatch and exact typed iteration-limit propagation; src/orchestrator.rs — Verify parallel on_finish receives status iteration_limit and exact count once
  finding: {"evidence":["src/parallel/dispatch.rs:2080-2106 converts IterationLimit into WorkspaceResult.error","src/orchestrator.rs:987-991 returns directly from parallel execution before src/orchestrator.rs:1104-1112 invokes on_finish"],"id":"parallel-iteration-limit-finish","required_changes":[{"description":"Preserve a typed iteration-limit result and exact attempt count across the parallel boundary","file":"src/parallel/dispatch.rs"},{"description":"Invoke on_finish exactly once with iteration_limit and the cumulative attempt count for parallel runs","file":"src/orchestrator.rs"}],"severity":"major","summary":"Parallel iteration_limit loses typed finish-hook routing","verification":[{"description":"Verify no extra dispatch and exact typed iteration-limit propagation","file":"src/parallel/tests/executor.rs"},{"description":"Verify parallel on_finish receives status iteration_limit and exact count once","file":"src/orchestrator.rs"}]}
  evidence: dispatch.rs records the typed OrchestratorState::ApplyIterationLimit (change, attempts, max) and Orchestrator::run_parallel_finish_hook emits on_finish once with status iteration_limit and that exact count; parallel::tests::executor parallel_iteration_limit_is_preserved_as_a_typed_record_without_another_dispatch and orchestrator::parallel_finish_hook tests pass
- [x] [serial-acceptance-fail-budget-independence] (major) Serial command failures consume FAIL-to-Apply cycle numbering | evidence: src/serial_run_service.rs:1260-1268 derives the first FAIL retry count from the latest Acceptance attempt number; Command failures are currently recorded as attempts, so two command failures make the first canonical FAIL cycle three | required_changes: src/serial_run_service.rs — Track canonical FAIL-to-Apply cycles independently from command and protocol attempts | verification: src/serial_run_service.rs — Test CommandFailed, CommandFailed, canonical FAIL starts FAIL repair cycle one and retains the full repair budget
  finding: {"evidence":["src/serial_run_service.rs:1260-1268 derives the first FAIL retry count from the latest Acceptance attempt number","Command failures are currently recorded as attempts, so two command failures make the first canonical FAIL cycle three"],"id":"serial-acceptance-fail-budget-independence","required_changes":[{"description":"Track canonical FAIL-to-Apply cycles independently from command and protocol attempts","file":"src/serial_run_service.rs"}],"severity":"major","summary":"Serial command failures consume FAIL-to-Apply cycle numbering","verification":[{"description":"Test CommandFailed, CommandFailed, canonical FAIL starts FAIL repair cycle one and retains the full repair budget","file":"src/serial_run_service.rs"}]}
  evidence: SerialRunService::acceptance_fail_cycles/next_acceptance_fail_cycle owns FAIL-to-Apply cycle numbering instead of the latest Acceptance attempt number; serial_run_service command_failures_leave_the_full_fail_to_apply_repair_budget proves CommandFailed, CommandFailed, canonical FAIL starts at cycle 1
- [x] [serial-acceptance-permission-routing] (major) Serial Acceptance permission denial is retried as a generic command failure | evidence: src/orchestration/acceptance.rs:1694-1728 converts every non-zero serial invocation into CommandFailed without permission classification; src/serial_run_service.rs:973-999 consumes command-recovery retries, unlike parallel permission routing at src/parallel/executor.rs:1933-1977 | required_changes: src/orchestration/acceptance.rs — Apply the shared permission-denial classification and repeated-unresolved hold semantics to serial Acceptance | verification: src/serial_run_service.rs — Verify repeated unchanged permission denial enters the existing non-terminal hold without corrective command retries or Apply
  finding: {"evidence":["src/orchestration/acceptance.rs:1694-1728 converts every non-zero serial invocation into CommandFailed without permission classification","src/serial_run_service.rs:973-999 consumes command-recovery retries, unlike parallel permission routing at src/parallel/executor.rs:1933-1977"],"id":"serial-acceptance-permission-routing","required_changes":[{"description":"Apply the shared permission-denial classification and repeated-unresolved hold semantics to serial Acceptance","file":"src/orchestration/acceptance.rs"}],"severity":"major","summary":"Serial Acceptance permission denial is retried as a generic command failure","verification":[{"description":"Verify repeated unchanged permission denial enters the existing non-terminal hold without corrective command retries or Apply","file":"src/serial_run_service.rs"}]}
  evidence: acceptance_test_streaming classifies denials via classify_acceptance_command_denial and returns AcceptanceResult::PermissionStalled on an unchanged repeat, which serial_run_service routes to the existing non-terminal hold; serial_run_service a_repeated_unresolved_permission_denial_enters_the_non_terminal_hold passes
- [x] [serial-apply-git-progress-accounting] (major) Serial command-failure stall accounting ignores Git progress | evidence: src/execution/apply.rs:1701-1816 performs Git/WIP progress accounting only when a WorkspaceManager exists; src/execution/apply.rs:1821-1839 falls back to checkbox progress only; src/serial_run_service.rs:743-755 passes no WorkspaceManager | required_changes: src/execution/apply.rs — Include repository Git/file progress in serial command-failure stall decisions | verification: src/serial_run_service.rs — Test max_iterations zero with continuing Git progress does not falsely stall while genuine no-progress failures still stop
  finding: {"evidence":["src/execution/apply.rs:1701-1816 performs Git/WIP progress accounting only when a WorkspaceManager exists","src/execution/apply.rs:1821-1839 falls back to checkbox progress only","src/serial_run_service.rs:743-755 passes no WorkspaceManager"],"id":"serial-apply-git-progress-accounting","required_changes":[{"description":"Include repository Git/file progress in serial command-failure stall decisions","file":"src/execution/apply.rs"}],"severity":"major","summary":"Serial command-failure stall accounting ignores Git progress","verification":[{"description":"Test max_iterations zero with continuing Git progress does not falsely stall while genuine no-progress failures still stop","file":"src/serial_run_service.rs"}]}
  evidence: repository_progress_fingerprint (HEAD + porcelain status) is taken around each dispatch when no WorkspaceManager exists, so a failing serial attempt that advanced Git is not an empty stall step; serial_run_service failing_serial_attempts_that_advance_git_do_not_falsely_stall and genuinely_stuck_serial_failures_still_reach_the_stall_threshold both pass with max_iterations 0
