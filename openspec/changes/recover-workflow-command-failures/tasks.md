## Implementation Tasks

- [ ] Update `src/execution/apply.rs` so an ordinary non-zero Apply result that remains after command-queue retries records its existing bounded `ApplyHistory` evidence, runs existing error-hook behavior, and continues to the next outer Apply iteration. Preserve cancellation, completion-finalized, blocked/rejecting, and repeated permission-denial routing. Completion requires tests proving a failed first command injects exit/stdout/stderr diagnostics into the successful next attempt and does not emit terminal error early. (verification: integration - `cargo test --lib execution::apply`; verification-id: workflow-command-recovery-tests)

- [ ] Correct the Apply iteration ceiling to apply only when `max_iterations > 0`, count command failures and final-commit repair attempts in the same total, and include the latest actionable failure on exhaustion. Completion requires fast tests for exact positive-limit exhaustion, no attempt beyond the limit, and multiple dispatched attempts with `max_iterations = 0`. (verification: unit - `cargo test --lib execution::apply`; verification-id: workflow-command-recovery-tests)

- [ ] Add a shared active-run Acceptance command-failure retry decision and latest bounded diagnostic context in `src/orchestration/acceptance.rs`, fixed at two retries after the initial failure and independent from missing-verdict/protocol, explicit-CONTINUE, and Apply-repair counters. Completion requires table-driven tests for retry, third-failure exhaustion, canonical reset, bounded output, and no fourth attempt. (verification: unit - `cargo test --lib orchestration::acceptance`; verification-id: workflow-command-recovery-tests)

- [ ] Rewire `src/serial_run_service.rs` and `src/parallel/dispatch.rs` to consume the shared policy and rerun Acceptance only against the same applied, clean workspace. Completion requires serial and parallel control-flow tests proving command failure does not invoke Apply/cleanup-review, does not append FAIL repair tasks, does not consume unrelated counters, and a later canonical result keeps its existing routing. (verification: integration - `cargo test --lib serial_run_service && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [ ] Refactor post-apply cleanup-review in `src/parallel/executor.rs` into at most three operation attempts above command-queue transport retries, classifying the latest result as command failure, marker missing/duplicate, dirty remains, Git-status inspection failure, cancellation, or permission denial. Completion requires each corrective attempt to receive exit code when available plus bounded stdout, stderr, marker observation, and current porcelain status, without accumulating all-attempt history. (verification: unit/integration - `cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [ ] Enforce cleanup-review's ordered dual success gate after every attempt: acceptable command completion, exactly one standalone `CLEANUP_REVIEW: CLEAN`, then a fresh clean-worktree query. Preserve dirty workspace evidence on exhaustion, route classified permission denial through existing permission/stall semantics, and observe the per-change cancellation token while streaming and waiting so cancellation terminates the child and starts no retry. Completion requires marker-only, clean-only, duplicate-marker, recover-on-second-attempt, three-attempt exhaustion, permission, and cancellation regressions. (verification: integration - `cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [ ] Extend `src/agent/prompt.rs` and `skills/cflx-cleanup-review/SKILL.md` so corrective cleanup prompts carry trusted required action and clearly delimited untrusted latest diagnostics while retaining blind-staging prohibition and the single-marker contract. Completion requires prompt tests proving bounded latest-only context, failure-kind/status fields, no instruction following from captured output, and no success relaxation. (verification: unit - `cargo test --lib agent::prompt`; verification-id: workflow-command-recovery-tests)

- [ ] Add restart and artifact regressions proving Acceptance and cleanup-review retry counters are process-local, no workspace-root report/retry checkpoint or external job/session identifier is required, and a fresh run chooses Acceptance or cleanup-review from unchanged workspace/Git evidence. (verification: integration - `cargo test --lib serial_run_service && cargo test --lib parallel::tests::executor`; verification-id: workflow-command-recovery-tests)

- [ ] Run the repository-local quality gate and resolve formatting, lint, compile, and targeted test failures without broadening Archive, command-queue, verdict, or frontend semantics. Completion requires every declared command to exit successfully and all default tests added by this change to remain under one second or carry the repository heavy-test classification. (verification: integration - `cargo test --lib execution::apply && cargo test --lib serial_run_service && cargo test --lib parallel::tests::executor && cargo test --lib agent::prompt && cargo fmt --check && cargo clippy -- -D warnings`; verification-id: workflow-command-recovery-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate recover-workflow-command-failures --archive-gate`.

## Future Work

- Evaluate Archive operation-level recovery separately if concrete logs show repository-local evidence can distinguish recoverable command crashes from unsafe partial archive state.
- Consider configurable operation retry bounds only if fixed safety bounds prove operationally insufficient and a stable cross-operation configuration contract is justified.
