## 1. 調査と計画
- [ ] 1.1 コマンド実行ロジックの重複箇所を棚卸しし、統合候補を整理する
- [ ] 1.2 `#[allow(dead_code)]` の対象一覧を作成し、削除/隔離/保持を分類する
- [ ] 1.3 レガシー (`opencode.rs`) の利用状況を確認し、削除または隔離方針を決定する

## 2. 実装（段階的リファクタリング）
- [ ] 2.1 `jj` コマンド実行の共通ヘルパーを導入し、`jj_workspace.rs` と `parallel_executor.rs` を統合する
- [ ] 2.2 `agent.rs` のコマンド実行ヘルパーと統合設計を揃え、重複を削減する
- [ ] 2.3 レガシーモジュールの削除または隔離を実施し、参照の有無に応じて整理する
- [ ] 2.4 `#[allow(dead_code)]` の削除・範囲縮小を行い、残す場合は理由を明示する

## 3. 検証
- [ ] 3.1 `cargo fmt` を実行して整形を確認する
- [ ] 3.2 `cargo clippy -- -D warnings` を実行して警告がないことを確認する
- [ ] 3.3 `cargo test` を実行して挙動が維持されていることを確認する
