## Implementation Tasks

- [ ] Add explicit external-blocked lifecycle state and blocker-kind metadata to the maintained proposal/reducer model while retaining stalled for no-progress and exhausted execution outcomes. Complete when transition and snapshot tests reject simultaneous or ambiguous blocked/stalled state and preserve dependency versus external blocker kinds. (verification: unit - `cargo test --all-features runtime::`; verification-id: blocker-lifecycle-classification)
- [ ] Centralize structured blocker validation and classification in the orchestrator result-handling boundary. Require supported source/category, non-empty evidence, verifiable unblock condition, next action, and resumability for external `blocked`; route repeated/no-progress/exhausted outcomes to `stalled` and malformed bare verdicts to bounded protocol correction. Complete when table-driven tests cover Apply, Acceptance, legacy token, missing-field, repository-fixable, repeated-finding, and retry-exhaustion cases. (verification: unit - `cargo test --all-features acceptance`; verification-id: blocker-lifecycle-classification)
- [ ] Wire external-blocked state into parallel scheduler eligibility, blocked-only drain, completion accounting, dependency propagation, and explicit retry/reconciliation without creating synthetic dependency edges. Complete when scheduler tests prove ordinary dispatch suppresses blocked changes, unrelated ready changes continue, dependents remain dependency-blocked, and operator retry reruns the blocked phase after workspace identity validation so fresh output controls reclassification. (verification: integration - `cargo test --all-features parallel::tests`; verification-id: blocker-lifecycle-classification)
- [ ] Preserve constitutional restart behavior by keeping blocker lifecycle state process-local and recomputing the next action from workspace and git evidence. Complete when restart tests prove deletion or absence of runtime state cannot alter routing for identical workspace contents and stale blocked metadata cannot authorize acceptance, archive, or merge. (verification: integration - `cargo test --all-features orchestration`; verification-id: blocker-lifecycle-classification)
- [ ] Propagate reducer-derived `blocked` and `stalled` plus machine-readable blocker kind and detail through TUI and WebSocket/API payloads. Complete when API and TUI tests distinguish dependency-blocked, external-blocked, and stalled rows without independently re-deriving status. (verification: integration - `cargo test --all-features tui server`; verification-id: blocker-lifecycle-classification)
- [ ] Extend `dashboard/src/api/types.ts`, `dashboard/src/components/ChangeRow.tsx`, and their component tests to support externally blocked changes while preserving stalled behavior. Complete when component tests render distinct labels/details and TypeScript rejects an unknown blocker kind. (verification: unit - `dashboard/src/components/ChangeRow.test.tsx` via `npm --prefix dashboard test -- --run && npm --prefix dashboard run lint && npm --prefix dashboard run build`; verification-id: blocker-lifecycle-classification)
- [ ] Update `skills/cflx-apply/SKILL.md`, `skills/cflx-accept/SKILL.md`, and their prompt contract tests so agents preserve the tasks.md `Implementation Blocker` section and `IMPLEMENTATION_BLOCKER:` stdout contract, emit complete structured facts, avoid recoverable `REJECTED.md`, and defer canonical blocked/stalled classification to Conflux. Complete when tests reject missing workspace-visible evidence and instructions that classify from prose/token text alone. (verification: integration - `cargo test --all-features prompt`; verification-id: blocker-lifecycle-classification)
- [ ] Run repository quality gates and confirm compatibility input parsing and unrelated dependency-blocked scheduling remain green. Complete when all Rust and dashboard checks pass without exclusions. (verification: integration - `cargo test --all-features && cargo clippy --all-targets --all-features -- -D warnings && npm --prefix dashboard test -- --run && npm --prefix dashboard run lint && npm --prefix dashboard run build`; verification-id: blocker-lifecycle-classification)

## Future Work

- Automatic external-condition polling or owner notification may be proposed separately after blocker metadata is stable.

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate classify-external-blockers --archive-gate`.
