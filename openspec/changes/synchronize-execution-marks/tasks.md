## Implementation Tasks

- [ ] Implement one shared typed-event execution-mark reconciler at the authoritative dispatch boundary. Completion requires a table-driven `event_mark_reconciliation_covers_failure_and_rejection_edges` test covering Error transitions from processing/apply/acceptance/archive/push/rejection-review failure, `ChangeRejected`, rejected refresh rows, successful dequeue/legacy stop, `on_merged` hook failure, late failures that cannot supersede final outcomes, duplicate delivery, and target-only mutation. (verification: unit - `cargo test --lib event_mark_reconciliation_covers_failure_and_rejection_edges -- --list | grep -q event_mark_reconciliation_covers_failure_and_rejection_edges && cargo test --lib event_mark_reconciliation_covers_failure_and_rejection_edges`; verification-id: execution-mark-event-regressions)

- [ ] Bind the same `ExecutionMarkStore` to every production event-dispatch path before TUI/Web fan-out, and make TUI event processing mirror row `selected` values from that store rather than retaining local-only clear decisions. Completion requires `tui_event_rows_follow_authoritative_marks` to start from deliberate row/store divergence, exercise failure, rejection, dequeue, hook-failure, and refresh paths, and prove the store wins without an event handler replacing unrelated remote marks. (verification: integration - `cargo test --lib tui_event_rows_follow_authoritative_marks -- --list | grep -q tui_event_rows_follow_authoritative_marks && cargo test --lib tui_event_rows_follow_authoritative_marks`; verification-id: execution-mark-event-regressions)

- [ ] Verify Web and `/api/v2` build the event candidate after mark reconciliation. Add `execution_event_clears_authoritative_mark_before_projection`; completion requires a real authoritative dispatch to publish the target's `execution_marked: false`, preserve another target's true mark, attach the failure/rejection event to that same state revision, and avoid another revision for duplicate unchanged delivery. (verification: integration - `cargo test --lib execution_event_clears_authoritative_mark_before_projection -- --list | grep -q execution_event_clears_authoritative_mark_before_projection && cargo test --lib execution_event_clears_authoritative_mark_before_projection`; verification-id: execution-mark-event-regressions)

- [ ] Pin preservation boundaries for dependency block, stalled/external hold, `ChangeSkipped`, ordinary MergeWait/ResolveWait, archive/merge/push success, `AllCompleted`, global fatal `Error`, and process-level `Stopped`. Completion requires `event_mark_reconciliation_preserves_unrelated_and_stopped_marks` to prove all marked IDs survive those events and the force-stop/resume contract remains unchanged. (verification: unit - `cargo test --lib event_mark_reconciliation_preserves_unrelated_and_stopped_marks -- --list | grep -q event_mark_reconciliation_preserves_unrelated_and_stopped_marks && cargo test --lib event_mark_reconciliation_preserves_unrelated_and_stopped_marks`; verification-id: execution-mark-event-regressions)

- [ ] Preserve explicit Error recovery after stale intent is cleared. Add `error_remark_remains_retryable`; completion requires a change-level Error event to clear both TUI/API mark projections, a supported Running or Stopped re-mark to create fresh authoritative intent, and the existing F5/Start/retry route to consume that intent without changing global Error mark admission or `RetryPlan.explicit_retry`. (verification: integration - `cargo test --lib error_remark_remains_retryable -- --list | grep -q error_remark_remains_retryable && cargo test --lib error_remark_remains_retryable`; verification-id: execution-mark-event-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate synchronize-execution-marks --archive-gate`.

## Future Work

- None. Durable intent and retry-policy redesign remain intentionally separate.
