## Implementation Tasks

- [ ] Define one shared active-limit query as typed `ApplyIterationLimit` evidence plus `RunSchedulerPort::is_running()` and wire the existing TUI supervisor task handle into command admission and command-capable projection. Completion requires `running=true` to block, `running=false` to retire the gate without clearing the record, the TUI loop to refresh its eligibility cache and publish one changed `/api/v2` snapshot on that liveness transition, the TUI finish-hook owner to retain the exact record through its sole `on_finish` attempt, and later admission to start rather than notify the exited scheduler. Keep headless CLI work regression-only: its existing sole finish-hook owner must still observe the record before the run returns, with no new admission or lifetime mechanism. Do not add a close operation, lock, generation, or closing barrier. (verification: integration - `cargo test --features web-monitoring active_iteration_limit_run_boundary`; verification-id: active-run-iteration-limit-regressions)

- [ ] Enforce one typed active-limit guard in `src/orchestration/operator_command.rs` and `src/orchestration/run_control.rs` before `retry_change` or the terminal-error queue-add/`set_queue_intent` alias applies any mutation. Extend `MarkExclusion` and bulk-mark planning so active-limited terminal-error rows are excluded before eligible Running queue intents are applied, preventing guard failure from aborting a partially applied bulk command. Completion requires refusal to preserve reducer status and error detail, failed classification, marks, dynamic queue, explicit-retry publications, queue hooks, scheduler notifications, and scheduler starts byte-for-byte/count-for-count, while ordinary error and acceptance-hold retry behavior remains unchanged. (verification: unit - `cargo test --features web-monitoring active_iteration_limit_retry_guard`; verification-id: active-run-iteration-limit-regressions)

- [ ] Make `retry_errors` classify one coherent bulk snapshot, exclude active-run-limited targets, and dispatch all other retryable targets once. Completion requires a mixed request to leave limited rows untouched, never claim them accepted, preserve `apply_iteration_limit_active` in the authoritative snapshot at the result revision, retry ordinary errors and resumable holds, and make an all-limited request a no-op without reducer, queue, edge, mark, hook, notify, or spawn effects. Do not add a per-target exclusion DTO to `RetryPlan` or `RunControlOutcome`. (verification: unit - `cargo test --features web-monitoring active_iteration_limit_bulk_retry`; verification-id: active-run-iteration-limit-regressions)

- [ ] Add `apply_iteration_limit_active` to `/api/v2` `ActionBlockedReason` and derive `retry_change` eligibility from typed evidence plus the same scheduler-liveness authority command admission uses. Completion requires projection tests for active, task-exited-with-record-retained, ordinary-error, and headless-unbound states plus generated OpenAPI serialization of the enum token. Do not add an unused per-change `{ attempts, max }` DTO. (verification: integration - `cargo test --features web-monitoring active_iteration_limit_projection && cargo test --features web-monitoring --test openapi_contract_tests`; verification-id: active-run-iteration-limit-regressions)

- [ ] Change `web/app.js::changeActions` to render Retry only from `change.actions.retry_change.allowed`, without inferring permission from `display_status`, error text, or iteration count. Completion requires `tests/web/destructive-actions.spec.js` to prove an `error` row blocked by `apply_iteration_limit_active` has no Retry control and emits no command, while a later allowed snapshot restores one non-confirming `retry_change` action. (verification: e2e - `npm --prefix tests/web test -- destructive-actions.spec.js`; verification-id: active-run-iteration-limit-browser-regressions)

- [ ] Synchronize active iteration-limit eligibility into TUI state and apply it before the optimistic row-Space mark flip, during bulk execution-mark selection, in F5 retry handling, and in retry guidance across `src/tui/state/selection_logic.rs`, command handlers, and rendering. Completion requires the limited row to retain its diagnostic but produce no mark, queue command, retry command, or retry-promising Space/F5 text; unrelated eligible rows remain operable and eligibility returns after scheduler-task exit. (verification: integration - `cargo test --features web-monitoring active_iteration_limit_tui`; verification-id: active-run-iteration-limit-regressions)

- [ ] Add cross-adapter regressions proving TUI and `/api/v2` use the same typed refusal and later-run recovery. Completion requires both adapters to reject the same active target without side effects, scheduler liveness true/false tests to prove the exited task is never notified, and a same-process later retry to create fresh active-run state from current workspace evidence without resetting or persisting the old budget. (verification: integration - `cargo test --features web-monitoring active_iteration_limit`; verification-id: active-run-iteration-limit-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate block-active-run-iteration-limit-retries --archive-gate`.

The tracked Rust pre-commit hooks are path-scoped, so the declared Rust verification retains explicit `cargo fmt --check` and all-target/all-feature Clippy coverage. Browser verification remains explicit because it is not owned by pre-commit.

## Future Work

- If operators later need automatic continuation after a closed boundary, propose it separately with an explicit safety policy rather than weakening the active-run ceiling.
