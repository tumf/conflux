## Implementation Tasks

- [ ] Audit every scheduler-capacity release path for workspace completion, manual resolve, automatic resolve RAII release, background merge merged/deferred/failed results, pending merge decrement, and resolve/reject wait retry completion; completion requires each distinct path to be mapped to an explicit wake edge, `SlotRecovery`, or bounded timer/debounce recovery with repository-visible evidence and no reliance on a sticky analysis reason. (verification: unit - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Make `ResolveCompletion`, `RepairCandidate`, and `SlotRecovery` one-shot in the active scheduler loop by replacing an unchanged one-shot loop-owned `reanalysis_reason` with non-bypass `Initial` only after the queued reanalysis/dispatch block has actually evaluated it; completion requires `perform_reanalysis_and_dispatch`, capacity calculation, and direct-call test semantics to remain unchanged. (verification: unit - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Add a loop-level regression test using paused Tokio time or an equally deterministic scheduler-loop harness that delivers one completion edge under zero capacity, advances multiple 500 ms timer wakes, and asserts exactly one analyzer invocation, retained queued work, and no apply dispatch; completion requires the test to fail against the pre-fix sticky-reason behavior and finish under one second. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Add loop-level coverage proving a second real completion edge re-arms immediate analysis and dispatches eligible queued work after capacity recovery without queue notification or user action. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Add coverage proving a queued-empty loop does not consume a one-shot reason before an eligible queued evaluation, and that a later explicit queue/reconciliation event is not lost or suppressed by the reason reset. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Add loop-level regressions proving a single `RepairCandidate` edge and a single `SlotRecovery` edge each bypass debounce once but are not replayed by later timer-only wakes. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Add or extend capacity-recovery tests for automatic resolve success/error/early-return and background merge merged/deferred/failed outcomes so each audited occupancy release either wakes immediate evaluation or demonstrably converges through bounded timer/debounce evaluation without permanent starvation. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Preserve zero-capacity and explicit-bypass contracts by running existing manual-resolve, auto-resolve, resolve-completion dispatch, queue-notification, repair-candidate, slot-recovery, and diagnostic-deduplication tests; completion requires no expectation or direct-call semantics to be weakened merely to pass the new regressions. (verification: integration - `cargo test parallel::tests --lib`; verification-id: scheduler-local-tests)
- [ ] Run Rust formatting, linting, and default-path tests; completion requires `cargo fmt --check`, `cargo clippy -- -D warnings`, and `cargo test` to succeed, with any test over one second optimized or marked heavy according to repository policy. (verification: integration - `cargo fmt --check && cargo clippy -- -D warnings && cargo test`; verification-id: scheduler-local-tests)

## Future Work

- Rename `ResolveCompletion` or separate ordinary workspace completion from resolve-specific trigger classification only in a dedicated follow-up change; this proposal intentionally preserves current mapping semantics.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate prevent-repeated-resolve-completion-analysis --archive-gate`
