## Implementation Tasks

- [ ] **Task 1: Add the invocation-scoped CLI contract** in `src/cli.rs` and `src/main.rs`: expose exact aliases `-u`/`--integrate-upstream[=<remote>]`, default a missing value to `origin`, accept `--upstream-verify-command`, keep the feature disabled when absent, and reject non-parallel mode, `--push`, detached HEAD, missing remote/ref, empty verification command, and non-Git resolution before mutation. (verification: unit - parser/startup tests plus `cargo test upstream_integration` prove alias equivalence, default-off behavior, `origin` defaulting, explicit remote, dry-run side-effect suppression, and every rejected combination; verification-id: upstream-integration-tests)

- [ ] **Task 2: Propagate an optional upstream checkpoint configuration** through `Orchestrator`, `ParallelRunService`, and `ParallelExecutor` without adding persistent-config enablement or affecting constructors when absent. (verification: unit - `tests/e2e_git_worktree_tests.rs`の`upstream_integration_*` construction testsと`cargo test upstream_integration` prove disabled instances install no checkpoint and enabled instances retain selected remote/base/verification command across restart-loop reconstruction; verification-id: upstream-integration-tests)

- [ ] **Task 3: Add Git/VCS observation and classification operations** for selected-remote fetch, same-name remote-base SHA resolution, ancestry classification, `MERGE_HEAD` detection, and unmerged-index detection; merge classification must not depend on localized stdout/stderr text. (verification: integration - `tests/e2e_git_worktree_tests.rs`のtemporary-repository `upstream_integration_*` testsと`cargo test upstream_integration` cover unchanged, local-ahead, remote-ahead, diverged, missing remote branch, fetch failure, conflict state under non-English output text, and non-conflict command failure; verification-id: upstream-integration-tests)

- [ ] **Task 4: Wire exclusive project base-lane checkpoint ownership** so the entire fetch/integrate/verify cycle starts only on a clean cumulative base with no active base merge/resolve/archive/rejection owner, blocks completed worktree results from entering base, permits independent per-change apply/acceptance to continue, and ignores stale completion events when releasing the lane. (verification: unit - `tests/e2e_git_worktree_tests.rs`のreducer/scheduler `upstream_integration_*` testsと`cargo test upstream_integration` prove no overlap, dirty/unsafe state performs no fetch, independent worktree activity continues, completed results wait, and stale events cannot release ownership; verification-id: upstream-integration-tests)

- [ ] **Task 5: Implement Conflux-owned cumulative-base integration** with `git merge --no-ff <fetched-sha>` for every fetched revision not already contained in local base, including strictly remote-ahead history; prohibit fast-forward, rebase, reset, amend, and force operations on the new path. (verification: integration - `tests/e2e_git_worktree_tests.rs`のtemporary-repository `upstream_integration_*` testsと`cargo test upstream_integration` prove remote-ahead and diverged histories create merge commits, fetched/local commits remain reachable with identity preserved, no-op invokes no merge, and ordinary conflict-free integration invokes no agent; verification-id: upstream-integration-tests)

- [ ] **Task 6: Reuse the existing bounded `resolve_command` only for repair** by running it from the cumulative base with merge state and upstream-specific revisions/status/cause, allowing forward repair commits, and requiring no unmerged entries, no unfinished merge, and fetched-SHA ancestry before success. (verification: integration - `tests/e2e_git_worktree_tests.rs`のscripted `resolve_command` `upstream_integration_*` testsと`cargo test upstream_integration` prove textual repair success, semantic repair success, non-zero exit, incomplete merge retry, retry exhaustion, history-rewrite rejection, and that no-op/conflict-free merge never starts the agent; verification-id: upstream-integration-tests)

- [ ] **Task 7: Execute the explicit full verification command after every changed upstream tree** and after each semantic repair, while skipping disabled and ancestry-proven no-op paths; block dispatch requiring new base evidence, base integration, and cumulative push until success. (verification: integration - `tests/e2e_git_worktree_tests.rs`のfake verification-command `upstream_integration_*` testsと`cargo test upstream_integration` prove conflict-free and repaired merges execute the full command, no-op/disabled/dry-run execute none, non-zero exit blocks later side effects, and bounded semantic repair must rerun the complete command; verification-id: upstream-integration-tests)

- [ ] **Task 8: Recover solely from repository evidence** so interruption during merge resumes repair and every restart with an unpushed upstream merge reruns full verification regardless of surviving/deleted logs, events, or external runtime state. (verification: integration - `tests/e2e_git_worktree_tests.rs`のrestart-oriented `upstream_integration_*` testsと`cargo test upstream_integration` cover interruption during merge, after merge before verification, and after verification before push; deleting runtime projections does not alter the safe next action; verification-id: upstream-integration-tests)

- [ ] **Task 9: Guard cumulative-base push with fresh fetch and optimistic non-force concurrency** by suppressing push when latest remote ancestry is missing and by returning race-time non-fast-forward rejection to bounded integration/reverification. (verification: e2e - `tests/e2e_git_worktree_tests.rs`でlocal bare remote advances before ancestry check and between check/push; `upstream_integration_*` proves stale push suppression, normal non-force rejection handling, reintegration/reverification, retry exhaustion, and no force push; verification-id: upstream-integration-tests)

- [ ] **Task 10: Emit non-authoritative lifecycle evidence and preserve out-of-scope surfaces** so enabled runs report remote/revisions and checkpoint outcomes, disabled runs emit no upstream events, and serial, TUI, server `git-sync`, per-change pre-sync, and `PushToRemote` retain existing routing. (verification: integration - `tests/e2e_git_worktree_tests.rs`のevent/snapshot and regression tests named `upstream_integration_*`と`cargo test upstream_integration`, plus targeted existing tests, prove observability cannot change routing and every excluded surface remains unchanged; verification-id: upstream-integration-tests)

## Future Work

- Persistent-config or TUI enablement after CLI-only behavior has operational evidence.
- Conflux-server endpoint/event wiring that requests the checkpoint without writing the workspace.
- Webhook-driven synchronization, debounce, and batching based on measured update frequency.
- Distributed project/base-tree leases across multiple Conflux instances.
- Tiered verification that preserves a mandatory complete gate while reducing intermediate latency.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate integrate-upstream-during-run --archive-gate`

Repository quality gates expected before acceptance: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test upstream_integration`.
