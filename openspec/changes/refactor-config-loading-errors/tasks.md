# タスク: 設定ファイル読み込みエラーの堅牢化

1. **[確認]** 既存の設定ローダーのテストを実行し、すべてのテストがパスすることを確認する (Characterization tests)。
2. **[リファクタリング]** `src/config/mod.rs` の `unwrap/expect` の使用箇所を特定し、`Result<Config, ConfigError>` などのカスタムエラー型、または `anyhow::Result` を返すように変更する。
3. **[検証]** リファクタリング後、既存の正常系テストがパスすることを確認する。さらに、不正な設定ファイルが指定された場合に、設定ロードが失敗し、パニックでなく適切なエラー情報が返されることを確認する。
