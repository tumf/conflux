## Implementation Tasks

- [x] 1. remove out-of-worktree workflow state readers/writers from `src/parallel/acceptance_state.rs`, `src/parallel/archive_state.rs`, `src/parallel/dispatch.rs`, and `src/parallel/executor.rs`, and ensure resume/archive control flow no longer depends on `~/.local/state/cflx/**` (verification: integration; repository evidence in `src/parallel/acceptance_state.rs`, `src/parallel/archive_state.rs`, `src/parallel/dispatch.rs`, and `src/parallel/executor.rs` shows durable read/write and routing dependencies removed)
- [x] 2. implement workspace-local acceptance/archive routing in `src/execution/state.rs` and `src/parallel/dispatch.rs` so `Applied` resumes re-run acceptance unless workspace-local evidence proves archive handoff readiness, and `Archiving` / `Archived` remain file/git-state-derived only (verification: integration; repository evidence in `src/execution/state.rs` and `src/parallel/dispatch.rs` shows `Applied -> Acceptance` and `Archiving -> Archive` routing without durable-state guards)
- [x] 3. add regression tests for external-state independence in `src/parallel/tests/` or equivalent so deleting or pre-populating `~/.local/state/cflx/acceptance-state` / `archive-resume-state` cannot alter next-phase selection for the same workspace contents (verification: integration; `src/parallel/dispatch.rs` test `decide_resume_action_routes_applied_to_acceptance_even_with_external_durable_state`; command `cargo test decide_resume_action_routes_applied_to_acceptance_even_with_external_durable_state`)
- [x] 4. update canonical and proposal-side specs under `openspec/specs/parallel-execution/` plus any related deltas so out-of-worktree durable workflow state is forbidden and observability outputs are documented as non-authoritative (verification: integration; source paths `openspec/specs/parallel-execution/spec.md` and `openspec/changes/eliminate-durable-workflow-state/specs/parallel-execution/spec.md`; runnable evidence commands `cflx openspec show eliminate-durable-workflow-state --deltas-only` and `cargo test decide_resume_action_routes_applied_to_acceptance_even_with_external_durable_state`)
- [x] 5. verify the proposal and implementation path with `cflx openspec validate eliminate-durable-workflow-state --strict --evidence warn`, targeted Rust tests for workspace-local routing, and project lint/type checks required by the repo (verification: integration; commands `cflx openspec validate eliminate-durable-workflow-state --strict --evidence warn`, `cargo test decide_resume_action_routes_applied_to_acceptance*`, and `cargo check`)

## Future Work

- Human review of whether any remaining non-workflow observability caches under `~/.local/state/cflx/` should be relocated or renamed for clarity.

## Acceptance #1 Failure Follow-up
- [x] Archive commit path blocker: acceptance follow-up edits are now applied and `git status --short` is clean in this worktree, so the archive commit precondition is satisfied (verification: manual; runnable evidence command `git status --short` returns no output in this worktree).
- [x] openspec/changes/eliminate-durable-workflow-state/tasks.md lines 3-7: add explicit verification ownership (`unit`, `integration`, `e2e`, `manual`, `benchmark`, or `not-testable`) and repository-verifiable evidence for each behavior-changing task (verification: integration; runnable evidence command `agent-exec run -- cflx openspec validate eliminate-durable-workflow-state --strict --evidence warn` passes with no warnings).
- [x] src/parallel/tests/executor.rs lines 1820-1857, 1863-2021, 4192-4200, 4370-4379: remove or rewrite stale durable-state assertions (`has_durable_acceptance_pass`, `load_acceptance_state`, archive guard on `durable acceptance-pass state missing`) to match the new workspace-local-only routing in src/parallel/acceptance_state.rs lines 24-75 and src/parallel/dispatch.rs lines 252-267 (verification: integration; `cargo test test_acceptance_failure_does_not_set_durable_pass_state` passes after replacing archive-block expectations with workspace-local archive execution assertions, and obsolete `test_acceptance_state_uses_end_revision_for_all_outcomes_when_head_changes` durable-state persistence test was removed).
