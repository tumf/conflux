## 1. 実装
- [ ] 1.1 動的キューに通知機構を追加する（Notify）。完了条件: `DynamicQueue::push` で通知され、再分析ループが通知経由で起床することを `src/tui/queue.rs` と呼び出し側の確認で検証する。
- [ ] 1.2 order-based 再分析ループを連続スケジューラに置き換える。完了条件: `execute_with_order_based_reanalysis` が JoinSet 完了・キュー通知・タイマーのいずれでも再分析を起動し、グループ完了バリアが不要であることを `src/parallel/mod.rs` の実装で確認する。
- [ ] 1.3 デバウンス判定をスロット可否から分離する。完了条件: `should_reanalyze` がキュー変化時間のみで判定され、スロットが空いていない場合でも再分析が可能なことを `src/parallel/mod.rs` で確認する。
- [ ] 1.4 available_slots == 0 の場合の挙動を定義し、再分析結果が次イベントで再評価されるようにする。完了条件: 空きがない場合でも再分析は走り、空きが生じた時点でディスパッチされることをログとコードで確認する。

## 2. 検証
- [ ] 2.1 ユニットテストの追加または既存テスト更新で、キュー追加後に re-analysis が完了イベントなしで起動することを確認する（`cargo test` で該当テストが通る）。
- [ ] 2.2 既存の parallel-execution 仕様に反する変更がないことを確認する（仕様差分をレビュー）。
