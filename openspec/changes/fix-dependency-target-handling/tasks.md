## Implementation Tasks

- [x] Task 1: Add or centralize dependency target classification for queued, in-flight, archived, and missing dependency IDs using repository-visible active change directories, in-flight IDs, and archive entries. (verification: unit - Rust tests cover exact archive directory names, date-prefixed archive names, active queued IDs, explicitly supplied in-flight IDs, and missing IDs)
- [x] Task 2: Update analyzer result handling so `Change.dependencies` metadata is unioned into `AnalysisResult.dependencies` for normal LLM output, single-change fast paths, and parse-success paths without removing LLM-discovered valid dependencies. (verification: unit - `src/analyzer.rs` tests prove a single queued change and an LLM response omitting metadata dependencies still return dependency edges)
- [x] Task 3: Update LLM analysis fallback in `src/parallel_run_service.rs` so fallback analysis is metadata-dependency-only rather than dependency-free. (verification: unit - `src/parallel_run_service.rs` tests simulate analysis failure/disabled LLM path and assert fallback result preserves `Change.dependencies`)
- [x] Task 4: Update analyzer dependency validation so archived dependency targets are allowed as satisfied references while missing targets still produce dedicated dependency diagnostics instead of generic JSON/parse failures. (verification: unit - analyzer tests cover archived dependency accepted, missing dependency rejected with missing classification, and archived dependency not reported as invalid JSON)
- [x] Task 5: Update dispatch-time dependency checks in `src/parallel/queue_state.rs` so queued/in-flight dependencies block until merged to base, archived dependencies are treated as resolved, and missing dependencies fail closed without dispatch. (verification: integration - parallel executor tests cover dependent change blocked while prerequisite is applying, dispatched after prerequisite archive/merge, archived dependency satisfied, and missing dependency not dispatched)
- [x] Task 6: Ensure user-facing events/logs distinguish dependency blocked, archived/satisfied dependency, and missing dependency cases without marking archived dependencies as rejected. (verification: integration - existing `DependencyBlocked`/error event tests or new parallel tests assert archived dependency does not emit `ChangeRejected` and missing dependency emits a dedicated diagnostic)
- [x] Task 7: Run targeted regression checks for analyzer and parallel scheduler dependency handling. (verification: integration - run `cargo test analyzer parallel::tests::executor --lib` or narrower equivalent tests covering the new cases)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-dependency-target-handling --archive-gate`
