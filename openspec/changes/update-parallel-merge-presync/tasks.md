## 1. 仕様・設計
- [ ] 1.1 既存のマージ/resolveフロー（Gitバックエンド）を読み解き、現状の統合点を特定する
- [ ] 1.2 事前同期（base → worktree）の適用箇所と、コンフリクト時の収束条件を設計に反映する

## 2. 実装
- [ ] 2.1 逐次マージ手順に「base → worktree 事前同期」を追加する
- [ ] 2.2 事前同期でコンフリクトが起きた場合に `resolve_command` で収束できるようにする
- [ ] 2.3 最終統合コミットの subject が `Merge change: <change_id>` になることを保証する

## 3. テスト
- [ ] 3.1 最小のE2E/統合テストで、baseが進んだ状態でもマージが収束することを検証する
- [ ] 3.2 事前同期でのコンフリクト発生ケースを追加し、worktree側で解消されることを確認する

## 4. 互換性・運用
- [ ] 4.1 既存挙動との差分（追加されるGit操作、履歴、ログ）を整理し、必要なら設定で切り替え可能にする
- [ ] 4.2 ドキュメント/ログ文言に「事前同期」が分かる形で反映する
