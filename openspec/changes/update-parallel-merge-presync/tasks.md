## 1. 仕様・設計
- [x] 1.1 既存のマージ/resolveフロー（Gitバックエンド）を読み解き、現状の統合点を特定する
- [x] 1.2 事前同期（base → worktree）の適用箇所と、コンフリクト時の収束条件を設計に反映する

## 2. 実装
- [x] 2.1 逐次マージ手順に「base → worktree 事前同期」を追加する（常に有効）
- [x] 2.2 事前同期（base → worktree）のマージコミット subject を `Pre-sync base into <change_id>` に統一する
- [x] 2.3 事前同期でコンフリクトが起きた場合に `resolve_command` で収束できるようにする（解消は worktree 側で完結）
- [x] 2.4 最終統合コミットの subject が `Merge change: <change_id>` になることを保証する
- [x] 2.5 事前同期と最終統合が同一の `resolve_command` 実行ループ内で完結するようにする

## 3. テスト
- [x] 3.1 最小のE2E/統合テストで、baseが進んだ状態でもマージが収束することを検証する
- [x] 3.2 事前同期でのコンフリクト発生ケースを追加し、worktree側で解消されることを確認する

## 4. 互換性・運用
- [x] 4.1 既存挙動との差分（追加されるGit操作、履歴、ログ）を整理する（設定での切り替えは行わない）
- [x] 4.2 ドキュメント/ログ文言に「事前同期」および `Pre-sync base into <change_id>` が分かる形で反映する
