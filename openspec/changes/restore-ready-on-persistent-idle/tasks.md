## Implementation Tasks

- [x] Add and exhaustively classify a typed persistent-scheduler idle event in `src/events.rs`, including stable `/api/v2` event vocabulary. Publish semantic lifecycle `idle` from the authoritative dispatch only when the event's guarded Running-to-Ready projection succeeds; a late event against Select/Stopping/Error/Stopped must publish no new idle transition. Completion requires event ownership tests to fail if the new variant is omitted from reducer/frontend/remote classification and to prove the event carries no change ID or completion claim. (verification: unit - `cargo test persistent_idle_lifecycle_is_idle -- --list | grep -q persistent_idle_lifecycle_is_idle && cargo test persistent_idle_lifecycle_is_idle`; verification-id: persistent-idle-ready-regressions)

- [x] Emit the event from `src/parallel/orchestration.rs` immediately before coherent persistent idle parking, using the existing idle predicate rather than a duplicate drain calculation. Add an idle-episode latch whose completion evidence proves fully-drained and blocked/waiting-only inputs emit once, repeated evaluation and no-op notifications emit nothing further, the same scheduler remains alive, and admitted work rearms the next idle edge. (verification: unit - `cargo test persistent_idle_event_is_edge_triggered -- --list | grep -q persistent_idle_event_is_edge_triggered && cargo test persistent_idle_event_is_edge_triggered`; verification-id: persistent-idle-ready-regressions)

- [x] Project the idle event in TUI and Web as a guarded Running-to-Ready/`select` transition with a process-local `persistent_scheduler_idle` idle-episode fact, without calling completion helpers or rewriting reducer-derived rows, queue intent, blocker metadata, worktree facts, diagnostics, timing evidence, or execution marks. Keep the fact true through Start notification and idle-origin Stopping, clear it on admitted work/Error/Stopped, and restore Ready on cancel-stop while it remains true. Completion requires tests for fully drained and blocked/stalled/waiting-only rows, no success message, retained Error/Stopping/Stopped modes, cancel-stop restoration, and unchanged non-mode snapshot fields. (verification: integration - `cargo test persistent_idle_projects_ready_without_completion -- --list | grep -q persistent_idle_projects_ready_without_completion && cargo test persistent_idle_projects_ready_without_completion`; verification-id: persistent-idle-ready-regressions)

- [x] Preserve live-scheduler command admission and discoverability while persistent idle is presented as Ready. `persistent_scheduler_idle` makes Web expose Start, graceful stop, and force stop; TUI exposes Start plus first-Esc graceful stop and the existing second-Esc force-stop progression; pre-run Select exposes only Start. Shared run control independently revalidates `scheduler.is_running()`: marks remain mark-only; Start applies existing queue intent and returns `SchedulerEffect::Notified` without spawning or prematurely projecting Running; graceful stop wakes the idle waiter after setting the stop request; force stop cancels the same scheduler; idle-origin cancel-stop restores Ready. Completion requires TUI and remote-adapter parity plus Web action-rendering tests. (verification: integration - `cargo test persistent_idle_commands_use_live_scheduler -- --list | grep -q persistent_idle_commands_use_live_scheduler && cargo test persistent_idle_commands_use_live_scheduler && make web-test`; verification-id: persistent-idle-ready-regressions)

- [x] Make existing typed admitted-work start events restore TUI and Web Running and clear `persistent_scheduler_idle` after Ready, covering ordinary `WorkspacePreparationStarted` and scheduler-owned resolve/rejection/base-lane work while leaving Start notification, queue notification, `AnalysisStarted`, and refresh events Ready with the idle fact retained. Completion requires a parity regression that traverses idle, a no-op wake, actual admitted work, and a second idle edge. (verification: integration - `cargo test admitted_work_restores_running_after_idle -- --list | grep -q admitted_work_restores_running_after_idle && cargo test admitted_work_restores_running_after_idle`; verification-id: persistent-idle-ready-regressions)

- [x] Verify `/api/v2` consumes the same idle dispatch and publishes `app_mode: select` plus `persistent_scheduler_idle: true` at that event's revision under the canonical serialized optimistic revision contract, with one event/revision for the first edge and no revision churn for repeated/no-op idle delivery. Include the field in the generated OpenAPI `InstanceSnapshot` schema. Completion requires tests to use the authoritative dispatch boundary and shared Web state rather than mutating a projected snapshot directly, and to assert admitted work/Error/Stopped clear the field coherently. (verification: integration - `cargo test --features web-monitoring persistent_idle_projects_api_ready_once -- --list | grep -q persistent_idle_projects_api_ready_once && cargo test --features web-monitoring persistent_idle_projects_api_ready_once`; verification-id: persistent-idle-ready-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate restore-ready-on-persistent-idle --archive-gate`.

## Future Work

- None. Scheduler lifetime, queue admission, and retry policy remain intentionally outside this presentation correction.
