## Implementation Tasks

- [x] Replace `gated` lifecycle/display derivation with `stalled` for acceptance-gate observations in reducer state handling, preserving blocker metadata such as `acceptance-gated` (verification: unit - reducer tests in `src/orchestration/state.rs` prove acceptance gate inputs derive `stalled`, dependency waits derive `blocked`, and terminal rejection derives `rejected`).
- [x] Remove user-facing `gated` status handling from TUI/Web/API state surfaces and update filters, colors, fixtures, and status maps to use `stalled` for non-terminal acceptance holds (verification: unit - tests covering `src/tui/state.rs`, `src/web/state.rs`, and `src/server/api/ws.rs` prove no visible status map emits or expects `gated`).
- [x] Keep acceptance verdict parsing compatibility for canonical `gated` and legacy `blocked` inputs while routing parsed blocker outcomes into stalled/rejecting flow instead of a `gated` display state (verification: unit - `src/acceptance.rs` tests continue to accept `ACCEPTANCE: GATED`, JSON `gated`, and supported legacy blocked inputs).
- [x] Update rejection/acceptance routing so review-required or resumable acceptance blockers remain non-terminal `stalled` or active `rejecting`, while only confirmed rejection reaches terminal `rejected` (verification: integration - tests under `src/parallel/tests/` and `src/orchestration/rejection.rs` prove `Confirm -> rejected`, `Resume -> applying`, and `Block -> stalled` or equivalent non-terminal hold with worktree preserved).
- [x] Update prompt, skill, command, and canonical spec wording so `gated` is not described as operator-facing lifecycle taxonomy; if retained, it is limited to acceptance verdict input compatibility/protocol wording (verification: integration - run `rg 'gated|acceptance-gated' .opencode/commands skills openspec src` and review `.opencode/commands/cflx-accept.md`, `skills/cflx-accept/SKILL.md`, `openspec/specs/orchestration-state/spec.md`, `openspec/specs/parallel-execution/spec.md`, `openspec/specs/cli/spec.md`, `openspec/specs/agent-prompts/spec.md`, and relevant `src/*.rs` matches to confirm lifecycle/display contracts no longer expose `gated` outside parser compatibility notes).
- [x] Run proposal and implementation verification (verification: integration - `cflx openspec validate remove-gated-status --strict --evidence warn`, targeted Rust tests for `src/acceptance.rs`, `src/orchestration/state.rs`, `src/tui/state.rs`, `src/web/state.rs`, and `cargo clippy --all-targets --all-features -- -D warnings` or the then-current documented lint/typecheck command).

## Future Work

- A later cleanup may remove acceptance `gated` verdict compatibility entirely after all bundled prompts, skills, and external integrations have migrated away from producing it.

## Acceptance #1 Failure Follow-up
- [x] docs/openapi.yaml:312-319 still documents queue_status as including "gated" in the canonical display taxonomy. Remove gated from the OpenAPI queue_status description (and regenerate/update any source that produces this file if applicable) so Web/API documentation no longer exposes gated as a lifecycle/display status.
- [x] openspec/specs/agent-prompts/spec.md:226 still says rejecting review distinguishes reject from a gated hold, which is operator-facing lifecycle wording. Reword the canonical prompt spec to distinguish terminal reject from a stalled hold / acceptance blocker compatibility verdict without describing a lifecycle hold as gated (verification: integration - `rg -n "gated hold|stalled hold" openspec/specs/agent-prompts/spec.md` shows the requirement line uses `stalled hold` wording, not `gated hold`).
