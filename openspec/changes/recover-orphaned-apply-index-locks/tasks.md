## Implementation Tasks

- [ ] Add a dedicated Apply `index.lock` reclamation boundary that captures managed-worktree lock candidates immediately before each normal or escalation Apply spawn and retains the observation only for that dispatch. Resolve symlink/canonical path variants through the existing `index_lock` primitives, distinguish absent, pre-existing, and unverifiable candidates, and make the observation single-use and process-local. Completion requires unit tests that fail if a pre-existing or unobserved lock becomes reclaimable, including worktree path aliases. (verification: unit - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

- [ ] Implement the post-quiescence two-point reclamation decision and unlink operation for Unix without `lsof`, wall-clock age, PID lookup, or new dependencies. Require `ProcessGroupQuiescence::Confirmed`, an absent pre-dispatch observation, a regular non-symlink zero-byte first observation, and after an injected 500 millisecond dwell an `O_NOFOLLOW` descriptor whose `fstat` preserves device, inode, zero size, and modification time. Treat disappearance and unlink `ENOENT` as natural convergence; return typed refusal for every other unsupported, changed, or failed condition. Completion requires deterministic decision tests plus temporary-filesystem tests for symlink refusal, inode replacement, size/mtime mutation, metadata/open failures, natural disappearance, successful unlink, and unlink failure. (verification: unit - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

- [ ] Wire the same reclamation operation into normal post-agent Apply finalization immediately after the existing process-group barrier and before task/Git inspection, and into `preserve_interrupted_apply_progress` immediately after confirmed cleanup and before dirtiness/progress inspection. A refusal must return an actionable Apply error and suppress WIP snapshot, final commit, cleanup review, rejecting handoff, and Acceptance; success or natural convergence may continue through existing repository gates. Completion requires integration tests for both consumers, one-use dispatch binding, normal completion, cancellation/runtime-limit preservation, and zero repository/handoff side effects after refusal. (verification: integration - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

- [ ] Extend structured diagnostics for reclamation outcomes with change ID, iteration, workspace, managed lock path, quiescence classification, refusal reason, and available device/inode/size/mtime evidence. Remove or narrow existing source comments that claim Conflux never deletes any lock while preserving the rule that retry modules never delete locks. Completion requires event/log assertions that distinguish reclaimed, naturally converged, pre-existing, unstable, unsupported, and unlink-failed outcomes without reading file contents. (verification: unit - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

- [ ] Change WIP snapshot and final Apply commit contention budgets to five total attempts with fixed 500 millisecond delays and no backoff, leaving structured command eligibility, repository hooks, cancellation checks, ambiguous-success repository proofs, and duplicate prevention unchanged. Completion requires injected-sleeper tests asserting exactly four waits of 500 milliseconds on exhaustion, suppression before the next attempt on cancellation, unchanged non-lock classification, and exactly one WIP/final commit after recovery. (verification: unit - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

- [ ] Add focused real-boundary coverage and a discovery-guarded `test-orphaned-apply-index-locks` Make target. The default focused set must run the deterministic unit/integration cases and fail if its filter discovers no tests; explicit heavy coverage must spawn a descendant that creates a real zero-byte managed-worktree lock, survives until forced cleanup, leaves the lock after quiescence, and prove reclamation permits exactly one expected Git finalization. A paired heavy case must prove a lock present before dispatch remains untouched and blocks finalization. (verification: integration - `make test-orphaned-apply-index-locks`; verification-id: orphaned-apply-index-lock-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate recover-orphaned-apply-index-locks --archive-gate`.

Repository-wide Rust formatting and clippy remain owned by the existing path-scoped `.pre-commit-config.yaml` hooks when Rust files are staged. Requirement-specific tests and the focused discovery gate remain explicit tasks because those hooks do not prove this behavior.

## Future Work

- Startup or restart-time stale-lock recovery remains manual because a new process cannot prove same-dispatch pre-absence.
- Windows reclamation requires a separate platform-specific evidence design; this change remains fail-closed for `NotApplicable` cleanup.
- If external concurrent Git on managed worktrees becomes supported, replace automatic unlink with a stronger repository-wide writer coordination design rather than adding `lsof` or age heuristics.
