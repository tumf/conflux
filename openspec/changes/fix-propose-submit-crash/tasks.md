## 1. 調査
- [ ] 1.1 `Ctrl+S` 送信時のクラッシュ再現と原因特定

## 2. 実装
- [ ] 2.1 Proposingモードの `Ctrl+S` 送信処理を安全化（panic回避/入力取得のガード）
- [ ] 2.2 送信成功時はSelectへ戻すが、失敗時は入力保持でProposing維持
- [ ] 2.3 `Enter` を改行専用に整理し、送信は `Ctrl+S` のみに統一
- [ ] 2.4 Proposingモードのキーヒントとタイトル表示を更新

## 3. テスト
- [ ] 3.1 送信成功時のモード遷移とログのユニットテスト更新
- [ ] 3.2 送信失敗時にProposing維持＋入力保持のユニットテスト更新
- [ ] 3.3 `Ctrl+S` 送信時にクラッシュしないことのテスト追加

## 4. 検証
- [ ] 4.1 `cargo test`
