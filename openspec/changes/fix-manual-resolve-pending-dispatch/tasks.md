## Implementation Tasks

- [x] Trace and normalize the manual resolve retry handshake across `src/tui/state.rs`, `src/tui/command_handlers.rs`, `src/tui/runner.rs`, `src/orchestration/state.rs`, `src/parallel/queue_state.rs`, and `src/parallel/orchestration.rs`. (verification: unit - targeted tests identify the exact accepted/blocked state transitions for `ResolveMerge`; completion condition: the implementation summary and tests show where pending is created, synchronized, dispatched, or rejected)
- [x] Ensure pressing `M` on a repository-visible manual merge-wait row records reducer-owned retry intent that remains scheduler-consumable for archived-but-not-merged changes. (verification: unit - add or update reducer tests in `src/orchestration/state.rs` proving the observed archived merge-wait case transitions into `ResolveWait` instead of `NoOp`; completion condition: reducer-visible `ResolveWait` membership exists after accepted manual resolve)
- [x] Update TUI command handling so scheduler notification and "scheduled" logging only occur when reducer-owned retry intent was actually accepted. (verification: unit - add or update `src/tui/command_handlers.rs` tests proving `ReduceOutcome::NoOp` does not emit scheduled-retry logs or false pending confirmation; completion condition: command handling distinguishes accepted vs rejected retry intent)
- [x] Fix scheduler retry consumption so reducer-owned manual resolve pending is reevaluated after queue notifications and slot-release events and dispatches retry work when repository-visible conditions allow it. (verification: integration - add or update targeted scheduler tests in `src/parallel/tests/executor.rs` or equivalent proving `ResolveWait retry dispatch started` occurs after the observed `M` -> pending -> slot available sequence; completion condition: the loop does not strand accepted manual pending when capacity later becomes available)
- [x] Ensure stranded or invalid manual resolve pending falls back to a truthful blocker/terminal state instead of remaining indefinitely at `resolve pending`. (verification: integration - tests cover the case where retry is no longer eligible and assert transition back to `merge wait`, `merged`, or another truthful state with visible evidence; completion condition: no stale display-only pending survives after dispatch rejection/failure)
- [x] Bound repeated manual-resolve retry diagnostics so unchanged pending/blocker loops do not spam user-visible logs. (verification: unit/integration - add or update tests for repeated identical retry/blocker diagnostics proving identical messages are deduped, rate-limited, or summarized until the blocker signature changes)
- [x] Run focused regression coverage for reducer, TUI, and scheduler manual resolve flows. (verification: unit - run exact reducer/TUI tests added for `ResolveMerge` acceptance and command handling; integration - run exact scheduler/manual-resolve regression tests proving accepted pending is consumed after queue notification or slot release)

## Final Validation

Expected archive gate: `cflx openspec validate fix-manual-resolve-pending-dispatch --archive-gate`
