## Implementation Tasks

- [x] Add canonical spec deltas in `openspec/specs/parallel-analysis/spec.md` and `openspec/specs/agent-prompts/spec.md` so the archived `update-analyze-use-proposal-frontmatter` metadata source rule is extended with a closed-world dependency target rule limited to the current queued set and explicit in-flight set. (verification: integration - `cflx openspec validate fix-analyze-json-dependency-contract --strict --evidence warn` succeeds, and `src/analyzer.rs` plus `src/parallel_run_service.rs` remain aligned with `openspec/changes/fix-analyze-json-dependency-contract/specs/parallel-analysis/spec.md` and `openspec/changes/fix-analyze-json-dependency-contract/specs/agent-prompts/spec.md`)
- [x] Update `skills/cflx-analyze/SKILL.md` and the analyze prompt builder in `src/analyzer.rs` so the guidance explicitly forbids returning unrelated active changes as dependency targets. (verification: unit - `cargo test analyzer` covers `src/analyzer.rs` prompt output, and `skills/cflx-analyze/SKILL.md` plus `src/analyzer.rs` show only queued IDs and in-flight IDs as allowed dependency targets)
- [x] Update invalid dependency diagnostics in `src/analyzer.rs` so parse errors include the violating dependency ID plus the allowed queued and in-flight IDs. (verification: unit - `cargo test analyzer` exercises `src/analyzer.rs` parser tests so unrelated active dependencies fail with `Invalid dependency reference` and include the allowed set in the error text)
- [ ] Update `src/parallel_run_service.rs` or the related log formatting path so dependency-analysis failures are surfaced as invalid dependency contract failures instead of generic invalid JSON. (verification: integration - `cargo test parallel_run_service` or equivalent logging coverage exercises `src/parallel_run_service.rs` so the invalid dependency root cause appears in repository-visible log/message output)
- [ ] Add regression tests in `src/analyzer.rs` or `src/agent/tests.rs` for queued dependencies, in-flight dependencies, and unrelated active dependencies so the `persist-archive-resume-reasons -> align-archive-readiness-failure-reporting` style out-of-scope reference cannot silently regress. (verification: unit - `cargo test analyzer` or `cargo test agent` covers `src/analyzer.rs` / `src/agent/tests.rs`, with queued and in-flight cases passing while the unrelated active case fails with a parse error)
- [ ] Run proposal and repository verification for the touched analyze paths. (verification: integration - `cflx openspec validate fix-analyze-json-dependency-contract --strict --evidence warn`, `cargo test analyzer`, `cargo test parallel_run_service`, and `cargo clippy --all-targets --all-features -- -D warnings` all succeed)

## Future Work

- LLM analyze failure 時の fallback policy を「全件並列」以外にも degradable order-preserving mode へ拡張する検討
- dependency analysis result を machine-readable telemetry として履歴保存する改善
