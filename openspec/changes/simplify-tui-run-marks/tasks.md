## Implementation Tasks

- [ ] Refactor the shared execution-mark lifecycle contract in `src/orchestration/operator_command.rs` so every visible non-terminal row accepts mark-only mutation in Select, Running, Stopping, Stopped, and Error modes without consulting active/retry/wait status, apply-limit state, or current parallel eligibility. Completion requires one shared classifier used by single and bulk TUI paths plus `/api/v2`, while archived/merged/pushed/rejected rows remain excluded with unchanged read-only terminal semantics. (verification: unit - add focused shared-service tests named with `run_mark_intent` covering all modes/status classes, bulk target-state calculation, and no mark-time eligibility refusal; run `cargo test --lib run_mark_intent`; verification-id: run-mark-contract-tests)

- [ ] Separate mark mutation from queue and runtime control in `src/orchestration/operator_command.rs`, `src/tui/state/selection_logic.rs`, and the TUI command adapter. Completion requires Space and `x` to write only target-scoped execution marks, never emit `AddToQueue`/`RemoveFromQueue`, run queue hooks, alter reducer queue intent, request cancellation, create retry/resolve intent, wake a scheduler, or change process mode; unmarking an already active or queued row must leave its current-run execution and queue state unchanged. (verification: integration - extend `src/orchestration/operator_command/tests.rs`, `src/tui/state/selection_logic.rs`, and `src/tui/command_handlers/cross_adapter_tests.rs` with side-effect recorders that fail on queue, hook, scheduler, cancellation, retry, resolve, or mode effects; run `cargo test --lib run_mark_intent`; verification-id: run-mark-contract-tests)

- [ ] Move run-target validity to final configured Start/F5 admission in `src/orchestration/run_control.rs`. Completion requires run control to consume one coherent mark snapshot, preserve the existing all-or-nothing worktree eligibility fence, classify retry-eligible recovery rows from Ready/Select, Stopped, and process-wide Error without relying on process mode alone, dispatch retry routes before ordinary-start routes, retain and report ordinary marked rows deferred by a mixed retry invocation, exclude other non-startable statuses with target-specific diagnostics, reject when zero runnable targets remain, and complete every refusal before queue, scheduler, retry-edge, or mode mutation; tests must prove `ProcessingError` followed by persistent idle, re-mark, and F5 dispatches the failed change through typed retry, mixed retry/ordinary marks do not apply run-wide explicit-retry semantics to fresh work, and unmarking after admission does not cancel or dequeue current work. (verification: integration - add focused `run_mark_intent_start_admission_*` tests in `src/orchestration/run_control/tests.rs` and the TUI/application adapter suites; run `cargo test --lib run_mark_intent_start_admission`; verification-id: run-mark-contract-tests)

- [ ] Reconcile archive completion with process-local mark ownership in `src/orchestration/mark_reconciliation.rs` and the authoritative dispatch/projection path. Completion requires preserving existing failure, rejection, rejected/parallel-ineligible refresh, dequeue, target-stop, and first `on_merged` recovery revocations while adding the first effective `ChangeArchived` edge to clear only its target mark in the same state revision; duplicate or stale archive events must be no-ops; unrelated marks must remain; and later merged/pushed transitions must not recreate the mark. (verification: integration - add focused archive-edge tests in `src/orchestration/mark_reconciliation/tests.rs` and remote snapshot revision coverage; run `cargo test --lib run_mark_intent_archive`; verification-id: run-mark-contract-tests)

- [ ] Update TUI checkbox rendering and mark hints in `src/tui/render.rs` plus Space routing behavior in `src/tui/state/selection_logic.rs`. Completion requires archived, merged, and pushed rows to render an exactly checkbox-width blank placeholder in both list layouts, preserve every following column position and preview-width calculation, omit Space/`x` mark affordances for post-archive rows, and treat Space on those rows as a silent no-op without warnings or pending mark writes. (verification: unit - replace archived `[x]` assertions with buffer-cell and column-offset regressions named `archived_checkbox_placeholder_*`, including selected/unselected fixtures and Select/Running/Stopped layouts; run `cargo test --lib archived_checkbox_placeholder`; verification-id: run-mark-contract-tests)

- [ ] Replace the legacy canonical mark/queue and process-mode-only retry contracts in `cli` Dynamic Execution Queue and Error Retry with F5 Key, `operator-command-execution` Mode-aware mark and queue behavior, `tui-error-handling` Change-Level Processing Errors Do Not Force App Error Mode, `tui-architecture` Active Change Input Lockout and both duplicate Queue State Synchronization requirements, and `tui-state-management` error-change-space-toggle-running-mode without dropping unrelated queue-service, resolve-wait, parallel queued-display, change-scoped failure, configured-start, refresh, active Apply-limit, or `@` guarantees. Completion requires archived promotion to contain no surviving requirement that aliases Space or bulk `x` to stop/DynamicQueue effects or requires process-wide Error before configured Start can consume a marked typed retry route, and no duplicate legacy requirement that can preserve the old semantics. (verification: integration - extend archive-promotion coverage in `src/openspec_cmd.rs`, run `cargo test openspec`, and assert the promoted cli, operator-command-execution, tui-error-handling, tui-architecture, and tui-state-management requirements contain the complete pure-mark and Ready/Select retry contract; verification-id: run-mark-contract-tests)

- [ ] Align TUI key-hint and remote-control projections with the pure-mark contract. Completion requires non-terminal rows to advertise `Space: mark` or `Space: unmark` across lifecycle modes, bulk hints to depend on visible non-terminal targets rather than execution mode, terminal rows to omit mark hints, active rows to keep the independent `K: kill` hint alongside mark intent, overlays to retain input ownership, and `/api/v2` action eligibility plus single/bulk command outcomes to match TUI semantics without queue aliases. (verification: integration - extend TUI render/key routing and `src/web/remote_control_api/tests` parity cases; run `cargo test --lib run_mark_intent`; verification-id: run-mark-contract-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate simplify-tui-run-marks --archive-gate`.

## Future Work

- A distinct UI for direct current-run DynamicQueue editing may be proposed later if operators still need that control; this change intentionally keeps queue APIs but removes their alias from execution marks.
- A compact row layout may be proposed separately. This change deliberately preserves the existing checkbox column width after archive.
