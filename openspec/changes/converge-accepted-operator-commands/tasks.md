## Implementation Tasks

- [x] Introduce one feature-independent process-local operator application coordinator and Core-owned lifecycle mode over the existing `OperatorCommandService`, `RunControlService`, reducer, resolve ledger, and authoritative event boundary. Completion requires final mode/status validation, changed/no-op/failed result typing, process-local restart behavior, and one serialization gate shared by TUI and v2 without turning process mode into per-change reducer or durable workspace state. Typed run activation such as `ProcessingStarted`, authoritative `Stopping`, `Stopped`, global `Error`, guarded `AllCompleted`, and the typed persistent-idle Ready event when available must transition the same Core mode so natural completion readmits Start. Add `accepted_operator_command_transaction` tests proving command and lifecycle-event mode transitions, invalid-mode no-effects, no-target Start behavior, and fresh Select/process-local state after reconstruction. (verification: unit - `cargo test --lib accepted_operator_command_transaction`; verification-id: accepted-operator-command-regressions)

- [x] Add one process-lifetime authoritative dispatch boundary plus the minimal typed operator-outcome vocabulary and projection path in `src/events.rs`, `src/orchestration/state.rs`, `src/tui/runner.rs`, `src/tui/orchestrator.rs`, `src/tui/run_supervisor.rs`, `src/tui/state.rs`, and `src/web/state.rs`, reusing `Stopping`, `Stopped`, and `ChangeDequeued` where exact. Completion requires runner-local producers, command outcomes in Select/Stopped/Error, and every orchestration run to share that boundary; run, cancel-stop, force-stop-pending, mark/queue delta, active/queued resolve, and lifecycle facts must reach Core mode, TUI, Web, `/api/v2`, and lifecycle consumers without reapplying reducer commands, per-run authority, direct command-handler Web application, or synthetic `ProcessingStarted`; exhaustive event classification and duplicate-delivery guards must compile and pass. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_mode_matrix`; verification-id: accepted-operator-command-regressions)

- [x] Refactor `RunSchedulerPort`, `RunControlService`, and `TuiRunSupervisor` to provide an event-silent prepared permit for Start, retry, and active resolve. Completion requires staged decision commit plus outcome dispatch to be atomic, the outcome revision to precede any scheduler-event revision, infallible activation or one wake to happen only after the transaction gate is released, queued resolve to avoid dispatch, and injected preparation failure to preserve reducer state, marks, dynamic queue, explicit-retry edges, resolve FIFO, mode, hooks, scheduler counts, event sequence, and projection revision. Run initialization may rebuild reducer state from accepted targets, but tests must assert the coherent outcome snapshot before activation rather than reducer allocation identity. Add `accepted_operator_command_scheduler_order` races for immediate progress, immediate terminal completion, live scheduler wake, new scheduler start, and failure rollback. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_scheduler_order`; verification-id: accepted-operator-command-regressions)

- [x] Route TUI commands and `SharedServiceExecutor` through the same coordinator, removing adapter-owned lifecycle application and command-side full-store mark replacement. TUI handlers must submit coordinator work outside the event-processing/render loop and receive accepted results through authoritative dispatch rather than awaiting the gate or a termination waiter. Completion requires remote mark, queue, run, retry, resolve, stop, cancel-stop, force-stop, and dequeue outcomes to reach the next TUI event-processing pass; local commands must reach Web at the same authoritative dispatch; command mark updates must touch only named IDs; Running queue checkboxes and hidden Error retry intent must remain distinct; Archived/global Stopped marks must survive while successful target dequeue clears only its target. Add `accepted_operator_command_tui_convergence` with one shared `ExecutionMarkStore`, `ResolveReservations`, reducer, TUI `AppState`, Web projection, coordinator, and recording scheduler. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_tui_convergence`; verification-id: accepted-operator-command-regressions)

- [x] Serialize `/api/v2` exact replay, ordinary final new-command revision validation, reservation, application, outcome dispatch, and settlement in `src/web/remote_control_api/commands.rs`, `executor.rs`, and `projection.rs`. Implement stop-and-dequeue as admission/cancellation and post-wait commit phases, keeping confirmation outside the gate and revalidating current target runtime state before commit without rechecking the consumed `expected_revision`. Completion requires `complete_command` to store an explicit revision; a changed command to store its single outcome revision; ordinary no-op/failure to store the unchanged admitted revision; two-phase timeout/post-wait refusal to store the explicit unchanged settlement revision without a dequeue event; later scheduler events not to rewrite it; two concurrent ordinary identities at one expected revision not both to execute; in-progress and completed exact replay not to repeat cancellation, waiter, or any other side effect; and mismatched identity to remain `idempotency_mismatch`. Add `accepted_operator_command_dequeue_liveness` with a never-completing waiter and short timeout proving unrelated commands, force stop, event drain, and TUI rendering remain live. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_dequeue_liveness`; verification-id: accepted-command-revision-regressions)

- [x] Extend `src/tui/command_handlers/cross_adapter_tests.rs` and remote-control integration fixtures into a table-driven parity matrix for Start/Stopped resume/Error retry, active and queued resolve, stop/cancel-stop, both force-stop safe-boundary classes, mark, queue intent, and stop-and-dequeue across valid, no-op, invalid-mode, ineligible-target, cancellation, scheduler-live/idle, and injected-failure cases. Completion requires each row to compare typed outcome/error, Core mode, reducer status, marks, queue/retry effects, resolve ledger, graceful-stop and cancellation calls, scheduler start/notify counts, TUI next-frame state, Web snapshot, event/revision cardinality, and unchanged unrelated targets; every default test must finish under one second or use the repository heavy-test policy. (verification: integration - `cargo test --features web-monitoring accepted_operator_command`; verification-id: accepted-operator-command-regressions)

## Notes

- evidence: `cargo test --features web-monitoring accepted_operator_command` passes with 28 selected tests, covering all five declared filters (`accepted_operator_command_transaction`, `_mode_matrix`, `_scheduler_order`, `_dequeue_liveness`, `_tui_convergence`).
- evidence: `cargo test --features web-monitoring accepted_command_revision` passes with 5 selected tests covering outcome-revision capture, no-op/failure revisions, concurrent same-revision submissions, idempotency mismatch, and two-phase timeout.
- evidence: `cargo test --features web-monitoring tui_and_v2` passes; the parity table compares 25 rows and 6 bulk-mark cases.
- The parity table now compares each row as one value across shared decision state (scheduler start/notify/cancel/graceful-stop sequence, reducer status, reducer queue, explicit-retry edges, resolve ledger, marks, Core mode) *and* both frontends (TUI mode, marked rows, resolving flag; Web `app_mode`, projected marks, `is_resolving`) plus authoritative dispatch and projection-revision cardinality. Each row also asserts its declared settlement against its own arrangement, so a `Changed` row that changed nothing and a `NoOp`/`Failed` row that mutated anything both fail.
- Scope boundary found during apply: the TUI's Space-key and `x` execution-mark writes are not `TuiCommand`s. They are classified by frontend guards and drained by the runner straight into `OperatorCommandService::apply_execution_mark`, so they set the shared store without publishing an accepted-command outcome and reach `/api/v2` only when a later event republishes the projection. Converging them means moving mark classification behind the coordinator, which this proposal leaves to the separate bulk/parallel-control work; the bulk parity comparison therefore stops at shared decision state and says so in `src/tui/command_handlers/cross_adapter_tests.rs`. Every path that *is* a command — including remote `set_execution_mark` and both queue-intent commands — publishes and converges.

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate converge-accepted-operator-commands --archive-gate`.

The hook-enabled final Apply commit owns format and all-target/all-feature Clippy coverage, so the declared Rust verification does not rerun them. No browser or external-service verification is required because this change modifies process-local service, event, TUI adapter, and API projection behavior without adding browser controls or deployed contracts.

## Future Work

- Complete `fix-force-stop-reducer-reconciliation` independently to strengthen the per-change reducer state consumed by the correctly ordered `Stopped` event.
- Keep full remotely meaningful TUI inventory auditing in the parent parity work; this proposal verifies every command it changes but does not classify local-only editor, shell, QR, or filesystem operations.
