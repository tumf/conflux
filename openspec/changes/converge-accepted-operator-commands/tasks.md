## Implementation Tasks

- [ ] Introduce one process-local operator application coordinator and Core-owned lifecycle mode over the existing `OperatorCommandService`, `RunControlService`, reducer, resolve ledger, and authoritative event dispatcher. Completion requires final mode/status validation, changed/no-op/failed result typing, process-local restart behavior, and one serialization gate shared by TUI and v2 without turning process mode into per-change reducer or durable workspace state. Add `accepted_operator_command_transaction` tests proving all accepted mode transitions, invalid-mode no-effects, no-target Start behavior, and fresh Select/process-local state after reconstruction. (verification: unit - `cargo test --features web-monitoring accepted_operator_command_transaction`; verification-id: accepted-operator-command-regressions)

- [ ] Add the minimal typed operator-outcome event vocabulary and projection path in `src/events.rs`, `src/orchestration/state.rs`, `src/tui/state.rs`, and `src/web/state.rs`, reusing `Stopping`, `Stopped`, and `ChangeDequeued` where exact. Completion requires one authoritative dispatch to project run, cancel-stop, force-stop-pending, mark/queue delta, and active/queued resolve facts to Core mode, TUI, Web, `/api/v2`, and lifecycle consumers without reapplying reducer commands or synthesizing `ProcessingStarted`; exhaustive event classification and duplicate-delivery guards must compile and pass. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_mode_matrix`; verification-id: accepted-operator-command-regressions)

- [ ] Refactor `RunSchedulerPort`, `RunControlService`, and `TuiRunSupervisor` to provide a prepared activation or equivalent event gate for Start, retry, and active resolve. Completion requires scheduler code to emit no event before the accepted outcome dispatch, activation/wake to happen exactly once afterwards, queued resolve to avoid dispatch, and injected preparation/activation failure to preserve reducer state, marks, dynamic queue, explicit-retry edges, resolve FIFO, mode, hooks, scheduler counts, event sequence, and projection revision. Add `accepted_operator_command_scheduler_order` races for immediate progress, immediate terminal completion, live scheduler wake, new scheduler start, and failure rollback. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_scheduler_order`; verification-id: accepted-operator-command-regressions)

- [ ] Route TUI commands and `SharedServiceExecutor` through the same coordinator, removing adapter-owned lifecycle application and command-side full-store mark replacement. Completion requires remote mark, queue, run, retry, resolve, stop, cancel-stop, force-stop, and dequeue outcomes to reach the next TUI event-processing pass; local commands must reach Web at the same authoritative dispatch; command mark updates must touch only named IDs; Running queue checkboxes and hidden Error retry intent must remain distinct; Archived/global Stopped marks must survive while successful target dequeue clears only its target. Add `accepted_operator_command_tui_convergence` with one shared `ExecutionMarkStore`, `ResolveReservations`, reducer, TUI `AppState`, Web projection, coordinator, and recording scheduler. (verification: integration - `cargo test --features web-monitoring accepted_operator_command_tui_convergence`; verification-id: accepted-operator-command-regressions)

- [ ] Serialize `/api/v2` exact replay, final new-command revision validation, reservation, application, outcome dispatch, and settlement in `src/web/remote_control_api/commands.rs`, `executor.rs`, and `projection.rs`. Completion requires `complete_command` to store an explicit command-produced revision; a changed command to store its single outcome revision; no-op/failure to store the unchanged admitted revision; later scheduler events not to rewrite it; two concurrent new identities at one expected revision not both to execute; in-progress and completed exact replay not to repeat any side effect; and mismatched identity to remain `idempotency_mismatch`. (verification: integration - `cargo test --features web-monitoring accepted_command_revision`; verification-id: accepted-command-revision-regressions)

- [ ] Extend `src/tui/command_handlers/cross_adapter_tests.rs` and remote-control integration fixtures into a table-driven parity matrix for Start/Stopped resume/Error retry, active and queued resolve, stop/cancel-stop, both force-stop safe-boundary classes, mark, queue intent, and stop-and-dequeue across valid, no-op, invalid-mode, ineligible-target, cancellation, scheduler-live/idle, and injected-failure cases. Completion requires each row to compare typed outcome/error, Core mode, reducer status, marks, queue/retry effects, resolve ledger, graceful-stop and cancellation calls, scheduler start/notify counts, TUI next-frame state, Web snapshot, event/revision cardinality, and unchanged unrelated targets; every default test must finish under one second or use the repository heavy-test policy. (verification: integration - `cargo test --features web-monitoring accepted_operator_command`; verification-id: accepted-operator-command-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate converge-accepted-operator-commands --archive-gate`.

The tracked Rust hooks are path-scoped, so the declared Rust verification retains explicit format and all-target/all-feature Clippy coverage. No browser or external-service verification is required because this change modifies process-local service, event, TUI adapter, and API projection behavior without adding browser controls or deployed contracts.

## Future Work

- Complete `fix-force-stop-reducer-reconciliation` independently to strengthen the per-change reducer state consumed by the correctly ordered `Stopped` event.
- Keep full remotely meaningful TUI inventory auditing in the parent parity work; this proposal verifies every command it changes but does not classify local-only editor, shell, QR, or filesystem operations.
