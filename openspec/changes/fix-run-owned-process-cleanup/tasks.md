## Implementation Tasks

- [ ] Add an invocation-scoped run command scope in the command-runner layer using existing Tokio synchronization primitives. Completion requires atomic open/closing admission, pre-task registration, a final spawn check serialized with scope shutdown, direct cancellation notification independent of `StreamingChildHandle`, retained operation/change/current-process identity, concurrent bounded quiescence waiting, and registration removal only after runner-task exit plus confirmed typed cleanup. Add `run_command_scope_refuses_spawn_after_shutdown` and `run_command_scope_suppresses_retry_after_shutdown`; each must execute real runner control flow and prove no command body or retry starts after closure. (verification: unit - `cargo test --lib run_command_scope_`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Create one fresh scope at each direct CLI and TUI-supervised run start and reuse the same scoped `AiCommandRunner` through `ParallelRunService` and `ParallelExecutor`. Completion requires analyze, Apply, Archive, Acceptance, cleanup review, rejection review, conflict resolve, and upstream repair production paths to share scope identity without adding operation-specific scope parameters; runners created only from a stagger timestamp are not allowed on these run paths. Add a construction-level regression that enumerates those surfaces and fails if any run-owned runner is unscoped or bound to a different scope. (verification: integration - the scoped-runner construction regression runs as part of `cargo test --lib run_command_scope_`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Wire global cancellation and run-fatal shutdown to close command admission and broadcast scope cancellation before workspace-future abort. Completion requires both exits to await runner-task/process-group quiescence or completed bounded managed escalation before Conflux-owned preparation release, worktree cleanup, terminal `Stopped`, or scheduler failure return; retry branches must recheck shutdown before delay and final spawn. Preserve exactly one prompt queue-owned run-fatal `Error`, no run-fatal `Stopped` or `AllCompleted`, and operator-cancellation classification when cleanup escalates. Add `run_fatal_error_precedes_cleanup_barrier` using a held fake registration to prove Error promptness and failure-return ordering. (verification: integration - `cargo test --lib run_fatal_error_precedes_cleanup_barrier`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Make per-change execution completion handshakes truthful. Completion requires workspace-task abort and registry removal to be insufficient for `done`; confirmed matching run-scope cleanup must precede acknowledgement, while unconfirmed cleanup leaves the waiter pending for its existing bounded timeout and preserves process identity for diagnostics/escalation. Add `execution_done_requires_process_quiescence` covering aborted task, pending handshake, confirmed cleanup, and unconfirmed timeout cases. (verification: unit - `cargo test --lib execution_done_requires_process_quiescence`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Align cancellation deadlines around one absolute start time: a 30-second concurrent run-command cleanup bound and the existing 90-second merge/base-lane drain must remain within the existing 120-second outer scheduler boundary without additive timeout reset. Completion requires tests or assertions for the ordering, concurrent cleanup of multiple registrations, bounded escalation diagnostics, and no new user configuration. (verification: unit - deadline-order and multi-registration cases run under `cargo test --lib run_command_scope_`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Retain the active run command scope in `TuiRunSupervisor` outside the spawned orchestrator task and consume it during local shutdown. Completion requires quit to close the scope immediately, wait through the scheduler cleanup boundary rather than the current five-second undercut, and on timeout force-clean and verify retained process identities before aborting and joining the task; remote client close behavior remains unchanged. Add `local_tui_shutdown_waits_for_run_command_scope` under `src/tui/runner.rs` for cancellation/deadline/remote-exclusion control flow; the real timeout process boundary is covered by `run_scope_tui_quit_cleans_process_group_after_timeout` in `tests/process_cleanup_test.rs`. (verification: integration - `cargo test --lib local_tui_shutdown_waits_for_run_command_scope && cargo test --features heavy-tests --test process_cleanup_test run_scope_tui_quit_cleans_process_group_after_timeout`; verification-id: run-owned-process-cleanup-regressions)

- [ ] Extend `tests/process_cleanup_test.rs` with heavy-gated Unix regressions `run_scope_global_cancellation_cleans_process_group`, `run_scope_run_fatal_cleans_process_group`, and `run_scope_tui_quit_cleans_process_group_after_timeout`. Completion requires real isolated process groups, a SIGTERM-immune descendant, PGID capture before shutdown, no members at the relevant `Stopped`, failure-return, or `AbortedAfterTimeout` boundary, exactly-one prompt Error assertion for run-fatal, and bounded self-cleanup on setup/assertion failure. (verification: integration - `cargo test --features heavy-tests --test process_cleanup_test run_scope_`; verification-id: run-owned-process-cleanup-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate fix-run-owned-process-cleanup --archive-gate`.

## Future Work

- Consider a separate compatibility cleanup for standalone non-run TUI worktree commands only if runtime evidence shows those caller-owned commands need the same multi-command scope.
- Consider configurable cleanup deadlines only after fixed internal bounds produce an operator-reported requirement; this change intentionally adds no configuration surface.
