## Implementation Tasks

- [ ] Identify the reducer/shared-state path that owns `ResolveWait` intent and add an explicit successful-merge cleanup operation for a change. (verification: unit - add or update tests in `src/orchestration/state.rs`, `src/parallel/tests/executor.rs`, or the closest existing reducer test module so `resolve_wait_change_ids()` no longer returns `alpha` after a `MergeCompleted`/successful retry transition)
- [ ] Update the deferred merge retry success path to clear both local `resolve_wait_changes` and reducer-owned/shared retry intent before sending `MergeCompleted` or allowing another scheduler sync. (verification: integration - add a regression in `src/parallel/tests/executor.rs` that calls `retry_deferred_merges()` success, then `sync_resolve_wait_from_shared_state_nonblocking()`, and asserts `has_resolve_wait()`/retry candidates stay empty)
- [ ] Prevent stale retry processing from invoking merge/hook work for a change already integrated into the base branch. (verification: integration - add a `src/parallel/tests/executor.rs` case with an already-merged archived change and stale retry entry, asserting no merge command, no `ResolveStarted`, and no second hook output/event is produced)
- [ ] Add hook idempotency regression coverage for deferred retry success so `on_merged` is observed exactly once for a change even if retry dispatch is triggered again. (verification: integration - add an event-capture or test hook counter in `src/parallel/tests/executor.rs` that fails unless exactly one `Running on_merged hook`/hook side-effect is observed for `alpha`)
- [ ] Preserve true-conflict and conflictless merge behavior while tightening retry cleanup. (verification: integration - run the affected `src/parallel/tests/executor.rs` tests, including conflictless merge retry and true conflict resolve tests, with `cargo test --test executor <test-name>` or the repository's equivalent targeted command discovered during implementation)
- [ ] Run proposal and repository verification. (verification: manual - run `cflx openspec validate fix-on-merged-deferred-retry-idempotency --strict --evidence warn`, `cargo fmt --all -- --check`, and targeted Rust tests in `src/parallel/tests/executor.rs`; manual coverage is intentional because final exact test names depend on implementation placement)

## Future Work

- Consider a separate release-hook hardening change for transactional cleanup if `make bump-patch` is interrupted after mutating files but before committing.
