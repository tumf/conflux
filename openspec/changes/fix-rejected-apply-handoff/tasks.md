## Implementation Tasks

- [x] 1. Extend `src/execution/apply.rs` completion detection with a `RejectingHandoff` kind for worktree-local `openspec/changes/<change_id>/REJECTED.md`. (verification: unit - `cargo test execution::apply::tests::test_detect_apply_completion_detects_rejected_handoff` asserts `REJECTED.md` is detected separately from `APPLY_BLOCKED/marker.md`)
- [x] 2. Return structured rejected handoff metadata from the apply loop without passing through empty WIP stall detection. (verification: integration - `cargo test execution::apply::tests::test_apply_loop_rejected_handoff_skips_empty_wip_stall` simulates incomplete tasks plus `REJECTED.md` and asserts no `Stall detected` error)
- [x] 3. Route rejected handoff from parallel apply into the existing `Rejecting` review path. (verification: integration - `cargo test parallel::tests::executor::test_rejected_handoff_enters_rejecting_review` or equivalent dispatch test asserts `src/parallel/dispatch.rs` calls the rejection review branch instead of retrying apply)
- [x] 4. Preserve `APPLY_BLOCKED/marker.md` behavior as resumable stalled/apply hold and keep it distinct from `REJECTED.md` rejecting handoff. (verification: unit - `cargo test execution::apply::tests::test_apply_blocked_and_rejected_handoffs_are_distinct` asserts both marker types map to separate outcomes)
- [x] 5. Validate the proposal and run targeted regression tests. (verification: integration - `cflx openspec validate fix-rejected-apply-handoff --strict --evidence warn` and `cargo test execution::apply parallel::tests::executor` cover files `src/execution/apply.rs` and `src/parallel/dispatch.rs`)

## Future Work

- Consider a separate prompt-hardening proposal if agents continue using `REJECTED.md` for recoverable environment blockers that should instead be `APPLY_BLOCKED`.

## Acceptance #1 Failure Follow-up
- [x] `openspec/changes/fix-rejected-apply-handoff/tasks.md` marks rejected-handoff regression tests complete, but `src/execution/apply.rs` has no tests named `test_detect_apply_completion_detects_rejected_handoff`, `test_apply_loop_rejected_handoff_skips_empty_wip_stall`, or `test_apply_blocked_and_rejected_handoffs_are_distinct` (the test module ends at line 1672 with blocked-handoff coverage only). Add repository-verifiable tests for REJECTED.md detection, skip-stall behavior, and APPLY_BLOCKED vs REJECTED distinction, or correct the checklist to match real evidence.
- [x] `src/execution/apply.rs::execute_apply_loop` only exits for `REJECTED.md` when `completion_finalized_run` is true after grace-driven child termination (lines 1002-1018). If the apply command exits normally after creating `openspec/changes/<change_id>/REJECTED.md`, the loop proceeds through post-apply/WIP snapshot/stall logic and can run another apply iteration before `rejected_handoff` is returned at lines 1207-1211. Add a post-command `detect_apply_rejected_handoff` short-circuit before post_apply hooks, WIP snapshots, and stall detection so incomplete tasks plus `REJECTED.md` never re-enter normal apply retry/stall policy.
- [x] `src/parallel/dispatch.rs` handles an apply-time `rejected_handoff` at lines 1079-1189, but the CONFIRM branch only logs/dequeues and returns `WorkspaceResult { rejected: Some(reason) }` at lines 1105-1138; unlike the resume-into-rejecting branch at lines 739-805, it does not call `execute_rejection_flow` to persist the confirmed `REJECTED.md` on the base branch and clean up the worktree. Wire the apply-generated rejecting CONFIRM branch through the same rejection flow as existing rejecting resume.
- [x] `src/parallel/dispatch.rs` ignores errors from `handle_resume_apply_from_rejecting` and `handle_blocked_from_rejecting` in the apply-time rejecting branch (`let _ = ...` at lines 1141 and 1154), while the resume-into-rejecting branch treats these as `RejectionReviewFailed` errors at lines 816-835 and 847-867. Propagate these errors and emit failure events so rejecting review transitions cannot silently fail. (verification: integration - `cargo test parallel::tests::executor` and `cargo test execution::apply` ensure rejecting branch error handling and apply handoff regressions remain covered)
