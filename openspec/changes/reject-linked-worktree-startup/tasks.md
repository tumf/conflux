## Implementation Tasks

- [x] Add one repository-evidence classifier for orchestration-owner startup that distinguishes the main worktree from a linked worktree without treating every `.git` pointer as linked; integrate it before repository-lock acquisition for bare TUI, explicit TUI, and `run`. Completion requires all three owner entrypoints to use the same preflight and all non-owner commands to bypass it. (verification: integration - `cargo test --test run_exit_tests linked_worktree_startup`; verification-id: linked-worktree-startup-tests)
- [x] Return a non-zero, actionable refusal containing the current linked-worktree path, the main worktree path derived from `git worktree list --porcelain`, and a start-from-main instruction, without automatic redirect, prune, cleanup, or mutation. Completion requires exact assertions over the diagnostic and side-effect absence. (verification: integration - `cargo test --test run_exit_tests linked_worktree_startup`; verification-id: linked-worktree-startup-tests)
- [x] Add real-Git regression fixtures covering linked-worktree refusal for all owner entrypoints, main-worktree eligibility, separate-Git-directory eligibility, and linked-worktree use by representative non-owner/client routing. Completion requires tests to fail against the pre-change binary and pass only when owner startup is rejected before lock files, owner metadata, sockets, logs, lifecycle hooks, AI commands, and managed worktrees can be produced. (verification: integration - `cargo test --test run_exit_tests linked_worktree_startup`; verification-id: linked-worktree-startup-tests)
- [x] Rewrite the existing `linked_worktrees_share_one_repository_lock` and `linked_worktrees_advertise_one_socket` regressions so they no longer start an owner from a linked worktree: assert preflight refusal separately, retain same-common-directory lock/socket identity through main-owner startup plus linked-worktree client/path resolution, and run the complete integration target to catch stale assertions. (verification: integration - `cargo test --test run_exit_tests`; verification-id: linked-worktree-startup-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate reject-linked-worktree-startup --archive-gate`.

## Future Work

- Stale worktree repair remains a separate operator action or future change.

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-non-git-socket-choice-scenarios-unretired] (major) Moving the Git refusal ahead of listener setup makes two canonical spec scenarios about the non-Git socket-choice error unsatisfiable, but the change retires neither and leaves the retirement unproven by any test. | evidence: src/main.rs:897 runs enforce_owner_startup_preflight before acquire_repository_lock and before any listener; src/startup_preflight.rs:137 returns 'conflux requires a git repository (.git directory not found)' for every owner entrypoint outside Git, so socket-path resolution is never reached.; Observed against the built binary: in a non-Git directory with no socket flags, `cflx run --all` exits 1 with 'Error: conflux requires a git repository (.git directory not found): worktree orchestration is the only execution model...' — the path-selection guidance never appears.; openspec/specs/cli/spec.md:1521 'Scenario: Non-Git invocation requires a decision' still requires 'startup exits non-zero before orchestration AND the error explains both explicit choices'; that outcome can no longer occur.; openspec/specs/web-monitoring/spec.md:69 'Scenario: Non-Git default path is unavailable' still requires 'startup exits non-zero with an actionable path-selection error'; that outcome can no longer occur.; The only test that covered it, a_non_git_invocation_refuses_before_any_orchestration_side_effect, asserted stderr.contains("--web-unix-socket") && stderr.contains("--no-web-unix-socket") at 5ef28654:tests/run_exit_tests.rs:1023 and was rewritten by this change to assert the Git error; the retirement now exists only as a doc comment at tests/run_exit_tests.rs:1249.; openspec/changes/reject-linked-worktree-startup/proposal.md 'Retired Scenarios' lists only 'cli: Repository-Scoped Orchestration Lock / Linked worktrees share one lock', so the two socket-choice scenarios survive into the canonical specs unsatisfiable.; AGENTS.md:397 still states 'Outside a Git repository the default has no identity to derive, so startup fails unless one of those two options is supplied', which no longer holds for any owner entrypoint. | required_changes: openspec/changes/reject-linked-worktree-startup/specs/cli/spec.md — Add a MODIFIED 'Web Monitoring Flags' requirement block that removes or replaces the 'Non-Git invocation requires a decision' scenario, so the canonical cli spec no longer demands the path-selection error for an owner entrypoint outside Git.; openspec/changes/reject-linked-worktree-startup/specs/web-monitoring/spec.md — Add a spec delta with a MODIFIED 'Configuration Options' requirement that removes or replaces the 'Non-Git default path is unavailable' scenario for the same reason.; openspec/changes/reject-linked-worktree-startup/proposal.md — List both scenarios under 'Retired Scenarios' with the reason (the Git refusal now precedes socket resolution), and add openspec/specs/web-monitoring/spec.md to the references list.; AGENTS.md — Correct the local API socket section so it states that an owner entrypoint outside a Git repository is refused by the startup preflight regardless of the socket options, instead of claiming startup succeeds when one of the two options is supplied. | verification: tests/run_exit_tests.rs — Extend a_non_git_invocation_refuses_before_any_orchestration_side_effect to assert positively that the owner refusal outside Git names the missing repository and does NOT contain '--web-unix-socket' or '--no-web-unix-socket', so the retirement is proven by an assertion rather than by a comment.
  finding: {"evidence":["src/main.rs:897 runs enforce_owner_startup_preflight before acquire_repository_lock and before any listener; src/startup_preflight.rs:137 returns 'conflux requires a git repository (.git directory not found)' for every owner entrypoint outside Git, so socket-path resolution is never reached.","Observed against the built binary: in a non-Git directory with no socket flags, `cflx run --all` exits 1 with 'Error: conflux requires a git repository (.git directory not found): worktree orchestration is the only execution model...' — the path-selection guidance never appears.","openspec/specs/cli/spec.md:1521 'Scenario: Non-Git invocation requires a decision' still requires 'startup exits non-zero before orchestration AND the error explains both explicit choices'; that outcome can no longer occur.","openspec/specs/web-monitoring/spec.md:69 'Scenario: Non-Git default path is unavailable' still requires 'startup exits non-zero with an actionable path-selection error'; that outcome can no longer occur.","The only test that covered it, a_non_git_invocation_refuses_before_any_orchestration_side_effect, asserted stderr.contains(\"--web-unix-socket\") && stderr.contains(\"--no-web-unix-socket\") at 5ef28654:tests/run_exit_tests.rs:1023 and was rewritten by this change to assert the Git error; the retirement now exists only as a doc comment at tests/run_exit_tests.rs:1249.","openspec/changes/reject-linked-worktree-startup/proposal.md 'Retired Scenarios' lists only 'cli: Repository-Scoped Orchestration Lock / Linked worktrees share one lock', so the two socket-choice scenarios survive into the canonical specs unsatisfiable.","AGENTS.md:397 still states 'Outside a Git repository the default has no identity to derive, so startup fails unless one of those two options is supplied', which no longer holds for any owner entrypoint."],"id":"acceptance-non-git-socket-choice-scenarios-unretired","required_changes":[{"description":"Add a MODIFIED 'Web Monitoring Flags' requirement block that removes or replaces the 'Non-Git invocation requires a decision' scenario, so the canonical cli spec no longer demands the path-selection error for an owner entrypoint outside Git.","file":"openspec/changes/reject-linked-worktree-startup/specs/cli/spec.md"},{"description":"Add a spec delta with a MODIFIED 'Configuration Options' requirement that removes or replaces the 'Non-Git default path is unavailable' scenario for the same reason.","file":"openspec/changes/reject-linked-worktree-startup/specs/web-monitoring/spec.md"},{"description":"List both scenarios under 'Retired Scenarios' with the reason (the Git refusal now precedes socket resolution), and add openspec/specs/web-monitoring/spec.md to the references list.","file":"openspec/changes/reject-linked-worktree-startup/proposal.md"},{"description":"Correct the local API socket section so it states that an owner entrypoint outside a Git repository is refused by the startup preflight regardless of the socket options, instead of claiming startup succeeds when one of the two options is supplied.","file":"AGENTS.md"}],"severity":"major","summary":"Moving the Git refusal ahead of listener setup makes two canonical spec scenarios about the non-Git socket-choice error unsatisfiable, but the change retires neither and leaves the retirement unproven by any test.","verification":[{"description":"Extend a_non_git_invocation_refuses_before_any_orchestration_side_effect to assert positively that the owner refusal outside Git names the missing repository and does NOT contain '--web-unix-socket' or '--no-web-unix-socket', so the retirement is proven by an assertion rather than by a comment.","file":"tests/run_exit_tests.rs"}]}
  evidence: specs/cli/spec.md — MODIFIED "Web Monitoring Flags" now states socket selection runs only after the owner preflight admits the workspace, and "Non-Git invocation requires a decision" no longer demands path-selection guidance: it requires the missing-repository refusal before any socket path is selected.
  evidence: specs/web-monitoring/spec.md — new delta with MODIFIED "Configuration Options"; "Non-Git default path is unavailable" now requires the owner preflight's missing-repository refusal before Unix path selection instead of a path-selection error.
  evidence: proposal.md — both scenarios listed under "Retired Scenarios" with the reason (the Git refusal now precedes socket resolution) and the in-place mechanism (MODIFIED merges, so a header cannot be dropped); openspec/specs/web-monitoring/spec.md added to references.
  evidence: AGENTS.md "Local API socket" — the non-Git bullet now says neither socket option makes an owner startable and the preflight refuses before path selection; the linked-worktree sentence now says only the main worktree may own orchestration.
  evidence: tests/run_exit_tests.rs — a_non_git_invocation_refuses_before_any_orchestration_side_effect now asserts stderr names the missing repository and contains neither "--web-unix-socket" nor "--no-web-unix-socket"; the same negative assertion was added to outside_git_every_executable_run_is_refused_before_side_effects for the explicit-path and opt-out cases.
  evidence: gates — `cargo test --test run_exit_tests` 38 passed 0 failed; `cargo fmt --check` clean; `cflx openspec validate reject-linked-worktree-startup --archive-gate` passed; `openspec validate --strict` reports the change valid with no scenario-loss error.
