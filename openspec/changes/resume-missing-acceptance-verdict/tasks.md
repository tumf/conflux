## Implementation Tasks

- [ ] Define a shared, mode-independent missing-verdict retry decision with a dedicated consecutive counter and a fixed maximum of two retries after the initial attempt, without changing explicit-`CONTINUE` accounting. (verification: unit - table-driven tests in `src/orchestration/acceptance.rs` cover first/second retry, third consecutive exhaustion, canonical reset, and independence from the configured CONTINUE count via `cargo test missing_verdict`)
- [ ] Extend the generic acceptance prompt context to mark a retry caused by `MissingVerdict`, carry bounded prior stdout/stderr and attempt evidence as untrusted data, and instruct the agent to finish or re-check owned verification before emitting exactly one canonical verdict. (verification: unit - `src/agent/prompt.rs` tests assert bounded escaping, corrective instruction presence only for protocol retry, and absence of harness session/resume/job fields)
- [ ] Wire serial orchestration to rerun the normal configured acceptance command while protocol budget remains, then preserve current terminal failure behavior with exhausted-attempt evidence after the limit. (verification: integration - focused `src/serial_run_service.rs` tests use a fake acceptance command sequence of missing/missing/pass and missing/missing/missing and assert routing, invocation count, context, and terminal outcome)
- [ ] Wire parallel dispatch through the same retry decision and generic acceptance execution path, keeping the workspace active during retry and returning terminal `WorkspaceResult` only on launch failure or exhausted missing verdicts. (verification: integration - focused `src/parallel/tests/executor.rs` tests assert missing/missing/pass succeeds without `terminal_error_retry_required`, while three missing verdicts produce one bounded terminal diagnostic)
- [ ] Preserve parser and canonical outcome semantics across PASS, FAIL, CONTINUE, GATED, permission-stalled, command-failed, and cancelled results, and reset only the consecutive protocol counter after any canonical verdict. (verification: unit - routing matrix tests in `src/acceptance.rs` and `src/orchestration/acceptance.rs` prove `MissingVerdict` remains distinct, explicit CONTINUE retains its configured budget, and command failure is never retried as missing verdict via `cargo test acceptance`)
- [ ] Keep retry control constitution-compliant: use active-run memory and existing acceptance history only, create no report/checkpoint, and rerun acceptance from workspace-derived state after restart. (verification: integration - restart regressions in `src/serial_run_service.rs` and `src/parallel/tests/executor.rs` remove runtime state between attempts and prove the unarchived workspace invokes acceptance again without inferred PASS or a generated `.cflx` acceptance checkpoint via `cargo test restart_reruns_acceptance`)
- [ ] Emit structured non-terminal protocol-retry logs/events with attempt and maximum values, and defer `terminal_error_retry_required` classification until retry exhaustion or command-launch failure. (verification: integration - queue/event tests in `src/parallel/tests/executor.rs` and `src/parallel/queue_state.rs` assert retry progress is visible, error events are absent before exhaustion, and reconciliation does not defer an actively retried change via `cargo test missing_verdict`)
- [ ] Run repository quality gates and keep new default tests under the project's one-second unit-test threshold. (verification: integration - `cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test`)

## Future Work

- Add configurable protocol-retry limits only if operators demonstrate a need beyond the fixed minimal bound.
- Add provider-specific resumability only as a separate proposal if Conflux later adopts an explicit harness adapter contract.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate resume-missing-acceptance-verdict --archive-gate`
