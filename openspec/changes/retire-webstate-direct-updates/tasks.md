## Implementation Tasks

- [x] Make the workspace-observation merge helper private, route `refresh_from_disk_at` through it, and expose only an explicitly named `#[doc(hidden)]` test-seeding entry point for integration crates. (verification: integration - `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`; verification-id: focused-tests)
- [x] Make the existing unit-test `update` helper and integration-test seed entry point delegate to the same merge implementation. (verification: integration - `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`; verification-id: focused-tests)
- [x] Repoint every unit and integration test caller, then run dispatcher single-owner regressions and both affected integration-test crates. (verification: integration - `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`; verification-id: focused-tests)

## Final Validation

Archive validation is authoritative. Expected archive gate: `cflx openspec validate retire-webstate-direct-updates --archive-gate`.

## Notes

- The declared `focused-tests` command is `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`, and that is exactly what was run: 42 passed / 0 failed for the lib filters, then 102 passed / 1 ignored and 19 passed for the two integration crates. The two filters must follow `--` so that both reach the one lib test binary; placing them before `--lib` is a cargo argument-parsing error.
- `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` are clean.
- `seed_workspace_observation_for_tests` carries `#[allow(dead_code)]`: it is `pub` for the `tests/` crates, and the binary target that compiles this module without them would otherwise report the intentional absence of a production caller as dead code.
- `merge_workspace_observation` keeps both prior merge policies rather than unifying them, because periodic disk refresh and mode-preserving test seeding differ in how they carry `persistent_scheduler_idle` and `iteration_number`; collapsing them would be a behavior change the proposal excludes.

## Future Work

- None.

## Current Acceptance Follow-up
- attempt: 2
- [x] [focused-tests-declared-command-unrunnable] (minor) The declared focused-tests evidence/rerun command fails at cargo argument parsing, so the change-blocking verification can never pass as declared | evidence: Running the declared command verbatim exits 1 before any test runs: `cargo test web::state tui::command_handlers::cross_adapter_tests --lib` -> "error: unexpected argument 'tui::command_handlers::cross_adapter_tests' found; Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]"; openspec/changes/retire-webstate-direct-updates/proposal.md frontmatter lines 14-15 declare that unrunnable command as both `evidence` and `rerun` for verification id `focused-tests` (phase pre-integration, completion_role change-blocking); openspec/changes/retire-webstate-direct-updates/tasks.md Notes line 13 admits the declared command 'needs `--lib` and a `--` separator' and documents that a different command was actually run, and the three task lines quote the same broken command in their verification annotations; The corrected form `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink` passes: 42 + 102 (1 ignored) + 19 tests, exit 0 | required_changes: openspec/changes/retire-webstate-direct-updates/proposal.md — Replace the `evidence` and `rerun` values of verification `focused-tests` with a command that actually executes, e.g. `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`; openspec/changes/retire-webstate-direct-updates/tasks.md — Update the three task verification annotations to quote the corrected runnable command and adjust the Notes entry so the declared command and the command actually run are the same | verification: openspec/changes/retire-webstate-direct-updates/proposal.md — Execute the updated `evidence`/`rerun` command exactly as declared in the frontmatter and confirm it exits 0 with the lib filters applied after `--` and both integration-test crates run
  finding: {"evidence":["Running the declared command verbatim exits 1 before any test runs: `cargo test web::state tui::command_handlers::cross_adapter_tests --lib` -> \"error: unexpected argument 'tui::command_handlers::cross_adapter_tests' found; Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]\"","openspec/changes/retire-webstate-direct-updates/proposal.md frontmatter lines 14-15 declare that unrunnable command as both `evidence` and `rerun` for verification id `focused-tests` (phase pre-integration, completion_role change-blocking)","openspec/changes/retire-webstate-direct-updates/tasks.md Notes line 13 admits the declared command 'needs `--lib` and a `--` separator' and documents that a different command was actually run, and the three task lines quote the same broken command in their verification annotations","The corrected form `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink` passes: 42 + 102 (1 ignored) + 19 tests, exit 0"],"id":"focused-tests-declared-command-unrunnable","required_changes":[{"description":"Replace the `evidence` and `rerun` values of verification `focused-tests` with a command that actually executes, e.g. `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`","file":"openspec/changes/retire-webstate-direct-updates/proposal.md"},{"description":"Update the three task verification annotations to quote the corrected runnable command and adjust the Notes entry so the declared command and the command actually run are the same","file":"openspec/changes/retire-webstate-direct-updates/tasks.md"}],"severity":"minor","summary":"The declared focused-tests evidence/rerun command fails at cargo argument parsing, so the change-blocking verification can never pass as declared","verification":[{"description":"Execute the updated `evidence`/`rerun` command exactly as declared in the frontmatter and confirm it exits 0 with the lib filters applied after `--` and both integration-test crates run","file":"openspec/changes/retire-webstate-direct-updates/proposal.md"}]}
  evidence: proposal.md focused-tests evidence/rerun and the three tasks.md verification annotations now quote `cargo test --lib -- web::state tui::command_handlers::cross_adapter_tests && cargo test --test client_cli_tests --test client_completion_sink`, the Notes entry states that same command as the one run, and executing it verbatim exits 0 (42 lib; 102 passed/1 ignored; 19).
