## Implementation Tasks

- [x] Enforce monotonic blocker precedence in `src/orchestration/state.rs` so `WorkspaceStatusUpdated { Blocked }` confirms but does not replace an existing structured `ExternalBlocked` or Acceptance-owned stalled hold. Completion requires preserving wait state, blocker kind, category/detail, origin, prerequisite owner, unblock condition, Acceptance ownership, and resumability while retaining the generic stalled fallback when no structured blocker exists. (verification: unit - `cargo test --lib structured_blocker_metadata_survives_workspace_blocked`; verification-id: blocker-metadata-regressions)

- [x] Add reducer event-sequence tests for `AcceptanceGated` and `ExecutionBlocked` followed by generic blocked workspace status, including Acceptance-origin external, Apply-origin external, Acceptance-owned non-external stalled, and unstructured fallback cases. Completion requires assertions against `display_status`, `blocker_view`, held-set membership, and every routing-relevant metadata field rather than only the rendered status string. (verification: unit - `cargo test --lib structured_blocker_metadata_survives_workspace_blocked`; verification-id: blocker-metadata-regressions)

- [x] Cover the real external-blocker dispatch sequence through a TUI-style `EventDispatcher` harness that applies both the structured event and compatibility workspace-status event to reducer state, then prove it leaves one held change instead of an ordinary queued candidate. Completion requires a repository-local test that distinguishes this reducer-owning boundary from the headless Web-only forwarder and fails if the applied workspace is eligible for another automatic Acceptance dispatch without explicit retry. (verification: integration - `cargo test --lib external_blocker_hold_survives_dispatch_status`; verification-id: blocker-metadata-regressions)

- [x] Extend operator/API projection regressions so a preserved resumable hold allows the existing acceptance-only retry route, a preserved non-resumable hold remains refused without evidence loss, and projected blocker fields stay reducer-derived. Completion requires exercising command/projection code after the full two-event sequence, not after `AcceptanceGated` alone. (verification: integration - `cargo test --lib external_blocker_hold_survives_dispatch_status && cargo test --lib orchestration::operator_command`; verification-id: blocker-metadata-regressions)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate preserve-external-blocker-metadata --archive-gate`.

## Future Work

- Consider removing redundant generic blocked events only in a separate event-contract cleanup after all consumers are proven independent of them.

## Current Acceptance Follow-up
- attempt: 1
- [x] [acceptance-cargo-fmt-check] (minor) cargo fmt --check fails on two hunks introduced by the Apply commit, so the declared change-blocking verification command chain blocker-metadata-regressions does not pass | evidence: cargo fmt --check exits 1 in this worktree (clippy and all declared test filters pass); src/orchestration/operator_command/tests.rs:1406: one-line assert_eq!(after, before, "a refused retry discards no blocker evidence"); must be split into rustfmt's multi-line form; src/orchestration/state.rs:4318: fn structured_blocker_metadata_survives_workspace_blocked_fallback_stays_generic_without_a_hold signature line-break style is rejected by rustfmt 1.8.0-stable; git blame attributes both hunks to commit 9a394f96 Apply: preserve-external-blocker-metadata; proposal.md declares cargo fmt --check inside the blocker-metadata-regressions rerun chain with completion_role: change-blocking, and the prek pre-commit hook runs cargo fmt --all -- --check for commits touching src/**/*.rs | required_changes: src/orchestration/operator_command/tests.rs — Run cargo fmt so the assert_eq! at line 1406 takes rustfmt's multi-line form; src/orchestration/state.rs — Run cargo fmt so the over-long test fn signature at line 4318 takes rustfmt's accepted form | verification: src/orchestration/operator_command/tests.rs — cargo fmt --check exits 0 and cargo test --lib external_blocker_hold_survives_dispatch_status still passes 5 tests; src/orchestration/state.rs — cargo fmt --check exits 0 and cargo test --lib structured_blocker_metadata_survives_workspace_blocked still passes 5 tests
  finding: {"evidence":["cargo fmt --check exits 1 in this worktree (clippy and all declared test filters pass)","src/orchestration/operator_command/tests.rs:1406: one-line assert_eq!(after, before, \"a refused retry discards no blocker evidence\"); must be split into rustfmt's multi-line form","src/orchestration/state.rs:4318: fn structured_blocker_metadata_survives_workspace_blocked_fallback_stays_generic_without_a_hold signature line-break style is rejected by rustfmt 1.8.0-stable","git blame attributes both hunks to commit 9a394f96 Apply: preserve-external-blocker-metadata","proposal.md declares cargo fmt --check inside the blocker-metadata-regressions rerun chain with completion_role: change-blocking, and the prek pre-commit hook runs cargo fmt --all -- --check for commits touching src/**/*.rs"],"id":"acceptance-cargo-fmt-check","required_changes":[{"description":"Run cargo fmt so the assert_eq! at line 1406 takes rustfmt's multi-line form","file":"src/orchestration/operator_command/tests.rs"},{"description":"Run cargo fmt so the over-long test fn signature at line 4318 takes rustfmt's accepted form","file":"src/orchestration/state.rs"}],"severity":"minor","summary":"cargo fmt --check fails on two hunks introduced by the Apply commit, so the declared change-blocking verification command chain blocker-metadata-regressions does not pass","verification":[{"description":"cargo fmt --check exits 0 and cargo test --lib external_blocker_hold_survives_dispatch_status still passes 5 tests","file":"src/orchestration/operator_command/tests.rs"},{"description":"cargo fmt --check exits 0 and cargo test --lib structured_blocker_metadata_survives_workspace_blocked still passes 5 tests","file":"src/orchestration/state.rs"}]}
  evidence: required_changes src/orchestration/operator_command/tests.rs — ran `cargo fmt`; the assert_eq! at line 1406 is now rustfmt's multi-line form (assert_eq!( / after, before, / "a refused retry discards no blocker evidence" / );)
  evidence: required_changes src/orchestration/state.rs — ran `cargo fmt`; the long test fn signature at line 4318 now breaks as `...without_a_hold(\n) {` per rustfmt 1.8.0-stable
  evidence: verification src/orchestration/operator_command/tests.rs — `cargo fmt --check` exits 0 and `cargo test --lib external_blocker_hold_survives_dispatch_status` reports 5 passed; 0 failed
  evidence: verification src/orchestration/state.rs — `cargo fmt --check` exits 0 and `cargo test --lib structured_blocker_metadata_survives_workspace_blocked` reports 5 passed; 0 failed
  evidence: rerun chain blocker-metadata-regressions — `cargo clippy --all-targets --all-features -- -D warnings` finished with no warnings after the fmt-only edits
