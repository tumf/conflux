## Implementation Tasks

- [ ] Task 1: Update `skills/cflx-accept/SKILL.md (.md)` to make stalled implementation blocker hold the primary concept while retaining `{"acceptance":"gated"}` and `ACCEPTANCE: GATED` only as runtime-compatible handoff tokens. (verification: manual - inspect `skills/cflx-accept/SKILL.md (.md)` and run `rg -n "FAIL vs GATED|^- GATED:|operator-facing lifecycle taxonomy" skills/cflx-accept/SKILL.md (.md)`; completion condition: the skill still instructs agents to emit the current parser-compatible tokens for valid Implementation Blockers and explicitly says operator-facing status is `stalled`.)
- [ ] Task 2: Update `skills/cflx-accept-with-speca/SKILL.md (.md)` to mirror the `cflx-accept` terminology without changing the shared verdict protocol or adding SPECA-specific outcomes. (verification: manual - inspect `skills/cflx-accept-with-speca/SKILL.md (.md)` and run `rg -n "pass/fail/continue/gated|^- GATED:|\*\*Gated\*\*" skills/cflx-accept-with-speca/SKILL.md (.md)`; completion condition: SPECA property outcomes route through pass/fail/continue/stalled-hold concepts and any `gated` wording is compatibility-token-only.)
- [ ] Task 3: Update related distributed skill documentation and references in `skills/cflx-workflow/SKILL.md (.md)`, `skills/cflx-workflow/references/cflx-accept.md (.md)`, `skills/cflx-archive/SKILL.md (.md)`, `skills/README.md (.md)`, and `skills/cflx-rejection-guide*` where applicable. (verification: manual - run `rg -n "GATED|gated|acceptance-gated" skills` and review each match path; completion condition: remaining matches are either runtime token examples, negative/reserved-marker guidance, historical ambiguity handling, or reason metadata rather than primary user-facing lifecycle wording.)
- [ ] Task 4: Update `.opencode/commands/cflx-accept.md (.md)` as a non-authoritative mirror so OpenCode users see the same stalled implementation blocker terminology while preserving current parser-compatible final verdict tokens. (verification: manual - inspect `.opencode/commands/cflx-accept.md (.md)` and run `rg -n "FAIL vs GATED|Output GATED|\{\"acceptance\":\"stalled\"\}" .opencode/commands/cflx-accept.md (.md)`; completion condition: the command template does not instruct `GATED` as a lifecycle/status concept and does not emit `{"acceptance":"stalled"}` before parser support exists.)
- [ ] Task 5: Update canonical prompt specs under `openspec/specs/agent-prompts/spec.md (.md)` with a requirement/scenario that distributed skills must present acceptance blockers as stalled holds and restrict `gated` to compatibility protocol tokens until parser migration. (verification: integration - run `cflx openspec validate migrate-skills-gated-wording-to-stalled --strict --evidence warn` and inspect `openspec/specs/agent-prompts/spec.md (.md)`; completion condition: the delta archives to a canonical rule that acceptance-related skills cannot regress to `GATED`-centered user-facing guidance.)
- [ ] Task 6: Update embedded skill drift/contract tests in `src/embedded_skills.rs` or neighboring test modules to enforce the new wording boundary for bundled skills. (verification: unit - run `cargo test embedded_skills` after updating `src/embedded_skills.rs`; completion condition: tests fail if distributed `cflx-accept` guidance again labels the primary blocker outcome as `GATED` rather than stalled implementation blocker hold.)
- [ ] Task 7: Run targeted and final verification for the implemented change. (verification: integration - run `cflx openspec validate migrate-skills-gated-wording-to-stalled --strict --evidence warn`, `cargo test embedded_skills`, and the repository lint/typecheck command documented for the changed surface; completion condition: validation and targeted tests pass, and any lint/typecheck failures are either fixed or documented with concrete blockers.)

## Future Work

- Add runtime parser support for `{"acceptance":"stalled","reason":"implementation_blocker"}` in a later, separate proposal.
- After runtime/parser migration and sufficient release adoption, remove `gated` compatibility output guidance and eventually remove parser compatibility.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate. Expected archive gate: `cflx openspec validate migrate-skills-gated-wording-to-stalled --archive-gate`.
