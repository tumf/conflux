## Implementation Tasks

- [ ] **Task 1: Add a process-local worktree resource registry** that allocates random 128-bit opaque IDs on first observation, retires IDs on disappearance, assigns new IDs after same-path recreation, and never uses paths, repository IDs, or branches as mutation identity. (verification: unit - registry lifecycle/property cases via `cargo test --lib remote_worktree -- --list | grep -q remote_worktree && cargo test --lib remote_worktree`; verification-id: remote-worktree-local)
- [ ] **Task 2: Define exact read routes and redacted worktree DTOs** for `/api/v2/worktrees` and `/api/v2/worktrees/{worktree_id}`, with repository-relative display paths, non-confidential 16-hex FNV correlation ID, nullable dirty state, operation eligibility, conflict/recovery evidence, and no canonical root/path target fields. (verification: unit - serialization, route, redaction, and hash-caveat cases via `cargo test --lib remote_worktree -- --list | grep -q remote_worktree && cargo test --lib remote_worktree`; verification-id: remote-worktree-local)
- [ ] **Task 3: Extract a shared TUI/API worktree operation service** for create, guarded delete, base merge, repository lock, hooks, reducer updates, and events. (verification: integration - adapter parity using real service fixtures via `cargo test --lib remote_worktree -- --list | grep -q remote_worktree && cargo test --lib remote_worktree`; verification-id: remote-worktree-local)
- [ ] **Task 4: Implement closed `create_worktree` semantics** with `target.change_id`, empty params, current managed base HEAD, server-derived branch/path, managed non-archived eligibility, `worktree_exists` conflict, inherited auth/revision/idempotency/capacity guards, and no generic/base/path input. (verification: integration - authenticated router/service success and negative schema/eligibility/existing cases via `cargo test --lib remote_worktree -- --list | grep -q remote_worktree && cargo test --lib remote_worktree`; verification-id: remote-worktree-local)
- [ ] **Task 5: Implement fail-closed delete by `target.worktree_id`** requiring empty params, known-clean/eligible state, mandatory teardown, and post-removal identity retirement; expose no skip-teardown or force parameter. (verification: e2e - `tests/e2e_git_worktree_tests.rs` teardown, dirty, unknown-dirty, stale revision, and recreation cases via `cargo test --features heavy-tests --test e2e_git_worktree_tests remote_worktree -- --list | grep -q remote_worktree && cargo test --features heavy-tests --test e2e_git_worktree_tests remote_worktree`; verification-id: remote-worktree-heavy)
- [ ] **Task 6: Implement conflict-preserving merge by `target.worktree_id`** with empty params, inherited guards, TUI-equivalent base merge and `on_merged` path, retained intermediate state/conflict files, `local_or_tui_required` recovery guidance, and `root_busy` until recovery. (verification: e2e - successful/conflicting Git merge, hook/event, and post-conflict busy cases via `cargo test --features heavy-tests --test e2e_git_worktree_tests remote_worktree -- --list | grep -q remote_worktree && cargo test --features heavy-tests --test e2e_git_worktree_tests remote_worktree`; verification-id: remote-worktree-heavy)
- [ ] **Task 7: Extend OpenAPI and negative security coverage** proving v2 omits absolute-path/branch/base-commit mutation targets, arbitrary `worktree_command`, editor/session operations, teardown bypasses, remote recovery commands, and unauthenticated mutation. (verification: integration - non-empty remote-worktree suite plus `make check-openapi`; verification-id: remote-worktree-local)

## Future Work

- A separately approved fail-closed remote abort/resolve contract if remote-only conflict recovery becomes necessary.
- Cross-process stable resource identity if a durable, Constitution-compatible use case emerges.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate secure-remote-worktree-operations --archive-gate`
