## Implementation Tasks

- [ ] 1. Add shared deterministic acceptance finding normalization and identity comparison with backward-compatible string findings and per-finding repository/external scope classification. (verification: unit - `cargo test acceptance::tests` covers order changes, whitespace, duplicates, stable path/code identity, legacy strings, mixed scopes, and finding-less FAIL; completion condition: serial and parallel callers consume the same normalized representation)
- [ ] 2. Implement a repository-visible semantic progress snapshot that includes committed and uncommitted source/test/config/spec/substantive-task changes and excludes runtime-managed follow-up regions, blocker markers, counters, logs, and UI/history state. (verification: unit - focused tests beside the shared progress helper in `src/orchestration/acceptance.rs` cover source change, spec change, substantive task change, runtime-section-only change, committed and uncommitted changes; completion condition: identical workspace semantics produce identical progress fingerprints)
- [ ] 3. Replace numbered acceptance follow-up accumulation with one runtime-owned `Current Acceptance Follow-up` section and migrate legacy numbered sections on the next update. (verification: unit - `cargo test task_parser::tests` asserts one-section upsert, stale finding removal, sort/dedup, active/archive path fallback, repository-only checkboxes, and non-checkbox external blocker metadata; completion condition: acceptance agents never need to edit `tasks.md`)
- [ ] 4. Reduce acceptance prompt context to current diff and latest normalized findings once, including bounded raw output only for CONTINUE or command diagnostics. (verification: unit - `cargo test history::tests` and agent prompt tests build 3+ attempts and assert older findings/output and duplicated latest content are absent; completion condition: prompt assembly retains actionable latest evidence without all-attempt replay)
- [ ] 5. Add a shared retry decision that permits one apply after FAIL, retries when semantic progress exists, and stalls before another apply when the same finding identities recur without progress. (verification: unit - tests in `src/orchestration/acceptance.rs` cover first FAIL, same finding/no progress, same finding/real progress, changed findings, mixed external blockers, and alternating CONTINUE/FAIL; completion condition: decision output includes retry or stalled reason plus evidence)
- [ ] 6. Integrate the shared retry decision into parallel dispatch and convert the 10-cycle ceiling from terminal Error to resumable stalled. (verification: integration - `src/parallel/tests/executor.rs` and `src/parallel/dispatch.rs` tests assert exactly one initial apply retry, no further apply on repeated no-progress findings, continued retry after real progress, and `acceptance_cycle_limit_exhausted` without an Error event; completion condition: preserved worktree and marker are observable after both stalled reasons)
- [ ] 7. Integrate the same retry and cycle-exhaustion semantics into serial execution. (verification: integration - `cargo test serial_run_service::tests` runs equivalent fixtures in `src/serial_run_service.rs` and asserts parity with parallel outcomes; completion condition: serial mode no longer enters an unbounded or terminal-error-only FAIL loop)
- [ ] 8. Persist acceptance-generated stalled evidence in the existing workspace-local `APPLY_BLOCKED/marker.md` contract and implement explicit retry consumption. (verification: integration - tests in `src/execution/state.rs` and `src/parallel/dispatch.rs` restart from marker-only workspace evidence, block ordinary dispatch, preserve reason/findings/external blockers/next action, then explicitly retry and clear only an acceptance-generated resumable marker; completion condition: unrelated apply blockers are not accidentally cleared)
- [ ] 9. Align canonical and bundled acceptance guidance with read-only review, runtime-owned current follow-up, mixed-finding preservation, and latest-only context. (verification: unit - `src/embedded_skills.rs` tests inspect `skills/cflx-accept/SKILL.md`, `skills/cflx-accept-with-speca/SKILL.md`, and compatibility guidance; completion condition: no authoritative guidance instructs acceptance to append numbered task sections)
- [ ] 10. Run affected Rust, serial/parallel regression, skill-installation, lint, and typecheck quality gates. (verification: integration - run repository commands `cargo test acceptance::tests task_parser::tests history::tests serial_run_service::tests`, targeted `src/parallel/tests/executor.rs` regressions, `cargo fmt --check`, and `cargo clippy --all-targets --all-features -- -D warnings`; completion condition: default tests remain under one second each or are marked `#[cfg_attr(not(feature = "heavy-tests"), ignore)]` when impractical)

## Future Work

- Resolve the separate canonical/default mismatch for `acceptance_max_continues` in a focused configuration change.
- Consider a native structured-finding-only protocol after all supported agent runtimes emit the structured form.

## Final Validation

Expected archive gate: `cflx openspec validate bound-acceptance-retry-cycles --archive-gate` exits 0.
