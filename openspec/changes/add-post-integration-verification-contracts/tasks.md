## Implementation Tasks

- [x] 1. Extend proposal metadata parsing with typed verification declarations and diagnostic-preserving strict parsing while retaining tolerant reads for legacy consumers. (verification: unit - `cargo test openspec::tests` covers valid pre/post declarations, absent declarations, malformed YAML, duplicate IDs, invalid enums, unknown nested keys, and explicit empty arrays; completion condition: `src/openspec.rs` exposes parsed declarations and strict diagnostics without changing dependency fallback semantics)
- [x] 2. Add deterministic strict and archive-gate validation for required fields, phase/owner relationships, unique IDs, non-empty values, and safe repository-relative automation files. (verification: unit - `cargo test openspec_cmd` covers missing fields, invalid owner/phase, absolute and parent-traversal paths, external symlinks, missing/non-regular files, and no-network behavior; completion condition: `src/openspec_cmd/validation.rs` reports matching actionable errors in strict and archive-gate modes)
- [x] 3. Require implementation and hybrid proposals to declare at least one pre-integration verification while preserving legacy/spec-only compatibility and treating natural-language phase inference as advisory only. (verification: unit - `cargo test openspec_cmd` fixtures in `src/openspec_cmd.rs` prove implementation/hybrid omission fails, spec-only omission passes, explicit structured phase controls behavior, and contradictory prose cannot override metadata; completion condition: validation uses declarations rather than prose for phase routing)
- [x] 4. Update `cflx-proposal` authoring guidance and templates to classify every claimed outcome as pre-integration or post-integration and emit complete verification declarations. (verification: unit - `src/embedded_skills.rs` and `tests/install_skills_test.rs` inspect `skills/cflx-proposal/SKILL.md` and installed skill content for all required fields and the prohibition on post-integration URL checks as pre-integration checkboxes; completion condition: an author can produce a valid declaration without inventing provider-specific fields)
- [x] 5. Update `cflx-accept`, `cflx-accept-with-speca`, and compatibility guidance so acceptance verifies post-integration automation wiring and local evidence without fetching undeployed targets. (verification: unit - `src/embedded_skills.rs` tests assert identical phase semantics and verdict compatibility across `skills/cflx-accept/SKILL.md` and `skills/cflx-accept-with-speca/SKILL.md`; completion condition: missing automation is FAIL, unautomatable external prerequisites are stalled, and merely pending operational evidence is not FAIL)
- [x] 6. Preserve verification declarations through archive and expose them through existing proposal inspection paths without introducing external durable workflow state. (verification: integration - `cargo test openspec_cmd` exercises `src/openspec_cmd/archive.rs`, archives a fixture, then reparses the archived `proposal.md` and asserts declaration equality; completion condition: deleting `~/.local/state/cflx/**` cannot change phase classification)
- [x] 7. Add end-to-end proposal fixtures for a repository-local verification, a CI-owned post-integration verification, and an invalid cyclic gate with no automation owner. (verification: integration - targeted `cargo test openspec_cmd` fixtures demonstrate valid pre/post contracts pass and the ownerless cyclic gate fails before apply; completion condition: no test requires credentials, network access, or a deployed service)
- [x] 8. Run the affected Rust, skill-installation, lint, and typecheck quality gates. (verification: integration - run repository commands `cargo test openspec::tests openspec_cmd`, `cargo test --test install_skills_test`, `cargo fmt --check`, and `cargo clippy --all-targets --all-features -- -D warnings`; completion condition: default tests remain under one second each or are marked `#[cfg_attr(not(feature = "heavy-tests"), ignore)]` when impractical)

## Future Work

- Add provider-specific post-deploy verification automation in each product repository that declares a post-integration outcome.
- Consider a separate constitutional change only if Conflux itself must own remote integration and deployment completion.

## Final Validation

Expected archive gate: `cflx openspec validate add-post-integration-verification-contracts --archive-gate` exits 0.

## Acceptance Notes

- `proposal.md` の pre-integration rerun/evidence を実行可能な `cargo test openspec_cmd` に修正し、`cargo test openspec_cmd` の成功で確認した。
- `.github/workflows/release-validation.yml` を追加し、default branch push と `workflow_dispatch` で `./scripts/release.sh --dry-run patch` を実行、ログを `release-validation-log` artifact として公開する配線を追加した。
- `src/openspec_cmd.rs` に missing fields、absolute path、external symlink、non-regular file、hybrid/prose contradiction、complete pre/post contract、ownerless post-integration gate の検証を追加し、`cargo test openspec_cmd` の77件成功で確認した。
