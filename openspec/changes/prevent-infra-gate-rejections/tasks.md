## Implementation Tasks

- [x] Update acceptance stalled-hold routing so `AcceptanceResult::Gated` records a non-terminal stalled hold instead of invoking terminal rejection flow. (verification: unit - add/adjust Rust tests around `src/parallel/dispatch.rs` or the extracted routing helper proving a gated acceptance result emits stalled/blocked workspace status, returns no `rejected` result, and does not call/create base `REJECTED.md`; completion condition: acceptance gated path preserves resumable worktree context and does not execute `execute_rejection_flow()` solely from the gated verdict)
- [x] Add structured stalled blocker metadata for infrastructure and pending-verification holds. (verification: unit - reducer/state tests in `src/orchestration/state.rs` or related event tests assert blocker category, failed phase/gate, observed error summary, resumable next action, and worktree preservation metadata are available for a stalled acceptance hold; completion condition: downstream consumers can distinguish stalled infrastructure blocker from dependency blocked and terminal rejected state)
- [x] Preserve terminal rejection behavior only for explicit terminal rejection evidence. (verification: integration - `cargo test rejection_review` exercises `REJECTION_REVIEW: CONFIRM`/`BLOCK` routing and repository evidence in `src/parallel/dispatch.rs`, `src/orchestration/rejection.rs`, and `src/orchestration/state.rs` keeps terminal rejection separate from infrastructure-only gate failure; completion condition: terminal rejection path remains functional and is not triggered by infrastructure-only gate failure)
- [x] Formalize rejecting review `BLOCK` across shipped skill guidance and embedded contract checks. (verification: unit - `cargo test embedded_skills` checks `skills/cflx-rejecting/SKILL.md` documents `REJECTION_REVIEW: BLOCK` alongside `CONFIRM` and `RESUME`; completion condition: `CONFIRM`, `RESUME`, and `BLOCK` are all documented with distinct lifecycle meanings)
- [x] Update apply and acceptance skill guidance so recoverable infrastructure blockers use stalled/blocker artifacts and not terminal `REJECTED.md`. (verification: unit - `cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal` asserts `skills/cflx-apply/SKILL.md`, `skills/cflx-accept/SKILL.md`, and `skills/cflx-workflow/SKILL.md` describe recoverable blockers as stalled/non-terminal while keeping `gated` a compatibility token; completion condition: prompt guidance cannot instruct agents to turn Docker/network/credential/pending-job blockers into terminal rejection)
- [x] Add regression coverage for the concrete infrastructure blocker classes. (verification: unit - tests cover representative messages such as Docker image pull DNS timeout, Docker daemon unavailable, missing non-mockable external credential, package registry timeout, and agent-exec job still running; completion condition: each representative class maps to stalled/pending verification behavior unless separate terminal-invalid evidence is present)
- [x] Ensure UI/server-visible lifecycle distinctions remain stable. (verification: integration - reducer/display tests assert terminal `rejected`, non-terminal `stalled`, dependency `blocked`, and repository-fixable failure/error states remain distinguishable; completion condition: `REJECTED.md` rows stay read-only while stalled rows are not misclassified as rejected)

## Final Validation

Archive validation is the authoritative final OpenSpec validation gate. Expected archive gate after implementation: `cflx openspec validate prevent-infra-gate-rejections --archive-gate`.

## Acceptance #1 Failure Follow-up
- [x] コミット経路ブロッカー: このリポジトリの実コミット経路には `.git/hooks/pre-commit` があり、`.pre-commit-config.yaml:17-28` により `cargo fmt --all` と `cargo clippy --locked --all-targets --all-features -- -D warnings` が常時実行されます。現状、少なくとも上記 `cargo test stalled` / `cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal` が失敗しており、archive-ready ではありません。pre-commit 本体は未実行ですが、通常コミット前にこれらの実装・契約不整合を修正してから、必要に応じて `prek run --all-files` または通常コミット経路で確認してください。解消確認: `cargo fmt --all && cargo test stalled && cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal && cflx openspec validate prevent-infra-gate-rejections --strict` が成功。
- [x] 検証失敗: `agent-exec run -- cargo test stalled` が失敗しています。失敗箇所は `src/orchestration/state.rs:2354` の `test_acceptance_gated_transitions_to_stalled_with_structured_metadata` で、Docker image pull DNS timeout の分類が期待値 `Some("acceptance-gated:infrastructure")` に対して実際は `Some("acceptance-gated:external_service")` でした。`src/events.rs:208-241` の `classify_stalled_blocker_category` は `registry`/`timeout` を Docker/DNS 判定より先に `external_service` として分類しており、タスク8/仕様の代表ケース分類とテスト期待が不整合です。Docker image pull DNS timeout を infrastructure とするか、仕様・テスト・分類方針を一貫させてください。解消: Docker/DNS/network/image 判定を registry/external_service より優先し、代表ケース期待値も infrastructure に統一。`cargo test stalled` 成功。
- [x] 検証失敗: `agent-exec run -- cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal` が失敗しています。失敗箇所は `src/embedded_skills.rs:515` で、`cflx-accept` 埋め込みスキル本文が `infrastructure` / `non-terminal` / `stalled` を同時に含むという契約を満たしていません。変更の Acceptance Criteria 58 とタスク7は配布 acceptance guidance でも recoverable infrastructure blockers を non-terminal stalled と明記することを求めているため、`skills/cflx-accept/SKILL.md`（および必要なら埋め込み参照）を更新してください。解消: `skills/cflx-accept/SKILL.md` と `skills/cflx-workflow/SKILL.md` に recoverable infrastructure blockers are non-terminal stalled holds と代表クラス（Docker/credential/pending 等）を明記。`cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal` 成功。

## Acceptance #2 Failure Follow-up
- [x] Clippy 失敗: `src/events.rs:238-242` の `classify_stalled_blocker_category` で `else if lower.contains("timeout") { "infrastructure" }` と最終 `else { "infrastructure" }` が同一ブロックになっており、`clippy::if_same_then_else` が `-D warnings` でエラーです。timeout 判定を Docker/DNS/infrastructure 判定側へ統合するか、分岐構造を重複しない形に整理してください。解消: timeout/registry timeout を infrastructure 側の判定に統合し、重複分岐を削除。`agent-exec run -- cargo clippy --locked --all-targets --all-features -- -D warnings`（job `f65ec6e27d5fdb22113f0e7403b86075`）成功。
- [x] Clippy 失敗: `src/orchestration/state.rs:1918-1931` のテストコードで `ChangeRuntimeState::default()` 生成後に `terminal` / `wait_state` を再代入しており、`clippy::field_reassign_with_default` が `-D warnings` でエラーです。`ChangeRuntimeState { terminal: ..., ..Default::default() }` / `ChangeRuntimeState { wait_state: ..., ..Default::default() }` の初期化式に変更してください。解消: display status テストの state 初期化を構造体リテラルへ変更。`agent-exec run -- cargo clippy --locked --all-targets --all-features -- -D warnings`（job `f65ec6e27d5fdb22113f0e7403b86075`）成功。
- [x] コミット経路ブロッカー: 実コミット経路の `.git/hooks/pre-commit` は `prek hook-impl` を実行し、`.pre-commit-config.yaml:17-28` で `cargo fmt --all` と `cargo clippy --locked --all-targets --all-features -- -D warnings` が always_run です。`agent-exec run -- cargo clippy --locked --all-targets --all-features -- -D warnings`（job `05e374187046008f01b41c9b6beb3f45`）が exit_code 101 で失敗しており、archive commit はこのままでは通りません。解消: `agent-exec run -- cargo fmt --all`（job `c8858aa5498045638517d96b85d18fec`）と `agent-exec run -- cargo clippy --locked --all-targets --all-features -- -D warnings`（job `f65ec6e27d5fdb22113f0e7403b86075`）成功。
- [x] 前回指摘のテスト自体は解消確認済みです。`agent-exec run -- cargo test stalled`（job `0fd168a003298eb4b5a2a745e9bb32e7`）は 11 passed、`agent-exec run -- cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal`（job `19b842bcf735190240db3f1134ea7214`）は 1 passed、`cflx openspec validate prevent-infra-gate-rejections --strict`（job `49f5ae07e977ca40e6befe6d6589178c`）も成功しています。ただし上記 pre-commit/clippy ブロッカーが残るため PASS にはできません。解消: 再確認として `agent-exec run -- cargo test stalled`（job `aa0721b0646eb5f7974d266221297da3`）11 passed、`agent-exec run -- cargo test test_apply_and_acceptance_skills_keep_recoverable_infra_blockers_non_terminal`（job `94ba96257c957187b809923dad626ee2`）1 passed、`agent-exec run -- cflx openspec validate prevent-infra-gate-rejections --strict`（job `f7144183d9e071eff539a81551c8962f`）成功。
