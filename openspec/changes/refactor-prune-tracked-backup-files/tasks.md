## 1. キャラクタリゼーション
- [ ] 1.1 バックアップファイル削除前に主要フローのスモークテストを実行し、現行挙動を記録する（確認: `cargo test`）
- [ ] 1.2 バックアップファイルに依存していないことを確認するテスト/検証を追加する（確認: 追加した検証が失敗時に検知可能）

## 2. リファクタリング
- [ ] 2.1 追跡中の `.bak` / `.backup` / `.bak2` ファイルを整理する（確認: `git ls-files "*.bak" "*.backup" "*.bak2"` が空）
- [ ] 2.2 将来の再混入を防ぐ自動検知ルールを追加する（確認: 同拡張子を追加すると検知失敗する）

## 3. 回帰確認
- [ ] 3.1 `cargo test` を実行し、機能回帰がないことを確認する
- [ ] 3.2 `cargo run -- run --dry-run` を実行し、CLI挙動が従来どおりであることを確認する
