## Implementation Tasks

- [x] Task 1: Update `skills/cflx-accept/SKILL.md` to make stalled implementation blocker hold the primary concept while retaining `{"acceptance":"gated"}` and `ACCEPTANCE: GATED` only as runtime-compatible handoff tokens. (verification: manual - repository evidence path `skills/cflx-accept/SKILL.md`; runnable evidence command `python3 - <<'PY'` can assert the file contains `operator-facing status is stalled`, contains the two gated compatibility tokens, and lacks `FAIL vs GATED` / `- GATED:` primary-outcome labels; completion condition: the skill still instructs agents to emit the current parser-compatible tokens for valid Implementation Blockers and explicitly says operator-facing status is `stalled`.)
- [x] Task 2: Update `skills/cflx-accept-with-speca/SKILL.md` to mirror the `cflx-accept` terminology without changing the shared verdict protocol or adding SPECA-specific outcomes. (verification: manual - repository evidence path `skills/cflx-accept-with-speca/SKILL.md`; runnable evidence command `python3 - <<'PY'` can assert SPECA outcomes use the shared pass/fail/continue/stalled-hold contract, still mention `{"acceptance":"gated"}` only as a compatibility token, and lack `- GATED:` / `**Gated**` primary labels; completion condition: SPECA property outcomes route through pass/fail/continue/stalled-hold concepts and any `gated` wording is compatibility-token-only.)
- [x] Task 3: Update related distributed skill documentation and references in `skills/cflx-workflow/SKILL.md`, `skills/cflx-workflow/references/cflx-accept.md`, `skills/cflx-archive/SKILL.md`, `skills/README.md`, and `skills/cflx-rejection-guide*` where applicable. (verification: manual - repository evidence paths under `skills/`; runnable evidence command `python3 - <<'PY'` can scan `skills/**/*.md` for `GATED|gated|acceptance-gated` and require every remaining match to be a runtime token example, negative/reserved-marker guidance, historical ambiguity handling, or reason metadata rather than primary user-facing lifecycle wording; completion condition: remaining matches are compatibility or metadata references rather than primary user-facing lifecycle wording.)
- [x] Task 4: Update `.opencode/commands/cflx-accept.md` as a non-authoritative mirror so OpenCode users see the same stalled implementation blocker terminology while preserving current parser-compatible final verdict tokens. (verification: manual - repository evidence path `.opencode/commands/cflx-accept.md`; runnable evidence command `python3 - <<'PY'` can assert the command template contains stalled implementation blocker hold wording, preserves `ACCEPTANCE: GATED` compatibility output, and lacks `{"acceptance":"stalled"}` plus `FAIL vs GATED` / `Output GATED` lifecycle wording; completion condition: the command template does not instruct `GATED` as a lifecycle/status concept and does not emit `{"acceptance":"stalled"}` before parser support exists.)
- [x] Task 5: Update canonical prompt specs under `openspec/specs/agent-prompts/spec.md` with a requirement/scenario that distributed skills must present acceptance blockers as stalled holds and restrict `gated` to compatibility protocol tokens until parser migration. (verification: integration - repository evidence path `openspec/specs/agent-prompts/spec.md`; runnable evidence commands `cflx openspec validate migrate-skills-gated-wording-to-stalled --strict --evidence warn` and `cargo test embedded_skills` cover the spec delta and bundled-skill contract boundary; completion condition: the delta archives to a canonical rule that acceptance-related skills cannot regress to `GATED`-centered user-facing guidance.)
- [x] Task 6: Update embedded skill drift/contract tests in `src/embedded_skills.rs` or neighboring test modules to enforce the new wording boundary for bundled skills. (verification: unit - run `cargo test embedded_skills` after updating `src/embedded_skills.rs`; completion condition: tests fail if distributed `cflx-accept` guidance again labels the primary blocker outcome as `GATED` rather than stalled implementation blocker hold.)
- [x] Task 7: Run targeted and final verification for the implemented change. (verification: integration - run `cflx openspec validate migrate-skills-gated-wording-to-stalled --strict --evidence warn`, `cargo test embedded_skills`, and the repository lint/typecheck command documented for the changed surface; completion condition: validation and targeted tests pass, and any lint/typecheck failures are either fixed or documented with concrete blockers.)

## Future Work

- Add runtime parser support for `{"acceptance":"stalled","reason":"implementation_blocker"}` in a later, separate proposal.
- After runtime/parser migration and sufficient release adoption, remove `gated` compatibility output guidance and eventually remove parser compatibility.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate. Expected archive gate: `cflx openspec validate migrate-skills-gated-wording-to-stalled --archive-gate`.

## Notes

Acceptance #1 follow-up was resolved by updating `openspec/changes/migrate-skills-gated-wording-to-stalled/tasks.md` task verification notes with concrete repository evidence paths and runnable evidence commands. Archive-readiness validation is intentionally kept in the non-checkbox `## Final Validation` section because it is the authoritative final gate rather than an implementation task.
