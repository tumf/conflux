## Implementation Tasks

- [x] Trace and normalize the manual resolve retry handshake across `src/tui/state.rs`, `src/tui/command_handlers.rs`, `src/tui/runner.rs`, `src/orchestration/state.rs`, `src/parallel/queue_state.rs`, and `src/parallel/orchestration.rs`. (verification: unit/integration - evidence in `src/orchestration/state.rs:1032-1067`, `src/tui/command_handlers.rs:582-657`, `src/parallel/queue_state.rs:152-170`, `src/parallel/queue_state.rs:743-940`, `src/parallel/orchestration.rs:178`, and regression command `cargo test manual_retry --lib`; completion condition: reducer/TUI/scheduler tests identify where pending is created, synchronized, dispatched, or rejected)
- [x] Ensure pressing `M` on a repository-visible manual merge-wait row records reducer-owned retry intent that remains scheduler-consumable for archived-but-not-merged changes. (verification: unit - `src/orchestration/state.rs:2842-2860` proves archived manual retry transitions to `ResolveWait` and `src/orchestration/state.rs:2862-2877` proves merged stale retry remains `NoOp`; runnable command: `cargo test test_archived_manual_retry_becomes_scheduler_consumable_resolve_wait test_merged_manual_retry_remains_noop --lib`; completion condition: reducer-visible `ResolveWait` membership exists after accepted manual resolve)
- [x] Update TUI command handling so scheduler notification and "scheduled" logging only occur when reducer-owned retry intent was actually accepted. (verification: unit - `src/tui/command_handlers.rs:582-657` checks `ReduceOutcome::NoOp` before notifying/spawning, and tests in `src/tui/command_handlers.rs:722-818` plus `src/tui/command_handlers.rs:1053-1102` cover scheduler startup/notification truthfulness; runnable command: `cargo test resolve_merge --lib`; completion condition: command handling distinguishes accepted vs rejected retry intent)
- [x] Fix scheduler retry consumption so reducer-owned manual resolve pending is reevaluated after queue notifications and slot-release events and dispatches retry work when repository-visible conditions allow it. (verification: integration - `src/parallel/queue_state.rs:152-170` makes trigger/snapshot changes consumable, `src/parallel/queue_state.rs:743-940` dispatches `ResolveWait` retry work, `src/parallel/tests/executor.rs:3623-3690` covers slot-release wakeup, and `src/parallel/tests/executor.rs:3692-3758` covers startup without queued work; runnable command: `cargo test manual_retry --lib`; completion condition: the loop does not strand accepted manual pending when capacity later becomes available)
- [x] Ensure stranded or invalid manual resolve pending falls back to a truthful blocker/terminal state instead of remaining indefinitely at `resolve pending`. (verification: integration - `src/parallel/queue_state.rs:913-933` maps non-auto-resumable retry deferral back to `MergeDeferred(auto_resumable=false)` / merge-wait state, `src/parallel/queue_state.rs:745-797` clears already-merged or stale missing-worktree retry intent, `src/parallel/tests/executor.rs:3662-3689` asserts retry attempt emits visible manual deferral, and `src/parallel/tests/executor.rs:4478-4582` verifies stale already-merged retry is cleared without merge/resolve events; runnable commands: `cargo test manual_retry --lib` and `cargo test test_stale_already_merged_resolve_wait_skips_merge_and_hook --lib --features heavy-tests`; completion condition: no stale display-only pending survives after dispatch rejection/failure)
- [x] Bound repeated manual-resolve retry diagnostics so unchanged pending/blocker loops do not spam user-visible logs. (verification: unit/integration - `src/parallel/queue_state.rs:152-170` snapshots `last_dispatched_resolve_wait_changes`/`last_dispatched_reject_wait_changes` and consumes triggers, `src/parallel/tests/executor.rs:4930-4960` proves unchanged resolve-wait intent is not retried again without a new trigger, and `src/parallel/queue_state.rs:201-229`, `src/parallel/queue_state.rs:1271`, `src/parallel/queue_state.rs:1582` bound repeated diagnostics by signature; runnable command: `cargo test manual_retry --lib`; completion condition: identical retry/blocker diagnostics are deduped, rate-limited, or summarized until blocker signature changes)
- [x] Run focused regression coverage for reducer, TUI, and scheduler manual resolve flows. (verification: unit/integration - focused regression command `cargo test manual_retry --lib` passed via `agent-exec run -- cargo test manual_retry --lib` job `09e919a3a50448736e1b41dc3f30d034` with exit 0; supporting paths include `src/orchestration/state.rs:2842-2877`, `src/tui/command_handlers.rs:582-657`, `src/parallel/tests/executor.rs:3623-3758`, and `src/parallel/tests/executor.rs:4930-4960`; completion condition: reducer/TUI/scheduler manual resolve regressions pass)

## Final Validation

Expected archive gate: `cflx openspec validate fix-manual-resolve-pending-dispatch --archive-gate`

## Acceptance #1 Follow-up Resolution

Archive-gate blocker from Acceptance #1 was resolved by replacing checklist-only follow-up items with repository-verifiable evidence in the completed task notes above. The focused regression evidence remains `agent-exec run -- cargo test manual_retry --lib` job `09e919a3a50448736e1b41dc3f30d034`, exit 0.
