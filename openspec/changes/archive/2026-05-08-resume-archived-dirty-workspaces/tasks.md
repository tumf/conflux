## Implementation Tasks

- [x] Task 1: Define reducer/runtime semantics for `Archiving (files moved, commit incomplete)` so archived dirty workspaces remain recoverable and scheduler-ownable after archive finalization failure. (verification: unit - `src/orchestration/state.rs` + `cargo test orchestration::state::tests::archive_resumed_clears_recoverable_archive_error_and_restores_archiving_activity`)
- [x] Task 2: Update workspace state discovery and scheduler candidate selection so a workspace with archive files present and active change directory absent can be rediscovered and queued for archive-finalization repair on later scheduler cycles. (verification: integration - `src/parallel/queue_state.rs` + `cargo test parallel::dispatch::tests::archived_dirty_repair_candidate_reads_archived_tasks_without_active_change_dir`)
- [x] Task 3: Add a dedicated retry/ownership path for archived dirty workspaces that resumes archive finalization without re-running the full archive command unless archive file-state regression is detected. (verification: integration - `src/parallel/tests/executor.rs` + `cargo test parallel::tests::executor::test_archived_dirty_finalization_resume_does_not_rerun_archive_command`)
- [x] Task 4: Distinguish archive command failure, archive finalization retry, archived dirty recoverable hold, and exhausted terminal archive failure in events/logging/display state. (verification: integration - `src/parallel/tests/executor.rs` + `cargo test parallel::tests::executor::test_archived_dirty_reconciliation_discovers_workspace_after_archive_failed_terminal_state`)
- [x] Task 5: Ensure terminal `Archive failed` is emitted only after archived dirty recovery policy is exhausted, not merely because one run ended with `Archive commit verification failed`. (verification: unit - `src/orchestration/state.rs` + `cargo test orchestration::state::tests::archive_resumed_clears_recoverable_archive_error_and_restores_archiving_activity`)
- [x] Task 6: Add regression coverage for the observed `fix-dependency-target-handling` shape: archive move done, dirty archive workspace remains, scheduler previously idled, and the new behavior resumes repair work. (verification: integration - `src/parallel/tests/executor.rs` + `cargo test parallel::tests::executor::test_archived_dirty_reconciliation_discovers_workspace_after_archive_failed_terminal_state`)
- [x] Task 7: Run targeted Rust verification for orchestration/archive recovery behavior. (verification: integration - ran `cargo test orchestration::state::tests::archive_resumed_clears_recoverable_archive_error_and_restores_archiving_activity`, `cargo test parallel::dispatch::tests::archived_dirty_repair_candidate_reads_archived_tasks_without_active_change_dir`, and `cargo test parallel::tests::executor::test_archived_dirty_finalization_resume_does_not_rerun_archive_command`)

## Future Work

- Consider whether archived dirty recovery should expose an explicit TUI action in addition to automatic scheduler reclamation.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate resume-archived-dirty-workspaces --archive-gate`

## Acceptance #1 Failure Follow-up
- [x] src/orchestration/state.rs:1443-1453 marks every ArchiveFailed as TerminalState::Error and queued_change_ids() at src/orchestration/state.rs:877-887 excludes terminal changes, so the scheduler-owned recovery path in src/parallel/queue_state.rs:1177-1306 cannot rediscover a workspace solely from repository-visible archived-dirty state after a prior archive finalization failure. The only reducer test added (src/orchestration/state.rs:2087-2111) requires a later ArchiveResumed event to clear the error, but no later scheduler cycle can emit ArchiveResumed unless the change is first queued/reconciled. This violates acceptance criteria lines 38-40 and spec lines 5-9/36-42. Action: on archive failure that is potentially commit-finalization-incomplete, preserve or restore queue ownership for archived-dirty candidates based on workspace-visible state, or make queue reconciliation scan existing workspaces for archived-dirty state independent of queued_change_ids(). (implemented: src/parallel/queue_state.rs now scans existing worktree change ids and reconstructs archived-dirty repair candidates from workspace-local archive files even when reducer queued_change_ids() is empty after ArchiveFailed)
- [x] src/parallel/queue_state.rs:1177-1306 only considers IDs already present in reducer queued intent, then optionally reconstructs an archived dirty candidate if list_changes_native_from() does not include it. Because archived dirty workspaces have active change dir absent and archive entry present, they are not in OpenSpec active changes; after ArchiveFailed the reducer currently has TerminalState::Error and no queued intent. Therefore the implementation does not meet Task 2/Task 6's claimed integration behavior: a later scheduler cycle with an archived dirty worktree but no active change directory will still have queued_intent_ids empty at line 1191 and return 0, leaving the scheduler idle. Action: add an integration regression that starts from the real post-failure reducer/workspace shape (ArchiveFailed + archive entry present + active dir absent) and asserts reconciliation/dispatch reclaims it, then fix the reconciliation/state derivation path. (implemented: src/parallel/tests/executor.rs adds `test_archived_dirty_reconciliation_discovers_workspace_after_archive_failed_terminal_state` covering ArchiveFailed + archive entry present + active dir absent with terminal-invisible reducer queue)

## Acceptance #2 Failure Follow-up
- [x] src/parallel/tests/executor.rs:3580 references `ExecutionEvent::Log(log)` without importing or qualifying `ExecutionEvent`, so the changed regression test does not compile. (verification: integration - `src/parallel/tests/executor.rs` + `cargo test parallel::tests::executor::test_archived_dirty_reconciliation_discovers_workspace_after_archive_failed_terminal_state && cargo test parallel::tests::executor::test_archived_dirty_finalization_resume_does_not_rerun_archive_command && cargo test parallel::dispatch::tests::archived_dirty_repair_candidate_reads_archived_tasks_without_active_change_dir`) Blocking verification/commit-path evidence: `agent-exec run -- zsh -lc 'cargo test parallel::tests::executor::test_archived_dirty_reconciliation_discovers_workspace_after_archive_failed_terminal_state && cargo test parallel::tests::executor::test_archived_dirty_finalization_resume_does_not_rerun_archive_command && cargo test parallel::dispatch::tests::archived_dirty_repair_candidate_reads_archived_tasks_without_active_change_dir'` failed with `error[E0433]: failed to resolve: use of undeclared type ExecutionEvent` at `src/parallel/tests/executor.rs:3580:16`. Action: qualify this pattern as `crate::events::ExecutionEvent::Log(log)` or add the correct import, then rerun the targeted tests. (implemented: pattern now qualifies `crate::events::ExecutionEvent::Log(log)` and targeted verification passes)
