## Implementation Tasks

- [x] 1. Remove the JSON checkpoint model and filesystem lifecycle while preserving only the independently required `APPLY_BLOCKED/marker.md` parsing, writing, and consumption behavior (verification: unit - `cargo test parallel::acceptance_state` and `rg 'acceptance-state\.json|AcceptanceStateStatus|load_acceptance_state|save_acceptance_state' src` show no production checkpoint implementation)
- [x] 2. Replace serial runtime checkpoint reads/writes with in-memory acceptance-attempt context for the active run and repository-visible follow-up/blocked evidence for durable outcomes (verification: integration - `cargo test serial_run_service` covers uninterrupted PASS-to-archive, FAIL-to-apply, stalled marker creation, and restart acceptance rerun)
- [x] 3. Replace parallel dispatch checkpoint reads/writes with in-memory cycle context, routing complete unarchived work to acceptance after restart and incomplete tasks to apply (verification: integration - `cargo test parallel::dispatch` and focused tests in `src/parallel/tests/executor.rs` cover Applied, Archiving, incomplete-task, and archived/base-integrated resume paths)
- [x] 4. Define restart semantics for pre-stall retry context: do not reconstruct retry count, prior finding identities, or semantic baseline from a generated checkpoint; rerun acceptance safely and persist only normal `tasks.md` follow-up or a final resumable stalled marker (verification: integration - focused restart tests in `src/serial_run_service.rs` and `src/parallel/tests/executor.rs` prove missing in-memory context cannot skip acceptance and marker-backed stalled work remains blocked)
- [x] 5. Delete checkpoint-specific cleanup and Git handling from archive, merge, queue, execution-state, semantic fingerprint, and workspace cleanup paths without weakening unrelated dirty-state checks (verification: integration - `cargo test execution::archive parallel::merge orchestration::acceptance` and `rg 'acceptance-state\.json|delete_acceptance_state' src`)
- [x] 6. Add an end-to-end regression fixture for `apply complete -> acceptance PASS -> archive commit -> post-archive merge` that asserts no `.cflx/acceptance-state.json` is created, no cleanup dirties the worktree, and no manual `MergeDeferred(auto_resumable=false)` occurs (verification: e2e - focused non-heavy test in `src/parallel/tests/executor.rs` reaches resolving/merged and inspects `git status --porcelain`)
- [x] 7. Preserve negative behavior for genuine blockers: unrelated dirty files, surviving active change directory, missing archive entry, and invalid nested archive layout still defer or fail with concrete evidence (verification: integration - focused tests in `src/parallel/merge.rs` and `src/execution/archive.rs` pass)
- [x] 8. Remove obsolete `.gitignore` and tests/documentation that exist only for `.cflx/acceptance-state.json`, while retaining ignore entries and runtime artifacts with independent purposes (verification: unit - `git diff -- .gitignore` plus `rg 'acceptance-state\.json' --glob '!openspec/changes/**'` returns no runtime/config references)
- [x] 9. Run repository quality gates and keep default tests under the project heavy-test policy (verification: integration - `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` pass; tests exceeding one second are optimized or marked heavy)

## Notes

- Evidence for task 1 — `src/parallel/acceptance_state.rs` now holds only `AcceptanceRetryContext` (in-memory) plus blocked-marker write/parse/consume. `rg 'AcceptanceStateStatus|load_acceptance_state|save_acceptance_state|delete_acceptance_state|record_acceptance_retry' src` returns nothing.
- Evidence for task 2 — `SerialRunService::acceptance_retry` (`src/serial_run_service.rs`) replaces the disk checkpoint; `process_acceptance_result` takes `&mut self` and drops the `revision` parameter. Covered by `serial_active_run_accumulates_acceptance_retry_context_without_a_checkpoint_file`, `serial_acceptance_pass_hands_off_in_memory_without_writing_a_checkpoint`, `serial_repeated_findings_without_progress_stall_before_another_apply`, `serial_cycle_limit_stalls_with_workspace_marker`.
- Evidence for task 3 — `src/parallel/dispatch.rs` keeps `acceptance_retry: AcceptanceRetryContext` local to one dispatch; `restore_acceptance_checkpoint` is deleted. Covered by `parallel_repeated_acceptance_failure_stalls_without_error_event`, `decide_resume_action_*` tests, and existing resume routing tests.
- Evidence for task 4 — `serial_restart_reruns_acceptance_without_reconstructing_retry_context` and `parallel_restart_ignores_generated_acceptance_state_when_deciding_retries` seed a stale on-disk `.cflx/acceptance-state.json` and prove it is neither consulted nor consumed; `test_detect_workspace_state_blocks_acceptance_marker_after_restart` and `test_resumed_workspace_marker_stops_parallel_dispatch_before_apply_acceptance_and_archive` prove marker-backed stalls still block.
- Evidence for task 5 — `delete_acceptance_state` call sites removed from `src/execution/archive.rs`, `src/parallel/executor.rs`, `src/parallel/merge.rs`, and `src/parallel/queue_state.rs`. The semantic fingerprint keeps its generic `.cflx/` runtime-directory exclusion (independent purpose) with the test generalized to `.cflx/runtime.json`.
- Evidence for task 6 — `parallel_pass_to_archive_to_merge_never_creates_or_cleans_an_acceptance_checkpoint` in `src/parallel/tests/executor.rs` runs apply -> acceptance PASS -> archive commit -> `attempt_merge`, asserts an empty `git status --porcelain`, no checkpoint in workspace or base, `is_archive_commit_complete == true`, no manual deferral, and a `ResolveStarted` event. Runs in ~0.8s (non-heavy).
- Evidence for task 7 — `test_is_archive_commit_complete_false_when_dirty`, `test_is_archive_commit_complete_false_when_change_exists`, `test_verify_archive_change_not_archived`, `test_verify_archive_rejects_nested_archive_layout`, the new `test_is_archive_commit_complete_rejects_invalid_nested_archive_layout`, plus `test_attempt_merge_dirty_base_remains_manual_deferred` and `test_archive_incomplete_without_base_integration_remains_manual_deferred` in `src/parallel/merge.rs`.
- Evidence for task 8 — `.gitignore` no longer lists `.cflx/acceptance-state.json`; `.cflx.jsonc` and all other entries retained. Remaining matches for the path are absence assertions and stale-state fixtures in tests only.
- Evidence for task 9 — `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` all pass (2128 lib tests, 0 failures). Each new/modified test was timed individually and runs under one second.

## Future Work

- Historical repositories may retain the path in old commits; this change does not rewrite published Git history.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate eliminate-acceptance-checkpoint --archive-gate`
