## Implementation Tasks

- [x] In `src/client/mcp.rs`, replace `read_line` with a byte-bounded reader that terminates on oversized or invalid-UTF-8 frames before dispatch; add newline-free overflow tests in `tests/client_mcp_integration.rs`. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_mcp_integration --features heavy-tests`)
- [x] In `src/web/completion_sink.rs`, replace `wait_with_output` with concurrent fixed-retention drains that continue discarding after overflow; timeout/cancellation must call kill and wait, while successful high-volume output exits normally with truncation diagnostics. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [x] Require exact instance/execution/change binding for sink GET and return registered argv only over UDS; TCP GET exposes presence/history but not argv. Update OpenAPI and add omission/mismatch/TCP tests. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [x] Create event files as `0400` in `0700`, remove the false immutability comment, never re-read payloads, and test default write refusal only under unprivileged EUID plus cleanup after reap. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [x] Enforce JSON-RPC 2.0 and initialization-at-successful-`initialize` response, allow pre-init `ping`, idempotently accept `notifications/initialized`, reject batches, and update existing unit/integration tests to handshake. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_mcp_integration --features heavy-tests`)
- [x] Keep delivery serialized; add test-injectable callback/shutdown deadlines and cancellation wiring that forbids new artifacts/deliveries after shutdown, kills/waits active children, and cleans only after reap. Verify ordering deterministically without performance thresholds. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test --test client_completion_sink`)
- [x] Update capability/docs/comments in `src/web/remote_control_api/mod.rs`, `src/web/completion_sink.rs`, and client documentation for truncation, UDS-only argv disclosure, default-read-only event artifacts, and serialized shutdown; run format and clippy. (verification-id: core-mcp-sink-hardening) (verification: unit - `cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && python3 -c "from pathlib import Path; p=Path('src/web/completion_sink.rs').read_text(); assert 'immutable for the callback' not in p"`)
- [x] Run focused and default regression suites and preserve existing execution identity, late registration, terminal classification, and callback failure behavior. (verification-id: core-mcp-sink-hardening) (verification: integration - `cargo test`)

## Final Validation

Expected archive gate: `cflx openspec validate harden-client-mcp-completion-sinks --archive-gate`

## Notes

- evidence (bounded MCP framing): `cargo test --lib client::mcp` — 18 passed, including `a_newline_free_oversized_frame_is_bounded_and_ends_the_session`, `an_oversized_terminated_frame_dispatches_nothing_after_it`, and `a_non_utf8_frame_ends_the_session_without_dispatching`.
- evidence (protocol state): the same suite covers `tools_are_refused_until_the_handshake_completes`, `a_frame_that_does_not_identify_json_rpc_2_is_an_invalid_request`, and `batches_and_unusable_ids_are_invalid_requests_answered_with_null`; the pre-existing direct unit tests now handshake first.
- evidence (owner sinks): `cargo test --test client_completion_sink` — 17 passed in 0.99s, including the new `binding_is_required_for_inspection_and_argv_is_disclosed_only_over_the_socket`, `artifact_is_owner_read_only_by_default_and_is_never_read_back`, `delivery_drains_a_callback_that_overflows_both_streams_without_killing_it`, and `shutdown_reaps_every_callback_before_removing_any_artifact`.
- evidence (retention bound): `cargo test --lib web::completion_sink` — 6 passed, including the in-memory `a_drain_retains_at_most_the_limit_and_still_consumes_the_rest`, which is the unit-scoped proof that retention is bounded while every byte is still consumed.
- evidence (end to end): `cargo test --test client_mcp_integration --features heavy-tests` — 9 passed, including the two new binary-level tests for the newline-free oversized frame and the pre-initialization/off-protocol refusals.
- evidence (regression): `cargo test` — every default-suite binary green, 0 failures; the lib suite alone reports 3863 passed.
- evidence (style and contract): `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and the false-immutability-comment check all pass; `cflx openapi` shows `sink_registered` and the now-required `instance_id`/`change_id` query parameters on `GET /api/v2/executions/{execution_id}/sink`.
- The shutdown deadline and callback timeout are injectable through `CompletionSinkRegistry::set_shutdown_deadline` / `set_callback_timeout`. The tests use them only to make the cancellation branch reachable inside a bounded run; every assertion is on event ordering and process state, never on elapsed time.
