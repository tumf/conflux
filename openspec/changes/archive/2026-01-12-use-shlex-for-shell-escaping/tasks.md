# Tasks: shlex によるサブコマンド全体のエスケープ強化

## Phase 1: 依存関係とエスケープ基盤の整備

- [x] 1.1 `Cargo.toml` に `shlex = "1.3"` を追加
- [x] 1.2 `src/config/expand.rs` に shlex ベースの共通エスケープヘルパーを追加
- [x] 1.3 `expand_prompt()` を `shlex::try_quote()` ベースに置き換え
- [x] 1.4 `expand_proposal()` を `shlex::try_quote()` ベースに置き換え
- [x] 1.5 `expand_conflict_files()` のエスケープ処理を統一
- [x] 1.6 `{change_id}` の安全性検証を追加（必要ならエスケープも検討）

---

## Phase 2: テンプレート互換性と設定の整理

- [x] 2.1 既存テンプレートのクォート有無を判定する互換ロジックを追加
- [x] 2.2 既定テンプレート（`.openspec-orchestrator.jsonc.example` 等）を新推奨形式に更新
- [x] 2.3 既存テンプレートが壊れないことを確認（後方互換テスト）

---

## Phase 3: テスト整備

- [x] 3.1 特殊文字（`$`, `` ` ``, `!`, `\`）を含む文字列のテスト
- [x] 3.2 改行・タブを含む文字列のテスト
- [x] 3.3 シングルクォートとダブルクォートの混在テスト
- [x] 3.4 日本語・絵文字などマルチバイト文字のテスト
- [x] 3.5 テンプレート互換性（クォートあり/なし）のテスト

---

## Phase 4: 実行経路の確認

- [x] 4.1 `agent.rs` のコマンド実行で安全に展開されることを確認
- [x] 4.2 `hooks.rs` のフック実行で安全に展開されることを確認
- [x] 4.3 `parallel/executor.rs` の並列実行で安全に展開されることを確認
- [x] 4.4 TUI からの提案実行で安全に展開されることを確認

---

## Phase 5: ドキュメントと最終確認

- [x] 5.1 `src/config/expand.rs` のコメントを更新してエスケープ方針を明記
- [x] 5.2 `DEVELOPMENT.md` にテンプレート推奨形式と互換性注意点を追記
- [x] 5.3 `cargo fmt`, `cargo clippy -- -D warnings`, `cargo test` を実行

---

## 検証項目

- [x] V.1 既存の全テストがパス
- [x] V.2 追加したエスケープテストがパス
- [x] V.3 既存テンプレートが壊れない
- [x] V.4 パラレル/TUI/CLI で実行が失敗しない
