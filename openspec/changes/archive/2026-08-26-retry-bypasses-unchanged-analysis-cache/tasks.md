## Implementation Tasks

- [x] Arm a target-specific explicit-retry scheduler edge for every accepted retry route, including the acceptance-stall route whose reducer command is `AddToQueue`, while keeping failed-classification release, blocked-fingerprint drop, and Apply-budget reset scoped to the terminal-error route and leaving refused or no-op retries arming nothing. (verification: integration - `cargo test --lib accepted_retry_publishes_explicit_retry_edge`; verification-id: explicit-retry-edge-creation)
- [x] Keep a consumed edge's bypass authority alive through the scheduler pass it authorizes, and leave the edge armed for the next eligible evaluation when a pass ends before analysis (cancellation, incomplete reducer view, early break), without weakening dependency, capacity, eligibility, or retry-budget guards. (verification: integration - `cargo test --lib retry_change_bypasses_unchanged_analysis_input_and_dispatches`; verification-id: explicit-retry-dispatch-regression)
- [x] Add a deterministic paused-time regression that starts from an accepted `retry_change` for a stalled target, derives the reanalysis reason from the consumed edge rather than from a test-supplied `ReanalysisReason`, and asserts one fresh analyzer invocation against a previously completed matching signature, a real Apply dispatch start that a no-op or stubbed dispatch would fail, suppression on the next unchanged timer wake, and no release of another target's failed classification. (verification: integration - `cargo test --lib retry_change_bypasses_unchanged_analysis_input_and_dispatches`; verification-id: explicit-retry-dispatch-regression)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate retry-bypasses-unchanged-analysis-cache --archive-gate`.
