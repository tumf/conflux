## Implementation Tasks

- [x] 1. implement archive attempt reason enum / structured field in `src/history.rs`, `src/agent/history_ops.rs`, `src/orchestration/archive.rs`, `src/parallel/executor.rs` and keep primary archive failure reason as typed fields (verification: unit - `src/history.rs` と `tests/` 配下の archive history formatting / attempt recording tests で verification failure, prerequisite blocker, command failure, stall を別 reason として保持することを確認する)
- [x] 2. implement durable archive resume state storage/load helpers and required fields (change_id, revision, attempt, status, primary_reason, summary, updated_at) under `src/orchestration` / `src/parallel` (verification: unit - `src/parallel/archive_state.rs` と `tests/` の state roundtrip tests で保存/復元/削除と revision mismatch handling を確認する)
- [x] 3. implement durable archive state lookup for `Archiving`/`Archived` branches in `src/execution/state.rs` and `src/parallel/dispatch.rs` to generate reason-aware handoff / retry context while keeping `Archived` terminal merge handoff behavior (verification: integration - `tests/` の workspace resume tests で `Archiving` resume の previous archive reason 復元と `Archived` merge handoff 維持を確認する)
- [x] 4. implement structured reason + summary propagation for archive retry / resume / failure events in `src/parallel/executor.rs`, `src/orchestration/archive.rs`, `src/events.rs` (verification: integration - `tests/` の executor / event bridge tests で archive retry scheduled, archive resumed, archive failed の reason-aware payload を確認する)
- [x] 5. implement prior-reason restoration after restart in `src/orchestration/archive.rs` and `src/agent/history_ops.rs` via archive history restore or durable state injection (verification: integration - `tests/` の simulated restart test で prior archive reason が次回 archive context に含まれることを確認する)
- [x] 6. update `openspec/changes/persist-archive-resume-reasons/design.md` and `openspec/specs/{parallel-execution,orchestration-events}/spec.md` so they align with archive reason taxonomy definitions in `src/orchestration/archive.rs` / `src/events.rs` and remain consistent with `align-archive-readiness-failure-reporting` (verification: manual - `src/events.rs` の taxonomy 定義と openspec proposal/design/spec を突合して責務境界の補完関係をレビューする)
- [x] 7. update test and verification code paths for archive reason persistence in `src/parallel/executor.rs`, `src/orchestration/archive.rs`, `src/events.rs`, `src/agent/history_ops.rs`, and verify no regressions with `cflx openspec validate persist-archive-resume-reasons --strict --evidence warn`, `cargo test`, `cargo clippy --all-targets --all-features -- -D warnings` (verification: integration - `cflx openspec validate persist-archive-resume-reasons --strict --evidence warn` と `cargo test` と `cargo clippy --all-targets --all-features -- -D warnings` の実行結果を確認する)
- [x] 8. update archive resume/runtime handoff implementation in `src/parallel/dispatch.rs` and `src/execution/state.rs` (`Archiving`/`Archived` branching and durable reason loading) and verify reason-aware context reconstruction after restart via integration test (verification: integration - `src/parallel/dispatch.rs`, `src/execution/state.rs`, `tests/` の restart resume integration test で reason-aware context 再構築を確認する)
