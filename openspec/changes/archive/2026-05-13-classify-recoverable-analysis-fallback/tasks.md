## Implementation Tasks

- [x] Adjust recoverable LLM-analysis fallback logging so the LLM failure plus successful metadata fallback is emitted below error severity while retaining a visible degraded-path diagnostic. (verification: unit - added `src/parallel_run_service.rs` tests around `analyze_order_with_sender` and `log_recoverable_analysis_fallback`; evidence: `agent-exec run -- cargo test fallback` job `dfeb1ab19af795759576b393da48d7de` passed, including `test_recoverable_fallback_log_uses_warn_level_only`; completion condition: a recoverable fallback path has no ERROR-level record but still logs fallback visibility)
- [x] Preserve metadata-dependency-only fallback behavior. (verification: unit - extended fallback dependency preservation coverage in `src/parallel_run_service.rs`; evidence: `agent-exec run -- cargo test fallback` job `dfeb1ab19af795759576b393da48d7de` passed, including `test_analyze_order_recoverable_fallback_preserves_metadata_dependencies` and existing disabled-LLM fallback test; completion condition: fallback output still contains declared metadata dependencies and does not become dependency-free)
- [x] Preserve actionable dependency blocker severity for unsafe cases. (verification: integration - ran dependency target tests in `src/analyzer.rs` and `src/parallel/tests/executor.rs` covering missing/rejected dependency blockers; evidence: `agent-exec run -- cargo test dependency_graph_reports` job `10b4cda143636fc79ebeb61835981f67` passed, and `agent-exec run -- cargo test test_single_queued_dependency_block_classes_fail_closed` job `33060ed9c3206b88930da0e5d5449d77` passed; completion condition: missing/rejected dependency references still fail closed and remain visible)
- [x] Confirm observability-only behavior. (verification: manual - inspected `src/parallel_run_service.rs`, `src/parallel/queue_state.rs`, and `scripts/cflx-log-mine.py`; evidence: `src/parallel_run_service.rs` only emits tracing diagnostics for fallback, `src/parallel/queue_state.rs` dispatch continues to derive decisions from `AnalysisResult`, reducer/shared state, workspace/git/base state, and dependency evaluation, and `scripts/cflx-log-mine.py` is a standalone read-only reporting script with no Rust call sites found by repository search; completion condition: workflow decisions still derive from workspace/git/base state and in-memory scheduler evaluation, not mined logs)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate classify-recoverable-analysis-fallback --archive-gate`
