## Implementation Tasks

- [x] Add explicit external-blocked lifecycle state and blocker-kind metadata to the maintained proposal/reducer model while retaining stalled for no-progress and exhausted execution outcomes. Complete when transition and snapshot tests reject simultaneous or ambiguous blocked/stalled state and preserve dependency versus external blocker kinds. (verification: unit - `cargo test --all-features runtime::`; verification-id: blocker-lifecycle-classification)
- [x] Centralize structured blocker validation and classification in the orchestrator result-handling boundary. Require supported source/category, non-empty evidence, verifiable unblock condition, next action, and resumability for external `blocked`; route repeated/no-progress/exhausted outcomes to `stalled` and malformed bare verdicts to bounded protocol correction. Complete when table-driven tests cover Apply, Acceptance, legacy token, missing-field, repository-fixable, repeated-finding, and retry-exhaustion cases. (verification: unit - `cargo test --all-features acceptance`; verification-id: blocker-lifecycle-classification)
- [x] Wire external-blocked state into parallel scheduler eligibility, blocked-only drain, completion accounting, dependency propagation, and explicit retry/reconciliation without creating synthetic dependency edges. Complete when scheduler tests prove ordinary dispatch suppresses blocked changes, unrelated ready changes continue, dependents remain dependency-blocked, and operator retry reruns the blocked phase after workspace identity validation so fresh output controls reclassification. (verification: integration - `cargo test --all-features parallel::tests`; verification-id: blocker-lifecycle-classification)
- [x] Preserve constitutional restart behavior by keeping blocker lifecycle state process-local and recomputing the next action from workspace and git evidence. Complete when restart tests prove deletion or absence of runtime state cannot alter routing for identical workspace contents and stale blocked metadata cannot authorize acceptance, archive, or merge. (verification: integration - `cargo test --all-features orchestration`; verification-id: blocker-lifecycle-classification)
- [x] Propagate reducer-derived `blocked` and `stalled` plus machine-readable blocker kind and detail through TUI and WebSocket/API payloads. Complete when API and TUI tests distinguish dependency-blocked, external-blocked, and stalled rows without independently re-deriving status. (verification: integration - `cargo test --all-features tui server`; verification-id: blocker-lifecycle-classification)
- [x] Extend `dashboard/src/api/types.ts`, `dashboard/src/components/ChangeRow.tsx`, and their component tests to support externally blocked changes while preserving stalled behavior. Complete when component tests render distinct labels/details and an unknown blocker kind is rejected at the untrusted-payload boundary. (verification: unit - `dashboard/src/components/ChangeRow.test.tsx` via `npm --prefix dashboard test -- --run && npm --prefix dashboard run lint && npm --prefix dashboard run build`; verification-id: blocker-lifecycle-classification)
- [x] Update `skills/cflx-apply/SKILL.md`, `skills/cflx-accept/SKILL.md`, and their prompt contract tests so agents preserve the tasks.md `Implementation Blocker` section and `IMPLEMENTATION_BLOCKER:` stdout contract, emit complete structured facts, avoid recoverable `REJECTED.md`, and defer canonical blocked/stalled classification to Conflux. Complete when tests reject missing workspace-visible evidence and instructions that classify from prose/token text alone. (verification: integration - `cargo test --all-features prompt`; verification-id: blocker-lifecycle-classification)
- [x] Run repository quality gates and confirm compatibility input parsing and unrelated dependency-blocked scheduling remain green. Complete when all Rust and dashboard checks pass without exclusions. (verification: integration - `cargo test --all-features && cargo clippy --all-targets --all-features -- -D warnings && npm --prefix dashboard test -- --run && npm --prefix dashboard run lint && npm --prefix dashboard run build`; verification-id: blocker-lifecycle-classification)

## Future Work

- Automatic external-condition polling or owner notification may be proposed separately after blocker metadata is stable.
- The dashboard has no `tsconfig.json` and no typecheck step (`build` is `vite build`; `lint` is `eslint .` over `.js`/`.jsx` only), so TypeScript annotations under `dashboard/src/` are erased and never verified by any gate. Adding a typecheck gate is repo-wide work outside this change; see Notes.

## Notes

- Task 6 originally read "TypeScript rejects an unknown blocker kind". No gate in this repository runs `tsc`, so a compile-time-only assertion would have been unverifiable and the task text was refined to match enforceable reality. The closed set is enforced where the untrusted server payload actually arrives: `BLOCKER_KINDS` and `toBlockerKind()` in `dashboard/src/api/types.ts`, exercised by `ChangeRow.test.tsx`. The `BlockerKind` union is still declared for editor-level type safety.
- Behavior change: a validated non-repository prerequisite now displays `blocked` with blocker kind `external` instead of `stalled`. Existing tests asserting `stalled` for that case were updated rather than preserved, because collapsing the two is the defect this change removes.
- Behavior change: in parallel dispatch, every acceptance stop that is not a validated external prerequisite — retry exhaustion, no semantic progress, a repeated finding ID, and a repair delta that missed its declared files — now emits an acceptance-phase `ExecutionBlocked` hold and returns a non-error `WorkspaceResult`, so they display `stalled` and keep the worktree preserved. This matches what serial mode already did through `ChangeProcessResult::Stalled`. The two repair-gate stops share `hold_repair_stop` in `src/parallel/dispatch.rs` with the retry-policy stall path, so the two modes cannot diverge again.
- `src/orchestration/blocker_classification.rs` is changed only in service of the finding above: `execution_stop_reason_for` now also maps the repair-gate stop reasons (`repeated_acceptance_finding`, `acceptance_remediation_mismatch`), so the single classifier — not the new dispatch call site — decides the stalled category. Without it a repeated-finding repair stop would silently classify as `no_semantic_progress`.
- Compatibility input parsing is unchanged: `gated` and legacy `blocked` verdict tokens still parse, and a bare token still routes to bounded protocol correction and sets neither lifecycle state.
- `blocker` payloads now require `unblock_condition`. A payload missing it is a protocol error handled by the existing bounded acceptance retry, never an inferred external wait. Both bundled acceptance skills document the five required fields.
- evidence: `cargo test --all-features --lib` passed 3008 tests, 0 failed.
- evidence: `cargo clippy --all-targets --all-features -- -D warnings` reported no findings.
- evidence: `npm --prefix dashboard test -- --run` passed 21 files / 89 tests; `npm --prefix dashboard run lint` and `npm --prefix dashboard run build` both succeeded.
- Pre-existing unrelated failure: `tests/e2e_proposal_session.rs::proposal_session_prompt_injects_backend_managed_spec_guidance` fails identically on the base commit (verified by stashing this change), so it is neither caused by nor in scope for this change.
- `parallel::tests::executor::test_manual_resolve_wait_retries_after_in_flight_apply_completes` failed once under full-suite load and passes both in isolation and on a full re-run; it is an unrelated pre-existing flake.

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate classify-external-blockers --archive-gate`.
