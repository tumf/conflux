## Implementation Tasks

- [x] acceptance verdict contract を JSON primary / text fallback に再定義する (verification: unit - `src/acceptance.rs` の parser regression test)
- [x] acceptance runtime が `opencode run` の strict JSON 1 行出力を直接解釈できるようにする (verification: unit - `src/acceptance.rs` / integration - `src/parallel/executor.rs`)
- [x] `opencode run --format json` のイベント出力から本文 verdict JSON を抽出して同一判定へ正規化できるようにする (verification: integration - acceptance runner の streaming test)
- [x] plain text standalone marker を後方互換 fallback として残し、JSON 不在時のみ利用する (verification: unit - legacy marker compatibility test)
- [x] `.opencode/commands/cflx-accept.md` を、新しい JSON verdict contract を primary とする指示へ更新する (verification: manual - command template review)
- [x] `skills/cflx-accept/SKILL.md` を、新しい machine-readable verdict contract と fallback 方針へ更新する (verification: manual - skill contract review)
- [x] archive 関連 skill / guidance で acceptance contract 前提が残っている箇所を追随更新する (verification: manual - `skills/cflx-archive/SKILL.md` diff review)
- [x] malformed text verdict では CONTINUE だった実ケースを、JSON verdict では PASS で handoff できる回帰テストを追加する (verification: integration - acceptance→archive handoff regression)
- [x] `cargo test` の対象回帰を実行し、acceptance parser / runner / handoff が通ることを確認する (verification: integration - repo test command)

## Future Work

- apply / archive / resolve など他 operation への JSON verdict contract 展開
- opencode upstream 側で structured output enforcement を強化する場合の追随

## Acceptance #1 Failure Follow-up

- [x] Restore JSON-primary canonical guidance in `.opencode/commands/cflx-accept.md` "Output format" section. The apply commit (bacf76a8) reverted this file to declare the legacy plain-text marker as the primary machine-readable payload ("Required for current Conflux compatibility — emit exactly ONE legacy standalone plain-text verdict marker as the final machine-readable payload", lines 74–76) and explicitly forbids JSON output ("Do NOT rely on JSON-only verdict output for this worktree until the runtime rollout is complete.", lines 85–86). This directly violates `openspec/changes/adopt-json-acceptance-verdict/specs/agent-prompts/spec.md` MODIFIED Requirement ("The primary acceptance verdict contract MUST be a strict JSON object ... canonical guidance MUST prefer the JSON contract") and the scenario "command template defines JSON-primary verdict contract" ("it defines a strict JSON verdict object as the primary machine-readable contract / it documents plain-text standalone verdict markers only as backward-compatible fallback guidance"). Re-apply the JSON-primary "Primary (REQUIRED)" / "Fallback (backward-compatible)" structure from commit bc4e722b, keep legacy markers only as documented fallback, and reflect the truthful state in task 5 (`.opencode/commands/cflx-accept.md` を、新しい JSON verdict contract を primary とする指示へ更新する).
- [x] Restore JSON-primary canonical guidance in `skills/cflx-accept/SKILL.md`. Current content states "Output: Exactly ONE legacy standalone verdict marker at the end" (line 22) and "For current Conflux compatibility, emit exactly ONE legacy standalone plain-text verdict marker on its own line" (lines 26–27) with "Do NOT rely on JSON-only verdict output from this worktree until the runtime rollout is complete." (lines 34–36). This redefines a text-only canonical output rule and violates the scenario "repo-local acceptance skills follow the same contract" ("they reference the same JSON-primary verdict contract / they do not redefine a conflicting text-only canonical output rule"). Restore the "Verdict Output Contract" section from commit bc4e722b that declares JSON as primary and legacy markers as fallback, and reflect that state in task 6.
- [x] Restore JSON-primary handoff description in `skills/cflx-archive/SKILL.md` "Acceptance Handoff Contract" section (lines 17–30). Current content states "For current compatibility, the acceptance verdict is expected to be resolved from a legacy standalone plain-text marker on stdout" and lists only the four legacy markers, dropping the JSON-primary contract entirely. This violates the same "repo-local acceptance skills follow the same contract" scenario and task 7 (archive 関連 skill / guidance で acceptance contract 前提が残っている箇所を追随更新する). Restore the JSON-primary + text-fallback description from commit bc4e722b.
- [x] Restore JSON-primary output guidance in `skills/cflx-workflow/SKILL.md` "Accept (Acceptance Review) > Output Format" section (lines 164–178). Current content declares "Output exactly ONE legacy standalone verdict marker on its own line with NO markdown formatting" and tells the agent "For current compatibility, do NOT switch this workflow to JSON-only verdict output yet." This is a sibling repo-local acceptance-related skill that also redefines a text-only canonical rule, violating the same scenario. Re-apply the "Primary (preferred) JSON verdict / Fallback (backward-compatible)" structure from commit bc4e722b.
- [x] Restore JSON-primary output guidance in `skills/cflx-workflow/references/cflx-accept.md` (lines 104–126). Current content mirrors the reverted command template and states "Required for current compatibility — emit exactly ONE legacy standalone plain-text verdict marker as the LAST machine-readable payload" and "Do NOT rely on JSON-only verdict output from this worktree until the runtime rollout is complete." Re-apply the JSON-primary / text-fallback structure from commit bc4e722b so the router reference and the command template stay in sync.
- [x] Reconcile the operational bootstrap concern (current running orchestrator may only recognize legacy markers) WITHOUT relaxing the spec. Acceptable resolutions: (a) instruct the acceptance agent to emit BOTH the strict JSON verdict AND the legacy `ACCEPTANCE: <KIND>` marker during the transition so both old and new runtimes finalize correctly, or (b) amend the spec delta in this change to explicitly permit the current text-primary phrasing as a temporary compatibility measure. Do NOT leave the current canonical guidance contradicting the spec. **Resolution: option (a)** — added "Transition guidance — emit BOTH during rollout" sections to `.opencode/commands/cflx-accept.md`, `skills/cflx-accept/SKILL.md`, `skills/cflx-workflow/SKILL.md`, and `skills/cflx-workflow/references/cflx-accept.md` requiring the agent to emit JSON verdict line followed by the matching legacy marker line so both runtimes finalize. JSON remains the canonical primary contract.
