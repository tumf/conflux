## Implementation Tasks

- [x] Add closed execution-observability DTOs and authenticated `GET /api/v2/execution-status` routing in `src/web/remote_control_api/dto.rs`, `reads.rs`, `mod.rs`, and OpenAPI declarations. Completion requires process and change execution state, current and completed phase, iteration, absolute UTC RFC 3339 boundaries, `observed_at`, latest activity, and latest structured log fields with nullable unknown evidence; no elapsed/age fields or relative-time text may exist. (verification: integration - add focused route/schema tests named `agent_execution_observability_status_*` and run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Implement one projection-owner read that coherently joins snapshot and retained structured logs without changing revision semantics. Completion requires exact-`change_id` latest-log selection, a process latest log, fixed-time test injection, `state_revision` stability across log-only updates, `event_sequence` advancement where applicable, and no fallback to persistent files, display strings, or substring matching. (verification: unit - `Projection::execution_observation` covered by `agent_execution_observability_projection_*` and the read-route cases in `src/web/remote_control_api/tests/execution_status_tests.rs`, kept beside the resource they describe; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Add shared process-local lifecycle facts fed synchronously under the authoritative typed-event dispatch boundary and consumed by execution status plus stop settlement. Completion requires per-change phases projected from existing reducer `ActivityState` (`preparing`, `apply`, `acceptance`, `rejection_review`, `archive`, `resolve`, typed push, `none`, `unknown`), retained non-empty `ApplyCompleted.revision` OID, process-level dependency-analysis/base-merge/conflict-resolution/branch-merge/workspace-cleanup activity, scheduler liveness from shared `RunBoundaryLiveness`, restart-empty behavior, and no second workflow authority. (verification: integration - add event/projection tests covering all per-change and process-level start/terminal transitions, persistent idle, completion, error, unknown, and Apply OID retention under `agent_execution_observability_phase_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Extend `OperatorOutcome::Dequeued`, application settlement, `ExecutionSummary`, `CommandOutcome`, and `CommandRecord` with a closed typed stop result captured after confirmed termination: Git evidence outside the application gate, then phase facts under the reacquired gate before `DequeueChange`. Completion requires settlement-active `cancelled_phase`, `none` for already-terminated/no-active-phase success, `last_completed_phase`, nullable Apply commit evidence, `effects_rolled_back=false`, presentation detail that explicitly denies rollback, and unchanged failure/no-dequeue semantics. (verification: integration - extend shared coordinator, executor, registry, and command tests with `agent_execution_observability_stop_result_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Add a managed-worktree Git evidence port for final Apply commit presence and OID at stop settlement. Completion requires reading the retained non-empty typed Apply-completion OID, resolving the worktree through server-owned change mapping, and proving the OID equals or is an ancestor of quiescent HEAD outside the application gate; missing facts after restart, absent worktree, Git failure, or non-ancestor evidence must produce null without subject matching, path disclosure, or dequeue changes. (verification: unit - add fake and temporary-repository cases under `agent_execution_observability_apply_commit_*` for equal HEAD, ancestor, missing/empty fact, restart, absent worktree, unrelated same-subject, non-ancestor, and command failure; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Add deterministic stop race coverage using barriers or channels. Completion requires a test where the final Apply commit and Apply-completed transition become visible before termination confirmation, then phase facts are read before dequeue clears them, proving the stored result identifies acceptance as cancelled, Apply as completed, the exact retained commit OID, and no rollback; companion cases must cover pre-commit Apply cancellation, archive/resolve cancellation, already-terminated `none`, and unknown phase without short wall-clock correctness thresholds. (verification: integration - add `agent_execution_observability_apply_commit_stop_race` and table cases in coordinator/API tests; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Enforce the transport-independent privacy boundary and API log access contract. Completion requires UDS and TCP responses, capabilities, OpenAPI, execution status, command results, logs, and events to contain no persistent log path, repository root, workspace path used as a log locator, or file URL; the execution-status latest-log DTO must omit existing `LogEntry.timestamp` and `workspace_path`, `/api/v2/logs` must explicitly retain its existing schema without introducing a log-file locator, and the new route must follow existing authentication/origin rules. (verification: integration - add authenticated/unauthenticated router cases and serialized-response privacy assertions under `agent_execution_observability_privacy_*`; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

- [x] Update canonical OpenAPI contract assertions and operator documentation for the new route, absolute-time contract, log access boundary, typed phase values, and typed command result. Completion requires `cflx openapi` and the live OpenAPI endpoint to expose matching schemas, generated contract tests to fail if any route/result/privacy field is omitted, and docs to instruct agents not to infer Apply commit absence from `display_status=applying` or generic command success. (verification: integration - extend `tests/openapi_contract_tests.rs`, `tests/openapi_cli_tests.rs`, and relevant tracked API docs with `agent_execution_observability` assertions; run `cargo test agent_execution_observability`; verification-id: agent-execution-observability-tests)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate expose-agent-execution-observability --archive-gate`.

## Future Work

- Increasing retained API log history or adding structured pagination may be proposed separately if the existing 1000-entry ring is insufficient.
- A browser-specific relative-time presentation may format absolute API timestamps independently; it must not change the wire contract.
