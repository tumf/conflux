## Implementation Tasks

- [x] **Task 1: Add the invocation-scoped CLI contract** in `src/cli.rs` and `src/main.rs`: expose value-less aliases `-u`/`--integrate-upstream` for `origin`, accept a named remote only as `--integrate-upstream=<remote>` with `=` required, accept `--upstream-verify-command`, make enabled mode own final cumulative-base push, keep the feature disabled when absent, and reject non-parallel mode, `--push`, detached HEAD, missing remote/ref, unrelated first-parent local-only history after initial fetch while allowing commit-tree-verified cumulative change merges and validated upstream recovery commits, empty verification command, and non-Git resolution before mutation. (verification: unit - parser/startup tests plus `cargo test upstream_integration` prove alias equivalence, default-off behavior, `origin` defaulting, explicit remote, dry-run side-effect suppression, and every rejected combination; verification-id: upstream-integration-unit)
- [x] **Task 2: Propagate an optional upstream checkpoint configuration** through `Orchestrator`, `ParallelRunService`, and `ParallelExecutor` without adding persistent-config enablement or affecting constructors when absent. (verification: unit - constructor/config tests colocated with `ParallelRunService`/`ParallelExecutor` and `cargo test upstream_integration` prove disabled instances install no checkpoint and enabled instances retain selected remote/base/verification command across restart-loop reconstruction; verification-id: upstream-integration-unit)
- [x] **Task 3: Add Git/VCS observation and classification operations** for selected-remote fetch, same-name remote-base SHA resolution, ancestry classification, `MERGE_HEAD`/unmerged-index detection, and first-parent local-history validation; recognize cumulative change integration only from `Merge change:`/`Merge changes:` plus matching archive/active-change commit-tree evidence, recognize upstream integration only from validated remote/branch/SHA trailers and Git parents, and classify non-first-parent payload without localized output text. (verification: unit - Git command/classifier tests named `upstream_integration_*` with local fixtures and `cargo test upstream_integration` cover unchanged, local-ahead, remote-ahead, diverged, missing remote branch, fetch failure, localized output independence, and non-conflict command failure; verification-id: upstream-integration-unit)
- [x] **Task 4: Wire deterministic exclusive project base-lane checkpoints** before first dispatch, immediately before each completed-result base integration, after successful scheduler drain before finalization, and after pre-push/race remote advancement; start only on a clean lane, batch results queued behind one checkpoint into one fetch, permit independent worktree apply/acceptance, prohibit scheduler/time polling, and ignore stale completion events when releasing ownership. (verification: unit - reducer/scheduler tests in `src/parallel/tests/` named `upstream_integration_*` and `cargo test upstream_integration` prove trigger ordering, no overlap, single-fetch batching, dirty/unsafe suppression, completed-result waiting, and stale-event ownership protection; verification-id: upstream-integration-unit)
- [x] **Task 5: Implement Conflux-owned cumulative-base integration** with `git merge --no-ff <fetched-sha>` and mandatory `Cflx-Upstream-Remote`, `Cflx-Upstream-Branch`, and `Cflx-Upstream-SHA` trailers validated against merge parents and ancestry for every fetched revision not already contained in local base, including strictly remote-ahead history; use the trailer plus ancestry for restart identification/SHA recovery and prohibit fast-forward, rebase, reset, amend, and force operations on the new path. (verification: unit - Git integration helper tests named `upstream_integration_*` and `cargo test upstream_integration` prove command construction, trailer validation, ancestry predicates, no-op suppression, and ordinary conflict-free agent suppression; verification-id: upstream-integration-unit)
- [x] **Task 6: Add a dedicated upstream textual/semantic repair entrypoint while reusing the existing bounded `resolve_command` runner**: pass verification command/failure output when semantic, require repair-start HEAD ancestry, clean worktree/index, no unfinished merge/unmerged entries, fetched-SHA ancestry, unchanged reachable identity trailers, and full verification success; add an upstream-integration mode to `skills/cflx-resolve/SKILL.md` that forbids amend/rebase/reset/push without changing the existing sequential-merge mode. (verification: unit - repair-goal and prompt tests named `upstream_integration_*` with scripted runner results plus `cargo test upstream_integration` prove textual/semantic predicates, non-zero exit, incomplete merge retry, retry exhaustion, history-rewrite rejection, and no-op/conflict-free agent suppression; verification-id: upstream-integration-unit)
- [x] **Task 7: Execute the explicit full verification command after every changed upstream tree, after each completed change result merges into cumulative base, after each semantic repair, and against final cumulative HEAD immediately before push**; keep the base lane closed and block later base-dependent dispatch after base-result integration until success, and skip only disabled/dry-run paths and the post-initial-fetch no-work zero-change branch. (verification: unit - verification-routing tests named `upstream_integration_*` with scripted command results plus `cargo test upstream_integration` prove changed-upstream and completed-result verification, no-op/disabled/dry-run/no-work suppression, failure blocking, and mandatory post-repair rerun; verification-id: upstream-integration-unit)
- [x] **Task 8: Recover solely from repository evidence** so interruption during merge resumes repair, every restart with a trailer-identified unpushed upstream merge reruns the newly supplied verification command, and cumulative parallel startup without `-u` refuses to proceed while such recovery evidence exists. (verification: unit - repository-evidence routing tests named `upstream_integration_*` plus `cargo test upstream_integration` cover unfinished merge, unpushed identity trailers, option-less refusal, and deletion of runtime projections without changing the safe next action; verification-id: upstream-integration-unit)
- [x] **Task 9: Return explicit scheduler outcomes and own finalization ordering**: distinguish `DrainedSuccessfully`, `BlockedOrStalled`, and `Cancelled`; only successful drain may run final checkpoint, verification, native non-force `git push --porcelain`, and `git ls-remote` confirmation before `AllCompleted`; define zero-change no-work and recovery behavior, allow multiple failed race attempts but at most one successful push, and prohibit finalization for every other outcome. (verification: unit - scheduler/finalization tests named `upstream_integration_*` plus `cargo test upstream_integration` prove outcome routing, `AllCompleted` ordering, zero-change branches, push-count invariants, and native-operation dispatch without `AgentRunner`; verification-id: upstream-integration-unit)
- [x] **Task 10: Classify final push failures before bounded agent handoff**: route machine-readable per-ref non-fast-forward/fetch-first/stale-info status directly to fetch/integration; invoke `resolve_command` only when post-failure `git status --porcelain=v2` shows tracked mutation or unmerged entries; stall every other failure without agent invocation and never use stderr text for routing; after repair, rerun convergence, full verification, fresh fetch, and native non-force push. (verification: unit - porcelain-status and post-failure repository-state tests named `upstream_integration_*` plus `cargo test upstream_integration` prove each classification, agent suppression, prohibition on agent push, mandatory post-repair verification, bounded retry, and Conflux-owned retry routing; verification-id: upstream-integration-unit)
- [x] **Task 11: Emit non-authoritative lifecycle evidence and preserve out-of-scope surfaces** so enabled runs report remote/revisions and checkpoint outcomes, disabled runs emit no upstream events, and serial, TUI, server `git-sync`, per-change pre-sync, and `PushToRemote` retain existing routing. (verification: unit - scheduler/reducer tests named `upstream_integration_*` plus `cargo test upstream_integration` prove observability cannot change routing and every excluded surface remains unchanged; verification-id: upstream-integration-unit)
- [x] **Task 12: Add real-boundary heavy E2E coverage** in `tests/e2e_git_worktree_tests.rs` for first-parent history classification, trailer identity/restart, deterministic checkpoint boundaries and batching, stale-result verification, semantic repair, explicit scheduler outcomes, zero-change behavior, push races/failures, and remote confirmation; keep all cases behind `heavy-tests`. (verification: e2e - `cargo test --features heavy-tests --test e2e_git_worktree_tests upstream_integration` executes the real Git/worktree/bare-remote/process fixtures and fails for stubbed/no-op wiring; verification-id: upstream-integration-heavy-e2e)

## Implementation Evidence Notes

Per-task implementation locations and the verification evidence observed for each completed task above.

- Task 1 implementation: `src/cli.rs` (`RunArgs::integrate_upstream`, `upstream_verify_command`, `RunArgs::upstream_integration`), `src/upstream/options.rs`, `src/upstream/startup.rs`, `src/main.rs` (startup validation before `LifecycleIntegration::start` and before any workspace mutation).
- Task 1 evidence: `cargo test --lib upstream_integration` — `cli::tests::upstream_integration_*` (7 cases), `upstream::options::tests::upstream_integration_*` (10 cases), `upstream::startup::tests::upstream_integration_*` (3 cases).
- Task 2 implementation: `Orchestrator::set_upstream_integration`, `ParallelRunService::set_upstream_integration`/`install_upstream_integration`, `ParallelExecutor::set_upstream_integration` (`src/parallel/builder.rs`). Constructors are unchanged; `None` installs nothing.
- Task 2 evidence: `parallel_run_service::tests::upstream_integration_is_absent_by_default`, `..._propagates_to_service_and_executor`, `parallel::tests::upstream_integration::upstream_integration_disabled_executor_installs_no_checkpoint`, `..._enabled_executor_installs_checkpoint`, `..._survives_restart_loop_reconstruction`.
- Task 3 implementation: `src/upstream/ports.rs` (`UpstreamGit`), `src/upstream/git_ops.rs` (real native Git), `src/upstream/classify.rs`, `src/upstream/spine.rs`.
- Task 3 evidence: `upstream::classify::tests::upstream_integration_*` (12 cases), `upstream::spine::tests::upstream_integration_*` (7 cases), `upstream::git_ops::tests::upstream_integration_strips_dated_archive_prefix`, `upstream::coordinator::tests::upstream_integration_initial_fetch_rejects_missing_remote_branch`, `..._fetch_failure_leaves_worktree_untouched`, `..._command_failure_does_not_start_an_agent`, `..._already_integrated_revision_is_a_noop`, `..._diverged_revision_is_integrated`.
- Task 4 implementation: `src/upstream/checkpoint.rs` (reducer), `src/parallel/upstream_lane.rs` (lane observation), call sites in `src/parallel/orchestration.rs` (before first dispatch, after drain) and `src/parallel/merge.rs` (`attempt_merge`, before base integration).
- Task 4 evidence: `parallel::tests::upstream_integration::upstream_integration_*` (7 cases: trigger ordering, single-fetch batching, queued-result waiting, unsafe-lane suppression, stale-event ownership, no-polling entry points), `upstream::checkpoint::tests::upstream_integration_*` (7 cases).
- Task 5 implementation: `src/upstream/trailers.rs`, `UpstreamCoordinator::integrate_revision`/`validate_identity` (`src/upstream/coordinator.rs`), `GitUpstreamOps::merge_no_ff` (only `merge --no-ff --no-edit`; no rebase/reset/amend/force operation exists on this path).
- Task 5 evidence: `upstream::trailers::tests::upstream_integration_*` (4 cases), `upstream::coordinator::tests::upstream_integration_remote_ahead_uses_no_ff_merge_with_trailers` (asserts exact command target and trailer-bearing message), `..._diverged_revision_is_integrated`, `..._already_integrated_revision_is_a_noop`.
- Task 6 implementation: `src/upstream/repair.rs` (`ResolveCommandRepairAgent` reusing `AiCommandRunner` + `resolve_command` template + retry budget; dedicated prompt), `UpstreamCoordinator::run_textual_repair`/`textual_repair_converged`/`verify_with_semantic_repair`/`semantic_repair_state_converged`, `skills/cflx-resolve/SKILL.md` "Upstream Integration Repair Mode" (sequential-merge mode unchanged).
- Task 6 evidence: `upstream::repair::tests::upstream_integration_*` (5 cases), `upstream::coordinator::tests::upstream_integration_textual_conflict_converges_through_repair`, `..._textual_repair_exhaustion_stalls`, `..._semantic_repair_rejects_history_rewrite`, `..._semantic_repair_exhaustion_blocks_base_lane`, `..._conflict_free_merge_with_semantic_failure_repairs_then_reruns`, `..._command_failure_does_not_start_an_agent`.
- Task 7 implementation: `src/upstream/verify.rs` (native `CommandVerifier`), `UpstreamCoordinator::verify_with_semantic_repair`, `verify_base_result`, `finalize`; `ParallelExecutor::run_upstream_base_result_verification` called from `attempt_merge` while the base lane is held. Dry-run returns before any verifier is constructed (`prepare_upstream_integration`), and `Orchestrator::run_parallel_dry_run` never installs the runtime.
- Task 7 evidence: `upstream::verify::tests::upstream_integration_*` (2 cases), `upstream::coordinator::tests::upstream_integration_completed_result_runs_full_verification`, `..._completed_result_verification_failure_blocks_dispatch`, `..._noop_run_still_verifies_before_push`, `..._zero_change_fresh_run_manufactures_no_history` (0 verification calls), `..._already_integrated_revision_is_a_noop` (0 verification calls), `..._conflict_free_merge_with_semantic_failure_repairs_then_reruns` (2 calls: fail then mandatory rerun), `..._final_verification_failure_never_pushes`.
- Task 8 implementation: `scan_unpushed_upstream_merges` + `upstream_recovery_refusal` (`src/upstream/coordinator.rs`, offline, remote-agnostic via trailers and local remote-tracking refs), `ensure_no_unpushed_upstream_recovery` (`src/upstream/startup.rs`), default-off refusal branch in `src/main.rs`.
- Task 8 evidence: `upstream::coordinator::tests::upstream_integration_scan_finds_unpushed_trailer_identified_merge`, `..._scan_ignores_published_and_untrailered_merges`, `..._restart_reruns_verification_for_unpushed_merge`, `..._initial_fetch_accepts_upstream_recovery_commit`; heavy E2E `upstream_integration_e2e_restart_recovery_identifies_unpushed_merge` additionally proves deleting runtime projections does not change the safe next action.
- Task 9 implementation: `SchedulerOutcome` tracked explicitly in `execute_with_order_based_reanalysis` (`src/parallel/orchestration.rs`); `AllCompleted` is emitted only after `finalize_upstream` returns true. `UpstreamCoordinator::finalize` owns ordering and the `pushed_head` single-success invariant. Push/fetch/verify run through `GitUpstreamOps`/`CommandVerifier`, never `AgentRunner` or `AiCommandRunner`.
- Task 9 evidence: `upstream::coordinator::tests::upstream_integration_blocked_and_cancelled_outcomes_never_push` (0 pushes, 0 fetches, 0 verifications), `..._successful_drain_pushes_once_and_confirms` (repeat finalization does not push again), `..._zero_change_fresh_run_manufactures_no_history`, `..._remote_only_advance_creates_no_synthetic_merge`, `..._restart_reruns_verification_for_unpushed_merge`, `..._remote_advance_before_push_returns_to_integration`.
- Task 10 implementation: `parse_push_porcelain`/`parse_porcelain_v2`/`classify_push_failure`/`classify_push_confirmation` (`src/upstream/classify.rs`), `UpstreamCoordinator::finalize` + `repair_push_repository`. `PushCommandResult` deliberately carries only porcelain stdout, so stderr text cannot reach routing.
- Task 10 evidence: `upstream::classify::tests::upstream_integration_parses_push_porcelain_race_rejection`, `..._non_race_rejection_without_mutation_stalls`, `..._push_failure_with_tracked_mutation_is_repairable`, `..._push_failure_with_unmerged_entries_is_repairable`, `..._untracked_files_are_not_repairable_mutation`, `..._stderr_text_cannot_change_push_routing`, `..._confirms_push_from_remote_observation`; `upstream::coordinator::tests::upstream_integration_race_rejection_returns_to_checkpoint_then_succeeds`, `..._non_repairable_push_failure_stalls_without_agent`, `..._repairable_push_failure_repairs_then_conflux_pushes`.
- Task 11 implementation: `src/upstream/ports.rs` (`UpstreamEvent`/`UpstreamObserver`), `src/parallel/upstream_bridge.rs` (projects onto the existing `ExecutionEvent::Log` channel; no new reducer variant, so no excluded surface changes routing). Disabled runs never construct the observer.
- Task 11 evidence: `parallel::upstream_bridge::tests::upstream_integration_*` (3 cases), `parallel::tests::upstream_integration::upstream_integration_events_are_reported_without_changing_routing`, `..._leaves_post_archive_action_untouched`, `upstream::coordinator::tests::upstream_integration_reports_lifecycle_without_becoming_routing_authority` (a repeated checkpoint is still a repository-derived no-op after events were emitted).
- Task 12 implementation: 14 `upstream_integration_e2e_*` cases in `tests/e2e_git_worktree_tests.rs` using real repositories, a real local bare remote, a second clone as a concurrent actor, a real `pre-receive` hook rejection, real verification-command processes, and a real repair script.
- Task 12 evidence: `cargo test --features heavy-tests --test e2e_git_worktree_tests upstream_integration` — 14 passed, 0 failed (3.14s). The whole file is `#![cfg(feature = "heavy-tests")]`, so none of it enters the default suite.

## Future Work

- Persistent-config or TUI enablement after CLI-only behavior has operational evidence.
- Conflux-server endpoint/event wiring that requests the checkpoint without writing the workspace.
- Webhook-driven synchronization, debounce, and batching based on measured update frequency.
- Distributed project/base-tree leases across multiple Conflux instances.
- Tiered verification that preserves a mandatory complete gate while reducing intermediate latency.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate integrate-upstream-during-run --archive-gate`

Repository quality gates expected before acceptance: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test upstream_integration`, and `cargo test --features heavy-tests --test e2e_git_worktree_tests upstream_integration`. Tests exceeding one second remain behind `heavy-tests` and do not enter the default suite.
