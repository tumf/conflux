# タスク一覧

## 実装タスク

### 1. エディタ起動ロジックの変更
- [x] `src/tui/utils.rs`の`launch_editor_for_change()`関数を修正
  - `proposal.md`パスの構築
  - ファイル存在確認ロジックの追加
  - proposal.mdが存在する場合: ファイルを直接開く
  - proposal.mdが存在しない場合: ディレクトリにフォールバック
  - ログメッセージの更新（開くターゲットを明示）

### 2. エラーハンドリングの確認
- [x] changeディレクトリが存在しない場合のエラーハンドリングが正常に動作することを確認
- [x] エディタコマンド実行失敗時のエラーハンドリングが正常に動作することを確認

### 3. コードレビューと品質チェック
- [x] `cargo fmt`でコードフォーマットを実行
- [x] `cargo clippy -- -D warnings`でリントチェックを実行
- [x] 既存のユニットテストが正常にパスすることを確認

### 4. 仕様の更新
- [x] `openspec/specs/tui-editor/spec.md`のRequirement「エディタ起動キーバインド」を更新
  - proposal.mdを優先的に開く動作を追加
  - フォールバック動作（proposal.mdがない場合）を記載
  - 新しいシナリオを追加

### 5. 検証とクリーンアップ
- [x] 全てのテストが成功することを確認（`cargo test`）
- [x] 変更内容のレビュー
- [x] `npx @fission-ai/openspec@latest validate open-proposal-file-directly --strict`で仕様検証

## Future work

手動テスト（人間による実際の操作確認）:
- proposal.mdが存在するchangeで`e`キーを押してファイルが直接開かれることを確認
- proposal.mdが存在しないchangeで`e`キーを押してディレクトリが開かれることを確認
- changeディレクトリが存在しない場合にエラーが適切に表示されることを確認
- 複数のエディタ（vi, nvim, VS Code等）で動作確認

## 依存関係

- タスク1は独立して実行可能
- タスク2-4はタスク1の完了後に実行
- タスク5はタスク1-4の完了後に実行
- タスク6は全タスク完了後に実行

## 並列実行可能なタスク

- タスク2, 3は並列実行可能（タスク1完了後）
- タスク4はタスク2, 3完了後に実行

## 検証方法

各タスクの完了後、以下を確認：
- コンパイルエラーがないこと
- 既存の機能が破壊されていないこと
- 新しい動作が期待通りに機能すること
