## Implementation Tasks

- [x] 1. apply outcome を `BLOCKED` と `REJECT_PROPOSAL` に分離する canonical contract を spec と prompt に追加する (verification: integration - `openspec/specs/agent-prompts/spec.md` と `openspec/specs/parallel-execution/spec.md` に apply-generated blocker / rejection proposal の別経路が定義され、`cflx openspec validate separate-apply-block-from-reject --strict --evidence warn` が成功する)
- [x] 2. runtime state / event / workspace status に resume-capable `Blocked` activity を追加し、worktree / WIP / task progress / blocker metadata を保持する遷移を定義する (verification: unit - reducer/state scenarios prove `Applying -> Blocked`, `Rejecting -> Blocked`, `Blocked -> Applying` and distinguish `Blocked` from terminal `Rejected`)
- [x] 3. apply runtime が recoverable blocker では `REJECTED.md` を生成せず blocked handoff を発火し、terminal rejection proposal でのみ dedicated rejecting flow に入るよう実装する (verification: integration - add/update orchestration tests covering `src/execution/apply.rs`, `src/execution/state.rs`, and `src/parallel/dispatch.rs` so apply-generated `BLOCKED` handoff and apply-generated `REJECTED.md` handoff are asserted separately)
- [x] 4. rejecting review verdict を `CONFIRM` / `RESUME` / `BLOCK` に拡張し、reject proposal 不採用時に即 apply 再開すべきケースと blocked 保留すべきケースを分離する (verification: integration - add/update rejecting review routing tests under `src/parallel/dispatch.rs` / `src/orchestration/rejection.rs` proving `REJECTION_REVIEW: BLOCK` returns the change to `Blocked` with worktree preserved)
- [x] 5. TUI / Web / server state surfaces が `Blocked` を read-write resumable stop state として表示し、`Rejected` terminal row や `Applying` active rowと混同しないことを確認する (verification: unit/integration - add/update state mapping tests covering `src/server/api/ws.rs`, `src/web/state.rs`, and TUI state snapshot assertions for blocked/rejected/applying rows)
- [x] 6. proposal delta と関連実装変更を strict validation、Rust tests、lint で確認する (verification: integration - run `cflx openspec validate separate-apply-block-from-reject --strict --evidence warn`, `cargo test`, and `cargo clippy --all-targets --all-features -- -D warnings`, including tests covering `src/execution/apply.rs`, `src/execution/state.rs`, and `src/parallel/dispatch.rs`)

## Future Work

- blocked reason の自動 dedupe / repeated blocker loop suppression policy を追加する
- blocked changes 向けの operator UX（resume with note / unblock reason editing / human handoff dashboard）を強化する

## Acceptance #1 Failure Follow-up
- [x] archive commit path blocker: `cargo clippy --all-targets --all-features -- -D warnings` fails on the current workspace, so the real final archive commit is not ready. Evidence: agent-exec job `8890c7544f434207762b1413c29d56de` exited 101; stderr shows `src/serial_run_service.rs:25` has unused imports (`RejectionReviewVerdict`, `handle_resume_apply_from_rejecting`, `run_rejection_review`) and `src/orchestration/state.rs:1016-1042` trips `clippy::match_ref_pats` because `match outcome` patterns unnecessarily use `&...`. Follow-up fix: removed unused imports in `src/serial_run_service.rs`, updated match pattern in `src/orchestration/state.rs`, and confirmed success with agent-exec job `74e28cd15e45067001c0f0331c22f1ca` (exit 0).
- [x] spec/runtime mismatch remains in serial fallback handling: `src/serial_run_service.rs:647-655` still maps `AcceptanceResult::Blocked` to `ChangeProcessResult::ApplyFailed { error: "Blocked acceptance reached unexpected fallback path" }` instead of a blocked/resumable state, so blocked changes are still not modeled per the new spec contract. Follow-up fix: `AcceptanceResult::Blocked` now returns `ChangeProcessResult::Stalled` in both acceptance branches (`src/serial_run_service.rs:517-525`, `636-644`) and serial stalled handling now emits reducer `WorkspaceStatus::Blocked` (`src/orchestrator.rs:1054-1066`) so UI/runtime status is blocked-resumable. Verification: `cargo test test_process_acceptance_result_blocked_returns_stalled_result` (agent-exec job `0ab758d68f6c2088cd8a200b1de4997a`, exit 0) and `cargo test test_stalled_result_marks_change_blocked_state` (agent-exec job `4f17476eb2a73e65eb77748c65b373ec`, exit 0).
- [x] spec/runtime mismatch remains in serial flow: `src/serial_run_service.rs:518-535` still treats `AcceptanceResult::Blocked` by immediately calling `execute_rejection_flow(...)` and returning `ChangeProcessResult::Rejected`, which violates `openspec/changes/separate-apply-block-from-reject/specs/parallel-execution/spec.md:7` and `openspec/changes/separate-apply-block-from-reject/specs/orchestration-state/spec.md:19-41` requiring recoverable blockers to remain blocked/resumable rather than enter rejection flow. Follow-up fix: blocked acceptance path now short-circuits to `ChangeProcessResult::Stalled` without entering rejection flow (`src/serial_run_service.rs:517-525`), and `process_acceptance_result` keeps blocked outcomes resumable (`src/serial_run_service.rs:636-644`). Verification: `cargo test test_process_acceptance_result_blocked_returns_stalled_result` (agent-exec job `0ab758d68f6c2088cd8a200b1de4997a`, exit 0).
- [x] verification gap in reducer tests: `src/orchestration/state.rs:1756-1803` covers `RejectionReviewCompleted` for `Resume` and `Confirm`, but there is no corresponding assertion for the required `Rejecting -> Blocked` transition or preservation of blocked metadata/worktree context from `RejectionOutcome::Block` as required by `openspec/changes/separate-apply-block-from-reject/specs/orchestration-state/spec.md:27-34` and tasks.md item 2/4. Follow-up fix: added `test_rejection_review_block_transitions_to_blocked_with_metadata` in `src/orchestration/state.rs:1805-1842` asserting blocked display status, `WaitState::RejectionBlocked`, and blocker/unblock/worktree metadata preservation. Verification: `cargo test test_rejection_review_block_transitions_to_blocked_with_metadata` (agent-exec job `7b56bb7d0e1b6d1e4d9c38ca02ff60ad`, exit 0).
## Acceptance #2 Failure Follow-up
- [x] blocked metadata preservation was placeholder-only and now carries blocker/unblock/worktree guidance aligned with blocked hold semantics. Follow-up fix: replaced generic `RejectionOutcome::Block` metadata in `src/orchestration/state.rs:1032-1040` and generic `WorkspaceStatus::Blocked` metadata in `src/orchestration/state.rs:1069-1076` with explicit unresolved-blocker / resume guidance and preserved worktree context text, then updated reducer assertion expectations in `test_rejection_review_block_transitions_to_blocked_with_metadata` (`src/orchestration/state.rs:1806-1842`). Verification: `cargo test test_rejection_review_block_transitions_to_blocked_with_metadata` (exit 0).
- [x] rejecting BLOCK cleanup no longer appends recovery tasks that reference deleted worktree-local `REJECTED.md`. Follow-up fix: changed recovery task template in `src/orchestration/rejection.rs:70-85` to require capturing unresolved blocker details directly in `tasks.md` without recreating the marker file, and added regression test `test_append_recovery_task_section_avoids_deleted_rejected_marker_reference` (`src/orchestration/rejection.rs:476-495`). Verification: `cargo test test_append_recovery_task_section_avoids_deleted_rejected_marker_reference` (exit 0).
