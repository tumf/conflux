## Implementation Tasks

- [x] Capture Clap parse errors before process exit and route only `cflx client` invocations with exact `--json` intent through the existing versioned `usage_error` envelope; preserve ordinary human/non-client Clap behavior and pre-startup side-effect suppression. (verification-id: client-cli-contract-regressions) (verification: integration - `cargo test --features web-monitoring --test client_cli_tests json_usage_errors` runs the compiled CLI across invalid change ID, invalid timeout, missing argument, unknown flag, human mode, and unrelated top-level errors, asserting one JSON stdout object only where promised)
- [x] Enforce one monotonic `wait` deadline across initial and repeated owner observations plus all repository/Git verification, and kill/reap Git subprocesses on expiry before returning typed `timeout`. (verification-id: client-cli-contract-regressions) (verification: integration - `cargo test --features web-monitoring --test client_cli_tests wait_deadline` uses deterministic stalled UDS and Git/remote fixtures to prove deadline cancellation, timeout outcome, process cleanup, and zero POST commands)
- [x] Validate environment bearer tokens as HTTP header values before connection/request construction, rejecting CR, LF, other C0 controls, and DEL with a typed redacted error while preserving valid tokens. (verification-id: client-cli-contract-regressions) (verification: integration - `cargo test --features web-monitoring --test client_cli_tests auth_header_validation` proves malformed values send zero request bytes, valid authentication still works, and neither stdout nor stderr reveals the token)
- [x] Make `partial_intent.detail.commands_submitted` derive from the actual per-invocation submission list so a pre-existing mark is not reported as newly submitted. (verification-id: client-cli-contract-regressions) (verification: integration - `cargo test --features web-monitoring --test client_cli_tests partial_intent_command_audit` covers both newly marked and pre-marked Start failures and asserts exact command records and envelope detail)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate fix-client-cli-contract --archive-gate`.
