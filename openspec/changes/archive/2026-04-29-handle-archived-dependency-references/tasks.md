## Implementation Tasks

- [x] 1. `src/execution/state.rs` と `src/analyzer.rs` が扱う dependency category（active / in-flight / archived / missing）に合わせて、Canonical spec へ archived dependency reference contract を追加する。 (verification: integration - `openspec/specs/parallel-execution/spec.md` と関連 spec delta に requirement/scenario が追加され、`cflx openspec validate handle-archived-dependency-references --strict --evidence warn` が成功する)
- [x] 2. `cflx openspec validate` 経路で active proposal frontmatter の dependency targets を分類し、archived dependency references を generic missing dependency と区別して報告する。 (verification: integration - validation coverage が `src/openspec_cmd.rs` または関連 validator 経路で archived dependency case を検出し、missing case と別の outcome/message を返すことを確認する)
- [x] 3. `src/analyzer.rs` の dependency validation と outer error shaping を更新し、archive 済み dependency 由来の failure を generic `Analysis returned invalid JSON` に潰さず dedicated dependency-contract diagnostics として surfacing する。 (verification: unit - analyzer tests が `src/analyzer.rs:214-238` 相当の error shaping を通しても archived dependency root cause が保持されることを確認する)
- [x] 4. queued / in-flight / archived / missing dependency の4分類 regression tests を追加し、`separate-apply-block-from-reject` のような archived prerequisite を参照する active proposal 相当ケースで current misleading failure が再発しないことを固定する。 (verification: unit/integration - analyzer/validation tests が queued と in-flight は pass、archived は specで定義した expected outcome、missing は dedicated invalid dependency failure になることを確認する)
- [x] 5. `skills/cflx-analyze/SKILL.md` と `openspec/changes/classify-acceptance-followup-routing/proposal.md` の authoring guidance を更新し、archive 後に dependency metadata をどう保守するかを repository evidence 付きで明示する。 (verification: integration - 参照ファイルの guidance diff と、`cflx openspec validate handle-archived-dependency-references --strict --evidence warn` 成功で確認する)
- [x] 6. proposal と関連実装変更をまとめて検証する。 (verification: integration - `cflx openspec validate handle-archived-dependency-references --strict --evidence warn` と、touchした analyzer/validation tests、および必要な lint/type checks が成功する)

## Acceptance #1 Failure Follow-up
- [x] Archive commit-path blocker: `src/openspec_cmd.rs:714-745` runs strict archive-time validation via `validate_change(Some(change_id), true, "error")`, and `cargo run -- openspec validate handle-archived-dependency-references --strict --evidence error` fails with `handle-archived-dependency-references: tasks.md: Runtime behavior is claimed without implementation-facing tasks`. Update `openspec/changes/handle-archived-dependency-references/tasks.md` wording to satisfy the behavior-task classifier in `src/openspec_cmd.rs:1285-1482`, or adjust that classifier so archived dependency handling work counts as implementation-facing behavior, then re-run the strict error-mode validation successfully. (verification: integration - `src/openspec_cmd.rs:714-745` の archive commit-path が `validate_change(Some(change_id), true, "error")` を呼ぶことを確認し、`cargo run -- openspec validate handle-archived-dependency-references --strict --evidence error` が成功する)
