## Implementation Tasks

- [x] Trace every current post-archive status path and document why the reported vibration can occur. (verification: manual - implementation summary cites exact source paths and event sequences, expected candidates include `src/tui/orchestrator.rs:829-862`, `src/parallel/merge.rs:141-268`, `src/parallel/merge.rs:299-362`, `src/orchestration/state.rs:1184-1268`, and `src/tui/state/event_handlers/completion.rs:46-60`; completion condition: the summary maps current events to visible `archived`, `resolve pending`, `merge wait`, `resolving`, and `merged` states)

- [x] Implement a single post-archive decision table for parallel mode: lane occupied → `resolve pending`, manual merge blocker → `merge wait`, otherwise → `resolving` then `merged`. (verification: unit - add/update reducer/orchestrator tests in `src/orchestration/state.rs`, `src/tui/orchestrator.rs`, or `src/parallel/tests/executor.rs`; completion condition: tests fail if no-blocker archive completion settles in `archived` or `merge wait` instead of entering merge handling)

- [x] Emit/apply a truthful active merge state for the no-blocker path. (verification: unit - add/update a Rust test in `src/orchestration/state.rs`, `src/tui/state.rs`, or `src/tui/orchestrator.rs` showing no-blocker archive completion produces visible `resolving` while merge handling is active and `merged` after `MergeCompleted`/`ResolveCompleted`; completion condition: the test fails if the UI/reducer jumps directly through unstable `archived`/`merge wait` states without an active merge state)

- [x] Restrict `merge wait` after archive completion to concrete manual deferral evidence. (verification: unit - add/update tests in `src/orchestration/state.rs` and/or `src/parallel/merge.rs` proving `ExecutionEvent::MergeDeferred { auto_resumable: false, .. }` sets `merge wait` with normal queue intent cleared, while no-blocker `ChangeArchived` alone does not; completion condition: `merge wait` is traceable to manual deferral or explicit retry state)

- [x] Preserve auto-resumable blocked behavior. (verification: unit - keep/update `src/tui/orchestrator.rs` tests such as `test_tui_archived_during_resolve`, `test_tui_archived_during_rejecting_emits_auto_resumable_deferred`, `test_tui_archived_during_applying_does_not_emit_auto_resumable_deferred`, and `test_tui_archived_with_terminal_rejected_change_does_not_emit_auto_resumable_deferred`; completion condition: targeted `cargo test test_tui_archived_during --lib` exits 0 and only active merge/resolving/rejecting lane blockers create auto-resumable `resolve pending`)

- [x] Preserve manual dirty-base deferral behavior and scheduler queue protection. (verification: unit - run/update `src/parallel/tests/executor.rs` coverage including dirty-base/manual deferral tests and `test_scheduler_reconciliation_does_not_requeue_manual_merge_deferred_change`; completion condition: targeted `cargo test test_scheduler_reconciliation_does_not_requeue_manual_merge_deferred_change --lib` exits 0 and manual merge-wait changes are not reintroduced as ordinary queued work)

- [x] Prevent final `merged` from regressing to `archived`, `merge wait`, or `resolve pending`. (verification: unit - add/update sequence tests in `src/orchestration/state.rs` or `src/tui/state.rs` for `MergeCompleted`/`ResolveCompleted` followed by later `ChangeArchived`, `ChangesRefreshed`, and archived workspace observations; completion condition: a later archive observation cannot resurrect any pre-merge status after `merged`)

- [x] Add regression coverage for both observed vibration classes. (verification: unit - add tests named or documented for `merged <> archived` vibration and no-blocker `merge wait -> merged` vibration in `src/tui/state.rs`, `src/tui/orchestrator.rs`, or `src/parallel/tests/executor.rs`; completion condition: tests would fail if repeated refresh/events can alternate visible state between `merged` and `archived`, or put a no-blocker archived change in `merge wait` before eventually merging)

- [x] Run targeted Rust verification for post-archive dispatch, merge deferral, reducer state, and TUI display behavior. (verification: integration - run targeted commands covering the changed tests, for example `cargo test test_tui_archived --lib`, `cargo test test_scheduler_reconciliation_does_not_requeue_manual_merge_deferred_change --lib`, and any new vibration/immediate-merge test filters; completion condition: commands exit 0 and any test taking over 1 second is optimized or marked heavy per AGENTS.md)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate for this change. Do not keep this as an implementation checkbox, because evidence validation evaluates checkbox tasks and would create a self-referential archive blocker.

Expected archive gate: `cflx openspec validate stabilize-parallel-archive-display --strict --evidence warn` exits 0 before archive promotion.

## Future Work

- If WebUI/server-mode monitoring separately shows stale post-archive display after the reducer/orchestrator fix, create a focused follow-up proposal for WebState display synchronization.

## Acceptance #1 Failure Follow-up
- [x] src/orchestration/state.rs:2821-2849 の test_parallel_mode_change_archived_transitions_to_merge_wait が stale な期待値 "merge wait" のまま残っており、openspec/changes/stabilize-parallel-archive-display/specs/orchestration-state/spec.md:45-53 の no-blocker path は resolving -> merged 要件と矛盾しています。`cargo test test_parallel_mode_change_archived_transitions_to_merge_wait --lib`（agent-exec job 02554a0dedb9441389e6e5e18a2093c5）は exit_code 101 で、src/orchestration/state.rs:2845 の assertion が left "resolving" right "merge wait" で失敗しました。テストを no-blocker archive completion が resolving に入る期待へ更新/リネームし、メッセージも spec と整合させてください。

## Acceptance #2 Failure Follow-up
- [x] `src/orchestration/state.rs:3082-3108` の `test_parallel_mode_merge_deferred_then_completed` も、手動 `MergeDeferred(auto_resumable=false)` の前段階で `ChangeArchived("c")` 直後に `merge wait` を期待しています。`cargo test test_parallel_mode_merge_deferred_then_completed --lib`（agent-exec job `cc2f4dcc8686b02bad8c1832960581b1`）は exit_code 101、`src/orchestration/state.rs:3091` で left `"resolving"` right `"merge wait"` です。spec の `openspec/changes/stabilize-parallel-archive-display/specs/orchestration-state/spec.md:35-43` および `:91-98` は manual blocker が `MergeDeferred(auto_resumable=false)` 等で検出されるまでは `merge wait` を表示しないことを求めています。`ChangeArchived` 直後は `resolving`、その後の concrete manual deferral event で `merge wait` になる期待へ更新してください。
- [x] 前回指摘の対象テスト自体は `src/orchestration/state.rs:2821-2868` で `test_parallel_mode_change_archived_transitions_to_resolving_without_blocker` に更新され、`cargo test test_parallel_mode_change_archived_transitions_to_resolving_without_blocker --lib`（agent-exec job `b554285905655809b2ca371677143b32`）は exit_code 0 でした。しかし同じファイル内に stale な no-blocker `ChangeArchived` 期待がまだ残っています。`src/orchestration/state.rs:3048-3074` の `test_parallel_mode_full_lifecycle` は `ChangeArchived("a")` 後に `merge wait` を期待しており、`cargo test test_parallel_mode_full_lifecycle --lib`（agent-exec job `7bbd98ff307598876b1c7ff3ab59a4ed`）は exit_code 101、`src/orchestration/state.rs:3066` で left `"resolving"` right `"merge wait"` です。これは `openspec/changes/stabilize-parallel-archive-display/specs/orchestration-state/spec.md:45-53` の no-blocker path は `resolving -> merged` という要件と矛盾します。テスト期待値とメッセージを no-blocker archive completion が `resolving` に入る前提へ更新してください。

## Acceptance #3 Failure Follow-up
- [x] `src/orchestration/state.rs:2870-2890` の `test_parallel_mode_archive_success_clears_prior_acceptance_error` が stale な期待値 `merge wait` のまま残っています。`cargo test test_parallel_mode_archive_success_clears_prior_acceptance_error --lib`（agent-exec job `06bd98205ac4971d8f49eea3fcd3f6a0`）は exit_code 101 で、`src/orchestration/state.rs:2885` の assertion が left `"resolving"` right `"merge wait"` で失敗しました。`openspec/changes/stabilize-parallel-archive-display/specs/orchestration-state/spec.md:45-53` は no-blocker archive completion が `resolving -> merged` に進むことを要求しているため、このテスト期待値とメッセージを `resolving` 前提へ更新し、必要なら `MergeCompleted` まで確認してください。修正後、`agent-exec run -- cargo test test_parallel_mode_archive_success_clears_prior_acceptance_error --lib`（job `5f7ad42c6ec12deeb41674b8326d9f31`）は exit_code 0。
- [x] `src/orchestration/state.rs:3087-3108` の `test_parallel_mode_merge_deferred_then_completed` は `ChangeArchived("c")` 直後の期待は `resolving` に更新されていますが、その後の concrete manual deferral event `ExecutionEvent::MergeDeferred { auto_resumable: false, reason: "base dirty" }` 後も reducer 表示が `merge wait` になっていません。`cargo test test_parallel_mode_merge_deferred_then_completed --lib`（agent-exec job `4327a8387169379d92d942e986eaec76`）は exit_code 101 で、`src/orchestration/state.rs:3108` の assertion が left `"resolving"` right `"merge wait"` で失敗しました。これは `openspec/changes/stabilize-parallel-archive-display/specs/orchestration-state/spec.md:35-43` の manual blocker enters merge wait 要件に反します。`ExecutionEvent::MergeDeferred(auto_resumable=false)` が active `Resolving` 中の archived change にも concrete manual deferral として適用され、`ActivityState` を解除して `WaitState::MergeWait` / display `merge wait` に遷移するよう reducer 実装とテストを整合させてください。修正後、`agent-exec run -- cargo test test_parallel_mode_merge_deferred_then_completed --lib`（job `1512c7561fc42519e5c510fd0da8f992`）は exit_code 0。 (verification: integration - `cargo test test_parallel_mode_merge_deferred_then_completed --lib`; evidence: `src/orchestration/state.rs` and agent-exec job `1512c7561fc42519e5c510fd0da8f992`; completion condition: command exits 0 and assertion enforces `MergeDeferred(auto_resumable=false)` => display `merge wait`)
