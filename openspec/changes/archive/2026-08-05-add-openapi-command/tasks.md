## Implementation Tasks

- [x] Add the `cflx openapi` parser and dispatch path so it bypasses orchestration startup and writes only generated YAML to stdout, with clear non-zero failure behavior when unavailable. Add focused tests for both feature-enabled output outside a Git repository and feature-disabled rejection. (verification: integration - `tests/openapi_cli_tests.rs` parses stdout as OpenAPI YAML outside a repository, asserts stderr stays empty on success, and asserts no repository lock or API socket is created when run inside one; `cargo test --no-default-features --test openapi_cli_tests` proves the unavailable path exits non-zero without schema output; verification-id: openapi-local-tests)
- [x] Reuse one serialization path for `cflx openapi` and `GET /api/v2/openapi.yaml`, preserving deterministic output and current authentication behavior. Rewrite generated-document banners and active source documentation so emitted YAML never names the removed artifact or Make targets. (verification: integration - `tests/openapi_contract_tests.rs` compares CLI output with the live router response byte-for-byte and asserts current export instructions; verification-id: openapi-local-tests)
- [x] Remove `docs/openapi.yaml`, the `openapi-gen` binary target/source, and static generation/check Make targets. Update `.github/workflows/ci.yml` comments and path-filter rationale that currently depend on the tracked schema, without adding a broad `docs/**` ignore that would hide executable documentation checks. (verification: integration - `rg 'docs/openapi\.yaml|check-openapi|openapi-gen' Cargo.toml Makefile .pre-commit-config.yaml scripts release.toml .github src docs README* CONTRIBUTING.md` proves no active build, workflow, source banner, or documentation invokes the removed artifact workflow; verification-id: openapi-local-tests)
- [x] Remove static OpenAPI ownership and version rewriting from release configuration, release scripts, and release scope tests while preserving manifest/lockfile release isolation; land these release updates atomically with deletion of the tracked artifact. (verification: integration - `cargo test --test release_bump_scope_tests` and `./scripts/release.sh --dry-run patch` prove releases remain scoped and no longer expect `docs/openapi.yaml`; verification-id: openapi-local-tests)
- [x] Convert static-artifact contract assertions into generated-document and executable-surface assertions covering routes, schemas, commands, errors, events, security, removed paths, and deterministic serialization. Replace the artifact drift hook with pull-request CI execution of `cargo test --features web-monitoring --test openapi_contract_tests`. (verification: integration - `tests/openapi_contract_tests.rs` fails on intentionally incomplete generated contracts and passes via the runnable `cargo test --features web-monitoring --test openapi_contract_tests` command without reading or regenerating a tracked schema; verification-id: openapi-local-tests)
- [x] Update all README variants, `CONTRIBUTING.md`, and development/API/release guides to point to `cflx openapi` and `GET /api/v2/openapi.yaml`. (verification: integration - repository-wide documentation/reference checks find no active link or instruction for the deleted tracked artifact or removed Make targets; verification-id: openapi-local-tests)

## Notes

- evidence: `cargo test --features web-monitoring --test openapi_contract_tests --test openapi_cli_tests` — 16 + 4 passed, 0 failed.
- evidence: `cargo test --no-default-features --test openapi_cli_tests` — 1 passed (`unavailable::refuses_clearly_and_writes_no_schema`), 0 failed.
- evidence: `cargo test --test release_bump_scope_tests` — 26 passed, 0 failed.
- evidence: `./scripts/release.sh --dry-run patch` on a clean scratch checkout of the release script and manifest exits 0 and reports only `Cargo.toml` version work; the script contains no `openapi` reference.
- evidence: `rg 'docs/openapi\.yaml|check-openapi|openapi-gen|make openapi'` over `Cargo.toml`, `Makefile`, `.pre-commit-config.yaml`, `scripts/`, `release.toml`, `.github/`, `src/`, `docs/`, `README*`, `CONTRIBUTING.md` returns nothing; the only remaining matches repository-wide are the negative assertion in `tests/openapi_contract_tests.rs`, the scratch fixture in `tests/release_bump_scope_tests.rs`, and the canonical specs this change's deltas replace at archive time.
- The verification command planned in the proposal for the feature-disabled path was `cargo test --no-default-features --bin cflx openapi`. That command cannot prove anything here: `src/main.rs` opens with `#![cfg(not(test))]`, so the `cflx` bin target compiles to an empty test harness and a `--bin cflx` filter matches zero tests and passes vacuously. The proof was implemented as `tests/openapi_cli_tests.rs`, which spawns the actual feature-disabled binary, and the proposal's `rerun` and completion-condition text were updated to name `cargo test --no-default-features --test openapi_cli_tests` instead.
- The shared cargo target directory used by this repository can hand a test run a stale binary built from another worktree (a first run of the contract tests failed against a `cflx` that predated the new subcommand). Every verification result recorded above was produced with a private `CARGO_TARGET_DIR`.

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate add-openapi-command --archive-gate`.
