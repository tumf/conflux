## Implementation Tasks

- [x] Allow same-change archive success to clear prior recoverable error state. Completion condition: `src/orchestration/state.rs` `ChangeArchived` handling no longer leaves a change in `TerminalState::Error` when the same change later archives successfully, while preserving rejected terminal behavior. (verification: unit - added `test_parallel_mode_archive_success_clears_prior_acceptance_error`; final exact `cargo test` filters run in repository verification task)
- [x] Allow same-change merge/resolve success to clear prior recoverable error state. Completion condition: `MergeCompleted` and `ResolveCompleted` for the same change can transition from prior `TerminalState::Error` to `Merged`, without resurrecting already rejected changes. (verification: unit - added `test_parallel_mode_acceptance_error_archive_then_merge_finishes_merged` and `test_merge_and_resolve_success_clear_prior_processing_error_but_not_rejected`; final exact `cargo test` filters run in repository verification task)
- [x] Ensure TUI display sync can leave error state after reducer success. Completion condition: `src/tui/state.rs::apply_display_statuses_from_reducer` or adjacent tests prove a row with `display_status_cache == "error"` updates to `"merged"` when reducer display status becomes merged. (verification: unit - added `test_apply_display_statuses_from_reducer_updates_error_row_to_merged`; final exact `cargo test` filter run in repository verification task)
- [x] Verify the observed dbot sequence is represented as regression coverage. Completion condition: a test models `AcceptanceFailed` followed by successful archive and merge for `add-skill-secret-ingestion`-like sequence and asserts final current status is not `error`. (verification: integration - added reducer regression `test_parallel_mode_acceptance_error_archive_then_merge_finishes_merged`; final exact `cargo test` filter run in repository verification task)
- [x] Run proposal and repository verification. Completion condition: OpenSpec validation passes and targeted Rust tests covering terminal error clearing pass. (verification: manual - passed `cflx openspec validate fix-terminal-error-cleared-by-success --strict --evidence warn`, `cargo fmt --all -- --check`, `cargo test test_parallel_mode_archive_success_clears_prior_acceptance_error`, `cargo test test_parallel_mode_acceptance_error_archive_then_merge_finishes_merged`, `cargo test test_merge_and_resolve_success_clear_prior_processing_error_but_not_rejected`, and `cargo test test_apply_display_statuses_from_reducer_updates_error_row_to_merged`)

## Future Work

- Consider retaining last recoverable error in a history panel while keeping current row status truthful after success.
