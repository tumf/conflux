## Implementation Tasks

- [x] 1. Add shared teardown execution primitives for managed Git worktree deletion. (verification: unit - tests in `src/vcs/git/commands/worktree.rs` or an equivalent module assert executable `.wt/teardown` is detected in the target worktree, runs with cwd set to the worktree root, receives `ROOT_WORKTREE_PATH`, uses null stdin, and captures exit status/stdout/stderr)
- [x] 2. Add teardown-aware worktree removal options with default abort and explicit skip behavior. (verification: unit - tests in `src/vcs/git/commands/worktree.rs` prove teardown success proceeds to removal, teardown failure returns an error before removal, `skip_teardown` proceeds while logging/skipping teardown, missing teardown proceeds, and non-executable teardown is not run)
- [x] 3. Wire parallel execution cleanup paths through teardown-aware removal. (verification: integration - tests in `src/vcs/git/mod.rs` or `src/parallel/tests/*` covering `GitWorkspaceManager::cleanup_worktree`, dependency-resolved stale cleanup, and inconsistent worktree cleanup observe teardown execution before worktree removal and worktree preservation on teardown failure)
- [x] 4. Wire rejection-flow cleanup through teardown-aware removal without hiding teardown failures. (verification: integration - tests in or near `src/orchestration/rejection.rs` assert rejected worktree teardown runs before deletion and failure leaves the worktree available for operator recovery with a warning/error event)
- [x] 5. Wire TUI/manual worktree deletion through teardown-aware removal and expose a skip-teardown recovery action. (verification: integration/manual - tests in `src/tui/command_handlers.rs` or a documented manual TUI check prove normal deletion aborts on teardown failure and the explicit skip path deletes after warning)
- [x] 6. Wire server/WebUI worktree deletion APIs through teardown-aware removal and expose `skip_teardown` as an explicit request option. (verification: integration - HTTP tests in `src/server/api/worktrees.rs` for `DELETE /api/v1/projects/:id/worktrees/:branch` prove default failure returns a non-success response without removing the worktree and `skip_teardown=true` proceeds with warning semantics)
- [x] 7. Wire legacy web API and proposal-session close/merge cleanup through teardown-aware removal or document a narrowly-scoped exclusion. (verification: integration/unit - tests or code review evidence in `src/web/api.rs` and `src/server/proposal_session.rs` cover their deletion paths)
- [x] 8. Add documentation for `.wt/teardown` lifecycle behavior. (verification: manual - review `.wt/AGENTS.md`, `openspec/specs/vcs-worktree-operations/spec.md`, and related `src/` deletion docs/comments, then run `rg "\.wt/teardown|ROOT_WORKTREE_PATH|state\.env|skip_teardown" .wt openspec/specs docs src` to confirm cwd, env, executable requirement, `.wt/state.env` convention, default failure abort, and skip-teardown recovery are documented)
- [x] 9. Run repository verification for the completed implementation. (verification: manual - `cargo test vcs::git::commands::worktree`, `cargo test`, dashboard test/lint commands from `dashboard/package.json` if dashboard files changed, and `cflx openspec validate add-worktree-teardown-hook --strict --evidence warn`; record any intentionally skipped heavy tests)

## Future Work

- Add optional end-to-end examples for Docker Compose cleanup only if a future repository fixture can run them quickly and without external credentials.
