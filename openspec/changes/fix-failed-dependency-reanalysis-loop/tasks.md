## Implementation Tasks

- [ ] Preserve reducer-admitted failed-dependent candidates in the scheduler-local queue, classify them as dependency-blocked without apply dispatch, and continue dispatching independent candidates. (verification: integration - `cargo test -p cflx failed_dependency` covers B membership and C dispatch in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Make reconciliation of an already represented blocked candidate report `queued_added == 0` without refreshing debounce or arming `QueueNotification`, while preserving genuine dynamic additions and relevant signature-triggered analysis. (verification: integration - `cargo test -p cflx failed_dependency` asserts exact reconciliation and analyzer counts in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Define failed-blocker epochs and emit exactly one compatibility `ChangeSkipped(B,A)`, one authoritative `DependencyBlocked(B)`, and one operator diagnostic per epoch; allow one new set after blocker change, retry/refailure, or dequeue/re-add. (verification: integration - `cargo test -p cflx failed_dependency_events` asserts exact event counts and fingerprints in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Update TUI and other `ChangeSkipped` consumers so failed-dependency compatibility events do not deselect or revoke still-accepted queue intent; use `DependencyBlocked` for blocked presentation. (verification: unit - `cargo test -p cflx failed_dependency_event_consumer` covers `src/tui/state/event_handlers/`; verification-id: failed-dependency-loop-tests)
- [ ] Carry only accepted state-changing `RetryError(change_id)` admission into the live scheduler as a target-ID-bearing one-shot explicit-retry edge; exclude refused/no-op retry, ordinary `AddToQueue`, and generic queue notification. (verification: integration - `cargo test -p cflx failed_dependency_retry_edge` covers `src/orchestration/run_control.rs` and scheduler notification plumbing; verification-id: failed-dependency-loop-tests)
- [ ] Add narrowly scoped `FailedChangeTracker` clear behavior and consume the explicit-retry edge before reconciliation/classification, clearing only the target failure and related notification epoch while leaving dependency resolution to repository evidence. (verification: unit - `cargo test -p cflx failed_change_tracker`; verification-id: failed-dependency-loop-tests)
- [ ] Preserve blocked-candidate dequeue/re-add semantics: revocation removes B locally and clears its epoch; later explicit re-add is a genuine queue addition and may emit a new bounded blocker transition. (verification: integration - `cargo test -p cflx failed_dependency_dequeue_readd`; verification-id: failed-dependency-loop-tests)
- [ ] Add paused-time real-loop regressions asserting initial analyzer invocation exactly once where required, exact event counts, stable local B membership, `queued_added == 0`, and actual C dispatch across repeated wakes. (verification: integration - `cargo test -p cflx failed_dependency_loop`; verification-id: failed-dependency-loop-tests)
- [ ] Using the same persistent executor and production `RetryError` path, test accepted retry, no-op/refused retry, unresolved A, authoritative A resolution, A refailure, and process-restart recomputation. (verification: integration - `cargo test -p cflx failed_dependency_retry`; verification-id: failed-dependency-loop-tests)
- [ ] Verify finite blocked-only scheduling returns blocked/stalled rather than all-completed, persistent blocked-only scheduling waits without polling, and genuine dynamic C addition wakes and dispatches normally. (verification: integration - `cargo test -p cflx failed_dependency_lifetime`; verification-id: failed-dependency-loop-tests)
- [ ] Run formatting, targeted/default tests, and lint, keeping default-path tests under one second or marking unavoidable slow coverage with the repository heavy-test feature. (verification: integration - `cargo fmt --check && cargo test -p cflx failed_dependency && cargo test -p cflx && cargo clippy -p cflx --all-targets -- -D warnings`; verification-id: failed-dependency-loop-tests)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-failed-dependency-reanalysis-loop --archive-gate`
