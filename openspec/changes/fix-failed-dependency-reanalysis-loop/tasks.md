## Implementation Tasks

- [ ] Preserve reducer-admitted failed-dependent candidates in the scheduler-local queue and classify them as dependency-blocked without allowing apply dispatch; retain normal dispatch for independent candidates. (verification: integration - `cargo test -p cflx failed_dependency` covers queue classification and dispatch selection in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Make queue reconciliation treat an already represented dependency-blocked candidate as unchanged, so it does not increment `queued_added`, refresh queue debounce state, or arm a synthetic `QueueNotification`. (verification: unit - `cargo test -p cflx failed_dependency` asserts reconciliation accounting in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Bound `ChangeSkipped` and operator-visible failed-dependency diagnostics by the existing blocker identity/fingerprint while preserving a new notification when the blocker changes or is re-established after retry. (verification: integration - `cargo test -p cflx failed_dependency` asserts event and diagnostic counts in `src/parallel/tests/`; verification-id: failed-dependency-loop-tests)
- [ ] Add a narrowly scoped `FailedChangeTracker` failure-clear operation and wire accepted explicit retry and authoritative success transitions to remove stale in-process failure classification without treating retry intent as success. (verification: unit - `cargo test -p cflx failed_change_tracker` covers mark, clear, retry, success, and refailure transitions in `src/parallel/types.rs`; verification-id: failed-dependency-loop-tests)
- [ ] Add a paused-time real scheduler-loop regression reproducing the failed-dependency queue cycle and proving analyzer invocation, reconciliation additions, and skip diagnostics remain bounded while blocked work stays represented. (verification: integration - `cargo test -p cflx failed_dependency`; verification-id: failed-dependency-loop-tests)
- [ ] Add scheduler-loop coverage proving explicit retry causes one fresh evaluation, unresolved dependency evidence keeps B blocked, successful dependency resolution permits B, refailure blocks B again, and unrelated C remains dispatchable throughout. (verification: integration - `cargo test -p cflx failed_dependency`; verification-id: failed-dependency-loop-tests)
- [ ] Run Rust formatting, targeted tests, default test suite, and lint checks, keeping every default-path test under one second or marking unavoidable slow coverage with the repository heavy-test feature. (verification: integration - `cargo fmt --check && cargo test -p cflx failed_dependency && cargo test -p cflx && cargo clippy -p cflx --all-targets -- -D warnings`; verification-id: failed-dependency-loop-tests)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-failed-dependency-reanalysis-loop --archive-gate`
