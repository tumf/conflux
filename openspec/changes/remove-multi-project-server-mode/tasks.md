## Implementation Tasks

- [ ] Remove `server`, `service`, and `project` command definitions, argument types, dispatch branches, repository-lock classifications, completion/help entries, and server URL/config helpers; add CLI integration coverage proving every removed command and `--server*` option is rejected before observable side effects while retained local commands still parse. (verification: integration - focused CLI tests in `tests/run_exit_tests.rs` and CLI parser tests; verification-id: repository-checks)
- [ ] Make TUI local-only by removing remote-client startup, polling/WebSocket mapping, remote command handlers, remote ID rendering assumptions, and server-token resolution while preserving local TUI state/event behavior. (verification: unit - `cargo test tui::` plus `rg 'RemoteClient|remote_client' src/tui src/main.rs`; verification-id: repository-checks)
- [ ] Delete `src/server/**` and `src/service/**`, then remove the server-only project registry, persistence DB, runner, proposal-session, terminal, worktree/git-sync, service-manager, and API wiring from module declarations and shared types without deleting core/local worktree services. (verification: integration - `cargo test` and `test ! -e src/server && test ! -e src/service`; verification-id: repository-checks)
- [ ] Remove server-client-only code from `src/remote/**` and relocate or retain only helpers demonstrably consumed by non-server features; prune reqwest/WebSocket and related dependencies only when no retained `src/web/**`, OpenAPI, or core caller needs them. (verification: integration - run `cargo check`, `cargo tree`, and source audit in `src/remote/`, `src/tui/`, and `src/main.rs`; verification-id: repository-checks)
- [ ] Delete `dashboard/**` and remove dashboard embedding from `build.rs`, Cargo package contents, Makefile build/install/cross-build targets, CI, release workflows, and OpenAPI generation inputs while keeping Rust-only builds and `openapi-gen` for API v2. (verification: integration - `cargo check && cargo run --bin openapi-gen --features web-monitoring` plus `test ! -e dashboard` and `rg 'dashboard|npm ci' build.rs Cargo.toml Makefile .github`; verification-id: repository-checks)
- [ ] Remove `ServerConfig`, server auth/default values, global server config loaders, templates, and server-only config validation/tests without changing unrelated configuration parsing. (verification: unit - `cargo test config::` plus `rg 'ServerConfig|ServerAuth|DEFAULT_SERVER' src`; verification-id: repository-checks)
- [ ] Update canonical specs and current user/developer documentation to remove claims for the obsolete commands, remote TUI, multi-project APIs, dashboard, proposal sessions, terminals, and service management; add manual cleanup guidance for previously installed services while preserving archived history. (verification: integration - `cargo test --test run_exit_tests` verifies help/removed surfaces; `tests/run_exit_tests.rs` and `openspec/specs/cli/spec.md` provide repository evidence; verification-id: repository-checks)
- [ ] Add retained-surface regressions that exercise local `cflx run --web` and `cflx tui --web` option availability plus `/api/v2` routing, authentication/bind validation, observation stream, and command behavior so a stubbed or accidentally removed web path fails. (verification: integration - existing and focused tests under `src/web/remote_control_api/tests` and CLI integration tests; verification-id: repository-checks)
- [ ] Run formatting, clippy, default tests, default-feature compile, and `cargo check --no-default-features`; resolve all dead imports, feature-gate errors, stale fixtures, and server-only dependencies revealed by the removal. (verification: integration - `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo check`, and `cargo check --no-default-features`; verification-id: repository-checks)

## Future Work

- Operators may manually stop and uninstall a previously installed `cflx server` OS service before or after upgrading, using the documented platform-specific cleanup command.
- Operators may manually archive or delete old server data after confirming it is no longer needed.

## Final Validation

Archive validation is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate remove-multi-project-server-mode --archive-gate`
