## Implementation Tasks

- [x] Remove `server`, `service`, and `project` command definitions, argument types, dispatch branches, repository-lock classifications, completion/help entries, and server URL/config helpers; add CLI integration coverage proving every removed command and `--server*` option is rejected before observable side effects while retained local commands still parse. (verification: integration - focused CLI tests in `tests/run_exit_tests.rs` and CLI parser tests; verification-id: repository-checks)
- [x] Make TUI local-only by removing remote-client startup, polling/WebSocket mapping, remote command handlers, remote ID rendering assumptions, and server-token resolution while preserving local TUI state/event behavior. (verification: unit - `cargo test tui::` plus `rg 'RemoteClient|remote_client' src/tui src/main.rs`; verification-id: repository-checks)
- [x] Delete `src/server/**` and `src/service/**`, then remove the server-only project registry, persistence DB, runner, proposal-session, terminal, worktree/git-sync, service-manager, and API wiring from module declarations and shared types; remove obsolete `tests/e2e_proposal_session.rs`, `tests/fixtures/mock_opencode_server.py`, and other server-only fixtures without deleting core/local worktree services. (verification: integration - `cargo test` and `test ! -e src/server && test ! -e src/service && test ! -e tests/e2e_proposal_session.rs`; verification-id: repository-checks)
- [x] Remove server-client-only code from `src/remote/**`; remove `reqwest` only after its remote-client ownership is confirmed, retain `tokio-tungstenite` while `/api/v2` stream tests use it, and preserve any helper with a retained caller. (verification: integration - run `cargo check`, `cargo tree`, and source audit in `src/remote/`, `src/tui/`, `src/web/remote_control_api/tests/`, and `src/main.rs`; verification-id: repository-checks)
- [x] Delete `dashboard/**` and remove dashboard embedding from `build.rs`, Cargo package contents, Makefile build/install/cross-build targets, Makefile `server-install/start/stop/restart/logs/status` targets, CI, and release workflows while keeping Rust-only builds and `openapi-gen` for API v2. (verification: integration - `cargo check && cargo run --bin openapi-gen --features web-monitoring` plus `test ! -e dashboard` and `rg 'dashboard|npm ci|server-install|server-start|server-stop|server-restart|server-logs|server-status' build.rs Cargo.toml Makefile .github`; verification-id: repository-checks)
- [x] Remove `ServerConfig`, server auth/default values, global server config loaders, templates, and server-only config validation/tests without changing unrelated configuration parsing; retain the `web-monitoring` feature but remove `rusqlite`, `portable-pty`, and server module entries from that feature while preserving `qrcode`, `local-ip-address`, and retained API dependencies. (verification: unit - `cargo test config::`, `cargo check --features web-monitoring`, and source audit of `Cargo.toml` plus `src/lib.rs`; verification-id: repository-checks)
- [x] Author every removal delta so archive promotion applies it, and make archive promotion delete a canonical capability directory once all of its requirement blocks are removed while preserving partially modified capabilities. Canonical `openspec/specs/**` promotion is performed by the archive step, not hand-edited during apply, because a pre-applied delta would make archive a rejected no-op. (verification: unit - `cargo test openspec_cmd::` covers the empty-capability cleanup and the partially-modified retention; verification-id: repository-checks)
- [x] Update current user/developer documentation to drop claims for obsolete commands, remote TUI, multi-project APIs, the React dashboard, proposal sessions, terminals, Git sync, persistence, and service management; add manual operator cleanup guidance while preserving archived history and retained local-web documentation. (verification: manual - documentation review of `README*.md`, `AGENTS.md`, and `docs/guides/**`; no automated assertion owns prose accuracy; verification-id: repository-checks)
- [x] Add retained-surface regressions that exercise local `cflx run --web` and `cflx tui --web` option availability plus `/api/v2` routing, authentication/bind validation, observation stream, and command behavior so a stubbed or accidentally removed web path fails. (verification: integration - existing and focused tests under `src/web/remote_control_api/tests` and CLI integration tests; verification-id: repository-checks)
- [x] Run formatting, clippy, default tests, default-feature compile, and `cargo check --no-default-features`; resolve all dead imports, feature-gate errors, stale fixtures, and server-only dependencies revealed by the removal. (verification: integration - `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo check`, and `cargo check --no-default-features`; verification-id: repository-checks)

## Future Work

- Operators may manually stop and uninstall a previously installed `cflx server` OS service before or after upgrading, using the documented platform-specific cleanup command.
- Operators may manually archive or delete old server data after confirming it is no longer needed.

## Notes

- evidence: `cargo fmt --all`, `cargo clippy --all-targets` (no warnings), `cargo check --all-targets`, and `cargo check --no-default-features --all-targets` all succeed.
- evidence: `cargo test` passes; new coverage is `removed_server_surfaces_are_rejected_without_side_effects`, `retained_local_web_options_still_parse`, `retained_web_bind_validation_still_refuses_an_unauthenticated_routable_listener`, `local_run_web_binds_a_real_port_and_publishes_its_url`, `killing_the_lock_owner_releases_the_repository_lock` (all in `tests/run_exit_tests.rs`), the `removed_*`/`help_no_longer_advertises_*` parser tests in `src/cli.rs`, and `test_archive_removes_canonical_capability_left_without_requirements` / `test_archive_keeps_canonical_capability_with_remaining_requirements` in `src/openspec_cmd.rs`.
- evidence: absence checks pass — `src/server`, `src/service`, `src/remote`, `dashboard/`, `tests/e2e_proposal_session.rs`, and the server-only fixtures are gone, and `rg 'dashboard|npm ci|server-install|…' build.rs Cargo.toml Makefile .github` returns nothing.
- evidence: `reqwest`, `rusqlite`, and `portable-pty` are absent from `Cargo.toml` and `Cargo.lock`; `qrcode`, `local-ip-address`, `tokio-tungstenite`, `axum`, `tower*`, and `utoipa*` are retained.
- The delta covers every canonical requirement of `dashboard-api`, `dashboard-overview`, `git-sync`, `git-sync-api-errors`, `proposal-session`, `proposal-session-backend`, `proposal-session-integration`, `proposal-session-ui`, `proposal-ws-streaming`, `server-api`, `server-mode`, `server-mode-dashboard`, `server-persistence`, `web-terminal`, and `webui-terminal`, so archive promotion empties and then deletes those capability directories.
- A `remote-control-api` MODIFIED delta was added beyond the original plan: two retained requirements still asserted that the removed multi-project `/api/v1` namespace must be preserved, which contradicted the change's own acceptance criterion that canonical specs describe only retained behavior.
- Pre-existing unrelated flake: `serial_run_service::tests::serial_restart_reruns_acceptance_after_missing_verdict_exhaustion` can fail with `Unable to read current working directory` when another test removes the process CWD concurrently. It passes in isolation and on repeat full runs; `src/serial_run_service.rs` is untouched by this change.

## Final Validation

Archive validation is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate remove-multi-project-server-mode --archive-gate`
