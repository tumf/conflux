## Implementation Tasks

- [ ] Add the `control` Clap namespace with `status`, `enqueue`, and `wait`, shared Unix-socket/auth-environment/output options, strict duration/change-ID validation, help examples, and feature-disabled rejection before startup side effects. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests cli_surface` and `cargo test --no-default-features --test control_cli_tests feature_disabled` assert parsing, help, exit codes, and absence of lock/socket/workspace mutation)
- [ ] Implement a bounded HTTP-over-Unix-socket v2 client that derives the canonical repository socket or accepts an override, reuses source DTOs, applies environment-only bearer authentication, checks health/capabilities/instance compatibility, bounds response sizes, and redacts secrets. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests transport` runs the compiled CLI against a real temporary Unix-socket router and covers default/override discovery, auth, oversized/invalid responses, and unavailable owner)
- [ ] Define and serialize the versioned stable result envelope and exit-code/outcome mapping for human and JSON modes, keeping JSON stdout to one object and diagnostics/secrets out of stdout. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests output_contract` parses every success/failure stdout object and asserts stderr separation, stable codes, and token redaction)
- [ ] Wire read-only `status` to capabilities, instance, authoritative state, and execution-status reads, producing a coherent owner/change summary without command submission or repository mutation. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests status` uses a real router and command spy to assert the summary fields and zero POST/lock/Git effects)
- [ ] Implement intent-based `enqueue` using authoritative action/lifecycle fields and the existing typed command service, including already-admitted no-op success, isolated mark-plus-start for idle owners, refusal rather than consumption of unrelated marks, queue admission for live owners, retry routing, command-record settlement, fresh idempotency identities, bounded stale-revision recomputation, and instance-change refusal. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests enqueue` proves real idle/live/retry admission plus unknown, terminal, blocked, worktree-ineligible, active-limit, unrelated-mark conflict, mark/start race, unbound-executor, stale-revision, partial-mark, and owner-restart outcomes with exact command-spy counts)
- [ ] Implement observation-only `wait` with event/poll recovery, instance continuity, typed terminal failures, configurable timeout, and repository-verifiable archive/base-integration success; refuse unsupported publication modes instead of treating disappearance or API narrative as completion. (verification-id: control-cli-tests) (verification: e2e - `cargo test --features web-monitoring --test control_cli_tests wait` deterministically drives real router and temporary Git fixtures through success, rejection, process failure, event gap, owner restart, push-mode refusal, and timeout while asserting zero submitted commands)
- [ ] Update active user and agent documentation to recommend `cflx control` for existing-owner delegation, explain the distinction from `cflx run`, document JSON outcomes and owner prerequisites, and retain `/api/v2` as the low-level generated contract. (verification-id: control-cli-tests) (verification: integration - `cargo test --features web-monitoring --test control_cli_tests documentation` checks active docs for command examples, run/control ownership distinction, and no recommendation to construct raw revision/idempotency envelopes)

## Final Validation

Archive validation is the authoritative final OpenSpec gate. Expected archive gate: `cflx openspec validate add-control-cli --archive-gate`.
