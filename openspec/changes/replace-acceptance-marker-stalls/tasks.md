## Implementation Tasks

- [ ] Amend `openspec/CONSTITUTION.md` so revision-bound, out-of-worktree runtime state may authoritatively control only temporary pause/resume routing, while repository/Git evidence remains authoritative for implementation, Acceptance PASS, archive, and merge outcomes; require fail-safe Acceptance rerun when the runtime record is absent or invalid. (verification: manual - run the runnable command `git diff -- openspec/CONSTITUTION.md openspec/changes/replace-acceptance-marker-stalls/specs` and review every delta scenario to confirm the amendment neither permits hidden completion evidence nor leaves the runtime store unconstitutional; completion condition: the same change contains the explicit constitutional amendment before dependent runtime tasks are considered complete)
- [ ] Extend the Acceptance verdict/result contract to retain a structured stalled-blocker payload and distinguish it from bare JSON/text `gated` and legacy `blocked` compatibility input. (verification: unit - table-driven tests in `src/acceptance.rs` cover bare and structured JSON, plain-text fallbacks, missing fields, empty evidence, unsupported category, and legacy aliases via `cargo test acceptance`; completion condition: callers can determine validated-stall versus bare protocol error without parsing narrative text)
- [ ] Add shared blocker validation and bare-GATED protocol retry policy, reusing the fixed two-retry missing-verdict bound while keeping counters/reset semantics explicit and independent from Apply and canonical CONTINUE budgets. (verification: unit - `src/orchestration/acceptance.rs` tests cover two retries, third-result exhaustion, canonical reset, invalid blocker fallback, explicit category preservation, no inferred category, and serial/parallel-equivalent decisions; completion condition: one shared decision API represents retry, validated stall, and terminal protocol error)
- [ ] Implement a versioned, atomic Acceptance stall repository under Conflux's XDG state area, keyed by repository identity plus change ID and bound to managed worktree identity/path, branch when available, Apply revision, phase, retry count, structured blocker evidence, resumability, next action, and timestamps. (verification: unit - isolated temporary-state tests in the Acceptance stall-state module cover atomic round-trip, schema rejection, corrupt record quarantine, repository/path/revision mismatch, path reuse, idempotent consume, and write failure via `cargo test acceptance_state`; completion condition: `rg 'write_acceptance_blocked_marker' src` shows no active Acceptance call site and no production write targets `openspec/changes/**` or another worktree path)
- [ ] Implement restart reconciliation that validates runtime records against repository identity, worktree ownership, current Git history, Apply revision ancestry, and active/unarchived change state, then restores `stalled` only for a valid record and otherwise derives the safe route from repository evidence. (verification: integration - fixture repositories in `src/execution/state.rs` and `src/parallel/tests/executor.rs` cover matching state, missing state, corrupt state, stale revision, deleted/recreated worktree, path reuse, archived/merged change, and complete unarchived Apply rerunning Acceptance; completion condition: runtime state alone never routes to archive or records PASS)
- [ ] Wire serial and parallel Acceptance handling so bare GATED stays in Acceptance for bounded retry, validated blockers persist runtime stall state and emit stalled lifecycle metadata, and neither path writes an Acceptance-origin `APPLY_BLOCKED` marker or reruns Apply. (verification: integration - fake acceptance command sequences in `src/serial_run_service.rs` and `src/parallel/tests/executor.rs` assert gated/gated/pass, gated exhaustion, structured stall, exact invocation counts, event/status outcomes, preserved Apply revision, and `git status --porcelain` remaining empty; completion condition: both modes use the shared decision/state APIs and have no active Acceptance marker writer)
- [ ] Replace explicit-retry and queue reconciliation behavior for Acceptance stalls with validated runtime-state consumption: prepare against current revision, resume at Acceptance, retain the record when dispatch preparation fails, and suppress ordinary stalled requeue without `Blocked -> Blocked` loops. (verification: integration - `src/parallel/queue_state.rs`, `src/parallel/dispatch.rs`, TUI command-handler, and serial retry tests cover ordinary queue suppression, successful Acceptance-only retry, non-resumable refusal, preparation failure retention, exhaustion requiring explicit retry, and absence of repeated Blocked events; completion condition: Acceptance retry no longer calls `consume_resumable_acceptance_marker`)
- [ ] Add a conservative one-time migration for legacy structured Acceptance-origin markers, binding them to verified repository/worktree/Apply revision before creating runtime state and removing generated marker residue without leaving a dirty worktree; preserve Apply-origin, unknown-origin, non-resumable, and malformed markers unchanged. (verification: integration - temporary Git worktree fixtures in `src/execution/state.rs` and `src/parallel/tests/executor.rs` cover migration success, idempotent restart, tracked/untracked generated marker cleanup, mismatched evidence refusal, Apply/unknown/non-resumable preservation, malformed input, and clean status via `cargo test acceptance_marker_migration`; completion condition: migration never silently deletes evidence it cannot prove is Acceptance-owned)
- [ ] Update workspace detection, lifecycle events, reducer state, operator status, and cleanup/archive/merge boundaries so runtime stalls affect only dispatch/status/retry presentation and never worktree cleanliness, Apply completion, PASS, archive readiness, or merge eligibility. (verification: integration - tests in `src/orchestration/state.rs`, `src/execution/state.rs`, `src/parallel/queue_state.rs`, and `src/parallel/tests/executor.rs` prove validated stalls display `stalled`, bare GATED emits no stalled event, Apply commit detection is not shadowed, and cleanup/archive/merge decisions are unchanged via `cargo test acceptance_stall`; completion condition: lifecycle consumers no longer require an Acceptance marker to reconstruct status)
- [ ] Update acceptance prompts, embedded `skills/cflx-accept/SKILL.md`, and mirrored guidance to require explicit category, concrete evidence, next action, and resumability for a stalled handoff; state that bare GATED is a protocol error and forbid prose-based credential/infrastructure inference. (verification: unit - contract tests in `src/agent/prompt.rs` and `src/embedded_skills.rs` assert required structured fields, mock-first FAIL guidance, compatibility wording, and absence of Acceptance marker instructions via `cargo test acceptance_prompt`; completion condition: `skills/cflx-accept/SKILL.md` and all embedded mirrors match the parser/runtime contract)
- [ ] Run focused and repository-wide quality gates, keeping default unit tests under one second or marking unavoidable heavy integration fixtures with the project `heavy` convention. (verification: integration - `cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test`; completion condition: all commands pass and focused acceptance/runtime-state tests exercise real decision and wiring paths rather than placeholder returns)

## Future Work

- Move Apply-origin `APPLY_BLOCKED` state outside the worktree in a separate proposal only after its independent apply/rejection handoff semantics are redesigned.
- Add configurable protocol retry limits only if operator evidence shows the fixed bound is insufficient.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate replace-acceptance-marker-stalls --archive-gate`
