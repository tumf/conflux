## Implementation Tasks

- [ ] Define and enforce a single retry-state ownership boundary. (verification: unit - add/update `src/orchestration/state.rs` and `src/parallel_run_service.rs` tests proving `ResolveMerge` acceptance, scheduler observation, retry clearing, and TUI display derive from the same shared reducer state; completion condition: there is no path where one reducer accepts `ResolveWait` while another empty reducer is used for retry dispatch)
- [ ] Preserve caller-owned reducer state through empty manual resolve startup. (verification: unit - add/update `src/parallel_run_service.rs` tests to construct a caller-provided `OrchestratorState` with `ResolveWait`, run the order-based empty startup path, and assert the executor observes that same membership; completion condition: the test fails if `ParallelRunService` replaces the caller state with its internal empty state)
- [ ] Make zero-change manual resolve scheduler startup consume reducer-owned lane-wait work before completion. (verification: integration - add/update `src/parallel/tests/executor.rs` or `src/parallel_run_service.rs` tests so an empty `changes` vector with shared `ResolveWait` emits retry-dispatch evidence such as `ResolveWait retry dispatch started` or a visible `MergeDeferred`; completion condition: no `AllCompleted`-equivalent success path occurs before retry evaluation)
- [ ] Prevent truthful-completion regressions while retry work remains pending or active. (verification: unit - add/update `src/tui/orchestrator.rs` tests for completion/log emission with shared `ResolveWait` / `RejectWait`; completion condition: zero-change success logs and `AllCompleted` are not emitted while reducer-owned lane-wait membership or active resolving/rejecting state remains)
- [ ] Surface stale or missing retry prerequisites as visible state rather than silent pending. (verification: integration - add/update retry tests in `src/parallel/queue_state.rs` / `src/parallel/tests/executor.rs` for missing archived workspace, stale workspace path, dirty base, and clean valid workspace; completion condition: each case transitions to `merge wait`, `resolving`, `merged`, or visible error/stalled state, never indefinite `resolve pending`)
- [ ] Preserve existing non-empty and active-scheduler manual retry behavior. (verification: integration - run or extend `src/parallel/tests/executor.rs` tests around `test_manual_resolve_wait_retries_after_in_flight_apply_completes`, `test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work`, queued-change dispatch while a retry exists, and active resolving/rejecting deferral; completion condition: existing queued work dispatch, active scheduler notification, auto-resumable deferral, dirty-base demotion, and merged-state non-regression semantics remain unchanged)
- [ ] Keep TUI display synchronized with reducer-owned retry membership. (verification: unit - add/update `src/tui/state.rs` or event-handler tests to assert a row cannot remain `resolve pending` when reducer-owned `ResolveWait` membership has been cleared or rejected; completion condition: display status follows reducer snapshots after retry completion, demotion, or stale-prerequisite handling)
- [ ] Run focused and full verification. (verification: manual - run focused `cargo test` targets for `parallel_run_service`, `parallel::tests::executor`, `tui::command_handlers`, `tui::orchestrator`, and `orchestration::state`, then run the repository lint/typecheck commands documented for the project if present; completion condition: commands pass or failures are documented with unrelated/flaky evidence)

## Future Work

- Manual operator verification in a real TUI session may be useful after implementation: reproduce `merge wait` after restart, press `M`, and confirm the row transitions away from indefinite `resolve pending` without zero-change success logs.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-manual-merge-retry-zero-change-startup --archive-gate`
