## Implementation Tasks

- [x] Define and enforce a single retry-state ownership boundary. (verification: unit - `ParallelRunService::run_parallel_order_based_with_executor` now preserves caller-provided reducer state via `ensure_shared_orchestrator_state`, `run_orchestrator_parallel` binds the service to caller-owned state, and focused tests cover `ResolveMerge` acceptance, scheduler observation, retry clearing, and TUI display deriving from the same shared reducer state; completion condition: there is no path where one reducer accepts `ResolveWait` while another empty reducer is used for retry dispatch)
- [x] Preserve caller-owned reducer state through empty manual resolve startup. (verification: unit - `src/parallel_run_service.rs::test_order_based_empty_resolve_wait_shared_state_enters_scheduler_path` constructs caller-owned `OrchestratorState` with `ResolveWait`, runs the order-based empty startup path, and now relies on `ensure_shared_orchestrator_state` so the executor observes that same membership; completion condition: the test fails if `ParallelRunService` replaces the caller state with its internal empty state)
- [x] Make zero-change manual resolve scheduler startup consume reducer-owned lane-wait work before completion. (verification: integration - `src/parallel/tests/executor.rs::test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work` and `src/parallel_run_service.rs::test_order_based_empty_resolve_wait_shared_state_enters_scheduler_path` exercise empty queue startup with shared `ResolveWait` and assert retry-dispatch evidence such as `ResolveWait retry dispatch started` before completion semantics can be considered; completion condition: no `AllCompleted`-equivalent success path occurs before retry evaluation)
- [x] Prevent truthful-completion regressions while retry work remains pending or active. (verification: unit - `src/tui/orchestrator.rs` now checks shared reducer `ResolveWait` / `RejectWait` membership and active base-mutating lane state before success logs and final `AllCompleted`; completion condition: zero-change success logs and `AllCompleted` are not emitted while reducer-owned lane-wait membership or active resolving/rejecting state remains)
- [x] Surface stale or missing retry prerequisites as visible state rather than silent pending. (verification: integration - added `src/parallel/tests/executor.rs::test_missing_workspace_retry_clears_resolve_wait_in_reducer` and `test_stale_workspace_retry_clears_resolve_wait_in_reducer`; existing dirty/clean and success-path tests cover dirty base and valid workspace behavior; completion condition: each case transitions away from indefinite `resolve pending` by clearing reducer-owned `ResolveWait`, demoting to `merge wait`, `resolving`, `merged`, or another visible state)
- [x] Preserve existing non-empty and active-scheduler manual retry behavior. (verification: integration - focused verification includes `src/parallel/tests/executor.rs::test_manual_resolve_wait_retries_after_in_flight_apply_completes`, `test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work`, and `test_dirty_to_clean_resolve_wait_wakes_retry_without_new_trigger`; existing queued work dispatch, active scheduler notification, auto-resumable deferral, dirty-base demotion, and merged-state non-regression semantics remain unchanged)
- [x] Keep TUI display synchronized with reducer-owned retry membership. (verification: unit - existing TUI reducer snapshot refresh tests plus new missing/stale workspace retry tests assert rows do not remain `resolve pending` after reducer-owned `ResolveWait` is cleared; completion condition: display status follows reducer snapshots after retry completion, demotion, or stale-prerequisite handling)
- [x] Run focused and full verification. (verification: manual - focused `cargo test` targets passed for `test_order_based_empty_resolve_wait_shared_state_enters_scheduler_path`, `test_parallel_startup_preserves_empty_manual_resolve_wait_state`, `test_scheduler_dispatches_synced_manual_resolve_wait_without_queued_work`, `test_manual_resolve_wait_retries_after_in_flight_apply_completes`, `test_missing_workspace_retry_clears_resolve_wait_in_reducer`, `test_stale_workspace_retry_clears_resolve_wait_in_reducer`, and `test_dirty_to_clean_resolve_wait_wakes_retry_without_new_trigger`; `cargo fmt --check` passed; final validation is pending below)

## Future Work

- Manual operator verification in a real TUI session may be useful after implementation: reproduce `merge wait` after restart, press `M`, and confirm the row transitions away from indefinite `resolve pending` without zero-change success logs.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate fix-manual-merge-retry-zero-change-startup --archive-gate`
