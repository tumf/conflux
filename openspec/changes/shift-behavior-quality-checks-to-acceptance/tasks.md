## Implementation Tasks

- [x] 1. Update `openspec/specs/cflx-proposal-validation/spec.md` と関連 delta で、native validator responsibility を deterministic authoring contract に限定し、runtime-behavior / implementation-facing-task adequacy heuristic を validator scope から外す canonical rule を追加する (verification: integration - `cflx openspec validate shift-behavior-quality-checks-to-acceptance --strict --evidence warn` が成功し、`openspec/changes/shift-behavior-quality-checks-to-acceptance/specs/cflx-proposal-validation/spec.md` の delta 文面が validator responsibility reduction を明示する)
- [x] 2. Update `openspec/specs/agent-prompts/spec.md` と `.opencode/commands/cflx-accept.md` で、behavior-changing work の implementation-task adequacy を acceptance review が concrete repository evidence で判断する requirement を acceptance contract に追加する (verification: integration - `openspec/changes/shift-behavior-quality-checks-to-acceptance/specs/agent-prompts/spec.md` と `.opencode/commands/cflx-accept.md` review で behavior-changing work の task/evidence adequacy review が archive ではなく acceptance に割り当てられていることを確認する)
- [x] 3. Implement `src/openspec_cmd.rs` の behavior-centric validation 変更として、`Runtime behavior is claimed without implementation-facing tasks` を発火する heuristic とその関連 aggregation を削除または縮退する (verification: unit - `src/openspec_cmd.rs` validator tests が runtime-behavior wording だけでは warning/error を出さず、他の deterministic checks は維持されることを確認する)
- [x] 4. Add acceptance-side contract tests または prompt validation coverage を追加し、behavior-changing proposal に implementation-facing tasks / concrete integration evidence が不足する場合は acceptance FAIL finding で扱えることを固定する (verification: integration - acceptance contract coverage tests または `.opencode/commands/cflx-accept.md` prompt-contract checks が archive validator without heuristic の状態でも actionable finding を返せることを確認する)
- [x] 5. Update `skills/cflx-accept/SKILL.md` と `skills/cflx-archive/SKILL.md` で、proposal-quality judgement は acceptance responsibility、archive は real archive readiness / commit-path blocker responsibility であることを明記する (verification: manual - `skills/cflx-accept/SKILL.md` と `skills/cflx-archive/SKILL.md` review で acceptance/archive の責務境界が一致し、archive 側に heuristic blocker を再導入していないことを確認する)
- [x] 6. Verify proposal delta と関連変更をまとめて検証する (verification: integration - `cflx openspec validate shift-behavior-quality-checks-to-acceptance --strict --evidence warn`, relevant Rust tests for `src/openspec_cmd.rs` and acceptance contract coverage, `cargo clippy --all-targets --all-features -- -D warnings`)

## Future Work

- verification ownership / executable-surface runnable coverage の deterministic checks も acceptance-first に寄せるべきか別 change で再評価する
- acceptance reviewer を structured rubric 化し、proposal-quality findings の分類をより安定化する
