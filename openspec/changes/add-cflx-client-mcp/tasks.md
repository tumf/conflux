## Implementation Tasks

- [ ] **Task 1: Define process-local execution identity and client envelope fields.** Add an owner-incarnation-bound execution ID created only after successful enqueue admission, return it from CLI/MCP results, and keep it non-authoritative and restart-scoped. (verification: unit - add execution-ID admission/no-op/retry/restart cases under `src/client/` and run `cargo test --lib client`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 2: Extract shared truthful completion observation.** Reuse the execution contract and repository completion oracle from `src/client/wait.rs` for both bounded waits and owner-side subscriptions without copying terminal classification logic. (verification: unit - add merged, base-published, branch-pushed, disappearance, rejection, failure, timeout, and owner-replacement cases under `src/client/` and run `cargo test --lib client`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 3: Implement the process-local completion-sink registry and bounded dispatcher.** Support exact instance/execution/change binding, argv-only callbacks, fixed environment variables, versioned bounded event files, event-edge dedupe, restart invalidation, timeout/output caps, and observability-only delivery failures. (verification: integration - add `tests/client_completion_sink.rs` using a fake callback, then run `cargo test --test client_completion_sink`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 4: Add `cflx client mcp`.** Implement stdio MCP initialization, `tools/list`, and `tools/call` for status, enqueue, wait, notify set/get/clear by calling the existing client boundary; preserve machine-readable errors and prevent owner acquisition or raw command exposure. (verification: unit - add JSON-RPC framing, schema, protocol-only stdout, and closed-tool tests under `src/client/mcp.rs`, then run `cargo test --lib client::mcp`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 5: Verify MCP against a live long-lived TUI owner.** Start a fixture owner, call MCP status/enqueue/notify, complete one change, and prove exactly one callback for its execution while the owner remains alive; retry the same change and prove a distinct execution ID. (verification: integration - add `tests/client_mcp_integration.rs` and run `cargo test --test client_mcp_integration`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 6: Add the optional OpenCode reference integration.** Add plugin, callback helper, install/use documentation, exact cflx MCP tool filtering, loopback/session validation, mandatory automation marker, dedupe, and untrusted-event guidance under `examples/integrations/opencode-auto-resume/`, with packaged-file coverage governed by the existing `Cargo.toml`. (verification: integration - add `examples/integrations/opencode-auto-resume/test.sh`, run it with fake `cflx` and `opencode` executables, and verify the integration is covered by the package include policy in `Cargo.toml`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 7: Document compatibility, security, and lifecycle semantics.** Explain in the existing `README.md` and the example README that MCP is client-only, TUI stays resident, notifications bind to execution rather than TUI exit, OpenCode messages are ordinary `role=user`, registrations are process-local, and tokens/event data are handled safely. (verification: integration - add help/README assertions to `tests/cli_integration.rs`, run `cargo test --test cli_integration`, and run `cargo run -- client mcp --help`) (verification-id: cflx-client-mcp-acceptance)
- [ ] **Task 8: Run repository quality gates.** Keep tests above the repository's default speed budget behind `heavy-tests`. (verification: integration - run `cflx openspec validate add-cflx-client-mcp --strict --evidence warn`, `cflx openspec validate add-cflx-client-mcp --archive-gate`, `cargo test`, and `cargo clippy --all-targets --all-features -- -D warnings`) (verification-id: cflx-client-mcp-acceptance)

## Acceptance Criteria

The implementation is acceptable when all Implementation Tasks are checked with their cited repository evidence. Together those checks must prove MCP admission into an existing TUI, exact execution binding, completion notification while TUI remains alive, retry identity isolation, non-authoritative failure handling, optional OpenCode same-session continuation, and compatibility of existing CLI/TUI/API behavior.
