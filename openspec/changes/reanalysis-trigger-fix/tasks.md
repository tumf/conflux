## 1. 仕様整合のための設計整理
- [ ] 1.1 re-analysis のトリガ条件を整理し、完了イベントおよびメイン実行ループ進行への依存を外す方針を明文化する（完了条件: 設計メモに「キュー変化/タイマーで再解析開始」「実行中グループ完了とメインループ進行を待たない」旨が明記されていること）。

## 2. 再解析トリガの非同期化
- [ ] 2.1 並列実行ループに re-analysis トリガ用の通知経路を追加する（完了条件: `execute_with_order_based_reanalysis` で通知受信時に再解析スケジュールが立つことをコードで確認できる）。
- [ ] 2.2 実行中グループ完了やメイン実行ループ進行を待たずに再解析が開始されるフローに変更する（完了条件: 再解析が完了イベントおよびループ進行に依存しない制御フローであることをコード上で追跡できる）。
- [ ] 2.3 スロット可否と再解析実行を分離し、スロットゼロでも分析が走ることを保証する（完了条件: スロットゼロ時でも分析開始ログが出る設計になっていることを確認できる）。

## 3. 既存デバウンスの維持
- [ ] 3.1 デバウンス判定をキュー変化にのみ依存させる（完了条件: `should_reanalyze` がキュー変化時刻のみで判定し、スロット可否に依存しないことをコードで確認できる）。
- [ ] 3.2 通知連打時の再解析頻発を抑制する（完了条件: デバウンスが有効な間は再解析が連続起動しないことをテストで確認できる）。

## 4. テストと検証
- [ ] 4.1 キュー追加後に完了イベントやメイン実行ループ進行に依存せず re-analysis が起動することを確認するテストを追加/更新する（完了条件: `cargo test` で該当テストが通る）。
- [ ] 4.2 仕様準拠の検証として `npx @fission-ai/openspec@latest validate reanalysis-trigger-fix --strict` を実行する（完了条件: エラーゼロ）。
