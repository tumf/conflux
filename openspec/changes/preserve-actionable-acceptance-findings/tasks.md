## Implementation Tasks

- [x] Add a shared structured repository-finding type and JSON FAIL parser support for stable `id`, `severity`, `summary`, non-empty `evidence`, repository-relative `required_changes`, and `verification`, while preserving legacy string findings and routing malformed structured payloads to bounded protocol handling. (verification: unit - table-driven `cargo test acceptance` cases cover valid objects, both severities, duplicate IDs, empty fields, path traversal, mixed legacy/object input, malformed objects, and unchanged legacy verdict syntax; completion condition: canonical structured findings reach callers without lossy string conversion and invalid objects cannot become path-only Apply work)
- [x] Separate complete latest finding payloads from retry identities and semantic fingerprints in Acceptance history/checkpoint APIs, and remove any update path where normalization overwrites actionable detail. (verification: unit - `cargo test history` and `cargo test orchestration::acceptance` prove identity/fingerprint updates preserve full evidence, required changes, and verification details; completion condition: history exposes distinct typed accessors and a regression fixture matching `repository|path|verification` cannot replace its source finding)
- [x] Extend runtime-owned current follow-up rendering, parsing, and reconciliation to preserve immutable finding IDs and actionable details separately from Apply-authored remediation evidence, with Acceptance-only close/reopen transitions. (verification: unit - `cargo test task_parser` covers open, remediation-claimed, repeated-ID reopen, absent-ID close/removal, interruption recovery, invalid metadata, and attempts by Apply hydration to self-close; completion condition: workspace evidence supports FAIL-to-Apply recovery without making a checked box equivalent to PASS)
- [x] Build focused Apply repair prompts from the complete latest open findings exactly once, rank them above completed proposal tasks and historical context, require per-item remediation evidence, and require a relationship explanation for additional files. (verification: unit - `cargo test agent::prompt` and `cargo test agent::runner` assert full fields survive JSON encoding, normalized IDs are not substituted, duplicate context is absent, legacy findings remain usable, and the prompt forbids treating completed tasks or remediation claims as closure; completion condition: an Apply invocation receives the concrete required change and verification instruction reported by Acceptance)
- [x] Implement shared remediation-to-diff coverage validation using the FAIL revision and repair Apply result, requiring every structured `required_changes` and `verification` path in the actual diff while preserving unrelated-file diagnostics and legacy compatibility. (verification: unit - `cargo test orchestration::acceptance` covers complete coverage, missing implementation path, missing verification path, calibration-only changes, comment-only unrelated progress, tracked/untracked changes, renamed paths, invalid evidence references, and legacy findings without path sets; completion condition: only complete declared coverage authorizes another Acceptance invocation and coverage never claims semantic resolution)
- [x] Implement per-finding automatic repair accounting so one repair Apply is allowed after first observation, the same ID on the next FAIL stops before another Apply regardless of semantic progress, new IDs receive one opportunity, and mixed repeated/new payloads stop atomically with all findings retained. (verification: unit - `cargo test orchestration::acceptance` covers first observation, repeated ID with unrelated progress, changed prose/line numbers, new ID, mixed IDs, and explicit retry reset semantics; completion condition: the shared decision API returns evidenced `repeated_acceptance_finding` without dispatching partial repair work)
- [x] Wire identical structured payload, diff-coverage, per-ID retry, and diagnostic decisions into serial and parallel execution, reusing the revision-bound temporary hold and explicit-retry lifecycle from `replace-acceptance-marker-stalls` without creating PASS state or an Acceptance-origin worktree marker. (verification: integration - fake agent sequences in `cargo test parallel::dispatch` and `cargo test serial_run_service` assert invocation counts and parity for detailed FAIL/apply/PASS, remediation mismatch, repeated ID, new ID, mixed IDs, restart, and explicit retry; completion condition: equivalent observations produce identical prompts, no second automatic Apply for a repeated ID, and neither mode can archive without current-revision PASS)
- [x] Emit operator diagnostics for remediation mismatch and repeated findings containing full findings, occurrence counts, revisions, required and actual files, coverage, unrelated files, remediation evidence, stop reason, resumability, and next action, then update portable Acceptance and Apply skills to match the implemented contract. (verification: unit - `cargo test events`, `cargo test embedded_skills`, and prompt contract tests assert structured diagnostics, stable-ID guidance, complete repair fields, Acceptance-owned closure, and no full-history replay; completion condition: CLI/TUI/event consumers can explain why automation stopped and how explicit retry proceeds without parsing narrative logs)
- [x] Run focused and repository-wide Rust quality gates, keeping default tests under one second or marking unavoidable heavy integration fixtures with the project heavy-test convention. (verification: integration - `cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test`; completion condition: all commands pass and the focused tests execute real parser, prompt, diff, serial, and parallel paths rather than placeholder returns)

## Future Work

- Consider configurable per-finding automatic repair budgets only after operator evidence shows one automatic repair is insufficient.
- Consider richer alternative-path groups in `required_changes` only when real findings need valid mutually exclusive repair locations.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate preserve-actionable-acceptance-findings --archive-gate`

## Current Acceptance Follow-up
- attempt: 1
- [x] repository|remediation-mismatch/repeated-id/new-id/mixed-id|verification
  evidence: real-cycle tests added — serial `process_change` (mismatch/repeated/new-id/malformed/explicit-retry) and parallel `dispatch_change_to_workspace` (mismatch/repeated/new-id), plus a parity test comparing two independent real runs
- [x] repository|src/orchestration/acceptance.rs|verification
  evidence: `FindingRepairLedger::reset_for_explicit_retry` now has production callers in both modes (serial `consume_explicit_acceptance_retry`, parallel explicit-retry dispatch branch) with budget-release and evidence-preservation tests
- [x] repository|src/serial_run_service.rs|verification
  evidence: coverage gate moved into `run_acceptance_loop` so every serial entry gates, repair stops now hold dispatch via `hold_repair_stop`, and `MalformedFinding` routes through `decide_acceptance_blocker` for the same 2 bounded retries parallel gets
